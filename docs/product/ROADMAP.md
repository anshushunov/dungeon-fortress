# Roadmap разработки

Статус: живой план  
Дата обновления: 2026-07-29  
Владелец направления: владелец проекта  
Источник конкретной работы: GitHub Issues и Pull Requests

## Зачем нужен этот документ

Roadmap показывает не все возможные механики, а **какую игровую гипотезу мы
проверяем сейчас, что уже доказано, в каком порядке стоит очередь работ и где
владелец выбирает дальнейшее направление**.

У roadmap нет календарных обещаний. Проект разрабатывается одним человеком с
агентами и движется короткими запускаемыми срезами. Поэтому следующий крупный
этап уточняется после человеческого playtest, а не планируется заранее до
деталей.

Стабильное продуктовое обещание находится в [`VISION.md`](VISION.md), главная
идея и core gameplay — в [`PITCH.md`](PITCH.md), текущее устройство механик — в
`docs/design/`, причины важных решений — в `docs/decisions/`. Этот документ
отвечает только на вопросы «где мы сейчас?», «что делается следующим?» и «какие
направления можно выбрать дальше?».

## Как владелец управляет направлением

После каждого playtest владелец даёт одну из команд:

| Команда | Что происходит |
|---|---|
| `CONTINUE` | Начинается следующий минимальный шаг текущего эксперимента. |
| `ADJUST: <что изменить>` | Следующий шаг не начинается; текущий slice исправляется и проверяется снова. |
| `PROMOTE: <направление>` | Указанное направление становится следующим активным прототипом. |
| `DEFER: <направление>` | Направление остаётся в roadmap, но не получает Issue. |
| `STOP` | Эксперимент закрывается без дальнейшего расширения. |
| `MERGE #N` | Владелец явно разрешает merge уже проверенного PR. |

Координационный чат записывает решение в **журнал ниже**, обновляет разделы
`NOW` и «Очередь» и только затем создаёт следующий implementation Issue.

## Журнал gate-решений

Решение, оставшееся только в комментарии PR или в чате, не считается принятым:
его нельзя ни найти, ни посчитать. Критерий отказа опирается на этот журнал, а
не на память участников.

Колонка `ADJUST` — счётчик подряд идущих `ADJUST` по **одной и той же
гипотезе**; она обнуляется, когда гипотеза закрыта или заменена.

| Дата | Что проверяли | Решение | `ADJUST` | Что из него вышло |
|---|---|---|---|---|
| 2026-07-27 | Prototype 1 «Подготовка к набегу» (#12) | `ITERATE` | 0 | Readability pass (#19) и первый art probe; [`PROTOTYPE_01_EVALUATION.md`](../playtests/PROTOTYPE_01_EVALUATION.md) |
| 2026-07-27 | Dungeon shaping — раскопка (#24) | `CONTINUE` | 0 | Шаг 2: логистика камня (#26) |
| 2026-07-28 | Dungeon shaping — логистика камня (#26) | `CONTINUE` | 0 | Шаг 3: функциональная комната (#48) |
| 2026-07-28 | Питч как продуктовый фокус (PR #31) | `PROMOTE` | — | [`PITCH.md`](PITCH.md) принят; ADR 0009; порядок кандидатов в roadmap |
| 2026-07-28 | Presentation pass A (#36, #39) | принято | 0 | Интерполяция движения и Control-вёрстка HUD |
| 2026-07-28 | Functional room (#48) | `ADJUST: визуал комнат выделить` | 1 | Issue #52; отложен в блок реализации ADR 0008 — см. правило отложенного `ADJUST` |
| 2026-07-28 | Панель на иконках (#55) | дефекты | — | Issue #58 (разметка на паузе), PR #59 (размер подсказок) |
| 2026-07-28 | Эксперимент #23 целиком | `PROMOTE SESSION LOOP` | сброшен | Срез 1 становится следующим gameplay-блоком; полная переработка roadmap |
| 2026-07-28 | Размер подсказок панели (PR #59) | `MERGE #59` | — | Смержен; в ремонтном блоке остаётся #58 |
| 2026-07-28 | Развилки 1 и 2 раздела 10 [`PITCH.md`](PITCH.md) | Смерть окончательна, притока нет; партия — один сеанс без сохранений | — | Условие входа в срез 1 выполнено; развилки перенесены в закрытые |
| 2026-07-28 | Автономность агентов и параллельная работа | Классы работы, партиция областей, потолок 2, авто-merge с границами, пакетный playtest | — | [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md); правила движения и `/coord` обновлены |
| 2026-07-29 | #38 — domain MCP держал цель сборки (PR #63) | Авто-merge по ADR 0014 | — | Каждая сессия MCP работает со своей копией; `verify.ps1` проходит при подключённом сервере |
| 2026-07-29 | #58 — разметка на паузе (PR #62) | Авто-merge по ADR 0014 | — | Намерение игрока видно немедленно; ремонтный блок закрыт, очередь переходит к срезу 1 |
| 2026-07-29 | Что такое комната ([ADR 0013](../decisions/0013-what-is-a-room.md)) | Вариант C сейчас, переход к B1 на срезе 5 | — | ADR 0013 принят; условие входа в блок реализации ADR 0008 выполнено; условие перехода к B1 записано в ADR |
| 2026-07-29 | Функция ценности среза 1 ([ADR 0015](../decisions/0015-session-of-waves-and-domain-score.md)) | **Дурная слава**; сводка владения входит в срез 1 | — | ADR 0015 принят; счёт не убывает; волна зафиксирована как частный случай события давления |
| 2026-07-29 | #64 — `.uid` отслеживались непоследовательно (PR #67) | Авто-merge по ADR 0014 | — | Единая стратегия для всех `*.uid`; чистое дерево после импорта в свежем worktree |
| 2026-07-29 | Темп разработки | Облегчённый цикл: меньше итераций и кругов review | — | Правило в «Правилах движения»; срез 1 выдан одним Issue #68; проверяется на этом блоке |
| 2026-07-29 | #40 — стадийный `verify.ps1` (PR #70) | Авто-merge по ADR 0014 | — | Девять стадий; правка в скриптах проверяется за 2 с вместо 69, полный прогон не подорожал. Условие ADR 0014 на подъём потолка закрыто; сам потолок не поднят. Пять замечаний review ушли в #71, а не новым кругом — первая проверка облегчённого цикла |
| 2026-07-29 | Конец партии и смысл силы владения (review PR #73) | Третья форма «разорено»; сила считает готовность, а не потенциал | — | Уточнения записаны в [ADR 0015](../decisions/0015-session-of-waves-and-domain-score.md). Обе развилки нашёл review: сводка называла разорённое владение удержанным, а павшее от голода — сильнейшим |
| 2026-07-29 | **Срез 1 — повторяющийся цикл угрозы и счёт владения (#68, PR #73)** | **`CONTINUE`** | 0 | Milestone A0 взят. Очередь переходит к блоку реализации ADR 0008. Находки playtest: занятость клетки (#76) привязана к срезу 5, обратная связь удара (#77) — сразу за ADR 0008 |
| 2026-07-29 | ADR 0008 шаг 1 — камера, масштаб тайла и съёмка кадра (#79, PR #81) | `MERGE #81` | — | Тайл 40 px, дискретный зум и панорама, HUD в `CanvasLayer`, воспроизводимая съёмка. Playtest владельца по ходу задачи дал два исправления камеры. Два круга независимого review: первый нашёл зависание headless-прогона на невалидном параметре, ослабленный HUD-guard и тавтологичное доказательство попадания клика — все закрыты вместе с доказательствами живости обоих guard'ов. Первая половина #49 закрыта попутно |

## Правила движения

- **Одновременно проверяется ровно один gameplay slice.** Это не ограничение
  стоимости: playtest — измерительный прибор, и два одновременных изменения
  симуляции делают ответ владельца неинтерпретируемым. Параллельно с ним могут
  идти задачи других классов; правила параллельной работы, партиция областей и
  потолок в два записывающих агента приняты
  [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md). Раздел
  «Очередь» задаёт порядок gameplay-блоков, а не разрешение проверять две
  гипотезы сразу.
- **Владелец играет пакетом.** Дефекты и обеспечивающая работа накапливаются и
  попадают в один playtest на границе gameplay-слайса, а не проверяются после
  каждого PR.
- Каждый gameplay-шаг заканчивается запускаемой игрой, зелёными проверками и
  playtest владельца.
- Следующий gameplay Issue не реализуется до решения
  `CONTINUE / ADJUST / PROMOTE / DEFER / STOP` и записи этого решения в журнал.
- **Отложенный `ADJUST`.** `ADJUST` разрешено принять как отложенный, если его
  исполнение целиком попадает в уже названный блок очереди. Тогда в журнале
  фиксируется, в какой именно блок он отложен, и слайс считается закрытым.
  Отложенный `ADJUST` продолжает считаться в счётчике критерия отказа. Без такой
  записи `ADJUST` блокирует запуск любого следующего блока.
- **Класс «блокирует проверяемость».** Обеспечивающая задача, которая мешает
  запустить проверки или воспроизвести состояние, не ждёт gate и не ждёт
  очереди: её ожидание дороже её исполнения. В потолок записывающих агентов она
  при этом входит. Класс присваивается явно в Issue, а не по желанию
  исполнителя.
- **Merge без владельца — только по границам ADR 0014.** Задачи классов
  `defect`, `support`, `blocks-verification` и `art` мержатся автоматически при
  зелёном CI, зелёном `verify.ps1` и зелёном независимом review, если не
  затронуты контракт, словарь команд, ADR и продуктовые документы и если PR не
  вводит нового игрового поведения. Восстановление уже обещанного поведения
  исключением не является; если оно меняет картинку, координатор даёт
  однострочную команду запуска для ближайшего пакетного playtest. Всё
  остальное — по команде `MERGE #N`.
- **Облегчённый цикл разработки.** Принято владельцем 2026-07-29: меньше
  итераций и меньше кругов review, чтобы обратная связь по игре приходила
  быстрее. Три следствия:
  1. **gameplay-слайс идёт одним PR**, когда его части бессмысленно проверять
     порознь. Волны без счёта владения дают playtest, на котором владелец снова
     не может сказать, хорошо ли он идёт, — это потраченное внимание без ответа;
  2. **глубина review соразмерна риску.** Независимый review остаётся
     обязательным для каждого PR — это инвариант [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md) §4
     и условие допустимости автоматического merge. Меняется только объём:
     review проверяет критерии готовности и читает diff, а самостоятельно
     переспрашивает все проверки лишь когда изменение касается детерминизма,
     контракта или самих проверок;
  3. **замечание, не ломающее критерий готовности, становится Issue, а не новым
     кругом доработки.** Возврат исполнителю остаётся для замечаний, из-за
     которых критерий не выполнен.
  Пункт 3 сужает формулировку ADR 0014 §4 «обычные замечания координатор
  возвращает исполнителю сам». Если облегчённый цикл переживёт блок среза 1 без
  отката смерженного PR, он оформляется поправкой к ADR 0014; если приведёт к
  откату — отменяется, и это записывается в журнал.
- Claude Code является основным пишущим агентом. Координационный чат ведёт
  roadmap, декомпозицию, продуктовые развилки и приёмку.
- **Изображения генерирует Codex.** Координационная сессия формулирует запрос —
  что нужно, в какой геометрии, в каком состоянии, — но не создаёт изображения
  сама. Подробности в [`ANIMATION_PIPELINE.md`](../art/ANIMATION_PIPELINE.md).
- Сгенерированные ассеты сначала получают prompt и provenance, затем передаются
  implementation-агенту для подключения.
- Roadmap обновляется на decision gate, а не после каждого технического коммита.
- Изменение tuning-слоя (числа, формулы, пороги, планировка) не требует
  обновления контракта и ADR — только зелёных проверок и записи в PR. Изменение
  инварианта требует ADR. Граница — в
  [ADR 0010](../decisions/0010-contract-invariants-and-tuning.md).
- Новые движки, SDK, MCP и генеративные инструменты проверяются по
  `docs/engineering/TECH_RADAR.md` на границе прототипов либо перед крупным
  обновлением, но не ради каждой новой версии.

### Чего мы не делаем

Эти формулировки дешевле принять заранее, чем спорить о них на decision gate.

- **Не строим симуляцию ради симуляции.** Система без наблюдаемого игроком
  последствия не входит в срез.
- **У каждой новой системы есть бюджет читаемости в UI.** Если её нельзя
  показать игроку, её не добавляем.
- **Не универсализируем заранее.** Один объект и одно последствие вместо
  каталога и редактора.
- **Не подменяем игру подготовкой.** Работа над представлением и инструментами
  не создаёт продуктового доказательства. Два таких блока подряд в очереди без
  gameplay-блока между ними не ставятся, а gameplay-дорожка не простаивает: пока
  активного gameplay-слайса нет, новая обеспечивающая задача не начинается.
- **Pre-production не длится бесконечно.** См. критерий отказа ниже.

### Критерий отказа

> Два `ADJUST` подряд по одной гипотезе без улучшения на playtest закрывают
> возможность третьего. Владелец выбирает `PROMOTE` или `STOP`.

Счёт ведётся по колонке `ADJUST` журнала gate-решений. Без записанного счётчика
это правило не срабатывает никогда, а `STOP` остаётся словом в словаре.

## Целевая игра

Продуктовый фокус и ответ на вопрос «что здесь самое интересное» находятся в
[`PITCH.md`](PITCH.md) — документ принят владельцем 2026-07-28. Roadmap ему не
противоречит и не пересказывает его: обоснование порядка срезов живёт в разделе
11 питча, здесь остаётся только очередь и состояние.

Обещание в одном абзаце:

> Игрок вырубает из скалы собственное подземное владение и населяет его
> существами, которые живут своей жизнью. Он не отдаёт приказов — он проектирует
> пространство, задаёт правила и иногда вмешивается рукой. Владение растёт,
> начинает притягивать новых обитателей, обзаводится производством, обороной и
> внутренними конфликтами. Регулярно приходят те, кто хочет забрать накопленное.
> Каждая партия оставляет несколько историй, которые игрок может пересказать
> словами.

Критерий приоритизации, вытекающий из питча: **работа, усиливающая столкновение
плана и людей, важнее работы, усиливающей подготовку или проверку.**

Игры, за которыми стоит идти за конкретными решениями, и то, за чем именно к
каждой из них идти, перечислены в [`REFERENCES.md`](REFERENCES.md).

### Milestones

Это не календарный план и не порядок работ. Это направление, по которому на
каждом gate можно сравнивать кандидатов между собой.

| Milestone | Содержание | Критерий выхода |
|---|---|---|
| **A0 — петля замкнулась** | Несколько волн нарастающей силы в одном владении, явная цена поражения, счёт владения, у партии есть конец. | Владелец доигрывает одну партию до конца и может назвать, что он сделал не так. |
| **A — Vertical Slice** | Одна сессия ~30 минут. Копание и функциональные комнаты; 2–3 связанные производственные цепочки; несколько волн нарастающей угрозы; явный счёт владения; нарративная лента; рука хозяина; свет, автотайлинг, интерполяция, звук. | Владелец играет три партии подряд по собственному желанию и может пересказать историю из каждой. |
| **B — Playable Loop** | Сессия 1–2 часа. Приток населения от условий владения; комнаты со свойствами; экипировка и мастерская; двери и ловушки; черты характера и лёгкие отношения; травмы, мораль, восстановление; сохранение/загрузка; именованные враги-герои. | Партия играется без участия разработчика и заканчивается по решению игрока, а не по таймеру. |
| **C — Content Alpha** | Кампания. Несколько видов существ с разными потребностями; процедурная карта либо набор авторских; глобальный слой (один внешний ресурс, одна угроза, одно решение); art-direction gate и production-пайплайн; баланс и кривая сложности. | Посторонний играет 2 часа без объяснений. |

A0 добавлен потому, что между текущим состоянием и milestone A лежит около
десяти систем: сравнивать с ним прогресс невозможно, и любой блок кажется
одинаково далёким от цели. A0 — ближайшая отметка, до которой реально
дотянуться, и она совпадает со срезом 1 питча.

Постоянные ограничения на всех milestone: 2D и посильный объём контента для
одного человека с агентами; детерминизм, replay и headless-проверки не
приносятся в жертву ни одной фиче; каждая новая система имеет бюджет
читаемости в UI.

## Текущее состояние

### COMPLETED — проверяемый фундамент

| Блок | Результат |
|---|---|
| Agent-ready foundation | Godot/.NET, фиксированный seed, headless tests, replay, structured state/logs, screenshots и domain MCP. |
| Prototype 1: Prepare for Raid | Пищевая экономика, потребности, тренировка, наблюдение, автоматический набег и бой связаны непрямыми командами. |
| Readability and first art pass | Исправлены кисть, HUD, объяснение Quarters и alive/downed; подключены первые generated goblin sprites. |
| Dungeon shaping — step 1 | Игрок обозначает скалу, существа автономно копают, `Rock → Floor`, появляется loose Stone. |
| Dungeon shaping — step 2 | Игрок создаёт MaterialStockpile; Stone проходит `loose → carried → stored` с capacity, reservations и conservation invariant. |
| Dungeon shaping — step 3 | Игрок размечает blueprint тренировочного поста, камень доставляется и **расходуется**, построенный пост создаёт настоящие Drill jobs. ADR 0012. |
| Presentation pass A | Рендер отделён от тика и интерполирует движение по `alpha`; HUD переверстан на Control-контейнеры; слой представления покрыт тестами без запуска движка. |
| UI control pass | Панель на иконках, подсказки, прямоугольное выделение мышью; первый icon pack. |
| Ремонт обратной связи | Разметка, blueprint, зоны и смена приоритета видны немедленно, включая паузу и `STEP`: проекция «отправлено, ещё не применено» поверх канонического состояния. Развязаны запуск domain MCP и solution-сборка. |
| **Срез 1 — повторяющийся цикл угрозы и счёт владения** | Партия стала последовательностью из четырёх волн нарастающей силы с окном обычной жизни между ними; три формы конца — выстояло, разорено, пало; два числа владения — не убывающая **дурная слава**, задающая силу следующей волны, и **сила владения** как готовность. Ухудшение владения строго не улучшает счёт. `verify.ps1` стадийный, единая стратегия `.uid`. ADR 0013, ADR 0015. |

### NOW — блок реализации ADR 0008: проекция, камера и читаемые помещения

Срез 1 закрыт 2026-07-29 решением владельца **`CONTINUE`**: гипотеза
подтвердилась, milestone A0 взят. Итог среза — в таблице COMPLETED выше, полный
разбор — в PR #73.

Активен блок представления, стоявший следующим в очереди. Его условие входа
выполнено: [ADR 0013](../decisions/0013-what-is-a-room.md) принят, комната есть
производная сущность поверх зоны и объекта, и блок знает, границу чего рисует.

Состав блока — раздел «Блок „реализация ADR 0008“» ниже: `TileMapLayer` с
autotile, тайл 22 → 32–48 px, стена «верх плюс фасад», Y-sort, камера с зумом и
панорамой, читаемость помещений (#52). За ним, но **не внутри него**, идёт
обратная связь удара (#77). Её условие входа снято шагом 1: при тайле 22 px
партиклы превращались в кашу и претензия пришла бы к эффектам вместо масштаба, а
при 40 px у них есть место.

Блок разрезан на два Issue, и рез проходит **по каноническому снапшоту**: камера
и масштаб его не трогают, читаемость помещений трогает.

**Шаг 1 выполнен** 2026-07-29:
[#79](https://github.com/anshushunov/dungeon-fortress/issues/79), PR #81. Тайл
поднят с 22 до **40 px** и стал проверяемым параметром в диапазоне 32–48; над
миром стоит `Camera2D` с пятью дискретными уровнями зума (`0.5`, `0.75`, `1`,
`1.5`, `2`) и панорамой, ограниченной центрами краевых тайлов; HUD уехал в
отдельный `CanvasLayer` и камерой не масштабируется; `stretch/aspect` переведён
на `expand`, поэтому больший кадр показывает больше мира; масштаб интерфейса
задаётся параметром запуска; клик переводится в клетку через живой canvas
transform; съёмка кадра получила явные кадр, позицию и зум. Симуляция,
канонический снапшот, словарь команд и design-контракт не затронуты.

Зум, положение камеры и масштаб интерфейса **не входят в checksum** — это
проверяется исполняемо, одним checksum на четырёх сочетаниях. Дискретные уровни
выбраны так, чтобы не зависеть от незакрытого art-direction gate «пиксельный
стиль или нет»: они совместимы и с пиксель-артом, и с воспроизводимостью снимка.

Шаг 2 — пункты 3 и 5 таблицы ниже: `TileMapLayer` с autotile, стена «верх плюс
фасад», Y-sort, затем читаемость помещений (#52). Теперь у стены есть место, где
показать фасад, — ровно та причина, по которой шаг 1 шёл первым.

Блок не является gameplay-слайсом, поэтому слот `gameplay` свободен. Правило «не
подставляем игру подготовкой» при этом продолжает действовать: два обеспечивающих
блока подряд без gameplay между ними не ставятся.

Граница, установленная ремонтным блоком и обязательная для всей дальнейшей
работы слоя представления: **проекция отвечает на то, что выводится из
опубликованных фактов, свёрнутых через неё; мир отвечает на то, что требует
прогона тика.** Приоритеты и запретные клетки — такое же намерение игрока, как
мазок кистью; достижимость породы, начало работы и выбор исполнителя остаются за
симуляцией. Инвентарь фактов с обоснованием по каждому — в docstring
`MapAccents` и в `docs/engineering/PROTOTYPE_GRAYBOX.md`. Правило появилось
после пяти раундов review, в которых граница четырежды проводилась по месту и
четырежды оказывалась неполной.

### Очередь

Порядок gameplay-блоков и того, что к ним привязано. Параллельная работа других
классов регулируется [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md),
а не этой таблицей.

| Позиция | Блок | Тип | Условие входа | Критерий выхода |
|---|---|---|---|---|
| Сейчас | Реализация [ADR 0008](../decisions/0008-three-quarter-projection.md), шаг 2: геометрия тайла (стена «верх + фасад», autotile, Y-sort) и читаемость комнат (#52). Шаг 1 — камера, масштаб и съёмка кадра — выполнен (#79, PR #81) | представление | Выполнено: [ADR 0013](../decisions/0013-what-is-a-room.md) принят 2026-07-29; тайл 40 px и камера есть | Владелец видит помещение как помещение, а не как пол другого оттенка |
| Сразу за ним | Обратная связь удара, процедурная анимация, иконки состояния (#77) | `support` | Тайл больше 22 px — **снято шагом 1**: тайл 40 px, эффектам есть место | Владелец без инспектора называет, кто кого ударил и с каким результатом |
| Следующий gameplay | Срез 2 — события владения читаются как история, либо другое направление по выбору владельца | `gameplay` | Решение владельца на gate | По разделу 11 [`PITCH.md`](PITCH.md) |
| Затем | Долг проверяемости (#42, #49, #71) и долг контракта (#72, #74, #75) | `support` | #75 — после решения владельца по счёту партии | Единый источник эталонных кадров, живой HUD-guard, контракт совпадает с кодом |
| Вне очереди | #46 (культурно-зависимый формат скорости) | `blocks-verification` | — | Текст HUD детерминирован |
| Привязано к срезу 5 | Занятость клетки (#76) вместе со стенами, дверями и светом | `gameplay` | Начало среза 5 | Узкий коридор физически ограничивает, сколько врагов дотягивается одновременно |

Закрыто: #38 — каждая сессия domain MCP работает со своей копией, `verify.ps1`
проходит при подключённом сервере без ручных действий. #64 — все `*.uid`
игнорируются по единой стратегии, свежий worktree больше не отдаёт грязное дерево
после импорта проекта; правило и триггер его пересмотра в
[`ENVIRONMENT_SETUP.md`](../engineering/ENVIRONMENT_SETUP.md).

Обеспечивающие блоки стоят по одному между gameplay-блоками намеренно: правило
«не подменяем игру подготовкой» запрещает ставить их подряд.

### Следующий gameplay-блок: срез 1

Гипотеза:

> Если угроза приходит несколько раз за сессию с нарастающей силой, а между
> волнами владение восстанавливается и перестраивается, игрок связывает своё
> решение с последствием и начинает управлять осознанно, а не наблюдать.

Минимальный slice: 3–5 набегов вместо одного, фаза восстановления между ними,
явный счёт владения по итогам сессии.

Почему выбран владельцем 2026-07-28:

- «влияние не ощущается» — самый старый незакрытый продуктовый дефект. Он
  зафиксирован в [`PROTOTYPE_01_EVALUATION.md`](../playtests/PROTOTYPE_01_EVALUATION.md)
  как *«Perceived influence: not confidently perceived»* и не адресован ни одной
  из работ, сделанных после. Единственный набег через ~25 минут после решения
  даёт ровно ту разрежённость обратной связи, которая к этому и привела;
- закрывается наблюдение P2 той же оценки, где гипотеза H2 помечена
  contradicted, потому что метрика потерь делает `overrun` «лучше». Это не
  дефект метрики, а отсутствие функции ценности: игра нигде не говорит, что
  такое «хорошо»;
- появляется ритм сессии: тишина → тревога → бой → разбор → перестройка, а
  поражение перестаёт быть концом и становится сюжетом;
- стоимость низкая: таймер угрозы, генератор налётчиков, правила боя и исход уже
  реализованы и покрыты тестами. Речь о параметризации существующего;
- **любой** следующий кандидат получает нормальный испытательный стенд: без
  повторяемости влияние снаряжения, ленты или сопротивления плану нечем мерить.

Рамки, принятые владельцем 2026-07-28 вместе с выбором среза:

- **смерть обитателей окончательна, притока новых нет.** Потери необратимы,
  поэтому у поражения есть цена, а у партии — естественный конец. Приток
  населения от условий владения остаётся за milestone B;
- **партия укладывается в один сеанс, сохранений нет.** Кампания с сохранением
  между сеансами относится к milestone B и требует отдельного блока работы.

Обе рамки перенесены в закрытые развилки раздела 10 [`PITCH.md`](PITCH.md).
Implementation Issue создаётся после закрытия ремонтного блока.

## Кандидаты после текущего блока

Это направления, а не утверждённая очередь; implementation Issues для них не
создаются до выбора владельца. Порядок и обоснование — раздел 11
[`PITCH.md`](PITCH.md), номера срезов оттуда же.

| Срез | Что доказывает | Минимальный slice | Состояние |
|---|---|---|---|
| 1 | Повторяющееся давление делает влияние ощутимым | Несколько волн нарастающей силы, фаза восстановления, счёт владения | **Сделано**, `CONTINUE` 2026-07-29 |
| 2 | События владения читаются как история | Агрегатор поверх существующего event log: имена и предложения вместо reason codes, редкие события помечены как «моменты сессии» | Кандидат; сырьё уже каноническое, не хватает слоя перевода |
| 3 | Обитатель может сопротивляться плану | Одна память или связь, один наблюдаемый отказ, одна видимая причина | Кандидат; здесь заканчивается отсрочка ADR 0002/0006. Идёт после ленты, иначе связь останется числом в снапшоте |
| 4 | Власть через людей богаче власти через ползунки | Один приспешник, один указ, одно искажение намерения характером | Кандидат; требует ADR со словарём команд ролей поверх [ADR 0009](../decisions/0009-adherent-role-boundary.md) |
| 5 | Пространство работает как оружие | Свет и тьма меняют исход волны | Кандидат; смыкается с блоком представления — освещение нужно и для читаемости |
| 6 | Подготовка окупается предметно | Сырьё → мастерская → простое оружие → автономное получение снаряжения → изменённый бой | Кандидат; стоит последним осознанно: без среза 1 снаряжение проверяется один раз за сессию |
| — | Глубина строительства: двери, стены, ловушки, свойства комнат | Один объект и одно последствие для маршрута или боя | Продолжение текущей линии, а не шаг питча. Доступно по `CONTINUE CONSTRUCTION`. Двери и строимые стены становятся обязательными на срезе 5 по условию перехода [ADR 0013](../decisions/0013-what-is-a-room.md) |

Кандидаты, пришедшие из обсуждения боя 2026-07-28 и разобранные 2026-07-29. Они
не образуют собственного шага питча, а достраивают уже названные срезы:

| Кандидат | Что доказывает | Куда пристёгнут | Состояние |
|---|---|---|---|
| **Локализованные травмы, которые остаются** ([`PITCH.md`](PITCH.md) 6.13) | Бой производит персонажей, а не числа; у последствий появляется деление между «цел» и «мёртв» | Достраивает срез 1: между волнами появляется выбор — чинить, беречь или выпускать | Кандидат; переиспользует уже реализованные injuries, новых видов управления не вводит |
| **Боевые черты, меняющие решение** — трус, осторожный, бешеный, толковый | Разные характеристики дают разное поведение, а не разное число | Смыкается со срезом 3: у решения уже есть reason code, поэтому «почему он побежал» показывается существующим механизмом | Кандидат; требует одной дешёвой вещи — оценки тайла по числу проходимых соседей, из которой выводятся «горло», «открытое место», «окружают» |
| **Мили и ранжед как роли** | Позиция значит разное для разных бойцов; свой в линии блокирует выстрел, поэтому строй возникает сам | Срез 6 | Кандидат. Линия видимости — основная цена, решение по ней остаётся во входящих до среза 6 |
| **Событийный слой давления** | Давление — чьё-то решение, а не таймер; волна становится одним событием из нескольких | Идёт после среза 1: сначала повторяемость, потом разнообразие | Кандидат, зафиксирован [ADR 0015](../decisions/0015-session-of-waves-and-domain-score.md). Смыкается с 6.7 «антагонист с именем» и с ролью storyteller |
| **Способности как черты** | Существо узнаётся по тому, что оно умеет, а не только по характеристикам | Срез 6 вместе со снаряжением | Кандидат. Разрешены срабатывающие и пассивные, у существа их несколько, приобретаются в том числе от пережитого, бывают разных тиров — [`PITCH.md`](PITCH.md) раздел 9 |

## Представление

Это **не** production art. Проекция закрыта
[ADR 0008](../decisions/0008-three-quarter-projection.md); здесь перечислена
работа слоя отрисовки, которая дважды блокировала playtest и оценивается по
отношению «эффект / стоимость».

Причина, по которой это отдельный от art раздел: плавности не было по
архитектурной причине, а не по художественной — `TicksPerSecond = 6.0` и позиция
существа, равная `tile * TileSize`. Никакой генеративный пайплайн этого не
исправляет; исправил Presentation pass A.

### Сделано

| # | Работа | Где |
|---|---|---|
| 1 | Рендер отделён от тика, позиция интерполируется по `alpha` | Presentation pass A (#36) |
| 2 | HUD на Control-контейнерах вместо абсолютных `Rect2` | Presentation pass A (#36), опирался на #28 |

### Блок «реализация ADR 0008» — один элемент очереди, а не фон

Самый дорогой невыполненный блок в проекте. Он блокирует #52 и требует нового
asset pack, поэтому не начинался до того, как срез 1 дал A0. Milestone взят
2026-07-29, блок активен.

| # | Работа | Issue | Что даёт |
|---|---|---|---|
| 4 | ✅ Тайл 22 → **40 px** как параметр, камера с дискретным зумом и панорамой, HUD в `CanvasLayer`, `aspect` `expand`, масштаб интерфейса параметром запуска, явные кадр/позиция/зум при съёмке | [#79](https://github.com/anshushunov/dungeon-fortress/issues/79), PR #81 | Выполнено 2026-07-29. При 22 px спрайт физически не способен показать направление и состояние, а карта не занимала и половины кадра |
| 3 | `TileMapLayer` с terrain/autotile, тайл верх + фасад, Y-sort | не заведён | Геометрия пещеры вместо контурных квадратов |
| 5 | Читаемость помещений: сущность комнаты в снапшоте, явная граница, покрытие пола по назначению, иконка, подпись с состоянием, показ незавершённой комнаты | [#52](https://github.com/anshushunov/dungeon-fortress/issues/52) | Отложенный `ADJUST` по функциональной комнате |

Порядок в таблице — порядок исполнения, а не нумерация: пункт 4 идёт первым,
потому что размер тайла и камера задают кадр, в котором рисуется всё остальное.
Пункт 5 идёт последним, потому что он единственный меняет состав канонического
снапшота.

Условие входа выполнено: [ADR 0013](../decisions/0013-what-is-a-room.md) принят
2026-07-29. Комната — **производная сущность** поверх зоны и объекта, которую
игрок размечает кистью; переход к модели «комната выводится из замкнутого
пространства» привязан к срезу 5 условием, записанным в ADR. Практическое
следствие для пункта 5: читаемость держится **собственным покрытием пола** по
назначению, а не плёнкой поверх общего пола — приём Dungeon Keeper; граница
рисуется явно, иначе повторяется известная жалоба на Dwarf Fortress.

Сущность комнаты входит в канонический снапшот, то есть меняет его состав. Это
инвариант по [ADR 0010](../decisions/0010-contract-invariants-and-tuning.md):
контракт Prototype 1 обновляется вместе с реализацией.

### Кандидаты слоя отрисовки

| # | Работа | Что даёт |
|---|---|---|
| 6 | `CanvasModulate` + `PointLight2D`, тьма вне освещённого | Сетка становится подземельем; больше эффекта, чем от любого art pass. Связано со срезом 5 — свет как механика |
| 7 | Процедурная анимация: facing и flip, bob при ходьбе, наклон, squash & stretch на ударе | ~80 % «живости» без единого нового кадра |
| 8 | Партиклы, hit-stop, вспышка, всплывающие числа урона | Делает бой читаемым |
| 9 | Иконки-мысли над головой (еда / устал / «!») | Читаемость намерений без открытия инспектора; на дальнем зуме читается не существо, а что с ним происходит |

Пункты 7–9 собраны в **[#77](https://github.com/anshushunov/dungeon-fortress/issues/77)** и идут **сразу за** блоком ADR 0008, а не внутри него. Жалоба подтверждена дважды независимо: сначала по одиночному набегу, затем на playtest среза 1 — «без какой-то визуализации атаки смотрится пока так себе». Срез 1 превратил разовую претензию в постоянную, потому что бой теперь повторяется четыре раза за партию. Раньше реализации ADR 0008 не делается: при тайле 22 px эффекты сливаются в кашу.

## LATER — только после подтверждения локального ядра

### Глобальный уровень

Фракции, поселения, торговые маршруты, вылазки и события внешнего мира нужны
игре, но пока не должны определять архитектуру локального прототипа. Первый
global spike начинается только когда:

- локальная экономика содержит минимум две связанные цепочки;
- пространство влияет на производство или защиту;
- именованные существа имеют хотя бы один проверенный источник индивидуальной
  истории;
- автоматический конфликт имеет понятные входы и последствия.

Глобальный уровень сначала поставляет локальному владению один ресурс, одну
угрозу и одно решение. Полная симуляция мира не является первым шагом.

### Production art и масштаб контента

**Представление и production art — разные вещи с разными gate.** Работа слоя
отрисовки идёт в общей очереди, потому что без неё playtest измеряет качество
рендера, а не качество механики. Откладывается сюда только production art.

До подтверждения игрового ядра используются небольшие воспроизводимые asset
packs в геометрии [ADR 0008](../decisions/0008-three-quarter-projection.md).
Полный художественный pipeline, большой каталог существ, анимаций, биомов и
интерфейсов, выбор палитры и уровня стилизации откладываются до отдельного
art-direction gate.

Способы получения анимации, включая cutout-скелет и пререндер 3D, описаны в
[`ANIMATION_PIPELINE.md`](../art/ANIMATION_PIPELINE.md). Специализированные
генеративные инструменты выбираются spike-процедурой
`docs/engineering/TECH_RADAR.md`, а не из обсуждения.

## Постоянные workstreams

| Направление | Текущее состояние | Следующее доказательство |
|---|---|---|
| Session loop | Четыре волны за партию, три формы конца, дурная слава и сила владения; партия кончается сама | Событийный слой: волна становится одним событием из нескольких, а давление — чьим-то решением. |
| Construction | Выкопать, построить и использовать комнату можно; ADR 0012 | Двери, стены, ловушки или свойства комнаты как выбор пространства. |
| Narrative | Event log и reason codes есть, слоя перевода нет | События владения читаются как история, а не как коды. |
| Economy | Пища и Stone logistics работают, камень расходуется | Ввести альтернативную стоимость и связь со снаряжением/боем. |
| Combat/RPG | Повторяющиеся волны, HP, injuries с заживлением, morale; дальность — свойство атаки | Читаемость боя (#77), затем занятость клетки (#76) на срезе 5 и роли со снаряжением на срезе 6. |
| Individuality/simulation | Имена, свойства и история решений есть; память/отношения отложены | Одна память или связь меняет следующее решение. |
| Global layer | Не начат | Один внешний источник ресурса/угрозы после подтверждения локального ядра. |
| Представление | Интерполяция, Control-вёрстка HUD, панель на иконках и немедленная обратная связь на паузе приняты; граница «проекция против мира» зафиксирована; кадр конца партии закрыт guard'ом; **тайл 40 px, камера с дискретным зумом и панорамой, HUD в `CanvasLayer`, воспроизводимая съёмка с явными параметрами кадра** (#79) | **Активное направление.** Шаг 2 ADR 0008: стена «верх + фасад», autotile, Y-sort и читаемые помещения (#52). |
| Art | Первый goblin pass и icon pack v1; проекция закрыта ADR 0008, реализации нет | Asset pack постройки в геометрии ADR 0008 — после реализации проекции. Генерация у Codex. |
| Agent tooling | Deterministic CLI/API/MCP работают; чистый слой представления в CI; domain MCP больше не блокирует сборку (#38); единая стратегия `.uid` (#64); `verify.ps1` стадийный (#40); негативные сценарии доказывают живость HUD- и camera-guard'ов (#79) | #46 вне очереди как блокирующий проверяемость; #42, #71 и остаток #49 — долг после среза 1. |

## Когда roadmap пересматривается

Обязательное обновление происходит:

1. после каждого owner gate — запись в журнал gate-решений и правка `NOW`;
2. при изменении порядка блоков в разделе «Очередь»;
3. перед первым global-layer spike;
4. при срабатывании критерия отказа — два `ADJUST` подряд без улучшения;
5. если новое технологическое ограничение меняет стоимость или проверяемость
   разработки агентами;
6. если меняется продуктовый фокус в [`PITCH.md`](PITCH.md) — порядок кандидатов
   следует за питчем, а не наоборот.

Если roadmap расходится с Accepted ADR или актуальным design-контрактом, он не
переопределяет их молча: сначала оформляется новое решение или обновляется
контракт, затем roadmap.
