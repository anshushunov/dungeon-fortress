# Roadmap разработки

Статус: живой план  
Дата обновления: 2026-08-02  
Владелец направления: владелец проекта  
Источник конкретной работы: GitHub Issues и Pull Requests

## Зачем нужен этот документ

Roadmap показывает не все возможные механики, а **какую игровую гипотезу мы
проверяем сейчас, что уже доказано, в каком порядке стоит очередь работ и где
владелец выбирает дальнейшее направление**.

У roadmap нет календарных обещаний. Проект разрабатывается одним человеком с
агентами и движется короткими запускаемыми срезами. Поэтому следующий крупный
этап уточняется после человеческого playtest, а не планируется заранее до
деталей.

Стабильное продуктовое обещание находится в [`VISION.md`](VISION.md), главная
идея и core gameplay — в [`PITCH.md`](PITCH.md), текущее устройство механик — в
`docs/design/`, причины важных решений — в `docs/decisions/`. Этот документ
отвечает только на вопросы «где мы сейчас?», «что делается следующим?» и «какие
направления можно выбрать дальше?».

## Как владелец управляет направлением

После каждого playtest владелец даёт одну из команд:

| Команда | Что происходит |
|---|---|
| `CONTINUE` | Начинается следующий минимальный шаг текущего эксперимента. |
| `ADJUST: <что изменить>` | Следующий шаг не начинается; текущий slice исправляется и проверяется снова. |
| `PROMOTE: <направление>` | Указанное направление становится следующим активным прототипом. |
| `DEFER: <направление>` | Направление остаётся в roadmap, но не получает Issue. |
| `STOP` | Эксперимент закрывается без дальнейшего расширения. |
| `MERGE #N` | Владелец явно разрешает merge уже проверенного PR. |

Координационный чат записывает решение в **журнал ниже**, обновляет разделы
`NOW` и «Очередь» и только затем создаёт следующий implementation Issue.

## Журнал gate-решений

Журнал вынесен в [`GATE_DECISIONS.md`](GATE_DECISIONS.md): дата, решение
владельца, исход и последствия каждого decision gate. «Критерий отказа» ниже
опирается на этот журнал, а не на память участников.

## Правила движения

- **Одновременно проверяется столько gameplay-слайсов, сколько вопросов владелец
  способен развести на одном playtest, и не больше двух.** Это не ограничение
  стоимости: playtest — измерительный прибор, и два изменения симуляции,
  отвечающие на один вопрос, делают ответ владельца неинтерпретируемым.
  Связывающая величина — **число вопросов**, а не число слайсов; условия, при
  которых открывается второй слот, и названное ослабление ADR — правило 2
  раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md). Параллельно идут задачи других
  классов; правила параллельной работы и классы работы приняты
  [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md). **Потолка числа
  записывающих агентов нет**: он снят решением владельца 2026-08-02 —
  правило 28 раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md), см. журнал.
  Единственным ограничителем параллельной работы остаётся **партиция по
  файлам**: разрешающим правилом является набор файлов, а не область; число
  gameplay-слайсов бюджетом не задаётся вовсе. Раздел «Очередь»
  задаёт порядок gameplay-блоков, а не разрешение занять второй слот: он
  открывается правилом 2, а не наличием следующего блока в очереди.
- **Владелец играет пакетом.** Дефекты и обеспечивающая работа накапливаются и
  попадают в один playtest на границе gameplay-слайса, а не проверяются после
  каждого PR.
- **Команда для playtest выдаётся рабочей и вместе с тем, что по ней смотреть.**
  Владелец не обязан помнить, смержен ли PR, достраивать команду до
  работоспособной и догадываться, на что смотреть. Поэтому координатор даёт три
  вещи одновременно:
  1. **команду, которая запускается как есть** на машине владельца и указывает на
     рабочую копию, где проверяемое изменение действительно присутствует. Корень
     репозитория — только когда изменение уже в `main`; пока PR открыт — путь к
     его рабочей копии. Команда проверяется до выдачи, а не предлагается по
     памяти или по образцу из документации;
  2. **что нового будет видно** по сравнению с прошлым запуском — иначе владелец
     ищет отличия вслепую;
  3. **что именно проверить** — вопрос, на который этот playtest должен ответить,
     и который нельзя закрыть прогоном тестов.

  Правило появилось 2026-07-30 после двух промахов подряд в одной сессии: команда
  была выдана на корень репозитория, где проверяемого счёта партии ещё не было, и
  с GUI-бинарём движка вместо console-варианта, из-за чего не стартовала вовсе.
  Общая причина у обоих одна — команда собиралась по памяти и не проверялась на
  том состоянии, в котором её получит владелец. Документация тут ни при чём и
  оба раза была права: `ENVIRONMENT_SETUP.md` требует console executable, а
  `Resolve-GodotExecutable` предпочитает именно его, то есть GUI-вариант не
  предлагается нигде.

  Правило **расширяет** [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md) §5,
  который обязывал координатора дать однострочную команду запуска в блок-отчёте
  после авто-merge. Расширение в трёх вещах: правило действует для любой команды,
  а не только после авто-merge; команда проверяется до выдачи; к ней добавляются
  две обязательные строки. Поправкой к ADR 0014 расширение не оформляется:
  процессное правило живёт в этом разделе — см. правило «процессные правила живут
  здесь» в разделе [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md).
- Каждый gameplay-шаг заканчивается запускаемой игрой, зелёными проверками и
  playtest владельца.
- Следующий gameplay Issue не реализуется до решения
  `CONTINUE / ADJUST / PROMOTE / DEFER / STOP` и записи этого решения в журнал.
- **Отложенный `ADJUST`.** `ADJUST` разрешено принять как отложенный, если его
  исполнение целиком попадает в уже названный блок очереди. Тогда в журнале
  фиксируется, в какой именно блок он отложен, и слайс считается закрытым.
  Отложенный `ADJUST` продолжает считаться в счётчике критерия отказа. Без такой
  записи `ADJUST` блокирует запуск любого следующего блока.
- **Класс «блокирует проверяемость».** Обеспечивающая задача, которая мешает
  запустить проверки или воспроизвести состояние, не ждёт gate и не ждёт
  очереди: её ожидание дороже её исполнения. Партиции по файлам она при этом
  подчиняется наравне с любой другой записывающей задачей — потолок снят
  правилом 28, партиция нет. Класс присваивается явно в Issue, а не по желанию
  исполнителя.
- **Merge без владельца — два пути, и у каждого свои условия.** Первый —
  границы [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md) §5,
  второй — правило 7 «Темпа разработки», которое сознательно выходит за них по
  классу задачи. Путь ADR 0014: задачи классов
  `defect`, `support`, `blocks-verification` и `art` мержатся автоматически при
  зелёном CI, зелёном `verify.ps1`, зелёном независимом review и при наличии
  Issue с наблюдаемыми критериями готовности, если не затронуты контракт,
  словарь команд, ADR и продуктовые документы и если PR не вводит нового
  игрового поведения. Восстановление уже обещанного поведения исключением не
  является; если оно меняет картинку, координатор даёт однострочную команду
  запуска для ближайшего пакетного playtest. Рядом с этим путём действует
  правило 7 раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md): PR, который только
  исполняет уже принятое владельцем решение, мержится без второй просьбы
  независимо от класса — при тех же зелёных проверках и том же требовании Issue
  с критериями. Команда `MERGE #N` остаётся обязательной для всего, что
  возвращается владельцу по правилу 7; **с 2026-08-02 это три категории
  [правила 24](../engineering/DEVELOPMENT_PACE.md#автономия-координатора-2026-08-02) плюс PR, которому независимый
  review отказал в квалификации под правило 7** — прежний семипунктовый список
  там же зачёркнут.
- **Облегчённый цикл разработки.** Принято владельцем 2026-07-29: меньше
  итераций и меньше кругов review, чтобы обратная связь по игре приходила
  быстрее. Три следствия:
  1. **gameplay-слайс идёт одним PR**, когда его части бессмысленно проверять
     порознь. Волны без счёта владения дают playtest, на котором владелец снова
     не может сказать, хорошо ли он идёт, — это потраченное внимание без ответа;
  2. **глубина review назначается заранее, а не усмотрением по ходу.**
     Независимый review остаётся обязательным для каждого PR — это инвариант
     [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md) §4 и условие
     допустимости автоматического merge. Меняется только объём, и признак цены
     задан правилом 3 раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md): названные
     маркеры риска и объём контекста, который придётся поднять, — но **не** класс
     изменения, не размер diff и не наличие кода;
  3. **замечание, не ломающее критерий готовности, не возвращается новым кругом
     доработки.** Оно становится Issue, если у него есть наблюдаемое последствие,
     и записью в [`DEBT_LEDGER.md`](../engineering/DEBT_LEDGER.md), если
     последствия нет, — правило 4 того же раздела. Возврат исполнителю остаётся
     для замечаний, из-за которых критерий не выполнен.
  Пункт 3 сужает формулировку ADR 0014 §4 «обычные замечания координатор
  возвращает исполнителю сам»; сужение названо здесь и в ADR не переносится —
  процессное правило живёт в этом разделе. Облегчённый цикл пережил блок среза 1
  и последующие блоки без единого revert'а в `main`, поэтому действует; если он
  приведёт к откату, он отменяется, и это записывается в журнал.
- Claude Code является основным пишущим агентом. Координационный чат ведёт
  roadmap, декомпозицию, продуктовые развилки и приёмку.
- **Изображения генерирует Codex.** Координационная сессия формулирует запрос —
  что нужно, в какой геометрии, в каком состоянии, — но не создаёт изображения
  сама. Подробности в [`ANIMATION_PIPELINE.md`](../art/ANIMATION_PIPELINE.md).
- Сгенерированные ассеты сначала получают prompt и provenance, затем передаются
  implementation-агенту для подключения.
- Roadmap обновляется на decision gate, а не после каждого технического коммита.
- Изменение tuning-слоя (числа, формулы, пороги, планировка) не требует
  обновления контракта и ADR — только зелёных проверок и записи в PR. Изменение
  инварианта требует ADR. Граница — в
  [ADR 0010](../decisions/0010-contract-invariants-and-tuning.md).
- Новые движки, SDK, MCP и генеративные инструменты проверяются по
  `docs/engineering/TECH_RADAR.md` на границе прототипов либо перед крупным
  обновлением, но не ради каждой новой версии.

### Темп разработки

Правила темпа, review, параллелизма и автономии координатора вынесены в
[`DEVELOPMENT_PACE.md`](../engineering/DEVELOPMENT_PACE.md). Ссылки вида
«правило N» ведут туда.

### Чего мы не делаем

Эти формулировки дешевле принять заранее, чем спорить о них на decision gate.

- **Не строим симуляцию ради симуляции.** Система без наблюдаемого игроком
  последствия не входит в срез.
- **У каждой новой системы есть бюджет читаемости в UI.** Если её нельзя
  показать игроку, её не добавляем.
- **Не универсализируем заранее.** Один объект и одно последствие вместо
  каталога и редактора.
- **Не подменяем игру подготовкой.** Работа над представлением и инструментами
  не создаёт продуктового доказательства — правило 1 раздела
  [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md) требует фичу в каждом блоке. Пока
  активного gameplay-слайса нет, новая обеспечивающая задача не начинается.
- **Pre-production не длится бесконечно.** См. критерий отказа ниже.

### Критерий отказа

> Два `ADJUST` подряд по одной гипотезе без улучшения на playtest закрывают
> возможность третьего. Владелец выбирает `PROMOTE` или `STOP`.

Счёт ведётся по колонке `ADJUST` журнала gate-решений. Без записанного счётчика
это правило не срабатывает никогда, а `STOP` остаётся словом в словаре.

## Целевая игра

Продуктовый фокус и ответ на вопрос «что здесь самое интересное» находятся в
[`PITCH.md`](PITCH.md) — документ принят владельцем 2026-07-28. Roadmap ему не
противоречит и не пересказывает его: обоснование порядка срезов живёт в разделе
11 питча, здесь остаётся только очередь и состояние.

Обещание в одном абзаце:

> Игрок вырубает из скалы собственное подземное владение и населяет его
> существами, которые живут своей жизнью. Он не отдаёт приказов — он проектирует
> пространство, задаёт правила и иногда вмешивается рукой. Владение растёт,
> начинает притягивать новых обитателей, обзаводится производством, обороной и
> внутренними конфликтами. Регулярно приходят те, кто хочет забрать накопленное.
> Каждая партия оставляет несколько историй, которые игрок может пересказать
> словами.

Критерий приоритизации, вытекающий из питча: **работа, усиливающая столкновение
плана и людей, важнее работы, усиливающей подготовку или проверку.**

Игры, за которыми стоит идти за конкретными решениями, и то, за чем именно к
каждой из них идти, перечислены в [`REFERENCES.md`](REFERENCES.md).

### Milestones

Это не календарный план и не порядок работ. Это направление, по которому на
каждом gate можно сравнивать кандидатов между собой.

| Milestone | Содержание | Критерий выхода |
|---|---|---|
| **A0 — петля замкнулась** | Несколько волн нарастающей силы в одном владении, явная цена поражения, счёт владения, у партии есть конец. | Владелец доигрывает одну партию до конца и может назвать, что он сделал не так. |
| **A — Vertical Slice** | Одна сессия ~30 минут. Копание и функциональные комнаты; 2–3 связанные производственные цепочки; несколько волн нарастающей угрозы; явный счёт владения; нарративная лента; рука хозяина; свет, автотайлинг, интерполяция, звук. | Владелец играет три партии подряд по собственному желанию и может пересказать историю из каждой; **в каждой партии он называет конкретное существо, которое изменило его план, объясняет его мотив и своё решение по нему.** |
| **B1 — Power Loop** | Сессия ~1 час. Полная модель страх/выгода/обида; приспешник и указы; притязание и переворот; приток населения от славы и условий владения; несколько архетипов характера; пороги славы открывают возможности, а не только угрозу. | Владелец выбирает приспешника по характеру и политическому риску, а не по максимальному навыку, и может назвать внутренний конфликт партии и его причину. |
| **B2 — Material Depth** | Сессия 1–2 часа. Экипировка и мастерская; двери и ловушки; комнаты со свойствами; полная форма локализованных травм; роли ближнего и дальнего боя. | Партия играется без участия разработчика и заканчивается по решению игрока, а не по таймеру; владелец может назвать, какой предмет или постройка решили исход волны. |
| **C — Content Alpha** | Кампания. Несколько видов существ с разными потребностями; процедурная карта либо набор авторских; глобальный слой (один внешний ресурс, одна угроза, одно решение); art-direction gate и production-пайплайн; баланс и кривая сложности. | Посторонний играет 2 часа без объяснений. |

A0 добавлен потому, что между текущим состоянием и milestone A лежит около
десяти систем: сравнивать с ним прогресс невозможно, и любой блок кажется
одинаково далёким от цели. A0 — ближайшая отметка, до которой реально
дотянуться, и она совпадает со срезом 1 питча.

B разделён 2026-07-30: при едином B социальная и материальная работа
конкурировали за один milestone, и более привычная материальная выигрывала бы
приоритет у недоказанной социальной — ровно тот механизм, которым проект
сползает в обычный colony sim. B2 не удалён, а поставлен за B1.
Сохранение/загрузка — инфраструктурная возможность для удлинения партии, а не
критерий milestone; решение о ней принимается на границе B1→B2.

Постоянные ограничения на всех milestone: 2D и посильный объём контента для
одного человека с агентами; детерминизм, replay и headless-проверки не
приносятся в жертву ни одной фиче; каждая новая система имеет бюджет
читаемости в UI.

## Текущее состояние

### COMPLETED — проверяемый фундамент

| Блок | Результат |
|---|---|
| Agent-ready foundation | Godot/.NET, фиксированный seed, headless tests, replay, structured state/logs, screenshots и domain MCP. |
| Prototype 1: Prepare for Raid | Пищевая экономика, потребности, тренировка, наблюдение, автоматический набег и бой связаны непрямыми командами. |
| Readability and first art pass | Исправлены кисть, HUD, объяснение Quarters и alive/downed; подключены первые generated goblin sprites. |
| Dungeon shaping — step 1 | Игрок обозначает скалу, существа автономно копают, `Rock → Floor`, появляется loose Stone. |
| Dungeon shaping — step 2 | Игрок создаёт MaterialStockpile; Stone проходит `loose → carried → stored` с capacity, reservations и conservation invariant. |
| Dungeon shaping — step 3 | Игрок размечает blueprint тренировочного поста, камень доставляется и **расходуется**, построенный пост создаёт настоящие Drill jobs. ADR 0012. |
| Presentation pass A | Рендер отделён от тика и интерполирует движение по `alpha`; HUD переверстан на Control-контейнеры; слой представления покрыт тестами без запуска движка. |
| UI control pass | Панель на иконках, подсказки, прямоугольное выделение мышью; первый icon pack. |
| Ремонт обратной связи | Разметка, blueprint, зоны и смена приоритета видны немедленно, включая паузу и `STEP`: проекция «отправлено, ещё не применено» поверх канонического состояния. Развязаны запуск domain MCP и solution-сборка. |
| **Срез 1 — повторяющийся цикл угрозы и счёт владения** | Партия стала последовательностью из четырёх волн нарастающей силы с окном обычной жизни между ними; три формы конца — выстояло, разорено, пало; два числа владения — не убывающая **дурная слава**, задающая силу следующей волны, и **сила владения** как готовность. Ухудшение владения строго не улучшает счёт. `verify.ps1` стадийный, единая стратегия `.uid`. ADR 0013, ADR 0015. |

### NOW — блок «момент истины»: срез 3 идёт, рядом восемь обеспечивающих задач

**Состояние на 2026-08-07.** Между этим блоком и предыдущим прошёл
**оптимизационный блок** — **шестнадцать** смерженных PR, ни одного игрового
изменения: маршрутизация моделей по классу задачи, разрез `Main.cs`,
`PrototypeWorld.cs` и design-контракта, замер расхода токенов, `verify.ps1`.
Число снято командой, а не перенесено:

```bash
gh pr list --state merged --limit 40 --json number,mergedAt \
  -q '[.[] | select(.mergedAt > "2026-08-06T12:00:00Z")] | length'
```

→ `16`; поимённо — `#277, #278, #288, #289, #290, #291, #292, #293, #297, #298,
#299, #300, #304, #305, #308, #309`.
Состав и одиннадцать решений — в [`GATE_DECISIONS.md`](GATE_DECISIONS.md), здесь
не пересказываются. Ни один его эффект **не измерен «после»**: срез назначен на
2026-08-21, инструмент — `scripts/token-budget-report.ps1`.

**Блок собран вокруг gameplay-слайса, как требует правило 1.** Слайс —
[#312](https://github.com/anshushunov/dungeon-fortress/issues/312), срез 3
«момент истины»: шаг 3 порядка доказательства [`PITCH.md`](PITCH.md) §11 и
механика §6.11. Условие входа выполнено —
[ADR 0019](../decisions/0019-verdict-not-order.md) принят 2026-08-01.

**Скоуп слайса выбран владельцем 2026-08-07 — полный по питчу**, против двух
предложенных сужений. Отклонённое сужение «карточки без последствия» названо в
журнале вместе с причиной отказа: срез, который не может провалиться, ничего не
измеряет.

| Что | Класс | Состояние |
|---|---|---|
| [#312](https://github.com/anshushunov/dungeon-fortress/issues/312) срез 3: три величины, карточки, вердикт, последствие | `tier:deep`, gameplay | `claimed` |
| [#295](https://github.com/anshushunov/dungeon-fortress/issues/295) геометрия отрисовки мира не пинается ничем | `tier:deep`, представление | `claimed` |
| [#311](https://github.com/anshushunov/dungeon-fortress/issues/311) три находки в реестр плюс разбор просроченной записи | `tier:fast` | `claimed` |
| [#310](https://github.com/anshushunov/dungeon-fortress/issues/310) тест замера расхода подключён к `verify.ps1` | `tier:fast` | `claimed` |
| [#306](https://github.com/anshushunov/dungeon-fortress/issues/306) `verify.ps1` оставляет свой временный каталог | `tier:fast` | `claimed` |
| [#250](https://github.com/anshushunov/dungeon-fortress/issues/250) docstring `DescribeMemory` несёт невоспроизводимое число | `defect`, `tier:fast` | `claimed` |
| [#296](https://github.com/anshushunov/dungeon-fortress/issues/296) покрытие двух контрольных сумм записано фактом | `tier:standard` | `claimed` |
| [#253](https://github.com/anshushunov/dungeon-fortress/issues/253) правило 17 получает механизм вместо текста | `tier:standard` | `claimed` |
| [#248](https://github.com/anshushunov/dungeon-fortress/issues/248) чем проверяется правка круга доработки | `tier:standard` | `claimed` |

Состояния сняты `gh issue view <N> --json state,labels` **2026-08-07 в 11:19
UTC**; отметка названа не для красоты — на PR #255 правка была верна в момент
коммита и стала ложной через четыре с половиной минуты, когда домержилась
соседняя дорожка.

**Партиция разведена по файлам, и две правки понадобились уже при выдаче.**
[#237](https://github.com/anshushunov/dungeon-fortress/issues/237) придержана:
метка `ready` снята, потому что она и #295 претендуют на `Main.Rendering.cs`, а
слепок draw-вызовов перехватывает файл целиком и именованными членами не
разводится. У [#253](https://github.com/anshushunov/dungeon-fortress/issues/253)
снято «либо-либо» в партиции: `MULTI_AGENT_WORKFLOW.md` держит #248, и выбор
файла исполнителем нарушал бы инвариант
[#182](https://github.com/anshushunov/dungeon-fortress/issues/182). Общий
`scripts/verify.ps1` держат три задачи по именованным членам, порядок merge
назван до выдачи: #310 → #306 → #253, rebase на координаторе (ADR 0017 §3).

**Что открыто и ждёт владельца** — три вопроса в coordination inbox
[#33](https://github.com/anshushunov/dungeon-fortress/issues/33): правило про
контрольные суммы (часть B #296), правило уборки worktree после merge (абзац
изъят из PR #297 и нигде не записан) и слепота счётчика токенов у агентов. Все
три — процессные, и все три упираются в мораторий правила 11.

---

**Состояние на 2026-08-06.** Владелец сыграл дуэльную сцену после merge PR #268
(коммит `8850209`) и назвал результат: «работает как нужно, но визуально прямо не
очень… нужно сделать чтобы прямо приятно было смотреть». Следом он усомнился в
самом направлении, и разбор дал **решение владельца 2026-08-06: бою ставится
потолок — читаемый, а не красивый**. Строка в журнале gate-решений выше, основания
и порядок правок — [#272](https://github.com/anshushunov/dungeon-fortress/issues/272).

| Что | Класс | Состояние |
|---|---|---|
| [#259](https://github.com/anshushunov/dungeon-fortress/issues/259) тело разворачивается к тому, кто его бьёт | `defect`, представление | ✅ PR #268, коммит `8850209` |
| [#266](https://github.com/anshushunov/dungeon-fortress/issues/266) + [#261](https://github.com/anshushunov/dungeon-fortress/issues/261) дата gate и шесть просроченных записей реестра | `defect` + `docs` | ✅ PR #267, коммит `fadaf17` |
| [#263](https://github.com/anshushunov/dungeon-fortress/issues/263) удар плохо видно на рабочем зуме | `support`, представление | **сужена до замера** решением 2026-08-06; правка не делается |
| [#270](https://github.com/anshushunov/dungeon-fortress/issues/270) запись реестра невидима для собственной переписи | `defect` | **`claimed` внешней дорожкой** 2026-08-05 в 20:09 UTC (`gh api …/issues/270/timeline`) |
| [#272](https://github.com/anshushunov/dungeon-fortress/issues/272) запись решения о потолке | координационный | эта правка |

**Почему потолок, а не продолжение.** Узкое место измерено и названо: весь размен
живёт **внутри одного тика** — `TicksPerSecond = 6.0` даёт 167 мс, на скорости 0.5
— 333 мс, при индустриальной норме ощущаемого удара 500–800 мс. Значит «не хватает
кадров» — следствие, а не причина: промежуточные позы в том же окне делают удар
плавнее и оставляют его быстрым. Дешёвый слой обратной связи при этом **уже снят**
([#210](https://github.com/anshushunov/dungeon-fortress/issues/210)): hit-stop,
вспышка, искры и числа урона лежат в `BlowEffects.cs`. Оставшееся лечение —
отставание представления и доработка раскроя — покупает красоту, а не ответ на
вопрос «интересно ли играть», и по [`PITCH.md`](PITCH.md) §3 и §7 уступает работе,
усиливающей столкновение плана и людей.

**Что отложено, а не отвергнуто.** Отставание представления на тик-два: приём
Source `cl_interp`, живёт в слое представления, снапшот и checksum не трогает.
Строка с условием возврата — в «Кандидатах слоя отрисовки».
[#260](https://github.com/anshushunov/dungeon-fortress/issues/260) и
[#262](https://github.com/anshushunov/dungeon-fortress/issues/262) переведены из
очереди в кандидаты; масштаб остаётся **170 %**, возврат к 150 % отклонён.

**Следующий блок — срез 3 «момент истины»**: карточки после волны, решение
наградить/наказать/игнорировать, последствие в следующей волне, все три величины —
страх, выгода, обида. Балансный слайс, идёт один. Условие входа выполнено:
[ADR 0019](../decisions/0019-verdict-not-order.md) принят.

---

**Состояние на 2026-08-04.** Проба закончена, владелец сыграл, **gate дан:
`CONTINUE`** — строка в журнале gate-решений выше. Ответ дословно: «на демке
плохо видно но движения в целом ок — виден переход между позами. Правда левый
стоит задом почему то». Способ производства анимации принят; условие замены
ADR 0020 не наступило.

**Из ответа вышли две работы, и они не смешиваются.** «Левый стоит задом» — это
дефект, а не мнение о риге: разворот к противнику сегодня получает **только
бьющий**, цель удара своего разворота не получает и остаётся в прежнем
направлении. Заведён [#259](https://github.com/anshushunov/dungeon-fortress/issues/259).
Следующий кусок владелец назвал сам — перевести на риг **все** состояния и
посмотреть **массовый бой** без скрытия тел: [#260](https://github.com/anshushunov/dungeon-fortress/issues/260),
и она ждёт merge #259, иначе вопрос playtest будет отравлен известным дефектом.

**Gate сдвинул счётчик реестра долга.** Пятый маркер сделал шесть записей
просроченными по правилу трёх gate; разбор — [#261](https://github.com/anshushunov/dungeon-fortress/issues/261).

| Что | Класс | Состояние |
|---|---|---|
| [#243](https://github.com/anshushunov/dungeon-fortress/issues/243) гоблин по частям с пивотами | `tier:art`, Codex | ✅ PR #249, коммит `f178500` |
| [#244](https://github.com/anshushunov/dungeon-fortress/issues/244) риг в игре: цепочка удара, отдача обеих сторон, дуэльная сцена | `tier:deep`, представление | ✅ PR #256, коммит `1e58568` |
| [#245](https://github.com/anshushunov/dungeon-fortress/issues/245), [#251](https://github.com/anshushunov/dungeon-fortress/issues/251), [#254](https://github.com/anshushunov/dungeon-fortress/issues/254), [#257](https://github.com/anshushunov/dungeon-fortress/issues/257) | координационные | ✅ PR #246 `2d8fc09`, #252 `51b4fc7`, #255 `098626b`, эта правка |

**Что владелец увидит и чем это смотреть.** Дуэльная сцена запускается так:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-game.ps1 -DemoDuel
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-game.ps1 -DemoDuel -FlatBody
```

Вторая команда рисует ту же сцену прежним паком склеенных поз — это A/B, которого
требует условие пересмотра ADR 0020. `[SPACE]` гонит размен, `[F]` листает удар
по одной двенадцатой тика, `[S]` проигрывает целый тик. Если Godot не в `PATH`,
обеим командам нужен `-GodotPath <путь к godot console exe>`.

**Что уже известно про ответ, до playtest.** Независимый review PR #256 смотрел
кадры и назвал три вещи. Риг **лучше** склеенной позы в двух местах: цель больше
не вздрагивает **до** удара — на плоском паке налётчик уже отброшен в позу
`flinch`, пока копьё ещё идёт вверх, — и копьё **едет**, тогда как плоский пак
весь тик держит один рисунок. Риг **хуже** в одном: амплитуда. Нарисованные
художником `windup` и `flinch` резче любого кадра цепочки; размах руки читается,
силуэт тела почти нет.

**Named risk ADR 0020 подтвердился частично и измерен, а не описан.** Щель —
фон, видимый **сквозь** тело, — в покое ноль по построению, в худшей фазе
(проводка) **782 px²**, то есть **21 world px²**. При этом **сквозного фона
проверяющий не увидел ни на одном из четырёх кадров** на рабочем зуме — эту
клаузу его вердикта здесь нельзя опускать, иначе измеренная величина читается как
видимая дыра. Что видно вместо неё: край, который не до конца сходится, — рваная
ступенька там, где дальняя рука примыкает к плечу и бедру, и рука читается
отдельным комком, а не приделанной конечностью. Форма цепочки выведена
из замера, а не из вкуса: замах поднимает копьё, а удар опускает, потому что
раскрой даёт 21–55 px² щели вверх против 2393 px² вниз.

**Сцена скрывает все тела, кроме двух, и это измерение, а не предпочтение.** В
журнале нет момента, на который можно просто навести камеру: у выбранного удара
(тик 2061) в двух клетках стоят ещё **пять** тел, у самого пустого удара сессии —
три, но он не боковой и читался бы как одно тело за другим. Скрытые тела
продолжают драться и дают тот же checksum; меняются только пиксели.

**Что осталось незакрытым и почему это не дефект.** `work`, `downed` и бегство
остались плоскими позами — non-goal пробы. Поведение в толпе не мерялось: контур
стороны вырос с 8 до 56 draw-вызовов на тело, и наблюдаемого последствия сегодня
нет — `--frame-pacing` идёт headless с `--fixed-fps` и просадку по построению не
видит. Обе находки лежат в [`DEBT_LEDGER.md`](../engineering/DEBT_LEDGER.md), и
исход им назначил сам review.

---

**Состояние на 2026-08-04, ночь.** Слот занят **пробой**
[ADR 0020](../decisions/0020-body-animation-cutout-rig.md) — решение принято и
смержено (PR #242, коммит `5505d0d`, `gh pr view 242 --json state,mergeCommit`);
независимый review признал правило 7 к нему неприменимым, потому что ADR не
исполняет решение, а является его записью.

**Вопрос, ради которого блок собран, владелец сформулировал сам:** увидеть
одного полностью анимированного персонажа, удар и отдачу, и понять, **приятно ли
на это смотреть**. Это вопрос playtest, а не критерий готовности задачи —
отвечать на него будет владелец, глядя на дуэль крупно.

| Что | Класс | Состояние |
|---|---|---|
| [#243](https://github.com/anshushunov/dungeon-fortress/issues/243) гоблин по частям с пивотами | `tier:art`, исполнитель — Codex | ✅ PR #249, коммит `f178500`: семь частей и `goblin_cutout_rig_v1.json` в дереве |
| [#244](https://github.com/anshushunov/dungeon-fortress/issues/244) риг в игре: цепочка удара, отдача обеих сторон, дуэльная сцена | `tier:deep`, представление | вход открыт merge #243; **задача выдана** координационной сессией 2026-08-04 |
| [#245](https://github.com/anshushunov/dungeon-fortress/issues/245) документы после принятия ADR 0020 | `tier:standard`, координационный | ✅ PR #246, коммит `2d8fc09` |
| [#251](https://github.com/anshushunov/dungeon-fortress/issues/251) состояние задач после merge #246 и #247 | `tier:standard`, координационный | ✅ PR #252, коммит `51b4fc7` |
| [#254](https://github.com/anshushunov/dungeon-fortress/issues/254) реестр держит повышенную запись правила 17 | `tier:standard`, координационный | эта правка |

Метки получены `gh issue view <N> --json state,labels`, состояние PR —
`gh pr view <N> --json state,isDraft`; **отметка измерения — 2026-08-04 11:30
UTC**, и она названа не для красоты: между коммитом этой ветки и её review
состояние #243 и #244 разошлось за четыре с половиной минуты, что поймал
независимый review PR #255. Строка про выданную задачу поэтому говорит о
**действии координатора**, а не о метке: метка `claimed` появится на #244, когда
исполнитель займёт её первым шагом, и утверждение от этого не станет ложным.

**Порядок назван до выдачи и партиция между задачами пробы чистая:** #243
владеет ассетами и `docs/art/**` и не трогает код; #244 владеет `Main.cs`, слоем
представления и тестами и не трогает ассеты. Запрос на генерацию для #243 —
метод резки, источник, геометрия и требование к машиночитаемым пивотам —
опубликован комментарием в самой Issue, шагом 1 пайплайна
[`ANIMATION_PIPELINE.md`](../art/ANIMATION_PIPELINE.md).

**Первая половина пробы закрыта, и она принесла не только части.** Свидетельства
#243 лежат в `evidence/243-goblin-cutout-contact-sheet.png` и
`evidence/243-goblin-cutout-joint-rotation-check.png`. Первое показывает, что
реконструкция `idle` из частей неотличима от склеенной позы v2 — базовая стойка
риска не несёт. Второе показывает, что **щели в суставах появляются уже на малых
углах**: при `arm_near −15°` и `−10°` между плечом и торсом видна светлая полоса,
при `leg_near +10°` — просвет в бедре, а сборщик проверял диапазон ±15°, тогда
как цепочка удара потребует больше. Это не дефект #243, а named risk самого
[ADR 0020](../decisions/0020-body-animation-cutout-rig.md), и #244 обязана его
проверить, а не подтвердить: законных выходов три — уменьшить угол, увести
разрыв за перекрытие, либо **назвать проблему неустранимой в этом риге**. Третий
выход не провал: ради него ADR и принимался с пробой.

**Правило 1 соблюдено, и это стоит назвать явно:** фича блока — #244, хотя её
класс `support`. Владелец получает из блока видимое изменение и вопрос playtest с
gate, а не одну подготовку.

**Названный риск пробы, который она обязана проверить, а не подтвердить:**
скелет из частей на 68 px может выглядеть **хуже** склеенной позы — стыки видны,
поворот руки вокруг сустава читается как поломка. Проект уже дважды ошибся в эту
сторону: кольцо стороны и переполненная легенда выглядели разумно до того, как их
увидели в игре. Условие пересмотра записано в самом ADR 0020.

**Свободны `ready` для любой дорожки:**
[#231](https://github.com/anshushunov/dungeon-fortress/issues/231) (ложный
`mismatch` прогона правила 31),
[#248](https://github.com/anshushunov/dungeon-fortress/issues/248) (чем
проверяется правка круга доработки),
[#250](https://github.com/anshushunov/dungeon-fortress/issues/250) (docstring
`DescribeMemory` несёт невоспроизводимое число),
[#253](https://github.com/anshushunov/dungeon-fortress/issues/253) (правило 17
обеспечено только текстом — нужен механизм, а не формулировка; повышена из
реестра долга, потому что её условие сработало третьим случаем).
[#215](https://github.com/anshushunov/dungeon-fortress/issues/215) **закрыта** —
PR #247, коммит `5d971e4`: три ложных утверждения документов приведены к факту.
Её файлы `PROTOTYPE_GRAYBOX.md` и `InspectorText.cs` освободились, и #250 берёт
второй из них по именованному члену — docstring `DescribeMemory`.
[#237](https://github.com/anshushunov/dungeon-fortress/issues/237) **придержана**
и в перечне свободных не стоит: метка `ready` снята с неё 2026-08-04 в 11:26 UTC
(`gh api repos/anshushunov/dungeon-fortress/issues/237/timeline`) на время работы
#244 — обе держат `BodyMotion` и поворот тел, и развести их именованными членами
нельзя. Метка вернётся после merge #244.

**Что открыто и ждёт решения владельца** — остаток измерения #228: «работы нет» и
«выход заперт телами» не соперники, а звенья одной цепи, те же **1376** тиков без
исключения. Перенос парковки уберёт толчею и не уберёт картину. Это
[#72](https://github.com/anshushunov/dungeon-fortress/issues/72), а не
[#76](https://github.com/anshushunov/dungeon-fortress/issues/76); числа — в
контракте §4.1 и `evidence/228-*.json`.

---

**Состояние на 2026-08-04, вечер — блок #221/#228 закрыт.** Второй блок дня
**закрыт**: смержены три PR,
открытых нет (`gh pr list --state open` — пусто).

| Что | Состояние |
|---|---|
| [#228](https://github.com/anshushunov/dungeon-fortress/issues/228) простой в покоях — **gameplay-слайс**, задача-вопрос | ✅ PR #236, коммит `fc1e2cb` |
| [#221](https://github.com/anshushunov/dungeon-fortress/issues/221) вертикаль 4 #77: процедурная анимация | ✅ PR #235, коммит `66a5ebc` |
| [#222](https://github.com/anshushunov/dungeon-fortress/issues/222) мелкие метки и легенда | ✅ PR #232, коммит `3018d7c` |

Состояние получено `gh issue view <N> --json state` и `gh pr list --state merged
--json number,mergeCommit` на момент правки; все три Issue `CLOSED`.

**Gate этот блок не даёт, и это не пропуск.** Вопрос слайса #228 был
измерительным: задача **не меняла поведение**, `src/**` в её диффе отсутствует,
поэтому играть не во что. Картинку меняет #221 — она класса `support` и своего
gate не требует, но владелец получает команду запуска для ближайшего playtest.
Следующий gate придёт от задачи, которая изменит поведение.

**Что вышло из блока в очередь, а не осталось в комментариях PR:**
[#237](https://github.com/anshushunov/dungeon-fortress/issues/237) — полярность
поворота закреплена текстом гарда, а текст переименовывается; пять мутантов
подтверждающего круга проходят.

**Что открыто и ждёт решения владельца.** Измерение #228 показало, что «работы
нет» и «выход заперт телами» — не соперники, а звенья одной цепи: те же **1376**
тиков, без единого исключения. Значит перенос парковки уберёт толчею и не уберёт
картину — половина времени в покоях приходится на тики, когда сопоставлению
нечего дать. Это [#72](https://github.com/anshushunov/dungeon-fortress/issues/72),
а не [#76](https://github.com/anshushunov/dungeon-fortress/issues/76). Числа — в
контракте §4.1 и `evidence/228-*.json`, копии здесь нет сознательно.

**Свободны `ready` для любой дорожки:**
[#231](https://github.com/anshushunov/dungeon-fortress/issues/231),
[#237](https://github.com/anshushunov/dungeon-fortress/issues/237).
[#215](https://github.com/anshushunov/dungeon-fortress/issues/215) в этот список
попала ошибочно: метка `claimed` стояла на ней с 2026-08-03
(`gh api repos/anshushunov/dungeon-fortress/issues/215/timeline` — событие
`labeled` `claimed`), то есть задача была занята уже в момент, когда абзац
писался. Исправлено 2026-08-04 при разборе состояния перед пробой ADR 0020 — там
же, где та же ошибка убрана из строки очереди про закрытый блок #221/#228.

**Почему абзац «Свободными висят `ready`» в разделе «Состояние на 2026-08-04,
день» ниже не правится**, хотя тоже называет #215 свободной: он **перечисляет
состав на момент сборки блока**, а не поддерживает список свободных задач.
Маркер эпохи стоит в нём самом — рядом с #215 назван #222, свободный при сборке
и закрытый сейчас; правка стёрла бы то, из чего блок собирался.

**Датой это не обосновывается, и обратное было бы неправдой.** Метка `claimed`
появилась на #215 2026-08-03 в 22:05 UTC (`gh api
repos/anshushunov/dungeon-fortress/issues/215/timeline`), а оба текста написаны
позже: абзац — коммитом `c94e926` в 22:36 UTC того же дня, строка очереди —
коммитом `7f0e90b` через 3 ч 16 мин после метки (`git log --format='%h %aI' -S…
-- docs/product/ROADMAP.md`). Ложными в момент написания были **оба**. Разница
между ними не во времени, а в назначении: строка очереди ведёт текущее состояние
и потому исправлена, абзац — снимок сборки и потому оставлен. Первая редакция
этого объяснения строила его на времени написания и называла разрыв «сутками»;
и то и другое опровергнуто независимым review PR #246 прогоном по дереву.

---

**Состояние на 2026-08-04, день — блок #201/#210.** Блок **закрыт**: gate-решение `CONTINUE` по слайсу
[#201](https://github.com/anshushunov/dungeon-fortress/issues/201) дано владельцем
на playtest, счётчик `ADJUST` остаётся 0 — строка журнала 2026-08-04, там же
названо, какая половина ответа владельца вопросом слайса не является. Смержены
все семь работ блока; открытых PR нет (`gh pr list --state open` — пусто).

| Что | Состояние |
|---|---|
| [#201](https://github.com/anshushunov/dungeon-fortress/issues/201) существо без работы уходит с места боя — gameplay-слайс | ✅ PR #217, коммит `51302ba` |
| [#210](https://github.com/anshushunov/dungeon-fortress/issues/210) удар читается на экране — фича блока | ✅ PR #218, коммит `4af03a5` |
| [#196](https://github.com/anshushunov/dungeon-fortress/issues/196) числа тела PR проверяются прогоном | ✅ PR #216, коммит `472fe06`; стало правилом 31 |
| [#183](https://github.com/anshushunov/dungeon-fortress/issues/183) ослабление ADR без поимённого списка | ✅ PR #224, коммит `a830647` |
| [#159](https://github.com/anshushunov/dungeon-fortress/issues/159) срок годности записи реестра | ✅ PR #223, коммит `49e48b1` |
| [#202](https://github.com/anshushunov/dungeon-fortress/issues/202), [#207](https://github.com/anshushunov/dungeon-fortress/issues/207) | ✅ PR #213 `cd6056f`, PR #214 `fe08076` |
| [#225](https://github.com/anshushunov/dungeon-fortress/issues/225) claim Issue первым шагом | ✅ PR #226, коммит `74aa56d` |

Состояние получено `gh issue view <N> --json state` и
`gh pr list --state merged --json number,mergeCommit` на момент правки; все семь
Issue `CLOSED`. Правила 29–31 приняты тем же днём (PR #227, коммит `41bccf0`).

**Чем блок продолжился** (состав следующего блока — в разделе выше, здесь только
то, что было решено этим). Gameplay-слайсом стал
[#228](https://github.com/anshushunov/dungeon-fortress/issues/228): после расхода
группа стоит в покоях, и неизвестно, что именно её там держит. Задача-вопрос,
взята **решением владельца 2026-08-04** — это вторая половина его ответа на gate
по #201, и до измерения любое лечение было бы угадыванием. Рядом
[#221](https://github.com/anshushunov/dungeon-fortress/issues/221) вертикаль 4
#77 — процедурная анимация, класс `support`, своего gate не требует. Обе вела
основная сессия, обе `tier:deep`; обе смержены, см. состояние вечера выше.

Правило 2 не нарушено: gameplay-слайс **один**. Партиция разводит их без общих
файлов — #228 держит `src/DungeonFortress.Simulation/**` и контракт, #221
держит `src/DungeonFortress.Game/Main.cs` и слой представления.

Свободными висят `ready`, к блоку не привязанные:
[#215](https://github.com/anshushunov/dungeon-fortress/issues/215) (три ложных
утверждения документов),
[#222](https://github.com/anshushunov/dungeon-fortress/issues/222) (мелкие метки
на телах без строки легенды) и
[#231](https://github.com/anshushunov/dungeon-fortress/issues/231) (прогон
правила 31 даёт ложный `mismatch` на русском заявлении). Первые две пишут в
`Main.cs` вместе с #221, разведены по именованным членам по правилу 29;
**порядок merge назван до выдачи: #215 → #222 → #221**, rebase на общих файлах
делает координатор. **Порядок изменён тем же днём на #221 → #222 → #215** и в
этом виде исполнен: #221 оказался готов и зелен, а #222 ушёл на круг доработки.
Смена порядка — такое же решение координатора, как первоначальное назначение,
поэтому названа здесь, а не в чате.

---

**Состояние на 2026-08-03.** Блок собран координационной сессией; скоуп согласован
владельцем до планирования, тремя ответами: тяжёлую фичу берёт основная сессия,
ширина блока средняя, балансный долг симуляции в блок не входит. Строка журнала —
2026-08-03, «Скоуп блока #201/#210».

| Что | Кто ведёт | Файлы |
|---|---|---|
| ✅ [#201](https://github.com/anshushunov/dungeon-fortress/issues/201) существо без работы уходит с места боя — **gameplay-слайс** | основная сессия | смержена PR #217, коммит `51302ba` |
| ✅ [#210](https://github.com/anshushunov/dungeon-fortress/issues/210) вертикаль 3 #77: удар читается на экране — **фича блока** | внешняя дорожка, `tier:deep` | смержена PR #218, коммит `4af03a5` |
| ✅ [#202](https://github.com/anshushunov/dungeon-fortress/issues/202) команда worktree в двух местах | `tier:fast` | смержена PR #213, коммит `cd6056f` |
| ✅ [#196](https://github.com/anshushunov/dungeon-fortress/issues/196) расхождение тела PR с деревом ловится прогоном | `tier:standard` | смержена PR #216, коммит `472fe06` |
| ✅ [#207](https://github.com/anshushunov/dungeon-fortress/issues/207) `test-take-task.ps1` в стадию `scripts` | `tier:fast` | смержена PR #214, коммит `fe08076` |
| ✅ [#183](https://github.com/anshushunov/dungeon-fortress/issues/183) ослабление ADR держится на поимённом списке | `tier:standard` | смержена PR #224, коммит `a830647` |
| ✅ [#159](https://github.com/anshushunov/dungeon-fortress/issues/159) реестр долга: срок годности записи | `tier:standard` | смержена PR #223, коммит `49e48b1` |

**Партиция проверена по коду, а не по названиям областей.** #210 читает удары из
канонического журнала (`combat_attack` с `raiderId` и `damage`,
`PrototypeWorld.cs:2565`) и симуляцию не трогает — иначе он делил бы файлы с
#201. #183 и #159 разведены: пункт 3 scope #183 передан в #159, потому что
`DEBT_LEDGER.md` может держать только одна задача. `ROADMAP.md` и
`DEBT_LEDGER.md` до merge этого PR держит координационная сессия, поэтому #183 и
#159 получают `ready` после него.

**Что владелец получит на выходе блока:** после боя группа расходится, а не
зависает на месте драки; удар видно на экране — поза замаха, вспышка, число
урона.

**Смержено блоком на 2026-08-03** (состояние получено `gh issue view <N> --json
state` и `git log --oneline -3 origin/main`): `cd6056f` —
[#202](https://github.com/anshushunov/dungeon-fortress/issues/202), команда
worktree осталась одна (PR #213, review ЗЕЛЁНЫЙ С ЗАМЕЧАНИЕМ); `fe08076` —
[#207](https://github.com/anshushunov/dungeon-fortress/issues/207),
`test-take-task.ps1` подключён в стадию `scripts` (PR #214, review ЗЕЛЁНЫЙ). Обе
Issue `CLOSED`.

**Закрыто бухгалтерией сессии 2026-08-03:**
[#130](https://github.com/anshushunov/dungeon-fortress/issues/130) (merge PR #205,
коммит `c53913f`) и
[#177](https://github.com/anshushunov/dungeon-fortress/issues/177) (merge PR #203,
коммит `6443d8d`; механизм заменён решением владельца — кольцо снято, контур по
силуэту, #208 и PR #209, коммит `a180b32`). Обе оставались `OPEN` после merge,
потому что тела PR не несли закрывающей ссылки.

**Реестр долга: 44 записи → 7, плюс две новые.** Правило трёх gate-решений сработало впервые и
сразу для 37 записей; ни одно условие повышения не сработало, ни одна запись не
повышена. Разбор, команды и перечень удалённого — протокол 2026-08-03 в
[`DEBT_LEDGER.md`](../engineering/DEBT_LEDGER.md); копии здесь нет сознательно.

---

**Состояние на 2026-08-02, после playtest владельца — блок #171/#77/#165.** Блок **закрыт**:
gate-решение `CONTINUE` по срезу «цена памяти о месте»
([#171](https://github.com/anshushunov/dungeon-fortress/issues/171)), счётчик
`ADJUST` остаётся 0. Слот gameplay свободен. **Ограничение основания этого gate
названо в самой строке журнала** — владелец наблюдал только вторую половину
вопроса слайса; здесь копии нет сознательно.

**Смержено этим блоком** — коммиты названы поимённо, потому что `git log`
перестаёт воспроизводить это дерево после следующего merge; соответствие PR и
коммита получено `gh pr list --state merged --json number,title,mergeCommit`:

- `949ca50` — [#171](https://github.com/anshushunov/dungeon-fortress/issues/171)
  цена памяти о месте (PR #178), сам gameplay-слайс блока;
- `2b837c0` — [#165](https://github.com/anshushunov/dungeon-fortress/issues/165)
  поза `flinch` держит копьё (PR #175), `art`;
- `92e662e` — guard от засорения пользовательского `PATH` у изолированного
  `DOTNET_CLI_HOME` (PR #180), `tooling`;
- `e397aff` — [#77](https://github.com/anshushunov/dungeon-fortress/issues/77)
  **первая вертикаль целиком**: масштаб 170 % и пак v2, прямоугольник 17:12
  (PR #185). PR #176 закрыт как вошедший в него — состояние проверено
  `gh pr view 176 --json state`;
- `cd2b8aa` — автономия координатора, правило 24 (PR #181), `decision`.

Закрыты Issue [#171](https://github.com/anshushunov/dungeon-fortress/issues/171)
и [#165](https://github.com/anshushunov/dungeon-fortress/issues/165); состояние
сверено `gh issue list --state all --json number,state`.
[#77](https://github.com/anshushunov/dungeon-fortress/issues/77) остаётся
открытой: сделана **первая** вертикаль из пяти.

**Что изменилось в процессе.** Два решения владельца и два их следствия:

- **правило 24** — координатор сам запускает агентов и сам мержит PR; правило 18
  снято (PR #181, раздел [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md));
- **ветка `main` защищена**: обязательная проверка `build-and-test`,
  `enforce_admins` включён, force-push и удаление запрещены — проверено
  `gh api repos/anshushunov/dungeon-fortress/branches/main/protection`. Merge при
  `pending` CI, с разбора которого началось правило 24, стал невозможен
  механически, а не только по правилу;
- **правило 25** — исполнитель общается через репозиторий: работа в ветку,
  результат и отчёт в тело PR, который он создаёт сам, остановка и вопрос
  комментарием в свою Issue. Владелец перестаёт быть транспортом между внешним
  исполнителем и координатором;
- **правило 26** — вердикт независимого review публикуется комментарием в PR
  перед merge, и переносит его **координатор**: reviewer read-only и за весь блок
  не опубликовал ни одного комментария сам. Введено **по ходу блока, а не с его
  начала**: комментарии есть у PR #180, #181 и #185, у #175 и #178 их нет
  (`gh pr view <N> --json comments`);
- следствие защиты `main`: у Issues появились метки класса работы
  `tier:fast|standard|deep|critical|art` и метки занятости `ready`/`claimed`
  (`gh label list`) — под автономную выдачу задач и под будущую команду
  `take-task` ([#182](https://github.com/anshushunov/dungeon-fortress/issues/182)).

**Что известно и ограничивает — и что из этого опровергнуто.** На момент
написания раздела было известно так: полный `verify.ps1` **не может стать
зелёным**, стадия `screenshots` падает на шуме старта рендерера Godot до первого
кадра ([#184](https://github.com/anshushunov/dungeon-fortress/issues/184)),
восемь стадий из девяти зелёные, и то, что причина не в изменении, доказано
откатом. Независимый review приложил к этому условие: если #184 не закрыт до
следующего PR, которому нужна стадия `screenshots`, красный прогон становится
блокером, а не примечанием.

**Исполнено по ходу блока 2026-08-02: утверждение «не может стать зелёным»
неверно.** Полный прогон даёт `scope: full` и девять стадий из девяти. Причина
падений найдена и лечится **PR #199**: дело не в самой стадии, а в **длине пути
профиля Godot** — за 254 символами каталог shader cache создаётся, но войти в
него нельзя. Измерение, число зелёных прогонов и разбор записаны **один раз, в
теле PR #199**; копии здесь нет сознательно (правило 8). Прогон этой задачи —
независимое подтверждение на своей ветке: `powershell -NoProfile
-ExecutionPolicy Bypass -File .\scripts\verify.ps1 -GodotPath <godot-console>
-TemporaryRoot C:\gamedev\_wt-173-process-rules\.verify-tmp` дал
`{"event":"verification_result","status":"ok","scope":"full",...
"stagesNotRun":[]}`.

**Условие независимого review остаётся в силе, пока #184 открыта.** Состояние на
момент правки — **`OPEN`** (`gh issue view 184 --json state`), PR #199 открыт и
review не проходил (`gh pr view 199 --json state`). Снимает условие закрытие
#184, а не эта запись.

**Два счёта, которые нельзя потерять.**

- Послабление потолка кругов review (правило 12) случилось **четвёртый раз**:
  [#139](https://github.com/anshushunov/dungeon-fortress/issues/139), пак существ
  v2, PR #174, PR #181. Запись журнала 2026-08-02 говорит, что при **третьем**
  срабатывании одного и того же аргумента разбирать надо само правило 12. Третье
  уже было. **Вопрос открыт и этим блоком не закрыт**; решения здесь не
  предлагается.
- Круг доработки оставил остаток **четвёртый раз подряд**: PR #175, #178, #176,
  #181. Структурную причину назвал независимый review: у круга доработки нет
  собственной проверки полноты, его читает целиком только следующий круг review.
  Его же наблюдение рядом: три из четырёх послаблений пришлись на PR, где
  предметом был **текст**, а не поведение.

**Что идёт следующим — пересмотрено 2026-08-03 двумя решениями владельца, см.
журнал.** Gameplay-слайсом —
[#201](https://github.com/anshushunov/dungeon-fortress/issues/201): существо без
работы уходит с места боя.
[#186](https://github.com/anshushunov/dungeon-fortress/issues/186) свою роль
исполнила и правки не несёт — она измеряла; виновным механизмом названа
занятость клетки
([#76](https://github.com/anshushunov/dungeon-fortress/issues/76)), и владелец
оставил её на срезе 6. Прежняя редакция этого абзаца называла #186 слайсом блока
и писалась до её измерения.
Рядом фичей блока — **читаемый удар, вертикаль
[#77](https://github.com/anshushunov/dungeon-fortress/issues/77)**: процедурная
анимация и обратная связь удара, названа владельцем на playtest своими словами.
Плюс [#173](https://github.com/anshushunov/dungeon-fortress/issues/173) — запись
**правил 27 и 28 и поправки к правилу 19**, принятых владельцем во второй
координационной сессии 2026-08-02, вместе с несделанным по правилам 16 и 17
(`task.md` ссылался на снятое правило 18, `coord.md` разрешал запись в корневой
копии). Порядок — «План сессий вперёд».

**Заведено за блок**, каждая с измерением в теле Issue:
[#177](https://github.com/anshushunov/dungeon-fortress/issues/177) (цветной диск
стороны ушёл под спрайт при теле 170 %),
[#179](https://github.com/anshushunov/dungeon-fortress/issues/179) (происхождение
паков непроверяемо; повышена из реестра),
[#182](https://github.com/anshushunov/dungeon-fortress/issues/182) (`take-task`),
[#183](https://github.com/anshushunov/dungeon-fortress/issues/183) (ослабление ADR
держится на поимённом списке; повышена из реестра),
[#184](https://github.com/anshushunov/dungeon-fortress/issues/184) (стадия
`screenshots` падает),
[#186](https://github.com/anshushunov/dungeon-fortress/issues/186).

Масштаб тела **применён в игре** — 170 %, пак v2 подключён, `DrawGoblin` переведён
с квадрата на прямоугольник 17:12. Ниже, до конца раздела, — свидетельство о
**прошлых** блоках на момент их выдачи.

---

**Состав блока боя (#129, #156, #160, #163), закрытого `CONTINUE` 2026-08-02, —
каким он выдавался.**

**Смержено блоком боя** — коммиты названы поимённо, потому что `git log`
перестаёт воспроизводить это дерево после следующего merge; соответствие PR и
коммита получено `gh pr list --state merged --json number,title,mergeCommit`:

- `35ea72d` — [#163](https://github.com/anshushunov/dungeon-fortress/issues/163)
  пак существ v2 под масштаб 170 % (PR #162). Картинка не изменилась: рантайм
  грузит v1, подключение — внутри
  [#77](https://github.com/anshushunov/dungeon-fortress/issues/77);
- `cbbd588` — [#156](https://github.com/anshushunov/dungeon-fortress/issues/156)
  граница помещения рисуется под существом (PR #166);
- `0f20868` — [#160](https://github.com/anshushunov/dungeon-fortress/issues/160)
  [ADR 0019](../decisions/0019-verdict-not-order.md) «вердикт о существе — можно,
  приказ — нельзя» (PR #161), условие входа среза 3;
- `c5aa31b` — состояние блока боя: журнал, `NOW`, очередь, реестр (PR #172);
- `15bab1b` — [#129](https://github.com/anshushunov/dungeon-fortress/issues/129)
  в бою цель движения — свободная клетка рядом с врагом (PR #164), сам
  gameplay-слайс блока.

Ничего из блока merge не ждёт: последним ушёл PR #164, и playtest владельца
проходил уже на этом дереве.

**Что ушло из блока в очередь**, всё — находки независимого review, каждая с
измерением: [#165](https://github.com/anshushunov/dungeon-fortress/issues/165),
[#173](https://github.com/anshushunov/dungeon-fortress/issues/173),
[#167](https://github.com/anshushunov/dungeon-fortress/issues/167),
[#168](https://github.com/anshushunov/dungeon-fortress/issues/168),
[#169](https://github.com/anshushunov/dungeon-fortress/issues/169),
[#170](https://github.com/anshushunov/dungeon-fortress/issues/170),
[#171](https://github.com/anshushunov/dungeon-fortress/issues/171).

**[#77](https://github.com/anshushunov/dungeon-fortress/issues/77) в этот блок не
вошла и это решение, а не пропуск:** она делит `Main.cs` с #156, а две
записывающие задачи не владеют одним файлом. Идёт следующей, и в неё же входит
подключение пака v2 — шаг класса `support`, включая перевод `DrawGoblin` с
квадрата на прямоугольник 17:12.

**[#157](https://github.com/anshushunov/dungeon-fortress/issues/157) в этот блок
не входит, и это следствие правила, а не забывчивость.** Задача класса
`gameplay`: она вводит новое поведение — политику накопления значимого и четыре
категории цвета, — и по прецеденту #140 и #145 на той же поверхности класс именно
такой. А #129 — балансный слайс, потому что бой самая балансная поверхность
проекта, и балансные слайсы **не параллелятся никогда**: разные вопросы playtest
второй слот в этом случае не открывают (правило 2 раздела
[«Темп разработки»](../engineering/DEVELOPMENT_PACE.md)). Понизить #157 до `support`, чтобы обойти
правило, было бы ровно тем самоосвобождающим суждением, против которого правило и
написано. Поэтому #157 идёт после блока боя — но **не первым же блоком**, и это
уточнено 2026-08-02 при сборке следующего блока: #157 правит `Main.cs` вместе с
[#77](https://github.com/anshushunov/dungeon-fortress/issues/77), а две
записывающие задачи не владеют одним файлом. То же правило, что развело её с
#129, разводит её и с #77, поэтому gameplay-слайсом следующего блока идёт
[#171](https://github.com/anshushunov/dungeon-fortress/issues/171) со стороны
симуляции, а #157 — блоком за ним. Порядок — в «Плане сессий вперёд».

Масштаб тела **выбран** — 170 %, решение владельца по спайку #142, см. журнал.

**Смержено сессией ленты владения и кромки стены** (проверено
`git log --oneline -3 origin/main`): дефолтный reasoning budget агентов
(PR #150), граница помещения против вертикальной кромки стены
([#147](https://github.com/anshushunov/dungeon-fortress/issues/147), PR #151),
лента владения показывает значимое, а не последнее
([#145](https://github.com/anshushunov/dungeon-fortress/issues/145), PR #152).
Раньше, сессией по бою и масштабу: scale-spike (PR #143), история существа
(PR #144), граница помещения против фасада (PR #148).

**Почему лента шла до playtest, а не после** — строка журнала gate-решений от
2026-08-01 «Итоги блока по бою и масштабу»; здесь копии нет сознательно.

**Что владелец увидел на playtest 2026-08-01 — историческая запись, gate уже дан.** Лента даёт по строке
на существо и показывает его худшее, а не последнее; шапка называет, сколько
существ и сколько событий осталось за кадром. Две оговорки, названные независимым
review по его собственным девяти кадрам, чтобы они не были приняты за дефект:
поздно в партии `baseline` все три строки — отказы по памяти места, они
однообразны по форме, и цена четвёртой волны с панели уходит; лента больше не
тикер, её верх может стоять сотни тиков, движение живёт строкой сводки выше. И
один **известный дефект**, а не оговорка: на фикстуре `neglected` шапка скажет
«0 значащих» о партии, вымирающей от истощения — задача
[#154](https://github.com/anshushunov/dungeon-fortress/issues/154).

**Состав блока боя, каким он выдавался.** Три записывающие задачи плюс дорожка
визуала: [#129](https://github.com/anshushunov/dungeon-fortress/issues/129)
(`gameplay`, балансный слайс — идёт один),
[#156](https://github.com/anshushunov/dungeon-fortress/issues/156) (`defect`),
[#160](https://github.com/anshushunov/dungeon-fortress/issues/160) (`decision`,
ADR о вердиктах) и [#163](https://github.com/anshushunov/dungeon-fortress/issues/163)
(`art`, исполняет Codex). Партиция файлов: #129 держит
`src/DungeonFortress.Simulation/**`, тесты симуляции и контракт; #156 —
`src/DungeonFortress.Game/Main.cs`, `src/DungeonFortress.Presentation/**` и тесты
представления; #160 — только `docs/decisions/**` и `docs/OPEN_QUESTIONS.md`;
#163 — `src/DungeonFortress.Game/assets/generated/goblins/**` и `docs/art/**`.
Порядок merge: **PR #162 → PR #166 → PR #161 → PR #164**.

**Партиция была нарушена, и это ошибка координатора, а не исполнителей.** Найдено
независимым review PR #172 сверкой списков файлов: `docs/engineering/PROTOTYPE_GRAYBOX.md`
правят обе задачи, а #129 правит ещё и `src/DungeonFortress.Presentation/HudText.cs`
вместе с тремя тестами представления — то есть файлы, закреплённые за #156.
Причина названа прямо: **правку тестов памяти в Presentation поручил #129 сам
координатор**, когда решение владельца по развилке потребовало привести проверки
в зелёное состояние, и не переназначил при этом партицию. Именованные регионы
[ADR 0017](../decisions/0017-raise-parallel-writing-ceiling.md) §3 при этом не
применялись, хотя набор файлов пересёкся, — а разрешающим правилом является
именно набор файлов (правило 5).

Вреда не случилось, и то, чем он был предотвращён, названо, чтобы не выглядело
везением: задачи шли **последовательно** — #156 смержен до того, как #129 получил
работу по тестам представления, — и `git merge-tree --write-tree origin/main
origin/agent/129-combat-approach-free-tile` сходится чисто, проверено review
собственным прогоном. Вывод для следующего блока: расширение scope записывающей
задачи — это и переназначение партиции, а не только новые критерии готовности.
[#77](https://github.com/anshushunov/dungeon-fortress/issues/77) в блок не вошла
именно из-за партиции: она делит `Main.cs` с #156.

Дорожка визуала в блоке **есть** — #163, задача класса `art` у Codex (правило 6);
подключение пака — шаг `support` внутри #77, и он стоит слот следующего блока.

---

**Ниже — состав блока «сессия по бою и масштабу» (#140, #139, #142), предыдущего,
каким он выдавался.** Всё, что сказано ниже до конца раздела, — свидетельство на момент
той выдачи; к блоку боя оно не относится, в том числе фраза про отсутствие
задачи `art`.

**Блок запущен 2026-08-01** координационной
сессией по бою и масштабу и содержит
**три** задачи. Две назначены владельцем при закрытии предыдущего блока, третья
добавлена этой сессией:

- **[#140](https://github.com/anshushunov/dungeon-fortress/issues/140)** —
  исполнение `ADJUST`: история существа отбирает значимые события, а не
  последние. Класс `gameplay`. Вопрос playtest: **называет ли игрок, что
  случилось с существом и как это изменило его следующее решение**. У задачи есть
  критерий, который можно провалить прогоном: история существа, у которого за
  партию был хотя бы один отказ по памяти, обязана его содержать;
- **[#139](https://github.com/anshushunov/dungeon-fortress/issues/139)** —
  граница помещения наезжает на фасад стены. Класс `defect`. Идёт **в том же
  блоке** по решению владельца. Собственного вопроса playtest не создаёт;
- **[#142](https://github.com/anshushunov/dungeon-fortress/issues/142)** —
  scale-spike: graybox-сцена боя в 100/150/200 % от текущего визуального размера
  тела. Класс `support`, добавлена решением владельца этой сессии. Собственного
  вопроса playtest не создаёт и в счёт gameplay-слайсов не входит: возвращает не
  изменение игры, а **выбор числа**, который владелец делает, глядя на три кадра.
  Идёт **строго до [#77](https://github.com/anshushunov/dungeon-fortress/issues/77)**,
  чтобы замах, партиклы и иконки настраивались под выбранный размер один раз.

Балансной поверхности нет ни у одной из трёх. Партиция файлов: #140 держит
`HudText.cs`, `tests/golden/ui/*.json` и `scripts/verify.ps1`; #139 держит
`RoomGeometry.cs`, `WorldRenderGeometry.cs` и `Main.cs`; #142 не пишет в `src/**`
вовсе — только новые файлы сцены. Пересечений нет, поэтому именованные регионы
[ADR 0017](../decisions/0017-raise-parallel-writing-ceiling.md) §3 в этом блоке
не применяются. Порядок merge: **#139 → #140 → #142**.

Задачи класса `art` в блоке **нет намеренно**:
[`ANIMATION_PIPELINE.md`](../art/ANIMATION_PIPELINE.md) запрещает заказывать
генерацию до того, как известна геометрия рендера, а масштаб тела выбирается
именно этим блоком. Отступление от правила 6 раздела
[«Темп разработки»](../engineering/DEVELOPMENT_PACE.md) названо, а не пропущено молча; дорожка
визуала возвращается следующим блоком вместе с #77.

**Что обязан знать исполнитель #140 и что должно попасть в его бриф.** Причина
провала измерена и лежит в теле Issue: панель показывала последние четыре
события, а подавляющее большинство событий существа — рутина ожидания и уступки
дороги, тогда как отказы по памяти составляют доли процента журнала. Ограничение
в четыре строки было выведено из бюджета панели, а не из вопроса — это ошибка
постановки #128, и повторять её нельзя.

Доля рутины **числом здесь не называется**: его печатает и охраняет тест
`Most_of_what_a_creature_decides_is_routine`
(`tests/DungeonFortress.Presentation.Tests`), падающий при уходе ниже порога.
Полный перечень мест, где число записано, ведёт сообщение об ошибке самого теста
— это единственный список, который не может разойтись с кодом. Прежняя копия
здесь называла 89 % и была неверна: то была доля четырёх кодов у одного существа,
обобщённая на всех. Копия закрыта удалением, а не подновлением — правило 8
раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md). Строка журнала gate-решений от
2026-08-01 остаётся как есть: там 89 % отнесены к одному существу и потому верны.

**Ставка этого блока была названа прямо: счётчик `ADJUST` достиг 2**, и критерий
отказа закрывал возможность третьего. **Ставка сыграна 2026-08-01: `PROMOTE`**,
см. журнал; счётчик сброшен. Абзац оставлен как запись того, чем блок был, когда
выдавался.

Ниже — состав **предыдущего** блока, каким он выдавался, и два вопроса playtest,
разведённые по правилу 2 раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md):

- **[#128](https://github.com/anshushunov/dungeon-fortress/issues/128) вместе с
  [#125](https://github.com/anshushunov/dungeon-fortress/issues/125)** —
  исполнение `ADJUST`: история решений одного существа становится читаемой, и
  отказ по памяти называет ту работу, от которой существо действительно
  отказалось. Вопрос playtest: **называет ли игрок, как память изменила решение
  конкретного существа**. Дефект правдивости отказа идёт внутрь слайса, а не
  отдельной задачей: показывать историю, в которой строки лгут о причине,
  бессмысленно;
- **[#52](https://github.com/anshushunov/dungeon-fortress/issues/52)** — шаг 3
  блока [ADR 0008](../decisions/0008-three-quarter-projection.md), отложенный
  `ADJUST` с 2026-07-28. Слот по составу канонического снапшота освободился
  merge #117 впервые с 28 июля. Вопрос playtest: **видит ли владелец помещение
  как помещение, а не как пол другого оттенка**. Форма решена
  [ADR 0013](../decisions/0013-what-is-a-room.md) — вариант C, «игрок размечает
  намерение»;
- **[#127](https://github.com/anshushunov/dungeon-fortress/issues/127)** —
  `defect` с playtest: подсказка кнопки меньше подписи, которую объясняет, и
  подсказки остаются единственной текстовой поверхностью HUD вне guard'а
  читаемости. Собственного вопроса playtest не создаёт.

Балансной поверхности ни у одной из трёх нет: веса, пороги и правила боя не
трогает ни одна.

**Партиция и порядок merge.** Три файла нужны более чем одной задаче —
`Main.cs`, `PrototypeWorld.cs`, `InspectorText.cs`; они разделены именованными
регионами по четырём условиям
[ADR 0017](../decisions/0017-raise-parallel-writing-ceiling.md) §3, владельцы
остатка названы в брифах. Порядок merge: **#127 → #128/#125 → #52**. Он не
произволен: #127 переписывает `HudReadability`, поэтому идёт первой, а #128
добавляет свою панель в guard после rebase; #52 владеет эталонными кадрами
`tests/golden/ui/*.json` и идёт последней, потому что кадры **пересобираются
командой, а не редактируются** — разделять их регионами нечем, и разрешает
конфликт порядок merge с rebase на координаторе, четвёртое условие ADR 0017 §3.
Откат, если он не сработает: rebase кадров потребовал ручного разбора → задачи
становятся последовательными, запись в журнал.

Что не начинается и почему:

- [#115](https://github.com/anshushunov/dungeon-fortress/issues/115) (счёт партии
  — число без опоры) ждёт merge #52 по той же причине, по которой ждала #117:
  эталонные кадры `tests/golden/ui/*.json` в блоке принадлежат одной задаче;
- [#116](https://github.com/anshushunov/dungeon-fortress/issues/116) (сила
  владения скачет почти вдвое при живых и здоровых) несёт **собственный decision
  gate**: предложение показывать максимум при здоровых возвращает на экран
  потенциал, а [ADR 0015](../decisions/0015-session-of-waves-and-domain-score.md)
  сознательно определил силу как готовность. Решение принимается **до**
  реализации, а не по ходу;
- [#120](https://github.com/anshushunov/dungeon-fortress/issues/120) (не видно,
  куда шёл вставший с `blocked_by_others`) стоит в очереди и смыкается с #77;
- [#129](https://github.com/anshushunov/dungeon-fortress/issues/129) (бойцы
  выстраиваются в линию и не обходят своих) **слота не получает по решению
  владельца**: бой — самая балансная поверхность проекта, и балансные слайсы не
  параллелятся никогда, поэтому рядом с #140 задача идти не может. Развилка,
  которая её блокировала, **закрыта 2026-08-01** — см. журнал; то же решение
  оставляло реализацию на срезе 6. **Тем же днём, но отдельным, более поздним
  решением владельца задача поднята из среза 6 в следующий блок**, вместе
  с [#77](https://github.com/anshushunov/dungeon-fortress/issues/77) — см. «План
  сессий вперёд»; на срезе 6 остаётся
  [#76](https://github.com/anshushunov/dungeon-fortress/issues/76) и обход своих
  при поиске пути;
- [#126](https://github.com/anshushunov/dungeon-fortress/issues/126) (контракт
  13.2 подпёрт числом, которое его не измеряет) ждёт освобождения контракта:
  в этом блоке его правят обе фичи.

Смержено блоком 2026-07-31 (проверено `git log --oneline -3` и `gh pr list
--state all`): семь решений владельца о темпе стали правилами и появился реестр
долга (`89ade73`, PR #112); читаемость HUD стала исполняемым правилом, а мир
растёт вместе с окном (`877d2b8`, PR #113, закрыты #86 и #49); паника стала
личной, бегство — ходьбой (`1b41555`, PR #114, закрыт #101). Раньше, сессией
2026-07-30/31: стартовый кадр выводится из экрана (#100, PR #107), правила
представления стали проверяемыми и рамка выделения получила геометрию подсветки
(#90 и #99, PR #110), guard стадий разбирает dot-source файлы (#102, PR #108),
правила версионирования снапшота записаны и проверяются (#103, PR #105), потолок
записывающих агентов поднят до пяти
([ADR 0017](../decisions/0017-raise-parallel-writing-ceiling.md), PR #104).

Смержено сессией 2026-07-31/08-01 (проверено `git log --oneline -1` и
`gh pr list --state all --json number,state,mergedAt`): ADR 0018 и состояние
блока стали записями в репозитории (PR #121, Issue #118); мораторий на
процессную документацию и сжатие калибровки (PR #123, Issue #122); **срез 2 —
память существа о месте и подземельная планировка** (`ff6d679`, PR #124,
закрыты #117 и #119).

Направление, заданное владельцем 2026-07-31: **time to market**. Семь решений о
темпе разработки приняты и **записаны в
[`DEVELOPMENT_PACE.md`](../engineering/DEVELOPMENT_PACE.md)** (PR #112); coordination
inbox [#33](https://github.com/anshushunov/dungeon-fortress/issues/33) больше не
является их единственным домом. Попытка оформить их **закрытым черновиком
ADR 0018 (PR #111)** не удалась — одиннадцать блокирующих находок независимого
review, две несущие посылки не подтвердились репозиторием; черновик в `main` не
попал, и номер 0018 занят другим, действующим ADR.

Срез 1 закрыт 2026-07-29 решением владельца **`CONTINUE`**: гипотеза
подтвердилась, milestone A0 взят. Итог среза — в таблице COMPLETED выше, полный
разбор — в PR #73. Второе решение `CONTINUE` от 2026-07-31 закрыло
**пакетный playtest** по срезу 1 и счёту партии — см. журнал выше.

Блок представления по [ADR 0008](../decisions/0008-three-quarter-projection.md)
**закрыт полностью** — шаг 3 (#52, PR #135) выполнен 2026-08-01, состояние Issue
сверено `gh issue list --state all --json number,state`; прежняя редакция этой
фразы писалась, пока шаг 3 шёл. Его условие входа было выполнено
[ADR 0013](../decisions/0013-what-is-a-room.md): комната есть производная
сущность поверх зоны и объекта, и блок знал, границу чего рисует.

Состав блока — раздел «Блок „реализация ADR 0008“» ниже: тайл 22 → 32–48 px,
стена «верх плюс фасад», Y-sort, камера с зумом и панорамой, читаемость
помещений (#52). За ним, но **не внутри него**, идут scale-spike по размеру
существ (пункт 10 раздела «Представление», добавлен 2026-07-30) и затем обратная
связь удара (#77). Её условие входа снято шагом 1: при тайле 22 px партиклы
превращались в кашу и претензия пришла бы к эффектам вместо масштаба, а при
40 px у них есть место.

Блок разрезан на два Issue, и рез проходит **по каноническому снапшоту**: камера
и масштаб его не трогают, читаемость помещений трогает.

**Шаг 1 выполнен** 2026-07-29:
[#79](https://github.com/anshushunov/dungeon-fortress/issues/79), PR #81. Тайл
поднят с 22 до **40 px** и стал проверяемым параметром в диапазоне 32–48; над
миром стоит `Camera2D` с пятью дискретными уровнями зума (`0.5`, `0.75`, `1`,
`1.5`, `2`) и панорамой, ограниченной центрами краевых тайлов; HUD уехал в
отдельный `CanvasLayer` и камерой не масштабируется; `stretch/aspect` переведён
на `expand`, поэтому больший кадр показывает больше мира; масштаб интерфейса
задаётся параметром запуска; клик переводится в клетку через живой canvas
transform; съёмка кадра получила явные кадр, позицию и зум. Симуляция,
канонический снапшот, словарь команд и design-контракт не затронуты.

Зум, положение камеры и масштаб интерфейса **не входят в checksum** — это
проверяется исполняемо, одним checksum на четырёх сочетаниях. Дискретные уровни
выбраны так, чтобы не зависеть от незакрытого art-direction gate «пиксельный
стиль или нет»: они совместимы и с пиксель-артом, и с воспроизводимостью снимка.

**Шаг 2 выполнен** 2026-07-30:
[#83](https://github.com/anshushunov/dungeon-fortress/issues/83), PR #85. Порода
получила поднятый верх, обращённый к наблюдателю фасад, кант и губу; чистая
`WallTopology` выбирает один из 16 вариантов по ортогональному соседству, поэтому
массив не распадается на шахматку; стены, постройки, существа и налётчики входят
в один детерминированный проход по Y, где глубина тела — **интерполированная**
позиция, а не позиция тика. Способ рендера выбран реализацией: остался
immediate-режим с фасадом и чистой сортировкой, потому что `TileMapLayer` не
переживал уже купленные гарантии — интерполяцию движения, тесты представления без
движка и воспроизводимую съёмку. Сетка, камера и геометрия ввода остались
ортогональными; новых ассетов нет, art-direction gate не открыт.

Блок дал побочный результат, важнее самой геометрии: **правила представления
переехали из адаптера в `DungeonFortress.Presentation`** — глубина, кодек
клетка↔id, геометрия видимой массы стены и набор открытых сторон. Мутационный
анализ подтверждает, что поломки этих правил теперь падают на тестах, а не
обнаруживаются глазом на кадре. Там же сформулировано правило прохода
информационных меток: метка, способная разделить клетку с телом, не имеет
непрозрачной заливки. Правило стало проверяемым в CI
([#90](https://github.com/anshushunov/dungeon-fortress/issues/90), PR #110) — до
#77, как и планировалось, поэтому новые метки эффектов не будут наследовать его
вручную.

**Шаг 3 — читаемость помещений (#52) — выполнен 2026-08-01** (PR #135); фраза
«идёт» осталась от редакции, писавшейся во время работы. Он вынесен отдельно,
потому что единственный в блоке меняет состав канонического снапшота и тянет за
собой контракт по
[ADR 0010](../decisions/0010-contract-invariants-and-tuning.md). Шаги 1 и 2
остались в областях `presentation` и `godot`, поэтому принимались, не открывая
контрактных вопросов; шаг 3 такой возможности не даёт.

Отложенный `ADJUST` по #52 ждал не очереди, а **свободного слота по составу
канонического снапшота**: он освободился merge #117 (PR #124) впервые с
28 июля. Прежняя причина отсрочки — «параллельно вести #52 и gameplay-слайс,
меняющий снапшот, нельзя, потому что оба правят контракт и состав снапшота» —
снята разделением контракта по разделам и порядком merge; условия и владельцы
регионов названы в разделе `NOW`.

Граница, установленная ремонтным блоком и обязательная для всей дальнейшей
работы слоя представления: **проекция отвечает на то, что выводится из
опубликованных фактов, свёрнутых через неё; мир отвечает на то, что требует
прогона тика.** Приоритеты и запретные клетки — такое же намерение игрока, как
мазок кистью; достижимость породы, начало работы и выбор исполнителя остаются за
симуляцией. Инвентарь фактов с обоснованием по каждому — в docstring
`MapAccents` и в `docs/engineering/PROTOTYPE_GRAYBOX.md`. Правило появилось
после пяти раундов review, в которых граница четырежды проводилась по месту и
четырежды оказывалась неполной.

### Очередь

Порядок gameplay-блоков и того, что к ним привязано. Параллельная работа других
классов регулируется [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md),
а не этой таблицей.

| Позиция | Блок | Тип | Условие входа | Критерий выхода |
|---|---|---|---|---|
| ✅ Сделано | Счёт партии, ранжирующий качество игры (#92, PR #96) | `gameplay` | Выполнено: развилка #75 закрыта [ADR 0016](../decisions/0016-party-score-separate-from-renown.md) | Выполнено 2026-07-30. **Gate-решение `CONTINUE` дано 2026-07-31** пакетным playtest — см. журнал. Запас между живым и погибшим владением записан **один раз**, в 13.4 контракта, вместе с командой, которой он получен; копии здесь нет — прежняя копия в этой строке успела разойтись с источником |
| ✅ Сделано | Бегство перестаёт быть стадным и телепортом (#101, PR #114) | `gameplay` | Слот освобождён merge #96 | Выполнено 2026-07-31: один павший союзник не обращает в бегство всю группу в одном тике, бегущее существо видно движущимся. `CONTINUE` тем же playtest; цена размена принята разменом — см. журнал |
| ✅ Сделано | Диагностика непригодного временного каталога и укрепление guard'а стадий `verify.ps1` (#89 и #71, PR #97) | `blocks-verification` | — | Выполнено 2026-07-30; остаток #102 закрыт PR #108 |
| ✅ Сделано | Читаемость HUD при максимизации и доказательство живости HUD-guard'а (#86 и #49 одной задачей, PR #113) | `support` | — | Выполнено 2026-07-31: на большом экране HUD читаем без параметров запуска, `--strict-hud-fit` перестал быть инертным |
| ✅ Сделано | Правила темпа разработки из решений владельца 2026-07-31 (PR #112) | `decision` | — | Выполнено: семь решений живут в разделе [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md), реестр долга — в [`DEBT_LEDGER.md`](../engineering/DEBT_LEDGER.md) |
| ✅ Сделано | **Срез 2 — существо помнит место, где ему было страшно** ([#117](https://github.com/anshushunov/dungeon-fortress/issues/117), PR #124); в той же задаче стартовая планировка стала подземельем и закрыт [#119](https://github.com/anshushunov/dungeon-fortress/issues/119) | `gameplay` | Слот освобождён `CONTINUE` 2026-07-31; форма памяти выбрана [ADR 0018](../decisions/0018-creature-memory-of-place.md) | Выполнено частично. **Gate-решение `ADJUST` дано 2026-08-01** — см. журнал: подземелье и метки владелец видит, «как это изменило следующее решение» — нет. Исправление в строке ниже |
| ✅ Сделано | **Исполнение `ADJUST`: история решений одного существа читается, и отказ по памяти называет ту работу, от которой существо отказалось** ([#128](https://github.com/anshushunov/dungeon-fortress/issues/128) вместе с [#125](https://github.com/anshushunov/dungeon-fortress/issues/125), PR #136) | `gameplay` | `ADJUST` по срезу 2 от 2026-08-01 | Выполнено 2026-08-01. Ложных отказов 11 из 80 против 101 из 169 до правки, партия бит-в-бит та же на всех пятнадцати прогонах. Gate-решение дано 2026-08-01 — **`PROMOTE`**, см. журнал; прежняя редакция строки писалась до playtest |
| ✅ Сделано | Реализация [ADR 0008](../decisions/0008-three-quarter-projection.md), шаг 3: читаемость помещений ([#52](https://github.com/anshushunov/dungeon-fortress/issues/52), PR #135) — сущность комнаты в снапшоте, граница, покрытие пола по назначению, иконка, подпись. Шаги 1 (#79, PR #81) и 2 (#83, PR #85) выполнены | `gameplay`, представление плюс контракт | Отложенный `ADJUST` с 2026-07-28; слот по составу канонического снапшота освобождён merge #117 | Выполнено 2026-08-01: комната стала сущностью снапшота с шестью свойствами [ADR 0013](../decisions/0013-what-is-a-room.md), версия снапшота осталась 3 — изменение аддитивно. Блок ADR 0008 закрыт полностью |
| ✅ Сделано | Подсказка кнопки меньше подписи, которую объясняет, и вне guard'а читаемости ([#127](https://github.com/anshushunov/dungeon-fortress/issues/127), PR #134) | `defect` | — | Выполнено 2026-08-01: тело подсказки вровень с подписью, подсказка масштабируется вместе с `UiScale`, оба флага-регрессии встроены в `verify.ps1`. Остаточный риск двойного масштабирования проверяется владельцем на playtest |
| ✅ Сделано | **Исполнение второго `ADJUST`: история существа отбирает значимые события, а не последние** ([#140](https://github.com/anshushunov/dungeon-fortress/issues/140)) | `gameplay` | Gate-решение `ADJUST` от 2026-08-01, второй подряд. **Последняя попытка по этой гипотезе** | История существа, у которого за партию был отказ по памяти, содержит его — проверяется прогоном; владелец после партии называет существо, что с ним произошло и как это изменило его следующее решение |
| ✅ Сделано | Граница помещения наезжает на фасад стены ([#139](https://github.com/anshushunov/dungeon-fortress/issues/139)) | `defect` | Решение владельца — в том же блоке | Ни одна граница комнаты не пересекается с фасадом стены на стартовой карте, проверяется исполняемо; перекрытие двух комнат по-прежнему читается как две линии |
| ✅ Сделано | **Лента владения показывает значимое, а не последнее** ([#145](https://github.com/anshushunov/dungeon-fortress/issues/145), PR #152) | `gameplay` | Решение владельца 2026-08-01: лента получает задачу **до** playtest, потому что до первого клика владелец видит тот экран, что вызвал жалобу | Выполнено 2026-08-01: доля окон ленты со значащим событием выросла с 3 из 48 до 22 из 48 на `baseline`/2400 — потолок партии, значащего до тика ~1300 нет вовсе. Числа воспроизведены независимым review собственными прогонами. Gate-решение дано 2026-08-01 — **`PROMOTE`**, см. журнал; прежняя редакция строки писалась до playtest |
| ✅ Сделано | Граница помещения сливается с вертикальной кромкой стены: остаток наблюдения по QUARTERS ([#147](https://github.com/anshushunov/dungeon-fortress/issues/147), PR #151) | `defect` | Заведена по находке review #139: у `quarters@19,2` северной стены нет вовсе, значит владелец видел там другой механизм | Выполнено 2026-08-01: минимальный зазор 1,0 референсных px по всем комнатам и всем четырём сторонам при tileSize 32/40/48. Попутно найдено и записано в реестр, что критерий готовности #139 был закрыт не полностью — её тест меряет прямоугольник фасада и не видит шва по его нижней кромке |
| ✅ Сделано | Граница помещения рисуется под существом ([#156](https://github.com/anshushunov/dungeon-fortress/issues/156), PR #166) | `defect` | Наблюдение владельца на playtest 2026-08-01 | Выполнено 2026-08-01: 226 пересечений штриха с телом → 0 на тайлах 32/40/48, число и список комнат пришпилены `Assert.Equal`. Единица решения — кусок кромки, а не кромка. Два круга review; второй нашёл разомкнутый угол `quarters` (5,0 реф. px), после лечения кадр угла побайтово равен `main`. Названная цена: южная кромка larder на двух клетках идёт полутолщиной |
| ✅ Сделано | Пак существ v2 под масштаб 170 % ([#163](https://github.com/anshushunov/dungeon-fortress/issues/163), PR #162) | `art` | Решение владельца по спайку #142 | Выполнено 2026-08-01: шесть состояний, холст 272×192, тело центрировано, вертикаль общая. Три круга review, третий — разово сверх потолка решением владельца. Картинка в игре не изменилась: подключение внутри #77 |
| ✅ Сделано | В бою цель движения — свободная клетка рядом с врагом ([#129](https://github.com/anshushunov/dungeon-fortress/issues/129), PR #164) | `gameplay`, балансный | Развилка закрыта решением владельца 2026-08-01, см. журнал | Бойцы расходятся вокруг врага, а не в колонну: доля занятых мест вокруг налётчика 0,418 → 0,512, трое одновременно бьющих стали достижимы. Инварианты 4 и 11 получили новое основание, проверенное переносом на `main`. Вопрос playtest — «перестал ли бой выглядеть очередью» — **закрыт: gate-решение `CONTINUE` дано 2026-08-02**, см. журнал; прежняя редакция строки писалась до playtest |
| ✅ Сделано | [ADR 0019](../decisions/0019-verdict-not-order.md) «вердикт о существе — можно, приказ — нельзя» ([#160](https://github.com/anshushunov/dungeon-fortress/issues/160), PR #161) | `decision` | Условие входа среза 3 | Принят владельцем 2026-08-01. Граница проверяется дважды — у команды набором полей, у значения правилом допустимости из четырёх условий. Остаток — [#167](https://github.com/anshushunov/dungeon-fortress/issues/167) |
| ✅ Сделано | Память о месте берёт с кладовой цену, которой не стоит ([#171](https://github.com/anshushunov/dungeon-fortress/issues/171), PR #178) | `gameplay`, балансный | Решение владельца 2026-08-01: канал 1 развилки #129 вынесен отдельно | Выполнено 2026-08-02: избегание кончается через 200 тиков, существо ниже сытости 30 не отказывается вовсе; проверено матрицей из пятнадцати прогонов и шестью партиями, а не одним seed. **Gate-решение `CONTINUE` дано 2026-08-02** — см. журнал, там же названо ограничение его основания. Issue закрыт (`gh issue list --state all --json number,state`) |
| ✅ Сделано | Поза `flinch` без копья: на переходе `combat → flinch` оружие мигает ([#165](https://github.com/anshushunov/dungeon-fortress/issues/165), PR #175) | `art` | Находка review PR #162 | Выполнено 2026-08-02: копьё на месте во всех боевых позах. Issue закрыт |
| ✅ Сделано | **Первая вертикаль [#77](https://github.com/anshushunov/dungeon-fortress/issues/77): масштаб тела 170 % и пак существ v2 в игре** (PR #185; вобрал закрытый PR #176) | `support` | Оба условия входа выполнены: тайл больше 22 px (шаг 1) и выбран масштаб тела 170 % по спайку #142 | Выполнено 2026-08-02: `DrawGoblin` переведён с квадрата на прямоугольник 17:12, подключены позы `windup` и `flinch`. **Принято владельцем на playtest** — см. журнал. Сам #77 **открыт**: сделана первая вертикаль из пяти. Остаток блока — [#177](https://github.com/anshushunov/dungeon-fortress/issues/177) (`OPEN`, `gh issue view 177 --json state`); #179 из остатка выбыл, закрыт PR #193 — см. его строку ниже |
| ✅ Сделано | Прогон не дописывает пользовательский `PATH`: guard у изолированного `DOTNET_CLI_HOME` (PR #180) | `tooling` | Найдено по ходу первой вертикали #77 | Выполнено 2026-08-02: правило «не оставлять следов в среде машины» записано в [`AGENTS.md`](../../AGENTS.md) и охраняется прогоном |
| ✅ Сделано | Автономия координатора: правило 24, снятие правила 18 (PR #181) | `decision` | Разбор нарушения — merge PR #178 при `pending` CI | Выполнено 2026-08-02: координатор сам запускает агентов и сам мержит PR, наверх идут блокеры, архитектурные и геймплейные решения. Остаток — [#183](https://github.com/anshushunov/dungeon-fortress/issues/183) |
| ⏳ Измерено, правки нет | После боя группа не может разойтись: существа блокируют друг друга ([#186](https://github.com/anshushunov/dungeon-fortress/issues/186), PR #198) | `gameplay`, балансный | Наблюдение владельца на playtest 2026-08-02, на дереве после merge PR #185 и PR #178 | Задача-вопрос: измеряла и не чинила, `src/` не тронут. Виновный механизм назван — **занятость клетки** ([#76](https://github.com/anshushunov/dungeon-fortress/issues/76)). **Решение владельца 2026-08-03: не чиним, #76 остаётся на срезе 6** — см. журнал; там же названа цена. Первое измерение утверждало, что толчеи нет, и это опроверг независимый review; после поправки наблюдение владельца подтвердилось. Числа — в контракте §4.1, копии здесь нет сознательно |
| ✅ Сделано | Существо без работы уходит с места боя, а не стоит там, где кончилась драка ([#201](https://github.com/anshushunov/dungeon-fortress/issues/201), PR #217) | `gameplay` | **Решение владельца 2026-08-03** по итогам измерения #186: вторая половина симптома к затору отношения не имеет — больше половины стоящих после боя не заблокированы вовсе. Берётся после merge PR #198, который держит контракт и harness | Существо без работы покидает место боя за названное число тиков, измеренное на матрице; доля «стоит без шага и без отказа» измеримо снизилась против `main` тем же способом, что в #186; выбор места назван и обоснован. `refused_zone_unreachable` (пятая часть простоя) в scope **не входит** и с «работы нет» не смешивается. **Выполнено 2026-08-03**: доля «стоит без шага и без отказа» 29,7 % → 17,1 %, последний уходит за 33 тика вместо 60 — числа в контракте §4.1 и `evidence/201-*.json`, копии здесь нет. **Gate-решение `CONTINUE` дано 2026-08-04** — см. журнал; вторая половина ответа владельца стала [#228](https://github.com/anshushunov/dungeon-fortress/issues/228) |
| ✅ Вертикали 3 и 4 сделаны | **Подзадачи [#77](https://github.com/anshushunov/dungeon-fortress/issues/77): обратная связь удара ([#210](https://github.com/anshushunov/dungeon-fortress/issues/210), PR #218, ✅) → процедурная анимация ([#221](https://github.com/anshushunov/dungeon-fortress/issues/221), PR #235, ✅)** | `support` | Названа владельцем на playtest 2026-08-02 своими словами: «не хватает анимаций». **Решение владельца 2026-08-02**: #77 исполняется последовательностью вертикальных подзадач — масштаб 170 % и пак v2 (сделано) → читаемый удар → поза движения и работы → добор обратной связи по итогам playtest → иконки состояния; один implementer без сабагентов, review — reviewer области `представление` (правило 19 с поправкой 2026-08-02; прежняя редакция строки называла блочного reviewer) вместе с правилом 20 | Владелец без инспектора называет, кто кого ударил и с каким результатом. Процедурная анимация покрывает и работу с объектом, а не только удар — расширение scope решением владельца 2026-08-01. **Вертикаль 3 принята на playtest 2026-08-04 со словами «поза замаха появилась, но это конечно не анимация»** — отсюда и вертикаль 4 |
| ✅ Сделано | **Правила 27 и 28, поправка к правилу 19** и приведение процессных документов к правилам 16 и 17 ([#173](https://github.com/anshushunov/dungeon-fortress/issues/173)) | `decision` | Находка F9 review PR #172: `coord.md` разрешает запись в корневой рабочей копии, которую правило 17 запрещает; `task.md` ссылается на снятое правило 18. **Расширено 2026-08-02** тремя решениями владельца второй координационной сессии дня — до merge они живут только в теле Issue | Правила 27 и 28 существуют в разделе «Темп разработки» с условиями отката, правило 19 несёт поправку про области, преамбула называет 28 среди ослабляющих ADR правил; журнал несёт строку от 2026-08-02 с отклонённым и отложенным вариантами; четыре процессных документа не противоречат друг другу по дефолтной модели review, границе потолка и правилам параллельной работы — показывается выводом `rg`; шаблон брифа требует `rework_table`, `coord.md` — секцию «Партиция» у `ready` Issue |
| ✅ Сделано | Стадия `screenshots` падает на шуме старта рендерера ([#184](https://github.com/anshushunov/dungeon-fortress/issues/184), PR #199) | `blocks-verification` | Найдено исполнителем первой вертикали #77 и доказано как не своё изменение. **Причина найдена по ходу блока и лечится PR #199**: длина пути профиля Godot, а не сама стадия; прежняя формулировка строки — «полный `verify.ps1` не может стать зелёным» — измерением опровергнута, разбор записан один раз в теле PR #199 | Полный `verify.ps1` зелёный на машине разработчика без ослабления guard'а стадии. **Условие независимого review действует, пока Issue открыта** (`OPEN` на 2026-08-02, `gh issue view 184 --json state`): красный прогон у PR, которому нужна эта стадия, — блокер, а не примечание |
| ✅ Сделано | Одна команда вместо носимого брифа: `take-task` выбирает свободный тикет, занимает его и печатает бриф ([#182](https://github.com/anshushunov/dungeon-fortress/issues/182)) | `support` | Метки `tier:*` и `ready`/`claimed` заведены в блоке 2026-08-02. **Поднята 2026-08-02 снятием потолка (правило 28):** #182 построен на инварианте «любые два `ready` тикета можно брать одновременно», и при снятом потолке весь этот инвариант держится на разметке координатора — проверить его глазами больше нельзя | Агент получает задачу одной командой; занятость видна на Issue, а не в чате |
| **Поднята правилом 28** | Разделение `CameraView.cs`: математика окна и математика мировой отрисовки живут в одном файле ([#109](https://github.com/anshushunov/dungeon-fortress/issues/109)) | `support` | **Поднята 2026-08-02 снятием потолка (правило 28):** узловые файлы перестали быть гигиеной и стали тем, что открывает параллельную работу, потому что единственный ограничитель параллелизма теперь — партиция по файлам. Выделена из строки с [#106](https://github.com/anshushunov/dungeon-fortress/issues/106), с которым шла одной строкой | Математика окна и математика мировой отрисовки перестают жить в одном файле (`wc -l src/DungeonFortress.Presentation/CameraView.cs` — 833). **Следующий кандидат на разбор — `src/DungeonFortress.Game/Main.cs`, 5517 строк** (`wc -l src/DungeonFortress.Game/Main.cs`): именно им этот документ объясняет, почему задачи разводятся по времени, — места перечисляются `rg -n 'Main\.cs' docs/product/ROADMAP.md`. Отдельной задачи под него здесь **не заводится** |
| ✅ Сделано | Цветной диск стороны ушёл под спрайт: при теле 170 % игрок не отличает защитника от налётчика ([#177](https://github.com/anshushunov/dungeon-fortress/issues/177)) | `defect` | Следствие первой вертикали #77 | Сторона существа различима без инспектора при теле 170 %; проверка краснеет от изменения кода. **Механизм заменён решением владельца 2026-08-03**: кольцо снято, принадлежность несёт контур по силуэту ([#208](https://github.com/anshushunov/dungeon-fortress/issues/208), PR #209, коммит `a180b32`), спека [`SIDE_INDICATOR.md`](../design/SIDE_INDICATOR.md). Побочный эффект — вышедшие из-под кольца мелкие метки, [#222](https://github.com/anshushunov/dungeon-fortress/issues/222) |
| ✅ Сделано | Происхождение паков существ непроверяемо ([#179](https://github.com/anshushunov/dungeon-fortress/issues/179), PR #193) | `support` | Повышена из реестра долга: скрипта пост-обработки нет в репозитории, у двух исходников нет контрольной суммы | Выполнено 2026-08-02: скрипт постобработки в репозитории, у источников есть SHA-256. Issue закрыт, состояние сверено `gh issue list --state all --json number,state`; коммит `19013ae` в `main` (`git log --oneline origin/main -1`) |
| ✅ Сделано | Ослабление ADR держится на поимённом списке, который надо вспомнить ([#183](https://github.com/anshushunov/dungeon-fortress/issues/183), PR #224) | `decision` | Повышена из реестра: механизм сработал ноль раз из одного — правило 24 назвали четвёртым ослабляющим ADR только после находки review | Ослабляющее ADR правило обнаруживается проверкой, а не памятью координатора |
| Затем | История существа: значимое накапливается и различается цветом ([#157](https://github.com/anshushunov/dungeon-fortress/issues/157)) | `gameplay` | Два наблюдения владельца на playtest 2026-08-01, на одной поверхности `HudText.cs` и потому одной задачей. **Рядом с #129 идти не может**: бой — балансный слайс, а балансные не параллелятся никогда. **Рядом с #77 тоже не может** и потому уступила очередь #171: проводка панели истории живёт в `Main.cs` (`CreatureStoryPanels`), который держит #77 | Значимое перестаёт вытесняться рутиной и свежестью; четыре категории цвета — позитивное, негативное, травма, сильно важное — различимы без легенды и закреплены проверкой; форма обоснована вопросом, а не бюджетом панели |
| Затем | Планка «бегство — это ходьба» просела с 84,5 % до 70,4 % ([#168](https://github.com/anshushunov/dungeon-fortress/issues/168)) | `defect` | Повышена из реестра: сработало условие «доля ниже 80 % на любой фикстуре» | Названо, что планка обещает после #129; проверка зелёная и краснеет от изменения кода. Механизм измерен: правило подхода меняет **место**, где ломается защитник, а не время |
| Затем | Граница разброса уступок 40 подогнана под одну ячейку ([#170](https://github.com/anshushunov/dungeon-fortress/issues/170)) | `defect` | Находка review PR #164, измерена на четырёх вариантах источника | Утверждение либо снято, либо зелено на всех шести ячейках `main` и краснеет от изменения кода. Перефит на 74 запрещён явно |
| Затем | Аудит пяти меток `StrokeOnly` ([#169](https://github.com/anshushunov/dungeon-fortress/issues/169)) | `defect` | Находка review PR #166: снятый аргумент «линия ничего не прячет» остался у пяти других меток | По каждой метке есть ответ, подпёртый проверкой; первый кандидат — `RoomLabel` |
| При входе в срез 3 | Пятое условие допустимости значения `verdict` ([#167](https://github.com/anshushunov/dungeon-fortress/issues/167)) | `decision` | Находка подтверждающего круга review PR #161 | `champion` и `demote` получают ответ «не допустимо» без обращения к автору; названа проверка, которой условие меряется |
| Затем | Проводка подписи комнаты: базовая линия откатывается без единого красного теста ([#153](https://github.com/anshushunov/dungeon-fortress/issues/153)) | `defect` | Повышена из реестра долга: сработало условие «любая правка `DrawRoomLabel`», внесённая #147 | Мутант «`labelTop + icon` → `labelTop`» краснит названную проверку; проверка краснеет при откате каждой половины |
| Затем | Лента говорит «ничего не случилось», пока владение вымирает от истощения ([#154](https://github.com/anshushunov/dungeon-fortress/issues/154)) | `defect` | Заведена по находке независимого review PR #152 и **поглотила** запись реестра про незащищённый порядок уровней `StoryWeight`: та же шкала вышла на экран по умолчанию, и у записи появился видимый игроку текст. Путь 2 раздела «Когда запись повышается до Issue» | На фикстуре `neglected` шапка не противоречит строкам под ней; взаимный порядок уровней значимости закреплён проверкой |
| Затем | `combat_joined` несёт два разных факта, и контракт утверждает, что один ([#146](https://github.com/anshushunov/dungeon-fortress/issues/146)) | `defect` | Заведена по находке review #140; симптом закрыт внутри представления, перегрузка кода осталась | Либо отмена работы получает собственный reason code, либо контракт называет оба вида записи; решение принимается явно |
| Затем | Счёт партии показывается числом без опоры ([#115](https://github.com/anshushunov/dungeon-fortress/issues/115)) | `support` | Эталонные кадры `tests/golden/ui/*.json` освобождены merge #52 | Владелец после партии называет, что стоило ему очков и что принесло, не считая в уме; разбор не расходится с суммой ни на одном сценарии матрицы |
| Требует решения до реализации | Сила владения скачет почти вдвое при живых и здоровых ([#116](https://github.com/anshushunov/dungeon-fortress/issues/116)) | `support` | **Decision gate внутри задачи**: показ «максимума при здоровых» возвращает на экран потенциал, а [ADR 0015](../decisions/0015-session-of-waves-and-domain-score.md) определил силу как готовность. Решение принимается до реализации | Владелец, увидев падение силы, называет причину — голод, усталость, травмы или выбывшие — без инспектора |
| Затем | Контракт 13.2: выбор момента караула подпёрт числом, которое его не измеряет ([#126](https://github.com/anshushunov/dungeon-fortress/issues/126)) | `defect` | Контракт освобождён merge #135 и #136 | Третий пункт цепочки 13.2 либо получает число, измеряющее выбор момента караула, либо перестаёт подпираться числом; 15.5 ссылается на единственное место записи |
| ✅ Сделано | Панель клетки в паузе либо молчит о зоне, либо противоречит сама себе ([#130](https://github.com/anshushunov/dungeon-fortress/issues/130), PR #205) | `defect` | Заведена по находке review #52: канонические комнаты и свёрнутая принадлежность расходятся в окне непринятого намерения | В паузе панель отвечает одним непротиворечивым утверждением, а непринятое стирание видно на карте так же немедленно, как покраска ([#58](https://github.com/anshushunov/dungeon-fortress/issues/58)) |
| Ждёт слот снапшота | Метка «объект без зоны» не достаёт лежанки и тайлы кладовой ([#131](https://github.com/anshushunov/dungeon-fortress/issues/131)) | `support` | Меняет состав канонического снапшота, поэтому конкурирует за тот же слот, что и любая такая задача | Лежанка вне покоев отмечается так же, как столб вне тренировочной зоны; граница §4.4 контракта снята |
| Затем | Не видно, куда шёл вставший с `blocked_by_others` ([#120](https://github.com/anshushunov/dungeon-fortress/issues/120)) | `support` | — | Игрок понимает, куда существо шло и ради какой работы, не открывая инспектор. Смыкается с #77 |
| ✅ Сделано | Scale-spike: одна graybox-сцена боя в масштабах 100/150/200 % от текущего визуального размера существ; сетка, клетка и pivot у ног не меняются ([#142](https://github.com/anshushunov/dungeon-fortress/issues/142)) | `support` | Решение владельца 2026-08-01: спайк идёт в этом блоке | Выбран масштаб тела: оружие различимо без панели, тяжёлая броня отличима от лёгкой, состояния читаются, 20–30 существ не скрывают клетки и проходы. **До #77**: эффекты и анимация настраиваются под выбранный масштаб один раз. Оружие и броня показываются graybox-слоями — снаряжения в симуляции нет до среза 7 |
| Затем | Остаток долга проверяемости (#42) и долг контракта (#72, #74) | `support` | — | Единый источник эталонных кадров, контракт совпадает с кодом, экономика после среза 1 разобрана |
| Затем | Происхождение выборки guard'а ширины HUD ([#106](https://github.com/anshushunov/dungeon-fortress/issues/106)) | `support` | — | Худший случай guard'а не зависит от половины весов. **`CameraView.cs` (#109) из этой строки выделен** и поднят выше правилом 28 |
| ✅ Сделано | Вертикаль 4 #77: процедурная анимация — facing, покачивание, наклон, squash & stretch ([#221](https://github.com/anshushunov/dungeon-fortress/issues/221)) | `support` | Названа владельцем на playtest 2026-08-04: «поза замаха появилась, но это конечно не анимация» | Тело обращено туда, куда идёт и бьёт; идущее качается, стоящее нет; ни одно движение не входит в снапшот и не меняет checksum ([ADR 0011](../decisions/0011-presentation-layer-without-engine.md)) |
| ✅ Сделано | Мелкие метки на телах вышли из-под снятого кольца стороны и не названы в легенде ([#222](https://github.com/anshushunov/dungeon-fortress/issues/222)) | `defect` | Находка playtest владельца 2026-08-04: «какие-то кружочки появились на персонажах в неожиданных местах». Метки не новые — их перестал перекрывать снятый индикатор стороны (#208) | На кадре толпы владелец не спрашивает, что это за точки: либо их нет, либо каждая названа в легенде, и полнота легенды проверяется прогоном |
| `ready`, свободна | Полярность поворота закреплена текстом гарда, а текст переименовывается ([#237](https://github.com/anshushunov/dungeon-fortress/issues/237)) | `support` | Находка Ф6 подтверждающего круга review PR #235: пять мутантов проходят, и один воспроизводит блокирующий дефект двумя переименованиями связок, не тронув закреплённый литерал. Метка `ready` снималась 2026-08-04 в 11:26 UTC на время работы #244 — обе держат `BodyMotion` и поворот тел — и **возвращена после её merge**, как и было обещано | Решение «повернуться к» живёт в политике и проверяется сравнением значений, а не текста; мутанты m3, m4 и m14 краснеют. Вторым пунктом — окно гарда покачивания: 1700 стоит на ступеньке, 2600 даёт 519 шагов при пороге 100 |
| `ready`, свободна | Прогон правила 31 даёт ложный `mismatch` на любом русском заявлении ([#231](https://github.com/anshushunov/dungeon-fortress/issues/231)) | `defect` | Найдено координационной сессией 2026-08-04 при первом применении правила 31 к собственному телу PR: заявление, которое в выводе команды есть, помечается расхождением. К блоку не привязана | Заявление с кириллицей, присутствующее в выводе, даёт `match`; отсутствующее по-прежнему даёт `mismatch`; оба случая закреплены в `test-check-pr-claimed-output.ps1` |
| ✅ Сделано | Три утверждения документов о репозитории ложны ([#215](https://github.com/anshushunov/dungeon-fortress/issues/215)) | `defect` | Находка F2 review PR #212: удаление записей реестра законно, но сами ложные утверждения остались в дереве | Выполнено 2026-08-04, PR #247, коммит `5d971e4`: ни одно из трёх мест не утверждает того, чего нет, по каждому названа команда или кадр |
| ✅ Сделано | Гоблин по частям с пивотами: первый персонаж под скелетную анимацию ([#243](https://github.com/anshushunov/dungeon-fortress/issues/243)) | `art` | [ADR 0020](../decisions/0020-body-animation-cutout-rig.md), первая из двух задач пробы; исполнитель — Codex | Выполнено 2026-08-04, PR #249, коммит `f178500`: семь частей и `goblin_cutout_rig_v1.json` в дереве, реконструкция `idle` неотличима от позы v2 на контактном листе, происхождение — `docs/art/goblin-cutout-v1-provenance.md` |
| ✅ Сделано | Риг гоблина в игре: цепочка удара, отдача обеих сторон, дуэльная сцена ([#244](https://github.com/anshushunov/dungeon-fortress/issues/244)) | `support`, представление | Вход открыт merge #243 (коммит `f178500`); задача выдана координационной сессией 2026-08-04. [#237](https://github.com/anshushunov/dungeon-fortress/issues/237) на это время снята с `ready` — развести её и риг именованными членами нельзя | Выполнено 2026-08-04, PR #256, коммит `1e58568`: цепочка из пяти фаз выводится из канонического журнала, отдача видна у обеих сторон, четыре прогона `frame_pacing` дали один checksum. Щель в худшей фазе — 21 world px², измерена и названа. Ответ владельца на «приятно ли смотреть» ещё не получен |
| ✅ Сделано | Документы после принятия ADR 0020 ([#245](https://github.com/anshushunov/dungeon-fortress/issues/245)) | `docs` | — | Выполнено 2026-08-04, PR #246, коммит `2d8fc09`: обязательство ADR о ссылке на пробу исполнено, `NOW` называет текущий блок, протокол реестра записан, атрибуция `42,2 %` однозначна. Три круга review, последняя находка вынесена в #248 |
| `claimed`, PR #320 | Чем проверяется правка круга доработки ([#248](https://github.com/anshushunov/dungeon-fortress/issues/248)) | `docs` | Условие, записанное в журнал после пятого пробоя потолка, сработало на PR #246. ~~четвёртый rework-случай из шести~~ — **опровергнуто измерением самой задачи 2026-08-07**: пробоев **семь** (PR #288 случился уже после заведения Issue), rework-случаев **пять**; аудит с командами — `evidence/248-rework-pierce-audit.json` | Все пробои перечислены с источником находки последнего круга; названо, что пропускает таблица правила 27; исход выражен правкой правила, прогоном или отказом с названной ценой |
| `ready`, свободна | Docstring `DescribeMemory` несёт то же невоспроизводимое число, что убрано из `DescribeRooms` ([#250](https://github.com/anshushunov/dungeon-fortress/issues/250)) | `defect` | Находка F2 независимого review PR #247: строка не входила в партицию #215, поэтому исход вынесен отдельной задачей, а не возвратом того PR | Число либо подпёрто прогоном и командой в самом docstring, либо убрано так же, как в `DescribeRooms`; комментарий теста, пересказывающий то же число, приведён в соответствие |
| ✅ Сделано | Состояние задач в `ROADMAP.md` после merge #246 и #247 ([#251](https://github.com/anshushunov/dungeon-fortress/issues/251)) | `docs` | — | Выполнено 2026-08-04, PR #252, коммит `51b4fc7`: каждое вхождение номеров прочитано и сверено с `gh issue view`; оба замечания review — про критерии самой Issue — исправлены до merge |
| `ready`, свободна | Правило 17 обеспечено только текстом ([#253](https://github.com/anshushunov/dungeon-fortress/issues/253)) | `blocks-verification` | Третье нарушение за четыре дня, каждое своим способом: переключённый `HEAD` корня, коммит из корня, worktree внутри корня. Повышена из реестра долга — её условие сработало | Механизм существует и краснеет на подставленном нарушении, не краснея на законном состоянии; по каждому из трёх исторических случаев сказано, сработал бы он или нет |
| ✅ Сделано | Реестр держит повышенную запись: условие правила 17 сработало ([#254](https://github.com/anshushunov/dungeon-fortress/issues/254)) | `docs` | — | Выполнено 2026-08-04, PR #255, коммит `098626b`: повышенная запись удалена, протокол называет событие и время; два круга review |
| Идёт | Блок пробы ADR 0020 закрыт: `NOW`, очередь, две записи реестра, калибровка ([#257](https://github.com/anshushunov/dungeon-fortress/issues/257)) | `docs` | — | Документы знают состояние блока, находки review PR #256 лежат в реестре с условиями повышения, расход назван числом с источником, gate себе не назначен |
| `ready`, свободна | Тело не разворачивается к тому, кто его бьёт ([#259](https://github.com/anshushunov/dungeon-fortress/issues/259)) | `defect` | Находка playtest владельца 2026-08-04 на дуэльной сцене: «правда левый стоит задом почему то». Разворот к цели получает только бьющий | Оба тела размена обращены друг к другу на кадре; случай равных колонок определён правилом, а не наследованием; мутант «развернуть цель от бьющего» краснеет отдельно от мутанта «убрать разворот» |
| **Кандидат** с 2026-08-06 | Все состояния тела на риг и массовый бой без скрытия тел ([#260](https://github.com/anshushunov/dungeon-fortress/issues/260)) | `support`, представление | Назван владельцем 2026-08-04 тем же сообщением, что дало gate: «всех на эту модель перевести… и посмотрим массовый бой». Идёт после #259, иначе бой смотрится с телами, стоящими спиной | Каждое состояние переведено или оставлено плоским с названной причиной; массовый бой смотрится без скрытия; читаемость и стоимость отрисовки названы числом, а не мнением. **Решением владельца 2026-08-06 переведена в кандидаты**: тиражировать риг на все тела до того, как на одну пару приятно смотреть, — умножение неприятного. Условие возврата — снятый потолок боя, то есть новое решение владельца, а не merge какой-либо задачи |
| `ready`, свободна | Шесть записей реестра стали просроченными после gate ([#261](https://github.com/anshushunov/dungeon-fortress/issues/261)) | `docs` | Пятый gate-маркер довёл группу «блок боя» до возраста 3 при пороге 3 | По каждой из шести — один из двух допустимых исходов с причиной; условия проверены командой отдельно от возраста; протокол перечитывания записан |
| Идёт, сужена до замера | Удар плохо видно на рабочем зуме: силуэт тела почти не меняется ([#263](https://github.com/anshushunov/dungeon-fortress/issues/263)) | `support`, представление | Первая половина ответа владельца на playtest 2026-08-04 и то же «хуже», которое до него назвал независимый review PR #256. Амплитуда упирается в измеренные щели, и просто увеличить её нельзя | Названо числом или кадром, что не читалось и что стало читаться; худшая щель не больше нынешних 782 px²; кадры «до» и «после» сняты на рабочем зуме, а не только крупно. **`ready` не ставится до merge #259 и не идёт одновременно с #260** — все три трогают позу тела. **Решением владельца 2026-08-06 сужена до замера**: задача отдаёт числа — сколько миллисекунд размен занимает на экране при 1.0 и 0.5, сколько различимых поз показывает цепочка, во что упирается «приятно» — и закрывается без правки амплитуды и тайминга. Замер становится основанием отложенного решения об отставании представления |
| **Кандидат** с 2026-08-06 | Слоты снаряжения в риге: предмет становится данными ([#262](https://github.com/anshushunov/dungeon-fortress/issues/262)) | `support`, представление | Вопрос владельца 2026-08-04 о кастомизации — шлемы, броня на части, оружие, окраска. Механика «предмет висит на кости» уже работает на оружии; не хватает словаря слотов | Слот описан в формате рига; предмет надевается данными без правки кода отрисовки; оружие живёт в слоте, а не в частном случае; замер щелей прогнан с занятым слотом и без. **`ready` не ставится до merge #260** — обе держат `BodyRig.cs` и сборку тела в `Main.cs`. **Решением владельца 2026-08-06 переведена в кандидаты** вслед за #260: снаряжение относится к срезу 7 и до подтверждённого ядра не берётся. Условие возврата — вход в срез 7 либо снятый потолок боя |
| ✅ Сделано | После расхода группа стоит в покоях: измерить, что именно её там держит ([#228](https://github.com/anshushunov/dungeon-fortress/issues/228)) | `gameplay`, задача-вопрос | Вторая половина ответа владельца на gate по #201, 2026-08-04. Слот освобождён тем же gate; **решением владельца 2026-08-04** #228 берётся следующим слайсом — та же строка в «Плане сессий вперёд», и это единственное место, где решение записано дважды сознательно: очередь называет **что**, план сессий — **рядом с чем** | Простой в покоях разложен по причинам на 100 % когорты; виновный механизм назван одним из трёх и обоснован числом. Занятость клетки не чинится — #76 остаётся на срезе 6 |
| Привязано к срезу 6 | Занятость клетки ([#76](https://github.com/anshushunov/dungeon-fortress/issues/76)) вместе со стенами, дверями и светом; там же обход своих при поиске пути | `gameplay` | Начало среза 6 «пространство как оружие» (до редакции 2026-07-30 — срез 5) | Узкий коридор физически ограничивает, сколько врагов дотягивается одновременно. Обход своих делается здесь, потому что здесь тело действительно занимает клетку |

Состояние каждой строки **этой таблицы** сверено с репозиторием на 2026-08-04
командой `gh issue list --state all --limit 300 --json number,state`, а не
перенесено из прошлой редакции: закрытая задача в очереди — тот самый дефект,
ради которого написано правило факта. Сверкой 2026-08-04 найдено **шесть** строк,
стоявших предстоящими при закрытой задаче: #130, #173, #177, #182, #183, #184.
Прежняя сверка (2026-08-02, при записи правил 27 и 28) нашла одну — #179; рост
объясним и назван: между сверками прошёл день с двенадцатью merge, а тела PR
закрывающую ссылку несли не всегда.

### План сессий вперёд

Порядок блоков. Прогноз, а не обязательство. Колонка «Сессия» после 2026-08-02
несёт положение относительно текущего момента, а не номер: номера сдвигались при
каждой вставке блока и разошлись бы со ссылками на них в тексте ниже.
**Развилка пройдена 2026-08-01: `PROMOTE`**, поэтому ветка `STOP`, описанная ниже,
не реализовалась, и таблица действует как есть. Абзац про `STOP` сохранён как
запись того, что решалось. Пересматривается на каждом gate; обоснование порядка —
раздел 11 [`PITCH.md`](PITCH.md), не повторяется здесь.

| Сессия | Gameplay-слайс | Рядом | Условие входа |
|---|---|---|---|
| ✅ Закрыта `CONTINUE` 2026-08-02 | **Цель боя — свободная клетка рядом с врагом** ([#129](https://github.com/anshushunov/dungeon-fortress/issues/129)), поднят из среза 6 решением владельца 2026-08-01 | [#156](https://github.com/anshushunov/dungeon-fortress/issues/156), пак под масштаб ([#163](https://github.com/anshushunov/dungeon-fortress/issues/163)) и ADR о вердиктах ([#160](https://github.com/anshushunov/dungeon-fortress/issues/160)) — **все три смержены**, см. `NOW`. [#77](https://github.com/anshushunov/dungeon-fortress/issues/77) и [#120](https://github.com/anshushunov/dungeon-fortress/issues/120) в блок **не вошли**: #77 делит `Main.cs` с #156 | Выбран масштаб тела ([#142](https://github.com/anshushunov/dungeon-fortress/issues/142)); развилка #129 закрыта |
| ✅ Закрыта `CONTINUE` 2026-08-02 | **Память о месте берёт с кладовой цену, которой не стоит** ([#171](https://github.com/anshushunov/dungeon-fortress/issues/171), PR #178) — балансный, шёл один | [#77](https://github.com/anshushunov/dungeon-fortress/issues/77) первая вертикаль (PR #185), [#165](https://github.com/anshushunov/dungeon-fortress/issues/165) (PR #175), guard `PATH` (PR #180), автономия координатора (PR #181) — **все смержены**, см. `NOW`. [#173](https://github.com/anshushunov/dungeon-fortress/issues/173) в блок **не вошла** и переходит в следующий | Слот освобождён `CONTINUE` 2026-08-02 по блоку боя. Ограничение основания gate — в журнале |
| ✅ Закрыта решением владельца 2026-08-03 | **После боя группа не может разойтись** ([#186](https://github.com/anshushunov/dungeon-fortress/issues/186)) — задача-вопрос: измерила и не чинила | — | Вывод: виновный механизм затора — занятость клетки ([#76](https://github.com/anshushunov/dungeon-fortress/issues/76)), оставлена на срезе 6; видимая половина симптома вынесена в #201 |
| ✅ Закрыта `CONTINUE` 2026-08-04 | **Существо без работы уходит с места боя** ([#201](https://github.com/anshushunov/dungeon-fortress/issues/201), PR #217) — вела основная сессия | **[#210](https://github.com/anshushunov/dungeon-fortress/issues/210) вертикаль 3 #77** — фича блока: удар читается на экране (PR #218); [#202](https://github.com/anshushunov/dungeon-fortress/issues/202), [#196](https://github.com/anshushunov/dungeon-fortress/issues/196), [#207](https://github.com/anshushunov/dungeon-fortress/issues/207), [#183](https://github.com/anshushunov/dungeon-fortress/issues/183), [#159](https://github.com/anshushunov/dungeon-fortress/issues/159) — **все смержены**, см. `NOW` | Скоуп согласован владельцем 2026-08-03 до планирования. Вертикаль 3 сужена до обратной связи удара; процедурная анимация — вертикаль 4. Почему gameplay-слайс снова сим-стороны, а не [#157](https://github.com/anshushunov/dungeon-fortress/issues/157): #157 правит `Main.cs` вместе с #210 |
| ✅ Закрыта без gate 2026-08-04 | **После расхода группа стоит в покоях** ([#228](https://github.com/anshushunov/dungeon-fortress/issues/228)) — задача-вопрос, вела основная сессия | ✅ [#221](https://github.com/anshushunov/dungeon-fortress/issues/221) вертикаль 4 #77, процедурная анимация (PR #235) и ✅ [#222](https://github.com/anshushunov/dungeon-fortress/issues/222) мелкие метки и легенда (PR #232) — **обе смержены**; остались `ready` и свободны для любой дорожки, к блоку не привязаны: [#231](https://github.com/anshushunov/dungeon-fortress/issues/231) (ложный `mismatch` прогона правила 31) и [#237](https://github.com/anshushunov/dungeon-fortress/issues/237) (полярность поворота закреплена текстом). [#215](https://github.com/anshushunov/dungeon-fortress/issues/215) отсюда убрана 2026-08-04: она `claimed` с 2026-08-03 и свободной не была ни в одной редакции этой строки | Слот освобождён `CONTINUE` 2026-08-04 по #201. **Решение владельца 2026-08-04** взять вторую половину его наблюдения следующим слайсом. Gate этот блок не даёт: #228 не меняла поведение, `src/**` в её диффе отсутствует | Виновный механизм назван и обоснован числом; «работы нет» из объяснения **не исключено** и остаётся предметом [#72](https://github.com/anshushunov/dungeon-fortress/issues/72) |
| Затем | **История существа: значимое накапливается и различается цветом** ([#157](https://github.com/anshushunov/dungeon-fortress/issues/157)) | по gate предыдущего блока | `Main.cs` освобождается merge подзадач #77 |
| Затем | **Срез 3 — момент истины**: карточки после волны, решение наградить/наказать/игнорировать, последствие в следующей волне, все три величины | [#115](https://github.com/anshushunov/dungeon-fortress/issues/115); [#130](https://github.com/anshushunov/dungeon-fortress/issues/130) | ADR о вердиктах принят. **Балансный слайс — идёт один, не параллелится** |
| Затем | Доработка среза 3 по gate | [#116](https://github.com/anshushunov/dungeon-fortress/issues/116) с развилкой внутри; [#126](https://github.com/anshushunov/dungeon-fortress/issues/126); [#131](https://github.com/anshushunov/dungeon-fortress/issues/131) | Параллельно **ADR словаря команд ролей** поверх [ADR 0009](../decisions/0009-adherent-role-boundary.md) |
| Затем | **Срез 4 — приспешник и указ**: одно искажение намерения характером | [#42](https://github.com/anshushunov/dungeon-fortress/issues/42); [#109](https://github.com/anshushunov/dungeon-fortress/issues/109) | ADR словаря ролей принят |
| Затем | **Срез 5 — возвращающийся герой**: переживает бегство, возвращается со шрамом и памятью | [#106](https://github.com/anshushunov/dungeon-fortress/issues/106); [#74](https://github.com/anshushunov/dungeon-fortress/issues/74) | Срез 4 закрыт |

**Что было бы при `STOP` по срезу 2 — не реализовалось, gate дал `PROMOTE`.** Сессия среза 3 не сдвигалась бы, а пересматривалась: момент
истины — решение игрока о **конкретном** существе, и он опирается на ту же
читаемость, которую `STOP` объявил недостигнутой; сдвиг на сессию опору не
возвращает. Поэтому при `STOP` срез 3 переоткрывается как вопрос: чем ещё
доказывается, что решение о существе имеет последствия, если история существа
читаться не научилась. На место среза 3 встаёт **срез 6 «пространство как
оружие»** ([#76](https://github.com/anshushunov/dungeon-fortress/issues/76)) — он
от читаемости индивида не зависит, — а срезы 4 и 5 сдвигаются на сессию.

**Чего эти сессии не закрывают:** [#72](https://github.com/anshushunov/dungeon-fortress/issues/72), срез 7 «снаряжение», две-три производственные цепочки, руку хозяина, свет, автотайлинг и звук — то есть **milestone A ими не берётся**, берутся срезы питча 3, 4 и 5 из семи.

Закрыто с последней редакции очереди: **#171** — память о месте перестала стоить
владению голодной смерти после выигранного боя (PR #178); **#165** — копьё
держится во всех боевых позах (PR #175). Тем же блоком, без своих Issue: guard от
засорения пользовательского `PATH` (PR #180), автономия координатора (PR #181) и
**первая вертикаль #77** — масштаб 170 % и пак v2 в игре (PR #185, вобрал закрытый
PR #176). Раньше: **#129** — в бою цель движения стала
свободной клеткой рядом с врагом (PR #164); **#160** — принят
[ADR 0019](../decisions/0019-verdict-not-order.md) (PR #161). Раньше в том же
блоке: **#156** — граница помещения ушла под
тела (PR #166); **#163** — пак существ v2 под масштаб 170 % (PR #162). Раньше:
**#117** и **#119** — существо помнит
место, стартовая карта стала подземельем, приказ уступить не выдаётся тому, кто
его не исполнит (PR #124); **#118** и **#122** — решения сессий стали записями
в репозитории (PR #121, PR #123). Раньше в этом же блоке: **#101** (PR #114),
**#86** и **#49** (PR #113), **#90** и **#99** (PR #110), **#100** (PR #107),
**#102** (PR #108), **#103** (PR #105), **#46** (внутри PR #96).

Закрыто раньше: #38 — каждая сессия domain MCP работает со своей копией, и
`verify.ps1` проходит при подключённом сервере без ручных действий. #64 — все
`*.uid` игнорируются по единой стратегии, свежий worktree больше не отдаёт
грязное дерево после импорта проекта; правило и триггер его пересмотра в
[`ENVIRONMENT_SETUP.md`](../engineering/ENVIRONMENT_SETUP.md).

Обеспечивающие задачи занимают оставшиеся слоты блока, а не образуют блок
целиком: правило «не подменяем игру подготовкой» усилено 2026-07-31 с «не два
обеспечивающих блока подряд» до «в каждом блоке есть хотя бы одна фича» — см.
[`DEVELOPMENT_PACE.md`](../engineering/DEVELOPMENT_PACE.md).

### Следующий gameplay-блок: срез 1

Гипотеза:

> Если угроза приходит несколько раз за сессию с нарастающей силой, а между
> волнами владение восстанавливается и перестраивается, игрок связывает своё
> решение с последствием и начинает управлять осознанно, а не наблюдать.

Минимальный slice: 3–5 набегов вместо одного, фаза восстановления между ними,
явный счёт владения по итогам сессии.

Почему выбран владельцем 2026-07-28:

- «влияние не ощущается» — самый старый незакрытый продуктовый дефект. Он
  зафиксирован в [`PROTOTYPE_01_EVALUATION.md`](../playtests/PROTOTYPE_01_EVALUATION.md)
  как *«Perceived influence: not confidently perceived»* и не адресован ни одной
  из работ, сделанных после. Единственный набег через ~25 минут после решения
  даёт ровно ту разрежённость обратной связи, которая к этому и привела;
- закрывается наблюдение P2 той же оценки, где гипотеза H2 помечена
  contradicted, потому что метрика потерь делает `overrun` «лучше». Это не
  дефект метрики, а отсутствие функции ценности: игра нигде не говорит, что
  такое «хорошо»;
- появляется ритм сессии: тишина → тревога → бой → разбор → перестройка, а
  поражение перестаёт быть концом и становится сюжетом;
- стоимость низкая: таймер угрозы, генератор налётчиков, правила боя и исход уже
  реализованы и покрыты тестами. Речь о параметризации существующего;
- **любой** следующий кандидат получает нормальный испытательный стенд: без
  повторяемости влияние снаряжения, ленты или сопротивления плану нечем мерить.

Рамки, принятые владельцем 2026-07-28 вместе с выбором среза:

- **смерть обитателей окончательна, притока новых нет.** Потери необратимы,
  поэтому у поражения есть цена, а у партии — естественный конец. Приток
  населения от условий владения остаётся за milestone B;
- **партия укладывается в один сеанс, сохранений нет.** Кампания с сохранением
  между сеансами относится к milestone B и требует отдельного блока работы.

Обе рамки перенесены в закрытые развилки раздела 10 [`PITCH.md`](PITCH.md).
Implementation Issue создаётся после закрытия ремонтного блока.

## Кандидаты после текущего блока

### Ведущее направление — визуальная проработка боя

> **Закрыто решением владельца 2026-08-06.** Направление свёрнуто, бою поставлен
> потолок «читаемый, а не красивый»; порядок задач ниже **больше не действует** и
> сохранён как запись того, из чего направление собиралось. Что стало с каждой
> задачей: #259 — сделана (PR #268, `8850209`); #263 — сужена до замера и правки
> не делает; #260 и #262 — переведены в кандидаты с условиями возврата в строках
> очереди. Основание и разбор — строка журнала gate-решений от 2026-08-06 и
> [#272](https://github.com/anshushunov/dungeon-fortress/issues/272). Следующий
> блок — срез 3 «момент истины».

**Решение владельца 2026-08-04**, строка в журнале gate-решений выше: «Бой —
важная часть игры, хочется её в начале проработать так чтобы было симпатично, без
этого дальнейшая разработка не имеет смысла». Это единственное место, где порядок
направления собран целиком; условия входа живут в самих задачах, и здесь они сводятся; там, где
условие ставится этой правкой впервые, это сказано в самой строке.

| Порядок | Задача | Состояние | Почему здесь |
|---|---|---|---|
| 1 | [#259](https://github.com/anshushunov/dungeon-fortress/issues/259) тело не разворачивается к тому, кто его бьёт | `ready` | Дефект, названный владельцем на playtest. Идёт первым: пока тела стоят спиной, любой следующий вопрос playtest отравлен известным дефектом |
| 2 | [#263](https://github.com/anshushunov/dungeon-fortress/issues/263) удар плохо видно на рабочем зуме | ждёт merge #259 | Вторая половина того же playtest — «на демке плохо видно» — и единственное «хуже», названное review PR #256. Амплитуда упирается в измеренные щели, поэтому это работа, а не подкрутка |
| 3 | [#260](https://github.com/anshushunov/dungeon-fortress/issues/260) все состояния тела на риг и массовый бой | ждёт merge #259 | Назван владельцем тем же сообщением. С #263 одновременно не идёт: обе трогают позу тела |
| 4 | [#262](https://github.com/anshushunov/dungeon-fortress/issues/262) слоты снаряжения в риге | ждёт merge #260 | Ответ на вопрос владельца о кастомизации: механика «предмет висит на кости» уже работает на оружии, не хватает словаря слотов. **Условие входа поставлено этой правкой, а не сведено из задачи** — основание: обе держат `BodyRig.cs` и сборку тела в `Main.cs`, одновременно идти не могут. Дописано и в саму #262 |

**Дата решения — 2026-08-04**, и это стоит сказать здесь, потому что соседние
записи её путают: смерженный PR #258 записал playtest, gate и состояние блока
днём **2026-08-05**, которого в дереве нет (`git log -1 --format='%aI'` по
коммитам той сессии даёт `2026-08-04T19:09+03:00`, `gh issue view … --json
createdAt` по задачам, вышедшим из playtest, — тот же день). Сдвиг найден
независимым review PR #265 и вынесен в
[#266](https://github.com/anshushunov/dungeon-fortress/issues/266) вместе с
[#261](https://github.com/anshushunov/dungeon-fortress/issues/261), потому что
журнал gate-решений — источник якорей для арифметики возраста записей реестра, и
разъезд дат между этими двумя правками стоил бы второго такого же дефекта.

**Что это значит для остальных кандидатов.** Список ниже не переписан и не
переупорядочен внутри себя: срез 3 «момент истины», занятость клетки, долг
экономики и всё прочее остаются на своих местах — они идут **после**
направления. Сроков владелец не называл, и roadmap их не придумывает.

**Чего в направлении нет намеренно:** второй расы, перекраски палитрой и
комплекта брони как арта. Снаряжение по срезу 7 начинается со слотов (#262) —
кода, а не картинок; комплект рисуется отдельной art-задачей, когда слоты
окажутся в дереве, и по тому же правилу «проба на одном персонаже», по которому
принят [ADR 0020](../decisions/0020-body-animation-cutout-rig.md).

### Список направлений

Это направления, а не утверждённая очередь; implementation Issues для них не
создаются до выбора владельца. Порядок и обоснование — раздел 11
[`PITCH.md`](PITCH.md), номера срезов оттуда же.

Нумерация — редакции раздела 11 [`PITCH.md`](PITCH.md) от 2026-07-30: прежние
срезы 2 и 3 объединены, момент истины и возвращающийся герой получили
собственные срезы, пространство сдвинулось 5→6, снаряжение 6→7.

| Срез | Что доказывает | Минимальный slice | Состояние |
|---|---|---|---|
| 1 | Повторяющееся давление делает влияние ощутимым | Несколько волн нарастающей силы, фаза восстановления, счёт владения | **Сделано**, `CONTINUE` 2026-07-29 |
| 2 | История влияет на поведение: событие прошлого меняет следующее решение существа, и это читается | Минимальный адаптер событий (имена и предложения вместо reason codes) плюс одно именованное существо, одна память, один наблюдаемый отказ с видимой личной причиной | **Сделано**, `PROMOTE` 2026-08-01 — см. журнал. Взят с третьего захода: два `ADJUST` подряд ушли на то, чтобы память существа стала **читаемой**, а не только правдивой. Форма памяти выбрана владельцем и записана [ADR 0018](../decisions/0018-creature-memory-of-place.md) — память **о месте**. Здесь заканчивается отсрочка ADR 0002/0006, но **только в части долговременной памяти**: отношения между существами остаются отложенными. Объединил прежние срезы 2 и 3 (2026-07-30): лента без события, меняющего будущее, не может провалиться на playtest и потому ничего не измеряет |
| 3 | Решение игрока о конкретном существе имеет последствия | Момент истины: 3–5 карточек после волны, одно-два решения — наградить, наказать, игнорировать; последствие видно в следующей волне | Кандидат; вводит **все три величины** — страх, выгоду и обиду — решением владельца 2026-07-30; обязателен поимённый разбор слагаемых в стиле 6.6 питча, и критерий выхода требует, чтобы игрок мог назвать, какая величина объясняет поведение |
| 4 | Власть через людей богаче власти через ползунки | Один приспешник, один указ, одно искажение намерения характером | Кандидат; требует ADR со словарём команд ролей поверх [ADR 0009](../decisions/0009-adherent-role-boundary.md) |
| 5 | Внешнее давление — чьё-то решение, а не таймер | Возвращающийся герой: именованный, переживает бегство, возвращается через одну-две волны с усилением, шрамом и текстовой памятью о прошлом столкновении | Кандидат; самый дешёвый генератор историй — бой уже канонический и воспроизводимый. Первое воплощение событийного слоя [ADR 0015](../decisions/0015-session-of-waves-and-domain-score.md); идёт после приспешника, потому что внутренний конфликт доказывается раньше внешнего слоя |
| 6 | Пространство работает как оружие | Свет и тьма меняют исход волны | Кандидат; смыкается с блоком представления — освещение нужно и для читаемости. Занятость клетки (#76) привязана сюда |
| 7 | Подготовка окупается предметно | Сырьё → мастерская → простое оружие → автономное получение снаряжения → изменённый бой | Кандидат; стоит последним осознанно: после социального слоя предмет — награда, статус и повод для конфликта (6.9 питча), а не только `+damage` |
| — | Глубина строительства: двери, стены, ловушки, свойства комнат | Один объект и одно последствие для маршрута или боя | Продолжение текущей линии, а не шаг питча. Доступно по `CONTINUE CONSTRUCTION`. Двери и строимые стены становятся обязательными на срезе 6 по условию перехода [ADR 0013](../decisions/0013-what-is-a-room.md) (в тексте ADR он назван «срезом 5» по нумерации до 2026-07-30) |

Кандидаты, пришедшие из обсуждения боя 2026-07-28 (разобраны 2026-07-29) и из
ревью референсов 2026-07-30. Они не образуют собственного шага питча, а
достраивают уже названные срезы:

| Кандидат | Что доказывает | Куда пристёгнут | Состояние |
|---|---|---|---|
| **Локализованные травмы, которые остаются** ([`PITCH.md`](PITCH.md) 6.13) | Бой производит персонажей, а не числа; у последствий появляется деление между «цел» и «мёртв» | Достраивает срез 1: между волнами появляется выбор — чинить, беречь или выпускать | Кандидат; переиспользует уже реализованные injuries, новых видов управления не вводит |
| **Боевые черты, меняющие решение** — трус, осторожный, бешеный, толковый | Разные характеристики дают разное поведение, а не разное число | Смыкается со срезом 2 «история влияет на поведение»: у решения уже есть reason code, поэтому «почему он побежал» показывается существующим механизмом | Кандидат; требует одной дешёвой вещи — оценки тайла по числу проходимых соседей, из которой выводятся «горло», «открытое место», «окружают» |
| **Мили и ранжед как роли** | Позиция значит разное для разных бойцов; свой в линии блокирует выстрел, поэтому строй возникает сам | Срез 7 | Кандидат. Линия видимости — основная цена, решение по ней остаётся во входящих до среза 7 |
| **Событийный слой давления** | Давление — чьё-то решение, а не таймер; волна становится одним событием из нескольких | Первое воплощение — срез 5 «возвращающийся герой»; полная форма после него | Кандидат, зафиксирован [ADR 0015](../decisions/0015-session-of-waves-and-domain-score.md). Смыкается с 6.7 «антагонист с именем» и с ролью storyteller |
| **Способности как черты** | Существо узнаётся по тому, что оно умеет, а не только по характеристикам | Срез 7 вместе со снаряжением | Кандидат. Разрешены срабатывающие и пассивные, у существа их несколько, приобретаются в том числе от пережитого, бывают разных тиров — [`PITCH.md`](PITCH.md) раздел 9 |
| **Совет приспешника на карточке момента истины** — одна строка совета, искажённого характером (приём King of Dragon Pass, см. [`REFERENCES.md`](REFERENCES.md)) | Характер читается до того, как приспешнику доверено направление; карточка становится сценой, а не отчётом | Пристёгнут к срезу 3, **вне его критерия выхода** — срез не растёт; смыкается со срезом 4 | Кандидат; принят владельцем 2026-07-30 на ревью референсов |
| **Труп как вещь, а несколько вещей — кучей на клетке** | Побеждённый перестаёт быть анимацией и становится предметом, который можно взять; клетка учится нести больше одного предмета | Срез 7 «подготовка окупается предметно» вместе со снаряжением; питч уже обещает это в 6.9 «Трофеи» | Кандидат; наблюдение владельца на playtest 2026-07-30 — «трупы на земле сейчас просто анимация, а не вещь». Смыкается с занятостью клетки (#76): и то и другое требует, чтобы клетка знала, что на ней лежит |

## Представление

Это **не** production art. Проекция закрыта
[ADR 0008](../decisions/0008-three-quarter-projection.md); здесь перечислена
работа слоя отрисовки, которая дважды блокировала playtest и оценивается по
отношению «эффект / стоимость».

Причина, по которой это отдельный от art раздел: плавности не было по
архитектурной причине, а не по художественной — `TicksPerSecond = 6.0` и позиция
существа, равная `tile * TileSize`. Никакой генеративный пайплайн этого не
исправляет; исправил Presentation pass A.

### Сделано

| # | Работа | Где |
|---|---|---|
| 1 | Рендер отделён от тика, позиция интерполируется по `alpha` | Presentation pass A (#36) |
| 2 | HUD на Control-контейнерах вместо абсолютных `Rect2` | Presentation pass A (#36), опирался на #28 |
| 12 | Правило «метка не скрывает тело» проверяется в CI, а рамка выделения повторяет видимую массу породы | #90 и #99, PR #110 |
| 13 | Стартовый кадр и масштаб интерфейса выводятся из экрана, а не из авторской рамки 1280×720 | #100, PR #107 |
| 14 | Читаемость HUD при максимизации стала исполняемым правилом, стартовый зум выводится из кадра, а `--strict-hud-fit` перестал быть инертным | #86 и #49, PR #113 |

### Блок «реализация ADR 0008» — один элемент очереди, а не фон

Самый дорогой невыполненный блок в проекте. Он блокирует #52 и требует нового
asset pack, поэтому не начинался до того, как срез 1 дал A0. Milestone взят
2026-07-29; шаги 1 и 2 выполнены, открыт только шаг 3, и он ждёт свободного
слота по составу канонического снапшота.

| # | Работа | Issue | Что даёт |
|---|---|---|---|
| 4 | ✅ Тайл 22 → **40 px** как параметр, камера с дискретным зумом и панорамой, HUD в `CanvasLayer`, `aspect` `expand`, масштаб интерфейса параметром запуска, явные кадр/позиция/зум при съёмке | [#79](https://github.com/anshushunov/dungeon-fortress/issues/79), PR #81 | Выполнено 2026-07-29. При 22 px спрайт физически не способен показать направление и состояние, а карта не занимала и половины кадра |
| 3 | ✅ Стена «верх + фасад», порядок отрисовки по Y по интерполированной глубине, 16 вариантов соединения; выбран immediate-режим, чтобы не терять интерполяцию и тесты без движка | [#83](https://github.com/anshushunov/dungeon-fortress/issues/83), PR #85 | Выполнено 2026-07-30. Геометрия пещеры вместо контурных квадратов; правила глубины и геометрии стены вынесены в `Presentation` и покрыты тестами |
| 5 | Читаемость помещений: сущность комнаты в снапшоте, явная граница, покрытие пола по назначению, иконка, подпись с состоянием, показ незавершённой комнаты | [#52](https://github.com/anshushunov/dungeon-fortress/issues/52) | Отложенный `ADJUST` по функциональной комнате |

Порядок в таблице — порядок исполнения, а не нумерация: пункт 4 идёт первым,
потому что размер тайла и камера задают кадр, в котором рисуется всё остальное.
Пункт 5 идёт последним, потому что он единственный меняет состав канонического
снапшота.

Условие входа выполнено: [ADR 0013](../decisions/0013-what-is-a-room.md) принят
2026-07-29. Комната — **производная сущность** поверх зоны и объекта, которую
игрок размечает кистью; переход к модели «комната выводится из замкнутого
пространства» привязан к срезу «пространство как оружие» условием, записанным в
ADR (там он назван срезом 5; с редакции 2026-07-30 это срез 6). Практическое
следствие для пункта 5: читаемость держится **собственным покрытием пола** по
назначению, а не плёнкой поверх общего пола — приём Dungeon Keeper; граница
рисуется явно, иначе повторяется известная жалоба на Dwarf Fortress.

Сущность комнаты входит в канонический снапшот, то есть меняет его состав. Это
инвариант по [ADR 0010](../decisions/0010-contract-invariants-and-tuning.md):
контракт Prototype 1 обновляется вместе с реализацией.

### Кандидаты слоя отрисовки

| # | Работа | Что даёт |
|---|---|---|
| 6 | `CanvasModulate` + `PointLight2D`, тьма вне освещённого | Сетка становится подземельем; больше эффекта, чем от любого art pass. Связано со срезом 6 — свет как механика |
| 7 | Процедурная анимация: facing и flip, bob при ходьбе, наклон, squash & stretch на ударе | ~80 % «живости» без единого нового кадра |
| 8 | Партиклы, hit-stop, вспышка, всплывающие числа урона | Делает бой читаемым |
| 9 | Иконки-мысли над головой (еда / устал / «!») | Читаемость намерений без открытия инспектора; на дальнем зуме читается не существо, а что с ним происходит |
| 10 | Scale-spike: одна и та же graybox-сцена боя — 20–30 существ, тело и оружие отдельными слоями, idle, ходьба, замах, попадание, падение, узкий проход — в масштабах 100/150/200 % при реальном разрешении и дефолтной камере | Выбор визуального размера существ до настройки эффектов #77. Правила: логическая клетка остаётся 1×1, позиция определяется клеткой, pivot у ног, Y-sort по ногам (уже так после #83), слои одного существа используют один канвас и один pivot; спрайт может выходить за клетку, не занимая её |
| 15 | **Отставание представления: анимация перестаёт помещаться в один тик.** Слой представления рисует на тик-два позади симуляции и получает окно 500–1000 мс вместо 167 мс (`TicksPerSecond = 6.0`) или 333 мс на скорости 0.5. Приём отраслевой: Source рисует клиента на 100 мс в прошлом (`cl_interp 0.1`) ровно затем, чтобы всегда иметь две известные точки; Warcraft 3 и DotA идут вторым путём — симуляция фиксирует эффект в damage point, а остаток анимации чисто визуален и отменяем | Удар получает время, которого ему не хватает: владелец на playtest 2026-08-06 назвал размен слишком быстрым **даже на скорости 0.5**. Затрагивает только представление — снапшот, checksum и детерминизм не меняются, [ADR 0011](../decisions/0011-presentation-layer-without-engine.md) не нарушается; цена — картинка отстаёт от состояния, и представление обязано хранить события наперёд. **Отложено, а не отвергнуто, решением владельца 2026-08-06** вместе с потолком боя. Условие возврата: снятый потолок либо новая жалоба владельца на бой после того, как ядро подтверждено. Реализация потребует ADR, сужающего ADR 0011, — до начала работы, а не постфактум |
| 11 | Состояние владения иконками и шкалами вместо строк: полоса до волны, иконка с числом налётчиков, запасы значком, а не текстом | Жалоба владельца на playtest 2026-07-30 — «очень много букв». Прежняя привязка «идёт вместе с #77» **снята решением владельца 2026-08-01**: пункт не делается сейчас, и ADR, сужающий ADR 0011, не заводится. Условие ниже остаётся в силе на случай возврата |

Пункт 11 добавлен 2026-07-30 и несёт **условие, без которого его нельзя делать**.
Текстовый HUD в этом проекте — не стилистика, а несущая конструкция
проверяемости: эталоны `tests/golden/ui/*.json` являются снятыми **текстами**,
`AssertLabelsFit` мерит строки, и весь отказ от пиксельных эталонов держится на
том, что состояние интерфейса выражено словами. Прямая замена строк иконками
уничтожает единственный способ проверять HUD без сравнения картинок, а
пиксельные эталоны отвергнуты осознанно — они падают от драйвера и повторно
открывают незакрытый art-direction gate.

Условие: **каноническим остаётся текстовое состояние, иконки и шкалы рисуются из
него.** Оно опирается на границу
[ADR 0011](../decisions/0011-presentation-layer-without-engine.md) —
`Presentation` отдаёт проверяемые данные, адаптер рисует, — но **сужает её**:
сам ADR 0011 проводит границу по сборкам и не требует, чтобы каноническая форма
состояния HUD оставалась текстом. Слой, отдающий структурные значения вместо
строк, ADR 0011 не нарушает; это условие — нарушает. Разница существенна,
потому что весь отказ от пиксельных эталонов держится именно на текстовой
форме. Если пункт 11 дойдёт до реализации, сужение оформляется ADR до начала
работы, а не постфактум. Так сделана панель на иконках, и так же обязан быть
сделан HUD, иначе следующая правка вёрстки перестанет ловиться чем-либо, кроме
глаза владельца.

Пункты 7–9 собраны в **[#77](https://github.com/anshushunov/dungeon-fortress/issues/77)** и идут **сразу за** блоком ADR 0008, а не внутри него. Решением владельца 2026-08-01 пункт 7 расширен: процедурная анимация покрывает и **работу с объектом** — существо видно работающим с лежанкой, столбом и складом, — а не только удар. Основание: владелец назвал «взаимодействием» и столкновение существ в бою, и обращение существа с объектом. Жалоба подтверждена дважды независимо: сначала по одиночному набегу, затем на playtest среза 1 — «без какой-то визуализации атаки смотрится пока так себе». Срез 1 превратил разовую претензию в постоянную, потому что бой теперь повторяется четыре раза за партию. Раньше реализации ADR 0008 не делается: при тайле 22 px эффекты сливаются в кашу.

Пункт 10 **выполнен** — Issue [#142](https://github.com/anshushunov/dungeon-fortress/issues/142),
PR #143. Оружие и броня в сцене — graybox-слои: снаряжения в симуляции нет до
среза 7, поэтому иначе спайк ответил бы только про обзор и ничего про экипировку.
Владелец выбрал **170 %**, прокликав варианты в живой сцене; сцена принимает
любое значение `--scale`, ограничение из трёх значений стоит только в
скрипте-обёртке. Решение и его размен — в журнале gate-решений.

Пункт 10 добавлен 2026-07-30 и идёт **перед #77**: владелец хочет более
читаемый бой с различимой экипировкой у крупных персонажей, и замах, партиклы и
иконки состояния настраиваются под выбранный масштаб тела один раз, а не
переделываются после смены размера. Прогноз: текущий размер окажется мал для
брони и оружия, 200 % — эффектно, но давит обзор, ~150 % — рабочий компромисс;
проверяется сценой, а не отдельными увеличенными спрайтами. Канвас спрайта —
tuning представления по [ADR 0010](../decisions/0010-contract-invariants-and-tuning.md),
ADR не требуется; сетка, снапшот и словарь команд не затрагиваются.

## LATER — только после подтверждения локального ядра

### Глобальный уровень

Фракции, поселения, торговые маршруты, вылазки и события внешнего мира нужны
игре, но пока не должны определять архитектуру локального прототипа. Первый
global spike начинается только когда:

- локальная экономика содержит минимум две связанные цепочки;
- пространство влияет на производство или защиту;
- именованные существа имеют хотя бы один проверенный источник индивидуальной
  истории;
- автоматический конфликт имеет понятные входы и последствия.

Глобальный уровень сначала поставляет локальному владению один ресурс, одну
угрозу и одно решение. Полная симуляция мира не является первым шагом.

### Production art и масштаб контента

**Представление и production art — разные вещи с разными gate.** Работа слоя
отрисовки идёт в общей очереди, потому что без неё playtest измеряет качество
рендера, а не качество механики. Откладывается сюда только production art.

До подтверждения игрового ядра используются небольшие воспроизводимые asset
packs в геометрии [ADR 0008](../decisions/0008-three-quarter-projection.md).
Полный художественный pipeline, большой каталог существ, анимаций, биомов и
интерфейсов, выбор палитры и уровня стилизации откладываются до отдельного
art-direction gate.

Способы получения анимации, включая cutout-скелет и пререндер 3D, описаны в
[`ANIMATION_PIPELINE.md`](../art/ANIMATION_PIPELINE.md). Специализированные
генеративные инструменты выбираются spike-процедурой
`docs/engineering/TECH_RADAR.md`, а не из обсуждения.

## Постоянные workstreams

| Направление | Текущее состояние | Следующее доказательство |
|---|---|---|
| Session loop | Четыре волны за партию, три формы конца, дурная слава и сила владения; партия кончается сама. Слава больше не служит счётом партии — [ADR 0016](../decisions/0016-party-score-separate-from-renown.md); принцип «порог славы открывает и угрозу, и возможность» записан в питч 2026-07-30, реализация порогов — B1. Счёт партии реализован (#92, PR #96) и получил `CONTINUE` 2026-07-31 | Счёт получает опору, по которой его читают (#115), и объяснимую силу владения (#116); затем событийный слой: первое воплощение — возвращающийся герой (срез 5), давление становится чьим-то решением. |
| Construction | Выкопать, построить и использовать комнату можно; ADR 0012 | Двери, стены, ловушки или свойства комнаты как выбор пространства. |
| Narrative | Event log и reason codes есть, слоя перевода нет | Срез 2 «история влияет на поведение» — **сделано**, `PROMOTE` 2026-08-01 (#117 и далее): минимальный адаптер событий плюс память о месте, меняющая решение существа ([ADR 0018](../decisions/0018-creature-memory-of-place.md)); затем момент истины (срез 3). |
| Economy | Пища и Stone logistics работают, камень расходуется | Ввести альтернативную стоимость и связь со снаряжением/боем. |
| Combat/RPG | Повторяющиеся волны, HP, injuries с заживлением, morale; дальность — свойство атаки | Scale-spike по размеру существ, читаемость боя (#77), затем занятость клетки (#76) на срезе 6 и роли со снаряжением на срезе 7. |
| Individuality/simulation | Имена, свойства и история решений есть; паника стала решением конкретного существа (#101). Форма долговременной памяти выбрана — [ADR 0018](../decisions/0018-creature-memory-of-place.md), память о месте; **отношения между существами остаются отложенными** [ADR 0006](../decisions/0006-defer-relations-from-prototype-1.md) | Срез 2 — сделано, `PROMOTE` 2026-08-01. Срез 3: страх, выгода и обида с поимённым разбором слагаемых. Отношения — отдельным ADR, среза за ними пока не закреплено. |
| Global layer | Не начат | Один внешний источник ресурса/угрозы после подтверждения локального ядра. |
| Представление | Интерполяция, Control-вёрстка HUD, панель на иконках и немедленная обратная связь на паузе приняты; граница «проекция против мира» зафиксирована; кадр конца партии закрыт guard'ом; тайл 40 px, камера с дискретным зумом и панорамой, HUD в `CanvasLayer`, воспроизводимая съёмка с явными параметрами кадра (#79); **объёмные стены, порядок по интерполированной глубине, правила глубины и геометрии стены вынесены в `Presentation`** (#83); **правило «метка не скрывает тело» проверяется в CI, рамка выделения повторяет объём стены** (#90 и #99); стартовый кадр выводится из экрана (#100); **читаемость HUD при максимизации стала исполняемым правилом** (#86 и #49, PR #113) | Шаг 3 ADR 0008 — читаемые помещения (#52), единственный шаг блока, меняющий канонический снапшот; выполнен (#52, PR #135); слот по снапшоту и контракту освободился merge #117 (PR #124), и обе задачи закрыты. Затем scale-spike и обратная связь удара (#77). |
| Art | Первый goblin pass и icon pack v1; проекция закрыта ADR 0008, реализации нет | Asset pack постройки в геометрии ADR 0008 — после реализации проекции. Генерация у Codex. |
| Agent tooling | Deterministic CLI/API/MCP работают; чистый слой представления в CI; domain MCP больше не блокирует сборку (#38); единая стратегия `.uid` (#64); `verify.ps1` стадийный (#40); негативные сценарии доказывают живость HUD- и camera-guard'ов (#79); воспроизводимый evidence bundle с манифестом и SHA-256, диагностика GitHub-аутентификации без секретов (#87); guard стадий разбирает dot-source файлы (#102, PR #108); правила версионирования снапшота записаны и проверяются (#103, PR #105); формат скорости не зависит от культуры машины (#46, внутри PR #96); **живость HUD-guard'а доказана, `--strict-hud-fit` перестал быть инертным** (#49 и #86, PR #113) | #42 — единый источник эталонных кадров; #106 — происхождение выборки guard'а ширины HUD; #109 — разделение `CameraView.cs`. Все три после текущего блока. |

## Когда roadmap пересматривается

Обязательное обновление происходит:

1. после каждого owner gate — запись в журнал gate-решений
   [`GATE_DECISIONS.md`](GATE_DECISIONS.md) и правка `NOW` здесь;
2. при изменении порядка блоков в разделе «Очередь»;
3. перед первым global-layer spike;
4. при срабатывании критерия отказа — два `ADJUST` подряд без улучшения;
5. если новое технологическое ограничение меняет стоимость или проверяемость
   разработки агентами;
6. если меняется продуктовый фокус в [`PITCH.md`](PITCH.md) — порядок кандидатов
   следует за питчем, а не наоборот.

Если roadmap расходится с Accepted ADR или актуальным design-контрактом, он не
переопределяет их молча: сначала оформляется новое решение или обновляется
контракт, затем roadmap.
