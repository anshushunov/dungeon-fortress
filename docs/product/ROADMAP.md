# Roadmap разработки

Статус: живой план  
Дата обновления: 2026-07-28  
Владелец направления: владелец проекта  
Источник конкретной работы: GitHub Issues и Pull Requests

## Зачем нужен этот документ

Roadmap показывает не все возможные механики, а **какую игровую гипотезу мы
проверяем сейчас, что уже доказано, в каком порядке стоит очередь работ и где
владелец выбирает дальнейшее направление**.

У roadmap нет календарных обещаний. Проект разрабатывается одним человеком с
агентами и движется короткими запускаемыми срезами. Поэтому следующий крупный
этап уточняется после человеческого playtest, а не планируется заранее до
деталей.

Стабильное продуктовое обещание находится в [`VISION.md`](VISION.md), главная
идея и core gameplay — в [`PITCH.md`](PITCH.md), текущее устройство механик — в
`docs/design/`, причины важных решений — в `docs/decisions/`. Этот документ
отвечает только на вопросы «где мы сейчас?», «что делается следующим?» и «какие
направления можно выбрать дальше?».

## Как владелец управляет направлением

После каждого playtest владелец даёт одну из команд:

| Команда | Что происходит |
|---|---|
| `CONTINUE` | Начинается следующий минимальный шаг текущего эксперимента. |
| `ADJUST: <что изменить>` | Следующий шаг не начинается; текущий slice исправляется и проверяется снова. |
| `PROMOTE: <направление>` | Указанное направление становится следующим активным прототипом. |
| `DEFER: <направление>` | Направление остаётся в roadmap, но не получает Issue. |
| `STOP` | Эксперимент закрывается без дальнейшего расширения. |
| `MERGE #N` | Владелец явно разрешает merge уже проверенного PR. |

Координационный чат записывает решение в **журнал ниже**, обновляет разделы
`NOW` и «Очередь» и только затем создаёт следующий implementation Issue.

## Журнал gate-решений

Решение, оставшееся только в комментарии PR или в чате, не считается принятым:
его нельзя ни найти, ни посчитать. Критерий отказа опирается на этот журнал, а
не на память участников.

Колонка `ADJUST` — счётчик подряд идущих `ADJUST` по **одной и той же
гипотезе**; она обнуляется, когда гипотеза закрыта или заменена.

| Дата | Что проверяли | Решение | `ADJUST` | Что из него вышло |
|---|---|---|---|---|
| 2026-07-27 | Prototype 1 «Подготовка к набегу» (#12) | `ITERATE` | 0 | Readability pass (#19) и первый art probe; [`PROTOTYPE_01_EVALUATION.md`](../playtests/PROTOTYPE_01_EVALUATION.md) |
| 2026-07-27 | Dungeon shaping — раскопка (#24) | `CONTINUE` | 0 | Шаг 2: логистика камня (#26) |
| 2026-07-28 | Dungeon shaping — логистика камня (#26) | `CONTINUE` | 0 | Шаг 3: функциональная комната (#48) |
| 2026-07-28 | Питч как продуктовый фокус (PR #31) | `PROMOTE` | — | [`PITCH.md`](PITCH.md) принят; ADR 0009; порядок кандидатов в roadmap |
| 2026-07-28 | Presentation pass A (#36, #39) | принято | 0 | Интерполяция движения и Control-вёрстка HUD |
| 2026-07-28 | Functional room (#48) | `ADJUST: визуал комнат выделить` | 1 | Issue #52; отложен в блок реализации ADR 0008 — см. правило отложенного `ADJUST` |
| 2026-07-28 | Панель на иконках (#55) | дефекты | — | Issue #58 (разметка на паузе), PR #59 (размер подсказок) |
| 2026-07-28 | Эксперимент #23 целиком | `PROMOTE SESSION LOOP` | сброшен | Срез 1 становится следующим gameplay-блоком; полная переработка roadmap |
| 2026-07-28 | Размер подсказок панели (PR #59) | `MERGE #59` | — | Смержен; в ремонтном блоке остаётся #58 |
| 2026-07-28 | Развилки 1 и 2 раздела 10 [`PITCH.md`](PITCH.md) | Смерть окончательна, притока нет; партия — один сеанс без сохранений | — | Условие входа в срез 1 выполнено; развилки перенесены в закрытые |

## Правила движения

- Одновременно активен **один** gameplay slice и не более одной обеспечивающей
  задачи по tooling, представлению или art. Раздел «Очередь» задаёт порядок, а
  не разрешение работать параллельно.
- Каждый шаг заканчивается запускаемой игрой, зелёными проверками и playtest
  владельца.
- Следующий gameplay Issue не реализуется до решения
  `CONTINUE / ADJUST / PROMOTE / DEFER / STOP` и записи этого решения в журнал.
- **Отложенный `ADJUST`.** `ADJUST` разрешено принять как отложенный, если его
  исполнение целиком попадает в уже названный блок очереди. Тогда в журнале
  фиксируется, в какой именно блок он отложен, и слайс считается закрытым.
  Отложенный `ADJUST` продолжает считаться в счётчике критерия отказа. Без такой
  записи `ADJUST` блокирует запуск любого следующего блока.
- **Класс «блокирует проверяемость».** Обеспечивающая задача, которая мешает
  запустить проверки или воспроизвести состояние, не ждёт gate и не занимает
  слот обеспечивающей задачи: её ожидание дороже её исполнения. Класс
  присваивается явно в Issue, а не по желанию исполнителя.
- Claude Code является основным пишущим агентом. Координационный чат ведёт
  roadmap, декомпозицию, продуктовые развилки и приёмку.
- **Изображения генерирует Codex.** Координационная сессия формулирует запрос —
  что нужно, в какой геометрии, в каком состоянии, — но не создаёт изображения
  сама. Подробности в [`ANIMATION_PIPELINE.md`](../art/ANIMATION_PIPELINE.md).
- Сгенерированные ассеты сначала получают prompt и provenance, затем передаются
  implementation-агенту для подключения.
- Roadmap обновляется на decision gate, а не после каждого технического коммита.
- Изменение tuning-слоя (числа, формулы, пороги, планировка) не требует
  обновления контракта и ADR — только зелёных проверок и записи в PR. Изменение
  инварианта требует ADR. Граница — в
  [ADR 0010](../decisions/0010-contract-invariants-and-tuning.md).
- Новые движки, SDK, MCP и генеративные инструменты проверяются по
  `docs/engineering/TECH_RADAR.md` на границе прототипов либо перед крупным
  обновлением, но не ради каждой новой версии.

### Чего мы не делаем

Эти формулировки дешевле принять заранее, чем спорить о них на decision gate.

- **Не строим симуляцию ради симуляции.** Система без наблюдаемого игроком
  последствия не входит в срез.
- **У каждой новой системы есть бюджет читаемости в UI.** Если её нельзя
  показать игроку, её не добавляем.
- **Не универсализируем заранее.** Один объект и одно последствие вместо
  каталога и редактора.
- **Не подменяем игру подготовкой.** Работа над представлением и инструментами
  не создаёт продуктового доказательства, поэтому не может стоять в очереди
  двумя блоками подряд без gameplay-блока между ними.
- **Pre-production не длится бесконечно.** См. критерий отказа ниже.

### Критерий отказа

> Два `ADJUST` подряд по одной гипотезе без улучшения на playtest закрывают
> возможность третьего. Владелец выбирает `PROMOTE` или `STOP`.

Счёт ведётся по колонке `ADJUST` журнала gate-решений. Без записанного счётчика
это правило не срабатывает никогда, а `STOP` остаётся словом в словаре.

## Целевая игра

Продуктовый фокус и ответ на вопрос «что здесь самое интересное» находятся в
[`PITCH.md`](PITCH.md) — документ принят владельцем 2026-07-28. Roadmap ему не
противоречит и не пересказывает его: обоснование порядка срезов живёт в разделе
11 питча, здесь остаётся только очередь и состояние.

Обещание в одном абзаце:

> Игрок вырубает из скалы собственное подземное владение и населяет его
> существами, которые живут своей жизнью. Он не отдаёт приказов — он проектирует
> пространство, задаёт правила и иногда вмешивается рукой. Владение растёт,
> начинает притягивать новых обитателей, обзаводится производством, обороной и
> внутренними конфликтами. Регулярно приходят те, кто хочет забрать накопленное.
> Каждая партия оставляет несколько историй, которые игрок может пересказать
> словами.

Критерий приоритизации, вытекающий из питча: **работа, усиливающая столкновение
плана и людей, важнее работы, усиливающей подготовку или проверку.**

Игры, за которыми стоит идти за конкретными решениями, и то, за чем именно к
каждой из них идти, перечислены в [`REFERENCES.md`](REFERENCES.md).

### Milestones

Это не календарный план и не порядок работ. Это направление, по которому на
каждом gate можно сравнивать кандидатов между собой.

| Milestone | Содержание | Критерий выхода |
|---|---|---|
| **A0 — петля замкнулась** | Несколько волн нарастающей силы в одном владении, явная цена поражения, счёт владения, у партии есть конец. | Владелец доигрывает одну партию до конца и может назвать, что он сделал не так. |
| **A — Vertical Slice** | Одна сессия ~30 минут. Копание и функциональные комнаты; 2–3 связанные производственные цепочки; несколько волн нарастающей угрозы; явный счёт владения; нарративная лента; рука хозяина; свет, автотайлинг, интерполяция, звук. | Владелец играет три партии подряд по собственному желанию и может пересказать историю из каждой. |
| **B — Playable Loop** | Сессия 1–2 часа. Приток населения от условий владения; комнаты со свойствами; экипировка и мастерская; двери и ловушки; черты характера и лёгкие отношения; травмы, мораль, восстановление; сохранение/загрузка; именованные враги-герои. | Партия играется без участия разработчика и заканчивается по решению игрока, а не по таймеру. |
| **C — Content Alpha** | Кампания. Несколько видов существ с разными потребностями; процедурная карта либо набор авторских; глобальный слой (один внешний ресурс, одна угроза, одно решение); art-direction gate и production-пайплайн; баланс и кривая сложности. | Посторонний играет 2 часа без объяснений. |

A0 добавлен потому, что между текущим состоянием и milestone A лежит около
десяти систем: сравнивать с ним прогресс невозможно, и любой блок кажется
одинаково далёким от цели. A0 — ближайшая отметка, до которой реально
дотянуться, и она совпадает со срезом 1 питча.

Постоянные ограничения на всех milestone: 2D и посильный объём контента для
одного человека с агентами; детерминизм, replay и headless-проверки не
приносятся в жертву ни одной фиче; каждая новая система имеет бюджет
читаемости в UI.

## Текущее состояние

### COMPLETED — проверяемый фундамент

| Блок | Результат |
|---|---|
| Agent-ready foundation | Godot/.NET, фиксированный seed, headless tests, replay, structured state/logs, screenshots и domain MCP. |
| Prototype 1: Prepare for Raid | Пищевая экономика, потребности, тренировка, наблюдение, автоматический набег и бой связаны непрямыми командами. |
| Readability and first art pass | Исправлены кисть, HUD, объяснение Quarters и alive/downed; подключены первые generated goblin sprites. |
| Dungeon shaping — step 1 | Игрок обозначает скалу, существа автономно копают, `Rock → Floor`, появляется loose Stone. |
| Dungeon shaping — step 2 | Игрок создаёт MaterialStockpile; Stone проходит `loose → carried → stored` с capacity, reservations и conservation invariant. |
| Dungeon shaping — step 3 | Игрок размечает blueprint тренировочного поста, камень доставляется и **расходуется**, построенный пост создаёт настоящие Drill jobs. ADR 0012. |
| Presentation pass A | Рендер отделён от тика и интерполирует движение по `alpha`; HUD переверстан на Control-контейнеры; слой представления покрыт тестами без запуска движка. |
| UI control pass | Панель на иконках, подсказки, прямоугольное выделение мышью; первый icon pack. |

### NOW — ремонт обратной связи перед срезом 1

Активный блок — не gameplay, а короткий ремонт: два дефекта, найденных на
playtest панели инструментов, ломают тот режим, в котором владелец и агенты
разбираются в механике.

| Задача | Состояние |
|---|---|
| [#58](https://github.com/anshushunov/dungeon-fortress/issues/58) — разметка на паузе не видна | В работе. Пауза — основной режим планировки, и ровно в нём обратной связи нет. То же ломает `STEP` как способ разбираться в механике по одному тику. |
| [PR #59](https://github.com/anshushunov/dungeon-fortress/pull/59) — размер подсказок | Смержен 2026-07-28 по `MERGE #59`. |

Критерий выхода блока: размеченная на паузе клетка подсвечена немедленно,
поведение одинаково на паузе, `STEP` и любой скорости, каноническое состояние и
replay от паузы не зависят, `scripts/verify.ps1` зелёный.

Развилка внутри #58 (показывать намерение отдельным слоем представления либо
применять designation-команды немедленно) закрывается исполнителем в пользу
первого варианта; второй меняет порядок внутри тика и по
[ADR 0010](../decisions/0010-contract-invariants-and-tuning.md) требует ADR —
это условие эскалации, записанное в Issue.

### Очередь

Порядок, а не разрешение работать параллельно: активен по-прежнему один блок.

| Позиция | Блок | Тип | Условие входа | Критерий выхода |
|---|---|---|---|---|
| Сейчас | Ремонт обратной связи (#58) | представление | — | См. выше |
| Следующий | **Срез 1 — повторяющийся цикл угрозы и счёт владения** | gameplay | Выполнено: развилки 1 и 2 раздела 10 [`PITCH.md`](PITCH.md) закрыты 2026-07-28 | Milestone A0 |
| Затем | Реализация [ADR 0008](../decisions/0008-three-quarter-projection.md) и читаемость комнат (#52) | представление | Принят [ADR 0013](../decisions/0013-what-is-a-room.md) «что такое комната» | Владелец видит помещение как помещение, а не как пол другого оттенка |
| Затем | Долг проверяемости (#40, #42, #49) | tooling | — | Стадийный `verify.ps1`, единый источник эталонных кадров, живой HUD-guard |
| Вне очереди | #38 (domain MCP держит сборку), #46 (культурно-зависимый формат скорости) | tooling, класс «блокирует проверяемость» | — | Проверки запускаются без ручных обходов; текст HUD детерминирован |

Обеспечивающие блоки стоят по одному между gameplay-блоками намеренно: правило
«не подменяем игру подготовкой» запрещает ставить их подряд.

### Следующий gameplay-блок: срез 1

Гипотеза:

> Если угроза приходит несколько раз за сессию с нарастающей силой, а между
> волнами владение восстанавливается и перестраивается, игрок связывает своё
> решение с последствием и начинает управлять осознанно, а не наблюдать.

Минимальный slice: 3–5 набегов вместо одного, фаза восстановления между ними,
явный счёт владения по итогам сессии.

Почему выбран владельцем 2026-07-28:

- «влияние не ощущается» — самый старый незакрытый продуктовый дефект. Он
  зафиксирован в [`PROTOTYPE_01_EVALUATION.md`](../playtests/PROTOTYPE_01_EVALUATION.md)
  как *«Perceived influence: not confidently perceived»* и не адресован ни одной
  из работ, сделанных после. Единственный набег через ~25 минут после решения
  даёт ровно ту разрежённость обратной связи, которая к этому и привела;
- закрывается наблюдение P2 той же оценки, где гипотеза H2 помечена
  contradicted, потому что метрика потерь делает `overrun` «лучше». Это не
  дефект метрики, а отсутствие функции ценности: игра нигде не говорит, что
  такое «хорошо»;
- появляется ритм сессии: тишина → тревога → бой → разбор → перестройка, а
  поражение перестаёт быть концом и становится сюжетом;
- стоимость низкая: таймер угрозы, генератор налётчиков, правила боя и исход уже
  реализованы и покрыты тестами. Речь о параметризации существующего;
- **любой** следующий кандидат получает нормальный испытательный стенд: без
  повторяемости влияние снаряжения, ленты или сопротивления плану нечем мерить.

Рамки, принятые владельцем 2026-07-28 вместе с выбором среза:

- **смерть обитателей окончательна, притока новых нет.** Потери необратимы,
  поэтому у поражения есть цена, а у партии — естественный конец. Приток
  населения от условий владения остаётся за milestone B;
- **партия укладывается в один сеанс, сохранений нет.** Кампания с сохранением
  между сеансами относится к milestone B и требует отдельного блока работы.

Обе рамки перенесены в закрытые развилки раздела 10 [`PITCH.md`](PITCH.md).
Implementation Issue создаётся после закрытия ремонтного блока.

## Кандидаты после текущего блока

Это направления, а не утверждённая очередь; implementation Issues для них не
создаются до выбора владельца. Порядок и обоснование — раздел 11
[`PITCH.md`](PITCH.md), номера срезов оттуда же.

| Срез | Что доказывает | Минимальный slice | Состояние |
|---|---|---|---|
| 1 | Повторяющееся давление делает влияние ощутимым | Несколько волн нарастающей силы, фаза восстановления, счёт владения | **Выбран владельцем**, см. выше |
| 2 | События владения читаются как история | Агрегатор поверх существующего event log: имена и предложения вместо reason codes, редкие события помечены как «моменты сессии» | Кандидат; сырьё уже каноническое, не хватает слоя перевода |
| 3 | Обитатель может сопротивляться плану | Одна память или связь, один наблюдаемый отказ, одна видимая причина | Кандидат; здесь заканчивается отсрочка ADR 0002/0006. Идёт после ленты, иначе связь останется числом в снапшоте |
| 4 | Власть через людей богаче власти через ползунки | Один приспешник, один указ, одно искажение намерения характером | Кандидат; требует ADR со словарём команд ролей поверх [ADR 0009](../decisions/0009-adherent-role-boundary.md) |
| 5 | Пространство работает как оружие | Свет и тьма меняют исход волны | Кандидат; смыкается с блоком представления — освещение нужно и для читаемости |
| 6 | Подготовка окупается предметно | Сырьё → мастерская → простое оружие → автономное получение снаряжения → изменённый бой | Кандидат; стоит последним осознанно: без среза 1 снаряжение проверяется один раз за сессию |
| — | Глубина строительства: двери, стены, ловушки, свойства комнат | Один объект и одно последствие для маршрута или боя | Продолжение текущей линии, а не шаг питча. Доступно по `CONTINUE CONSTRUCTION` |

## Представление

Это **не** production art. Проекция закрыта
[ADR 0008](../decisions/0008-three-quarter-projection.md); здесь перечислена
работа слоя отрисовки, которая дважды блокировала playtest и оценивается по
отношению «эффект / стоимость».

Причина, по которой это отдельный от art раздел: плавности не было по
архитектурной причине, а не по художественной — `TicksPerSecond = 6.0` и позиция
существа, равная `tile * TileSize`. Никакой генеративный пайплайн этого не
исправляет; исправил Presentation pass A.

### Сделано

| # | Работа | Где |
|---|---|---|
| 1 | Рендер отделён от тика, позиция интерполируется по `alpha` | Presentation pass A (#36) |
| 2 | HUD на Control-контейнерах вместо абсолютных `Rect2` | Presentation pass A (#36), опирался на #28 |

### Блок «реализация ADR 0008» — один элемент очереди, а не фон

Самый дорогой невыполненный блок в проекте. Он блокирует #52 и требует нового
asset pack, поэтому не начинается до того, как срез 1 даст A0.

| # | Работа | Что даёт |
|---|---|---|
| 3 | `TileMapLayer` с terrain/autotile, тайл верх + фасад | Геометрия пещеры вместо контурных квадратов |
| 4 | Тайл 22 → 32–48 px, камера с зумом и панорамой, Y-sort | При 22 px спрайт физически не способен показать направление и состояние |
| 5 | Читаемость помещений (#52): граница, иконка назначения, подпись с состоянием | Отложенный `ADJUST` по функциональной комнате |

Условие входа — принятый ADR 0013: сейчас комната это **два независимых
понятия**, зона и объект, которые игрок совмещает вручную. Пока не решено, что
такое комната, непонятно, границу чего рисовать. Варианты и рекомендация — в
[ADR 0013](../decisions/0013-what-is-a-room.md) со статусом `Proposed`.

### Кандидаты слоя отрисовки

| # | Работа | Что даёт |
|---|---|---|
| 6 | `CanvasModulate` + `PointLight2D`, тьма вне освещённого | Сетка становится подземельем; больше эффекта, чем от любого art pass. Связано со срезом 5 — свет как механика |
| 7 | Процедурная анимация: facing и flip, bob при ходьбе, наклон, squash & stretch на ударе | ~80 % «живости» без единого нового кадра |
| 8 | Партиклы, hit-stop, вспышка, всплывающие числа урона | Делает бой читаемым — прямая жалоба из playtest |
| 9 | Иконки-мысли над головой (еда / устал / «!») | Читаемость намерений без открытия инспектора |

## LATER — только после подтверждения локального ядра

### Глобальный уровень

Фракции, поселения, торговые маршруты, вылазки и события внешнего мира нужны
игре, но пока не должны определять архитектуру локального прототипа. Первый
global spike начинается только когда:

- локальная экономика содержит минимум две связанные цепочки;
- пространство влияет на производство или защиту;
- именованные существа имеют хотя бы один проверенный источник индивидуальной
  истории;
- автоматический конфликт имеет понятные входы и последствия.

Глобальный уровень сначала поставляет локальному владению один ресурс, одну
угрозу и одно решение. Полная симуляция мира не является первым шагом.

### Production art и масштаб контента

**Представление и production art — разные вещи с разными gate.** Работа слоя
отрисовки идёт в общей очереди, потому что без неё playtest измеряет качество
рендера, а не качество механики. Откладывается сюда только production art.

До подтверждения игрового ядра используются небольшие воспроизводимые asset
packs в геометрии [ADR 0008](../decisions/0008-three-quarter-projection.md).
Полный художественный pipeline, большой каталог существ, анимаций, биомов и
интерфейсов, выбор палитры и уровня стилизации откладываются до отдельного
art-direction gate.

Способы получения анимации, включая cutout-скелет и пререндер 3D, описаны в
[`ANIMATION_PIPELINE.md`](../art/ANIMATION_PIPELINE.md). Специализированные
генеративные инструменты выбираются spike-процедурой
`docs/engineering/TECH_RADAR.md`, а не из обсуждения.

## Постоянные workstreams

| Направление | Текущее состояние | Следующее доказательство |
|---|---|---|
| Session loop | Один набег за сессию, функции ценности нет | **Активное направление.** Несколько волн подряд меняют решения игрока между ними. |
| Construction | Выкопать, построить и использовать комнату можно; ADR 0012 | Двери, стены, ловушки или свойства комнаты как выбор пространства. |
| Narrative | Event log и reason codes есть, слоя перевода нет | События владения читаются как история, а не как коды. |
| Economy | Пища и Stone logistics работают, камень расходуется | Ввести альтернативную стоимость и связь со снаряжением/боем. |
| Combat/RPG | Автоматический raid, HP, injuries, morale | Показать влияние одного произведённого предмета или навыка. |
| Individuality/simulation | Имена, свойства и история решений есть; память/отношения отложены | Одна память или связь меняет следующее решение. |
| Global layer | Не начат | Один внешний источник ресурса/угрозы после подтверждения локального ядра. |
| Представление | Интерполяция, Control-вёрстка HUD и панель на иконках приняты; кадр 1280×720 | Реализация ADR 0008: тайл 32–48 px, стена «верх + фасад», Y-sort, камера, читаемые помещения. |
| Art | Первый goblin pass и icon pack v1; проекция закрыта ADR 0008, реализации нет | Asset pack постройки в геометрии ADR 0008 — после реализации проекции. Генерация у Codex. |
| Agent tooling | Deterministic CLI/API/MCP работают; чистый слой представления в CI | #38 и #46 вне очереди как блокирующие проверяемость; #40, #42, #49 — долг после среза 1. |

## Когда roadmap пересматривается

Обязательное обновление происходит:

1. после каждого owner gate — запись в журнал gate-решений и правка `NOW`;
2. при изменении порядка блоков в разделе «Очередь»;
3. перед первым global-layer spike;
4. при срабатывании критерия отказа — два `ADJUST` подряд без улучшения;
5. если новое технологическое ограничение меняет стоимость или проверяемость
   разработки агентами;
6. если меняется продуктовый фокус в [`PITCH.md`](PITCH.md) — порядок кандидатов
   следует за питчем, а не наоборот.

Если roadmap расходится с Accepted ADR или актуальным design-контрактом, он не
переопределяет их молча: сначала оформляется новое решение или обновляется
контракт, затем roadmap.
