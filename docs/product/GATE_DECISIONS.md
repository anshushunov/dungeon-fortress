# Журнал gate-решений

Решения владельца, принятые на decision gate. Как владелец управляет
направлением и что означает каждый ответ — раздел «Как владелец управляет
направлением» в [`ROADMAP.md`](ROADMAP.md). Правила темпа, на которые
ссылаются записи, — [`DEVELOPMENT_PACE.md`](../engineering/DEVELOPMENT_PACE.md).

Решение, оставшееся только в комментарии PR или в чате, не считается принятым:
его нельзя ни найти, ни посчитать. Критерий отказа опирается на этот журнал, а
не на память участников.

Колонка `ADJUST` — счётчик подряд идущих `ADJUST` по **одной и той же
гипотезе**; она обнуляется, когда гипотеза закрыта или заменена.

| Дата | Что проверяли | Решение | `ADJUST` | Что из него вышло |
|---|---|---|---|---|
| 2026-07-27 | Prototype 1 «Подготовка к набегу» (#12) | `ITERATE` | 0 | Readability pass (#19) и первый art probe; [`PROTOTYPE_01_EVALUATION.md`](../playtests/PROTOTYPE_01_EVALUATION.md) |
| 2026-07-27 | Dungeon shaping — раскопка (#24) | `CONTINUE` | 0 | Шаг 2: логистика камня (#26) |
| 2026-07-28 | Dungeon shaping — логистика камня (#26) | `CONTINUE` | 0 | Шаг 3: функциональная комната (#48) |
| 2026-07-28 | Питч как продуктовый фокус (PR #31) | `PROMOTE` | — | [`PITCH.md`](PITCH.md) принят; ADR 0009; порядок кандидатов в roadmap |
| 2026-07-28 | Presentation pass A (#36, #39) | принято | 0 | Интерполяция движения и Control-вёрстка HUD |
| 2026-07-28 | Functional room (#48) | `ADJUST: визуал комнат выделить` | 1 | Issue #52; отложен в блок реализации ADR 0008 — см. правило отложенного `ADJUST`. **Исполнен 2026-08-01**, PR #135: комната стала сущностью канонического снапшота с границей, покрытием пола по назначению, иконкой и подписью с состоянием. Самый старый незакрытый `ADJUST` проекта — открыт был четыре дня |
| 2026-07-28 | Панель на иконках (#55) | дефекты | — | Issue #58 (разметка на паузе), PR #59 (размер подсказок) |
| 2026-07-28 | Эксперимент #23 целиком | `PROMOTE SESSION LOOP` | сброшен | Срез 1 становится следующим gameplay-блоком; полная переработка roadmap |
| 2026-07-28 | Размер подсказок панели (PR #59) | `MERGE #59` | — | Смержен; в ремонтном блоке остаётся #58 |
| 2026-07-28 | Развилки 1 и 2 раздела 10 [`PITCH.md`](PITCH.md) | Смерть окончательна, притока нет; партия — один сеанс без сохранений | — | Условие входа в срез 1 выполнено; развилки перенесены в закрытые |
| 2026-07-28 | Автономность агентов и параллельная работа | Классы работы, партиция областей, потолок 2, авто-merge с границами, пакетный playtest | — | [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md); правила движения и `/coord` обновлены |
| 2026-07-29 | #38 — domain MCP держал цель сборки (PR #63) | Авто-merge по ADR 0014 | — | Каждая сессия MCP работает со своей копией; `verify.ps1` проходит при подключённом сервере |
| 2026-07-29 | #58 — разметка на паузе (PR #62) | Авто-merge по ADR 0014 | — | Намерение игрока видно немедленно; ремонтный блок закрыт, очередь переходит к срезу 1 |
| 2026-07-29 | Что такое комната ([ADR 0013](../decisions/0013-what-is-a-room.md)) | Вариант C сейчас, переход к B1 на срезе 5 | — | ADR 0013 принят; условие входа в блок реализации ADR 0008 выполнено; условие перехода к B1 записано в ADR |
| 2026-07-29 | Функция ценности среза 1 ([ADR 0015](../decisions/0015-session-of-waves-and-domain-score.md)) | **Дурная слава**; сводка владения входит в срез 1 | — | ADR 0015 принят; счёт не убывает; волна зафиксирована как частный случай события давления |
| 2026-07-29 | #64 — `.uid` отслеживались непоследовательно (PR #67) | Авто-merge по ADR 0014 | — | Единая стратегия для всех `*.uid`; чистое дерево после импорта в свежем worktree |
| 2026-07-29 | Темп разработки | Облегчённый цикл: меньше итераций и кругов review | — | Правило в «Правилах движения»; срез 1 выдан одним Issue #68; проверяется на этом блоке |
| 2026-07-29 | #40 — стадийный `verify.ps1` (PR #70) | Авто-merge по ADR 0014 | — | Девять стадий; правка в скриптах проверяется за 2 с вместо 69, полный прогон не подорожал. Условие ADR 0014 на подъём потолка закрыто; сам потолок не поднят. Пять замечаний review ушли в #71, а не новым кругом — первая проверка облегчённого цикла |
| 2026-07-29 | Конец партии и смысл силы владения (review PR #73) | Третья форма «разорено»; сила считает готовность, а не потенциал | — | Уточнения записаны в [ADR 0015](../decisions/0015-session-of-waves-and-domain-score.md). Обе развилки нашёл review: сводка называла разорённое владение удержанным, а павшее от голода — сильнейшим |
| 2026-07-29 | **Срез 1 — повторяющийся цикл угрозы и счёт владения (#68, PR #73)** | **`CONTINUE`** | 0 | Milestone A0 взят. Очередь переходит к блоку реализации ADR 0008. Находки playtest: занятость клетки (#76) привязана к срезу 5, обратная связь удара (#77) — сразу за ADR 0008 |
| 2026-07-29 | ADR 0008 шаг 1 — камера, масштаб тайла и съёмка кадра (#79, PR #81) | `MERGE #81` | — | Тайл 40 px, дискретный зум и панорама, HUD в `CanvasLayer`, воспроизводимая съёмка. Playtest владельца по ходу задачи дал два исправления камеры. Два круга независимого review: первый нашёл зависание headless-прогона на невалидном параметре, ослабленный HUD-guard и тавтологичное доказательство попадания клика — все закрыты вместе с доказательствами живости обоих guard'ов. Первая половина #49 закрыта попутно |
| 2026-07-30 | Воспроизводимый evidence bundle и учёт токенов (#87, PR #88) | Merge по разрешению владельца | — | `capture-evidence.ps1` снимает кадр дважды и сравнивает байты; манифест несёт команду, коммит, checksum и SHA-256; диагностика GitHub-аутентификации не выдаёт секретов и не путает чтение с записью. Два круга review. Промах оценки почти втрое (585k против 180–300k) разобран как находка: внешняя интеграция и доказательство отсутствия утечки требуют того же множителя, что балансная работа — записано в калибровку |
| 2026-07-30 | **Счёт партии против дурной славы ([#75](https://github.com/anshushunov/dungeon-fortress/issues/75))** | Счёт отделён от славы и виден **только в итогах** | — | [ADR 0016](../decisions/0016-party-score-separate-from-renown.md). Две роли требовали несовместимых свойств: драйверу сложности обязательно неубывание, счёту обязательно различать сохранённое и потерянное. Перевешивание отклонено — в формуле нет члена, отличающего «выстояло» от «пало», поэтому погибнуть поздно выгоднее выжить при любых весах. Пара «слава + сила» отклонена — на итогах вырождается и сила. Слава не изменилась ни в чём. Реализация — [#92](https://github.com/anshushunov/dungeon-fortress/issues/92) |
| 2026-07-30 | **ADR 0008 шаг 2 — объёмные стены и порядок по Y (#83, PR #85)** | Merge по разрешению владельца | — | Порода читается объёмом, массив не распадается, тело сортируется по интерполированной глубине. **Четыре круга review**, и все четыре нашли один класс дефекта: непрозрачная метка поверх существа, которую `verify.ps1` увидеть не может. Итог сильнее самой геометрии: глубина, кодек клетка↔id и геометрия массы стены вынесены в `Presentation` и покрыты мутационно проверенными тестами; правило прохода меток сформулировано один раз. Последнюю правку внёс координатор по решению владельца. Открыты #89 (`TEMP` в `verify.ps1`) и #90 (сделать правило проверяемым в CI) |
| 2026-07-30 | **Геймдизайн-ревью роадмепа** — два внешних ревью и вопрос визуального масштаба существ. Большая часть предложений ревью уже принята питчем 2026-07-28; подтвердились три дефекта: критерий milestone A закрывался без социального ядра, срез «нарративная лента» не мог провалиться на playtest, milestone B не работал как decision gate | Семь решений владельца: срезы 2 и 3 объединены в «история влияет на поведение»; момент истины — отдельный срез за ним **со всеми тремя величинами** (страх, выгода, обида) — выбор владельца при рекомендации начать с двух, риск «какая ось виновата» компенсируется обязательным поимённым разбором слагаемых; критерий A дополнен социальным условием; B разделён на B1 Power Loop и B2 Material Depth, сохранения — инфраструктура; возвращающийся герой — срезом после приспешника; принцип «порог славы открывает и угрозу, и возможность» записан в питч; scale-spike 100/150/200 % до #77 | — | Новая редакция раздела 11 [`PITCH.md`](PITCH.md); Milestones, очередь и кандидаты обновлены; нумерация срезов сдвинута: пространство 5→6, снаряжение 6→7. ADR не изменялись и не противоречат решению |
| 2026-07-30 | **Ревью референсов** — что именно берём из Majesty, KeeperRL, Frostpunk, King of Dragon Pass и Darkest Dungeon; развилки: форма флага награды, совет приспешника на карточке, мета-память мира | Три решения владельца: флаг награды — **только на место** (награда «за голову» словарём v2 невыразима и откладывается до ADR о вердиктах, там же решается граница «вердикт о существе — можно, приказ — нельзя» для момента истины); совет приспешника на карточке — кандидат, пристёгнутый к срезу 3 вне его критерия выхода; вопрос мета-памяти мира между партиями заведён с решением на границе B1 | — | Пять записей в [`REFERENCES.md`](REFERENCES.md) с границами «что не брать»; кандидат в таблице достроек срезов; вопрос мета-памяти в [`OPEN_QUESTIONS.md`](../OPEN_QUESTIONS.md). ADR не изменялись; ADR о вердиктах требуется **до среза 3** и в этом ревью не оформлялся |
| 2026-07-30 | **Порядок очереди: #52 против #92** — оба меняют состав канонического снапшота и правят один контракт, поэтому параллельно невозможны по [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md); при продолжении блока ADR 0008 очередь получала три обеспечивающих блока подряд при свободном слоте `gameplay` | Счёт партии (#92) занимает слот `gameplay` сейчас; шаг 3 ADR 0008 (#52) возвращается сразу за ним и остаётся отложенным `ADJUST`. Параллельно идёт `blocks-verification` в непересекающейся области `tooling` (#89 и #71 одним PR) | — | Раздел `NOW` и «Очередь» перестроены. #46 явно помечен как несовместимый с #92 по `HudText`; #89 снят из «вне очереди», потому что получил исполнителя |
| 2026-07-30 | **`verify.ps1` и guard стадий (#89, #71, PR #97)** | Авто-merge по [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md) | — | Прогон отвергает непригодный временный каталог **до стадий**, называя каталог, отказ ОС и три способа обхода; неудача уборки стала предупреждением. Guard переписан со списка разрешённых имён команд на список разрешённых мест, плюс AST-модель порядка `APPDATA` по 45 сочетаниям стадий. Два круга review; второй собрал собственный харнесс guard'а и нашёл четыре обхода — ни один не регресс. Находка, ради которой стоило вернуть PR: `[IO.Directory]::Delete` проходит в `C:\WINDOWS\TEMP`, а `Remove-Item -Recurse -Force` там же падает, поэтому проба обязана удалять тем же вызовом, что и уборка, — теперь это AST-утверждение, а не комментарий. Остаток — [#102](https://github.com/anshushunov/dungeon-fortress/issues/102) |
| 2026-07-30 | **Пакетный playtest владельца** на дереве PR #96 | По основной фиче — **ок**; пять наблюдений разобраны | — | Заведены [#99](https://github.com/anshushunov/dungeon-fortress/issues/99) (рамка выделения не следует объёму стены: `BrushGridRect` не знает геометрии, которую [#83](https://github.com/anshushunov/dungeon-fortress/issues/83) дал `CellInteractionRect`), [#100](https://github.com/anshushunov/dungeon-fortress/issues/100) (игра открывается в кадре 1280×720 при `UiScale 1.0`; смыкается с #86), [#101](https://github.com/anshushunov/dungeon-fortress/issues/101) (порог бегства общий на всё владение, поэтому обрыв стадный; `creature.Position` присваивается напрямую, поэтому бегство выглядит телепортом). Труп как вещь и куча предметов на клетке — кандидат к срезу 7. «Очень много букв» — пункт 11 раздела «Представление» с условием сохранения текстового канонического состояния |
| 2026-07-30 | **Потолок параллелизма** — оба условия подъёма из [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md) §3 выполнены: Issue #40 закрыт, бюджет токенов владельца перестал быть дефицитным | Потолок два → **пять** записывающих агентов; партиция областей заменена файловой, с именованными регионами в `Main.cs` и `verify.ps1`; сериализация полных прогонов `verify.ps1` снята | — | [ADR 0017](../decisions/0017-raise-parallel-writing-ceiling.md). Простой замены числа было недостаточно: шести областей физически не хватает на пять задач, потому что типичная задача держит две, а `Main.cs` и `verify.ps1` — узлы. Возврат к потолку два — без нового ADR, при дефиците бюджета либо при появлении revert'а в `main`. Независимый review ADR вернул его с семью находками, и три из них меняли решение по существу: партиция не «уточняется», а **ослабляется**, потому что непересечение по файлам не влечёт непересечения по областям; review-агенты нельзя исключать из потолка аргументом «не конфликтуют», раз потолок задан бюджетом, а review стоит как записывающая задача; снятие сериализации `verify.ps1` держится на **worktree**, а не на PR #97, который дал не собственный временный каталог, а параметр и диагностику. Первое применение правила пропустило третий узловой файл — [#109](https://github.com/anshushunov/dungeon-fortress/issues/109) |
| 2026-07-31 | **Темп разработки** — направление владельца «нам нужен time to market»: слишком много инфраструктуры и документации, циклы review долгие и дорогие, визуал не появляется постепенно | Семь решений: в блоке обязателен gameplay-слайс; до двух gameplay-слайсов, и считаются вопросы playtest, а не слайсы; глубина review назначается заранее по названным маркерам риска и объёму контекста; находка review становится Issue только при наблюдаемом последствии; именованный регион разрешён в любом файле при всех четырёх условиях [ADR 0017](../decisions/0017-raise-parallel-writing-ceiling.md) §3; постоянная дорожка визуала — одна задача `art` на блок у Codex; одобрение прикрепляется к решению, а не к каждому PR. Плюс полномочие координатора на бухгалтерию Issue | — | Раздел [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md) «Правил движения» и новый реестр долга [`DEBT_LEDGER.md`](../engineering/DEBT_LEDGER.md); `MULTI_AGENT_WORKFLOW.md` и `/coord` приведены в соответствие. Попытка оформить решения черновиком ADR 0018 (PR #111) **закрыта после независимого review с одиннадцатью блокирующими находками**, и решения от этого в силе — ADR был только их записью. Две несущие посылки не подтвердились репозиторием: «все шесть инфраструктурных задач породил review» (#42 завёл исполнитель, #99 и #86 породил playtest) и «семь изменений в `main`, ни одного игрового» (за сессию сели шесть PR, и #96 — gameplay-слайс, который владелец на этом же блоке playtest'ил). Третья находка меняла само правило: тариф review **по классу изменения** противоречит калибровке проекта, которая на PR #98 показала, что цену задаёт объём поднимаемого контекста. Отсюда принятое направление: процессные правила живут в «Правилах движения», а ADR остаётся для продуктового обещания, архитектуры, контракта и труднообратимой технологии — семь ADR за четыре дня, из них три процессных, и есть та стоимость, которую блок должен снижать |
| 2026-07-31 | **Пакетный playtest на `main` после трёх merge** — срез 1 целиком и счёт партии ([#92](https://github.com/anshushunov/dungeon-fortress/issues/92)), чьё gate-решение ждало playtest с 2026-07-30 | **`CONTINUE`** | 0 | Владелец ответил на все три вопроса playtest: «Стало сильно лучше, паника теперь видна по хп и бегут более логично», «С UI все хорошо, ресайзится правильно», «Силу вижу, счёт вижу». Проверялось дерево из трёх коммитов `main` — `89ade73` (правила темпа, PR #112), `877d2b8` (читаемость HUD и остаток #49, PR #113), `1b41555` (паника и бегство, PR #114); состав дерева и merge проверены `git log --oneline -3` и `gh pr list --state all --json number,state,mergeCommit`. Гипотеза среза 1 подтверждена, слайс принят, счётчик `ADJUST` остаётся 0. Три находки playtest ушли в очередь **обеспечивающей работой**, а не исправлением слайса: [#115](https://github.com/anshushunov/dungeon-fortress/issues/115) (счёт — число без опоры), [#116](https://github.com/anshushunov/dungeon-fortress/issues/116) (сила скачет, причина не видна), [#120](https://github.com/anshushunov/dungeon-fortress/issues/120) (не видно, куда шёл вставший с `blocked_by_others`) |
| 2026-07-31 | **Размен функции ценности, сделанный [#101](https://github.com/anshushunov/dungeon-fortress/issues/101)**: счёт партии платит за сломленного защитника столько же, сколько за убитого, хотя сломленный жив и вернётся к следующей волне | **Принять и назвать разменом** | — | Альтернативы рассмотрены и не выбраны: научить счёт различать сломленного и убитого — это поправка к [ADR 0016](../decisions/0016-party-score-separate-from-renown.md) плюс отдельный балансный слайс; перекрутить веса морали обратно — отклонено ещё внутри #101, потому что возвращает стадность, за которую задача и платилась. Довод за принятие — оценка владельца по игре («стало сильно лучше») и то, что подготовленное владение стало падать **реже**, а `baseline` перестал падать вовсе. Числа размена — зазор между планами на матрице и строгая доля на 180 seed — записаны **один раз**, в `docs/design/PROTOTYPE_01_CONTRACT_HISTORY.md` — «Размен, который сделали два круга #101, и его цена» и «Что измерено вне матрицы» (после разреза раздела 13.4 [#280](https://github.com/anshushunov/dungeon-fortress/issues/280)/PR #290 обе главы живут там, а не в `PROTOTYPE_01_PREPARE_FOR_RAID.md`; правка этого указателя — 2026-08-07, исходный текст называл прежнее место); копии здесь нет сознательно, и это же — свидетельство **на дату**: числа сняты на текущей карте, а [#117](https://github.com/anshushunov/dungeon-fortress/issues/117) меняет планировку и перемеряет матрицу заново. ADR 0016 настоящим решением не изменяется |
| 2026-07-31 | **Мораторий на процессную документацию и сжатие калибровки** — третья координационная сессия дня, в ответ на измерение: PR #121 стоил 563k и восемь последовательных запусков агентов, не дав игре ни одного нового поведения ([Issue #122](https://github.com/anshushunov/dungeon-fortress/issues/122)) | Шесть решений: мораторий на процессную документацию (правило 11); потолок кругов review — один разбор плюс один подтверждающий (правило 12); бриф самодостаточен, Issue #33 выходит из обязательного чтения исполнителей (правило 13); маршрутизация модели и effort по классу круга обязательна в брифе (правило 14); проверка утверждений в review становится выборочной (правило 15); калибровка сжата до пяти строк и трёх выводов | — | Правила 11–15 раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md); [`CALIBRATION_HISTORY.md`](../engineering/archive/CALIBRATION_HISTORY.md) несёт полную калибровку без потерь; `MULTI_AGENT_WORKFLOW.md`, `.claude/commands/coord.md` и `docs/decisions/README.md` приведены в соответствие. Суммарная длина `ROADMAP.md` и `MULTI_AGENT_WORKFLOW.md` уменьшена, а не выросла — `wc -l` до и после в PR |
| 2026-08-01 | **Срез 2 — существо помнит место, где ему было страшно** ([#117](https://github.com/anshushunov/dungeon-fortress/issues/117), PR #124). Владелец играл на `main` после merge; состав дерева и merge проверены `git log --oneline -1` и `gh pr list --state all --json number,state,mergedAt` | **`ADJUST`: показать историю решений одного существа** | 1 | Первый вопрос playtest — «видит ли владелец подземелье вместо зала» — закрыт. Второй закрыт на две трети: «Метки вижу, они остаются на месте боев. Но без лога событий по каждому персонажу трудно понять, как он реагирует на них.» Критерий выхода среза требует, чтобы игрок назвал **как память изменила следующее решение** существа, и эта треть опирается на то, чего в игре нет: лента `feedback` — общий поток, инспектор — срез «сейчас». Гипотеза «история влияет на поведение» не опровергнута и не подтверждена, поэтому счётчик `ADJUST` = 1, а не сброс. Исправление — [#128](https://github.com/anshushunov/dungeon-fortress/issues/128) вместе с [#125](https://github.com/anshushunov/dungeon-fortress/issues/125): показывать историю решений, в которой строки лгут о причине отказа, бессмысленно, поэтому дефект правдивости отказа идёт внутрь того же слайса. Две другие находки playtest — [#127](https://github.com/anshushunov/dungeon-fortress/issues/127) (подсказка меньше подписи и вне guard'а читаемости) и [#129](https://github.com/anshushunov/dungeon-fortress/issues/129) (бойцы выстраиваются в линию); #129 слота не получает по решению владельца и ждёт среза 6 вместе с [#76](https://github.com/anshushunov/dungeon-fortress/issues/76) |
| 2026-08-01 | **Что считается честным отказом по памяти и допустимо ли правило, добавленное сверх постановки** — независимый review вывел PR [#125](https://github.com/anshushunov/dungeon-fortress/issues/125)/[#128](https://github.com/anshushunov/dungeon-fortress/issues/128) из-под правила 7 по двум поводам сразу | **Честен отказ по изменённому намерению, а не по изменённому исходу; третье правило принято как правило design-контракта** | — | Отказ по памяти верен, если память отвернула существо от работы, которую оно поставило бы первой, — независимо от того, получило бы оно её после конкуренции. Критерий готовности 1 в #125 переформулирован под это, а измеряемая величина записана рядом: совпадение с **назначением** в тике без памяти — 69 из 80. Решение опирается на разбивку остатка, измеренную после разбора review: **девять** случаев из одиннадцати — существо простаивает в обоих мирах (`idleEitherWay 9 из 9`, по `currentJobId` того же тика), то есть память верна про его предпочтение и не объясняет бездействия; **два** — существо взялось бы за другое. Альтернатива «честен исход» отклонена по названной цене: сопоставление с выключенной памятью в живом прогоне на каждом тике плюс объяснение, мерцающее по мере занятости соседей. Третьим правилом отказ записывается, только если отвергнутая работа выигрывала у всего, что существо ещё свободно взять; на матрице это 323 отказа → 142, то есть объяснение памятью показывается вдвое реже и каждое верно. Без него на `prepared/20260726` ложными оставались все двадцать. Правило названо новым прямо: постановка #125 его не содержала, и владелец принял его как новое, а не как деталь исполнения |
| 2026-08-01 | **Срез 2, второй playtest** — на дереве после merge четырёх PR блока (#133, #134, #135, #136) | **`ADJUST`: история существа отбирает значимые события, а не последние** | **2** | Подсказки приняты («нормально»), помещения — «стало получше», но граница наезжает на фасад стены ([#139](https://github.com/anshushunov/dungeon-fortress/issues/139)). Вопрос про существо не закрыт второй раз: «выглядит как прошлая версия… реакции на них не видно без каких-то записей, а лог очень короткий». **Причина измерена, а не предположена**: на `baseline`/2400 тиков в журнале 4425 событий, отказов по памяти 27 (0,6 %); у одного существа 820 событий, из них 726 (89 %) — четыре рутинных кода ожидания и уступки дороги, а значимого 12 (1,5 %). Панель показывала **последние четыре**, поэтому отказ по памяти в неё практически не попадал. Задача [#125](https://github.com/anshushunov/dungeon-fortress/issues/125) доказала, что отказы правдивы, но **никто не измерил, можно ли их увидеть** — ограничение в четыре строки выведено из бюджета панели, а не из вопроса, и это ошибка постановки [#128](https://github.com/anshushunov/dungeon-fortress/issues/128), а не исполнения. Исправление — [#140](https://github.com/anshushunov/dungeon-fortress/issues/140) с измеримым критерием: история существа, у которого за партию был отказ по памяти, обязана его содержать. **Счётчик достиг 2: критерий отказа закрывает возможность третьего `ADJUST`**, следующий gate по этой гипотезе даёт `PROMOTE` или `STOP` |
| 2026-08-01 | **Бои, анимации, взаимодействия и эффекты** — координационная сессия по запросу владельца: адресовать масштаб 150–200 % и закрыть развилки боевого блока | Четыре решения: «взаимодействия» — это существо↔существо в бою, существо↔объект и работа, и сам удар (игрок↔мир остаётся в очереди как было); **scale-spike идёт сейчас, [#77](https://github.com/anshushunov/dungeon-fortress/issues/77) — следующим блоком, после выбора масштаба**; пункт 11 «Представления» (иконки и шкалы вместо строк) не делается, и ADR, сужающий [ADR 0011](../decisions/0011-presentation-layer-without-engine.md), не заводится; развилка [#129](https://github.com/anshushunov/dungeon-fortress/issues/129) закрыта в пользу **«цель боя — свободная клетка рядом с врагом»** | — | Заведена [#142](https://github.com/anshushunov/dungeon-fortress/issues/142) — graybox-сцена боя в 100/150/200 %. **Развилка #129 переформулирована по коду, и это изменило решение**: постановка называла причиной только «поиск пути не обходит существ» — верно (`PrototypeMap.NextStep` тел не видит, `PrototypeWorld.Move` проверяет занятость уже после выбора шага), — но сильнее вторая, в постановке не названная: `PrototypeWorld.ActCombatant` назначает целью движения **саму занятую клетку налётчика**, поэтому все бойцы, у которых ближайший враг общий, получают один и тот же коридор BFS. Цель при этом выбирается на каждое существо по ближайшему, а не одна на всю группу — уточнено независимым review; линия возникает и там, где обход физически есть, а обход своих сам по себе приводит всех в ту же точку другой дорогой. Отсюда выбор дешёвого и точного варианта вместо дорогого. Ориентир движения — автошахматы, запись в [`REFERENCES.md`](REFERENCES.md); реализация остаётся на срезе 6 вместе с [#76](https://github.com/anshushunov/dungeon-fortress/issues/76). В #77 добавлен критерий про работу с объектом |
| 2026-08-01 | **Итоги блока по бою и масштабу**: три задачи прошли независимый review, и один PR упёрся в конфликт правил — потолок кругов review исчерпан, а критерий готовности 4 не выполнен. Понизить такую находку до реестра не вправе ни координатор, ни review | Четыре решения: **третий круг доработки [#139](https://github.com/anshushunov/dungeon-fortress/issues/139)** разово сверх потолка; **лента владения получает задачу до playtest** ([#145](https://github.com/anshushunov/dungeon-fortress/issues/145)); **[#129](https://github.com/anshushunov/dungeon-fortress/issues/129) поднят из среза 6 в следующий блок** вместе с [#77](https://github.com/anshushunov/dungeon-fortress/issues/77); план пяти сессий фиксируется строками «Очереди» | — | Послабление потолка разовое и не меняет правило 12: третий круг открыт потому, что находка ломала критерий готовности, а такая находка не может стать ни Issue, ни записью реестра. Лента заведена **не по мнению, а по измерению** независимого review #140: значащее событие попадает в окно «последние 3» в **1 случае из 47**, и до первого клика владелец видит тот же экран, что вызвал жалобу «а лог очень короткий»; ставка — счётчик `ADJUST` равен 2, и `STOP` из-за не того экрана третья попытка не отыграет. #129 поднят, потому что иначе у блока с #77 нет gameplay-слайса (правило 1), а сам #77 показал бы читаемый бой, всё ещё выглядящий очередью. Заведены также [#146](https://github.com/anshushunov/dungeon-fortress/issues/146) и [#147](https://github.com/anshushunov/dungeon-fortress/issues/147) по находкам review |
| 2026-08-01 | **Выбор визуального размера тела** по спайку [#142](https://github.com/anshushunov/dungeon-fortress/issues/142). Владелец прокликал все варианты в живой сцене, а не по кадрам | **170 % от прежнего размера**, то есть тело **61,8 px** при клетке 40 px — получено прогоном сцены с `--scale 170`, поле `drawSizePx` в её же выводе | — | 100 % отвергнут владельцем сразу; 200 % отвергнут как «слишком». Промежуточные 170 и 175 сцена приняла без правки кода — ограничение стояло только в обёртке-скрипте. Размен назван: на 150 % между бойцами в коридоре есть зазоры и видна сетка, на 170–175 они стоят вплотную, но пол ещё читается, на 200 % пол под скоплением пропадает. Выбор — в пользу читаемости существа против читаемости строя; **условие пересмотра**: если срез 6 «пространство как оружие» не сможет показать горло коридора, масштаб возвращается к обсуждению. Канвас спрайта — tuning представления по [ADR 0010](../decisions/0010-contract-invariants-and-tuning.md), ADR не требуется; сетка, снапшот и словарь команд не затрагиваются. Реализация — вход [#77](https://github.com/anshushunov/dungeon-fortress/issues/77) |
| 2026-08-01 | **Дефолтный reasoning budget агентов** — владелец поднял effort по умолчанию у своих моделей и попросил, чтобы брифы это отражали | Дефолт `reasoning_budget` — **`high`**; `xhigh` только для суперсложной задачи и с названной причиной в брифе | — | Дефолт действует в остатке: таблица классов, раздел «Точность» и правило 14 продолжают предписывать дешёвый бюджет там, где предписывали, и маршрутизация по классу круга остаётся обязательной. Записано в раздел «Provider-neutral выбор модели» [`MULTI_AGENT_WORKFLOW.md`](../engineering/MULTI_AGENT_WORKFLOW.md) и в текст правила 14 ниже — то есть там, где координатор читает его каждую сессию. Требование называть причину для `xhigh` — **новое** требование к содержимому брифа, и оно принято явным решением владельца: мораторий (правило 11) для конкретного случая отменяется именно так. Отдельная оговорка, найденная независимым review: подтверждающий круг ведёт возобновлённый агент, которому нельзя сменить effort, поэтому его дешевизна держится на сохранённом контексте, а не на понижении бюджета — вывод 14 калибровки |
| 2026-08-01 | **Блок ленты владения и кромки стены** — последняя работа перед gate по срезу 2. Три PR, шесть кругов независимого review, из них четыре круга вернули работу | Merge всех трёх без запроса владельца: #151 по границам [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md) §5 как `defect`, #152 по правилу 7 «Темпа разработки», PR #150 по явной команде владельца `MERGE #150` | — | **Квалификацию #152 под правило 7 подтвердил независимый review, а не координатор** — суждение «PR только исполняет уже принятое решение» разблокирует merge, и выносить его в одиночку той стороне, чей merge разблокируется, нельзя. Review нашёл шесть блокирующих находок, и все шесть — класса «документ утверждает о репозитории неправду»: запись решения об effort требовала обосновывать то, что правило 14 само предписывает, и молча выводила из-под правила 14 все подтверждающие круги; контракт #145 называл держателем бюджета ленты стадию `godot`, а держит его `screenshots`; тело PR #147 приписывало постановке замер, которого она не делала, считало десять мутантов девятью и утверждало, что правка не задела реестровую находку, которую она углубила на 1,25 реф. px и расширила с пяти назначений на восемь. Ни одна не была найдена автором. Побочный результат, важнее самих правок: критерий готовности **#139 был закрыт не полностью** — её тест меряет прямоугольник фасада и не видит шва по его нижней кромке, поэтому у `farm@1,1` оставалась ровно та теснота, ради которой заведён #147. Реестр долга прошёл **первое в истории проекта третье перечитывание**: одиннадцать записей эпохи 2026-07-31 разобраны поимённо, у одной условие оказалось ненаблюдаемым по собственной формулировке и переформулировано, **одна удалена, ни одна не повышена** — оба повышения (#153, #154) пришлись на записи 2026-08-01, а не на разбираемые одиннадцать. Первая редакция этой строки удаляла из одиннадцати три записи и приписывала им оба повышения; независимый review PR #155 показал, что две из трёх удалены по ложной посылке, и они восстановлены |
| 2026-08-01 | **Срез 2 — история влияет на поведение**, третий и последний заход. Владелец играл на `main` после merge PR #151 и #152, то есть на дереве `de5b16b` — коммиты `de5b16b`, `2e64191`, `a43c26d` названы здесь поимённо: команда `git log --oneline -3` уже не воспроизводит это дерево, потому что после playtest сел PR #155 | **`PROMOTE`** | сброшен | **Решение принято между `PROMOTE` и `STOP`, потому что счётчик `ADJUST` был равен 2 и третьего захода критерий отказа не давал.** Ответ владельца целиком: «История действительно появилась. Проблема, что в ней всего 3 записи — наверно они должны дополняться и важные события должны оставаться в логе», и общий вердикт «в целом по фиче ок». **Третья часть критерия выхода — чтобы игрок назвал, как память изменила следующее решение существа, — им прямо не названа**, и запись этого не утверждает: вторая половина той же фразы стала Issue [#157](https://github.com/anshushunov/dungeon-fortress/issues/157) о нечитаемости той же панели. Прежние два gate считали эту треть непокрытой и говорили об этом прямо; третий закрывает слайс не потому, что треть закрылась, а потому, что владелец, увидев работающую историю, выбрал двигаться дальше, а остаток назвал обеспечивающей работой. Так это и записано — основанием служит решение владельца, а не доказательство критерия. Четыре замечания playtest ушли **обеспечивающей работой, а не исправлением слайса**: [#156](https://github.com/anshushunov/dungeon-fortress/issues/156) граница помещения поверх существа, [#157](https://github.com/anshushunov/dungeon-fortress/issues/157) значимое в истории вытесняется и события не различаются цветом; наблюдения про бой приложены комментариями к [#129](https://github.com/anshushunov/dungeon-fortress/issues/129) и [#76](https://github.com/anshushunov/dungeon-fortress/issues/76) по решению владельца «к этому вернёмся потом». В наблюдении о бое найдена половина, которой нет в постановке #129: «атакует только один, даже когда окружили с двух сторон» может следовать из правил боя, а не из выбора цели движения, и это отдельное решение |
| 2026-08-01 | **Срок годности записи реестра долга** — координатор применил правило трёх перечитываний с третьим исходом «разобрана, условие подтверждено», и независимый review PR #155 показал, что вместе с ним вводился сброс счётчика, делающий храповик недостижимым: реестр перечитывает каждая сессия | **Единица возраста — gate-решение, а не координационная сессия**; исходов по-прежнему два | — | Процессное правило меняется решением владельца — форма, которую мораторий (правило 11) для своей отмены и предусматривает. Владелец выбрал из трёх предложенных вариантов третий: не оставлять правило буквальным (живые записи удалялись бы по возрасту) и не дописывать третий исход, а починить единицу измерения. **Переходная оговорка получила отдельное решение владельца**, а не была проведена как следствие выбора единицы: независимый review PR #158 показал, что она решает другой вопрос — судьбу девяти записей, просроченных по букве нового правила в день его введения, — и что для них её эффект тождественен отклонённому третьему исходу. Причина выбора, сама оговорка и известная дыра ([#159](https://github.com/anshushunov/dungeon-fortress/issues/159)) записаны один раз — в [`DEBT_LEDGER.md`](../engineering/DEBT_LEDGER.md), раздел «Кто и когда перечитывает» |
| 2026-08-01 | **Форма адресации в момент истины** — ADR 0019 `Proposed` предложил границу «вердикт о существе — можно, приказ — нельзя»; развилку внутри него поднял сам ADR, а независимый review подтвердил, что она настоящая | **Принять ADR 0019 как есть**: адресуется существо, `{tick, kind, creatureId, verdict}`, список адресных команд растёт с одной до двух | — | Вариант «адресовать карточку» (`cardId`) отклонён владельцем при названной цене обеих сторон: он не расширяет список адресных команд вовсе, но делает косвенность формальной — [ADR 0009](../decisions/0009-adherent-role-boundary.md) сделал адресацию явной и **пересчитанной**, и второй иначе устроенный вход обесценивает сам счёт. Цена принятого варианта названа владельцу до «да» и записана в ADR: **из пяти рычагов раздела 6.6 [`PITCH.md`](PITCH.md) границей выразимы два** («наградить», «унизить»), «дать больше власти» доступно со среза 4 и без клаузулы обмена, «отправить в опасную вылазку» и «столкнуть с соперником» требуют собственного решения; и журнал команд больше нельзя признать законным, не прогнав его, — проверка окна момента истины живёт в `ApplyCommand`, а не в статическом pre-flight. Два круга review: первый вернул восемь находок, из них три меняли решение по существу — проверка закрывала форму команды и оставляла открытым перечисление значений (`verdict: "sortie"` проходил все четыре вопроса, будучи приказом); по букве проверки приказом оказывалась принятая команда роли ADR 0009; структурная гарантия окна висела на валидаторе, у которого нет состояния мира. Остаток — [#167](https://github.com/anshushunov/dungeon-fortress/issues/167): условия проверяют имя значения и род эффекта, но не его отменяемость и охват |
| 2026-08-01 | **Инварианты 13.4 против правила подхода** ([#129](https://github.com/anshushunov/dungeon-fortress/issues/129)) — бой стал читаемее, а инварианты 4 и 11 перестали выполняться на двух seed. Причин оказалось три, и разбор их развёл: канал 1 — память о месте (20260728, владение гибнет от голода при выигранном бое), канал 2 — правило подхода помогает слабой фикстуре сильнее, чем сильной (20260727, где память инертна), канал 3 — слава | **Принять новые коридоры вместе с новым основанием для инвариантов**; канал 1 вынесен отдельной задачей | — | Решение принято при названной цене: на одном seed из трёх неподготовленное владение обгоняет подготовленное, разрыв 62 → −28, и это принимается, пока у подготовки не появится новый источник преимущества. Ключевое различение, без которого выбор был бы другим: **выходы закрывают разные каналы** — «ослабить память» чинит 20260728 и не трогает 20260727, где ослаблять нечего. Инвариант 11 разложен на три клаузы, и условие названо **исходом**, а не именами фикстур: прежняя форма перестала быть про выживание, как только падать смогла не только `neglected`. Инвариант 4 стал одной ценой набега вместо двух счётов, нормированной на число волн, и в одной части **строже** прежнего — считает всех потерянных защитников, закрывая размен «бегство на выбывание». Что новое основание не подогнано под ветку, доказано переносом новых проверок на `main`: там они зелёные. Побочные находки блока стали задачами: [#168](https://github.com/anshushunov/dungeon-fortress/issues/168) планка бегства (84,5 % → 70,4 %, причина — место, где ломается защитник, а не время), [#170](https://github.com/anshushunov/dungeon-fortress/issues/170) граница разброса уступок, подогнанная под самую спокойную ячейку (на `main` та же величина даёт 226), [#171](https://github.com/anshushunov/dungeon-fortress/issues/171) цена памяти о месте у кладовой |
| 2026-08-01 | **Пак существ v2 и потолок кругов review** — второй круг review нашёл, что правка первого круга срезала оружие и ухо о край канваса, то есть находка ломала критерий готовности и не могла стать ни Issue, ни записью реестра | **Третий круг разово**, сверх потолка правила 12; правило не меняется | — | Прецедент — послабление 2026-08-01 по [#139](https://github.com/anshushunov/dungeon-fortress/issues/139), и основание то же: находка, ломающая критерий готовности, не может стать ни Issue, ни записью реестра. **Альтернатива была и названа отклонённой**: координатор чинит сам и мержит без третьего круга — так закрыт PR #85 2026-07-30 («последнюю правку внёс координатор по решению владельца»), и правило 3 требует независимого review на **PR**, а не на каждую правку. Здесь она не годится: правка была в бинарных ассетах, где «посмотреть глазами» и есть вся проверка, а смотреть на собственную работу — то же самоосвобождающее суждение, против которого написаны правила 4 и 7. **Цена исключения названа прямо**: потолок правила 12 пробит вторым разом за двое суток по одному аргументу, и если он прозвучит третий раз, разбирать надо не находку, а само правило 12. Форма канваса решена координатором как tuning представления по [ADR 0010](../decisions/0010-contract-invariants-and-tuning.md), без ADR — тем же порядком, что выбор масштаба 170 %: **272×192**, тело по центру; вариант «уменьшить пак под квадрат» отвергнут как самоотрицающий, он роняет тело со 168 до ~121 px и выкидывает выбранный масштаб. Пак смержен по границам [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md) §5; картинка в игре не изменилась — рантайм грузит v1, подключение внутри [#77](https://github.com/anshushunov/dungeon-fortress/issues/77). Остаток — [#165](https://github.com/anshushunov/dungeon-fortress/issues/165) |
| 2026-08-01 | **Два процессных решения по ходу блока** — владелец назвал их сам: не хватает ожиданий при выдаче задачи, и Codex дважды переключил ветку корневой рабочей копии, пока в ней шла координация | Правило 16 — задача выдаётся с саммари «**До → Меняем → После**»; правило 17 — **корневая рабочая копия принадлежит координационной сессии** | — | Оба — форма отмены моратория (правило 11), предусмотренная им самим: явное решение владельца. Правило 17 записано в [`AGENTS.md`](../../AGENTS.md) с командой создания worktree, а не только здесь, потому что Codex читает `AGENTS.md` напрямую; измеренная причина — независимый review чужого PR нашёл корневую копию не на той ветке и не мог отличить это от постороннего вмешательства, то есть цена не в неудобстве, а в потере доверия проверяющего к дереву |
| 2026-08-02 | **Блок боя — правило подхода** ([#129](https://github.com/anshushunov/dungeon-fortress/issues/129), PR #164). Владелец играл на `main` после **пяти** merge блока; дерево названо коммитами поимённо, потому что `git log` перестаёт его воспроизводить после следующего merge: `15bab1b`, `c5aa31b`, `0f20868`, `cbbd588`, `35ea72d`. Список и соответствие коммит ↔ PR получены `gh pr list --state merged --json number,title,mergeCommit`, а не выписаны из `git log` | **`CONTINUE`** | 0 | Ответ владельца целиком: «да, стали расходиться и выглядит более живым». Вопрос слайса — «перестал ли бой выглядеть очередью» — закрыт. **Важно для чтения этого gate: масштаб 170 % в игре ещё не применён.** Пак существ v2 лежит в `main` (PR #162), но рантайм грузит v1, и множителя в коде нет — `LoadGoblinSprites` собирает пути `goblin_{state}_v1.png`, `CameraView` знает только `ReferenceGoblinDrawSize = 20.0` при `ReferenceTileSize = 22.0`, то есть тело рисуется 36,4 px при клетке 40. Владелец спросил об этом сам, и ответ записан здесь, а не в чате: улучшение боя получено **одним правилом подхода**, без единого пикселя нового арта. Побочная выгода для следующего playtest — эффект масштаба будет отделим от эффекта правила. Слот gameplay освобождается; следующий блок — [#77](https://github.com/anshushunov/dungeon-fortress/issues/77): масштаб 170 %, подключение пака v2, обратная связь удара |
| 2026-08-02 | **Потолок кругов review не удержал третий раз подряд** — на PR #174 подтверждающий круг нашёл два противоречия, внесённых самим кругом доработки, и они ломают критерий готовности, то есть по правилу 4 не могут стать ни Issue, ни записью реестра. Развилка вынесена владельцу самим review, отказавшимся предлагать координатору открыть третий круг: по записи от 2026-08-01 третье срабатывание того же аргумента разбирает **правило 12**, а не находку | **Разовый третий круг**; правило 12 не меняется. Отдельно — **правило 18: координатор не запускает агентов без команды владельца** | — | Предложение координатора сменить единицу счёта потолка (не считать круг, проверяющий исправление находок, если он ограничен диффом исправления) владельцем **не принято** и в раздел не записано. Наблюдение, ради которого оно предлагалось, фиксируется здесь **в том объёме, в каком подтверждается**: из трёх пробоев потолка так устроены **два** — пак существ v2 (правка первого круга срезала оружие и ухо о край канваса, см. строку выше) и этот PR. Третий, #139, устроен иначе: внесённое исправлением там поймал **первый** круг, а блокирующая находка второго — непокрытая мутацией проводка, существовавшая независимо от правки (тело PR #148, `gh pr view 148`). Первая редакция этой строки называла три случая из трёх; счёт исправлен независимым review, перепроверившим #139 по репозиторию. Правило 18 принято тем же ответом и записано в разделе [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md); его цена — ожидание владельца между готовым брифом и работой, то есть замедление против направления time to market, и она названа сознательно |
| 2026-08-02 | **Экономия токенов и лимитов** — разбор расхода по предложению внешнего ревью процесса (Codex): фиксированная цена процесса вокруг задачи сравнялась с ценой самой задачи. Три пункта предложения оказались уже действующими правилами (13; 4 и 12; 11); review по уровням риска отклонён — он противоречит выводу 1 калибровки: цену review задаёт объём поднимаемого контекста, а не глубина проверки, поэтому «дешёвый review новым агентом» не существует | Семь решений владельца: **блочный reviewer** — один возобновляемый review-агент на все PR блока (правило 19, использует вывод 2 калибровки); **потолок записывающих возвращён к двум** — исполнение встроенного отката [ADR 0017](../decisions/0017-raise-parallel-writing-ceiling.md) §5, владелец объявил бюджет дефицитным; **вложенные сабагенты запрещены по умолчанию** (правило 20); **мутация каждой половины сужена** до симуляции, детерминизма и контракта (правило 21); **полный `verify.ps1` один раз на PR** (правило 22); **выдача одиночной задачи лёгким режимом `/task`** (правило 23); **#77 идёт последовательностью вертикальных подзадач** после включения блочного reviewer — одно наблюдаемое поведение на задачу, один implementer без сабагентов | — | Правила 19–23 раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md); `MULTI_AGENT_WORKFLOW.md` и `/coord` приведены в соответствие, добавлен `.claude/commands/task.md`. Сохранено без изменений: независимый review каждого PR (ADR 0014 §4), worktree-изоляция, seed, checksum, replay, оценки токенов и checkpoints |
| 2026-08-02 | **Автономия координатора** — разбор нарушения: координатор смержил PR #178 по правилу 7 при `pending` CI, назвал это сам и вынес владельцу вопрос о полномочиях. Независимый review PR #181 нашёл, что запись оставляет два расходящихся списка эскалации и пункт про квалификацию без последствия; подтверждающий круг не подтвердил квалификацию и вернул PR второй раз — обход мест со старым списком оказался неполон, потому что словарь команды обхода не ловил список, выписанный другими словами | **Правило 24**: координатор сам запускает агентов и сам мержит PR; наверх — блокеры, архитектурные и геймплейные решения. **Правило 18 снято.** Двумя добавочными ответами владельца: три категории **заменяют** семипунктовый список правила 7, риск безопасности/лицензии/приватности/расходов и труднообратимость переходят к координатору **под условием строки в этом журнале**; отказ review в квалификации под правило 7 — процедурный триггер, такой PR требует `MERGE #N`. Плюс **разовый третий круг** сверх потолка правила 12; само правило не меняется | — | Правило 24 раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md); секция правила 18 помечена снятой со ссылкой на правило 24; [`AGENTS.md`](../../AGENTS.md), [`MULTI_AGENT_WORKFLOW.md`](../engineering/MULTI_AGENT_WORKFLOW.md) и `/coord` приведены в соответствие тем же набором изменений — старый перечень стоял там как действующий, а `AGENTS.md` за границами полномочий отсылает как раз к общему процессу. Правило 24 названо **четвёртым** ослабляющим ADR правилом, преамбула раздела исправлена с «2, 5 и 7» на «2, 5, 7 и 24»; тем самым сработало условие записи реестра от 2026-07-31 — повышена до [#183](https://github.com/anshushunov/dungeon-fortress/issues/183). Послабление потолка правила 12 — **четвёртое** в истории проекта (после #139, пака существ v2 и PR #174); основание прежнее: находка ломает критерий готовности и не может стать ни Issue, ни записью реестра. Не отменены: независимый review каждого PR, исход находки за review, квалификация под правило 7 за review вместе с последствием отказа, потолок кругов, gate владельца, условия merge из правила 7. ADR 0014 не изменялся |
| 2026-08-02 | **Срез: цена памяти о месте** ([#171](https://github.com/anshushunov/dungeon-fortress/issues/171), PR #178), gameplay-слайс. Владелец играл на `main` после merge; состав дерева и соответствие PR ↔ коммит проверены `gh pr list --state merged --json number,title,mergeCommit` | **`CONTINUE`** | 0 | Ответ владельца по смыслу: «метки видны, в целом всё работает». **Основание половинчатое, и это ограничение gate, а не оговорка.** Вопрос слайса состоял из двух половин, и владелец наблюдал только вторую. Первая — «перестал ли выигранный бой кончаться голодной смертью» — в игре не показывается вовсе: видимый хост `scripts/run-game.ps1` гоняет фикстуру `baseline`, а её seed — **20260726** (`scenarios/prototype1/baseline.commands.v2.json`), тогда как дефект жил на **20260728**; параметра seed у скрипта нет (`-Fixture` есть, seed не параметризован). Первая половина закрыта **доказательством, а не впечатлением владельца**: матрица из пятнадцати прогонов `--evaluate-prototype` плюс шесть партий `PrototypeMemoryCostTests`, и независимый review воспроизвёл все шесть ячеек отчёта своими прогонами. Записано прямо — тем же порядком, каким прежние строки журнала называли непокрытые трети критерия |
| 2026-08-02 | **Вердикт независимого review жил только в контексте координатора** — при потере сессии доказательства того, что PR проходил review, не осталось бы, а после снятия правила 18 и принятия правила 24 review — единственная внешняя проверка перед merge. Дыру нашёл независимый review, разбирая PR #187 | **Правило 26: вердикт публикуется комментарием в PR перед merge, и публикует его координатор**, а не reviewer | — | Формулировка про «кто публикует» точна намеренно: **reviewer read-only**, его бриф запрещает публиковать комментарии, за весь блок он не опубликовал ни одного — вердикты уходили handoff'ом, переносил координатор. Отвергнуты с названной ценой: файл `review/<PR>.md` в ветке (наружу ничего не идёт, но вердикт мержится вместе с кодом, который одобрял) и «ничего не менять» (при автономном merge единственная внешняя проверка остаётся невидимой). Введено **по ходу блока**, и записано в том объёме, в каком подтверждается: комментарии есть у PR #180, #181, #185, у #175 и #178 их нет (`gh pr view <N> --json comments`). **Цена:** перенос идёт руками, и оболочка уже съела фразу из опубликованного вердикта PR #180, выполнив обратные кавычки как команды. Откат: перенос продолжает искажать вердикт → публикацию берёт reviewer, и его read-only мандат сужается явным решением. Правило 26 раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md) |
| 2026-08-02 | **Кто создаёт PR и куда идёт отчёт исполнителя** — владелец заметил, что при разнородном флоте агентов он сам оказывается транспортом: внешние инструменты (Codex, opencode, локальная модель) не в сессии координатора, их отчёт возвращается владельцу, и он вставляет его руками | **Правило 25: исполнитель общается через репозиторий, а не через владельца** — работа в ветку, результат и отчёт в тело PR, который исполнитель создаёт сам, остановка и вопрос комментарием в свою Issue | — | Прежний запрет «публикация наружу сначала показывается черновиком» писался, когда публиковал только координатор; к собственному PR в собственном репозитории он не относится — рискованный шаг это **merge**, а не открытие PR. Не изменены: независимый review каждого PR, merge за координатором при зелёном CI и review, отказ review под правило 7 → `MERGE #N`, право координатора править описание, черновик перед публикацией в чужие системы. **Цена:** описание PR пишет тот, чью работу оно описывает, и приукрасить его легче; защита — тот же независимый review, который приукрашивание в этом блоке и ловил. Побочно снимается ручное переписывание чужих чисел координатором — на нём он в блоке ошибся дважды. **Правило 8 названо в новом месте — теле PR**: между исполнителем и деревом больше нет второго читателя, первым читателем описания становится review, и число без команды делает его выборочную проверку невозможной. Из находок блока к классу «описание расходится с деревом» относятся минимум четыре (PR #180, #176, #185, #178), и ни одна не нашлась чтением — все нашлись прогоном команды из описания. Откат: review пропустил приукрашенное описание → правило пересматривается. Правило 25 раздела [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md); [`MULTI_AGENT_WORKFLOW.md`](../engineering/MULTI_AGENT_WORKFLOW.md) и `/coord` приведены в соответствие тем же набором изменений |
| 2026-08-02 | **Масштаб тела 170 % и пак существ v2 в игре** ([#77](https://github.com/anshushunov/dungeon-fortress/issues/77), первая вертикаль, PR #185) | принято владельцем на playtest | — | Класс `support`, своего gate не требует. Выбор масштаба, сделанный по спайку [#142](https://github.com/anshushunov/dungeon-fortress/issues/142), **подтверждён в игре**: «по размеру ок, давай так двигаться. Видно нормально, не хватает анимаций конечно но наверно это скоро будем делать». Следующая подзадача названа прямыми словами владельца — **процедурная анимация и обратная связь удара**. Заведена [#186](https://github.com/anshushunov/dungeon-fortress/issues/186): после боя группа не может разойтись |
| 2026-08-02 | **Как облегчить review и как перестроиться на разнородный флот** — вторая координационная сессия дня. Поводы измерены и лежат в `NOW`: круг доработки оставил остаток четыре раза подряд (PR #175, #178, #176, #181), а блочный reviewer при снятом потолке становится очередью — merge пятого PR ждёт review первых четырёх | Три решения владельца: **правило 27** — дефолт reviewer дешёвая модель (сильная по маркерам риска правила 3), записанное условие отката, и круг доработки отдаёт таблицу «находка → правка → команда»; **правило 28** — потолок записывающих агентов снят, единственный ограничитель параллельной работы — партиция по файлам; **поправка к правилу 19** — reviewer возобновляется на область (`симуляция + контракт`, `представление`, `tooling + docs`), а не на блок | — | Правила 27 и 28 — секция [«Облегчение review и снятие потолка»](../engineering/DEVELOPMENT_PACE.md#облегчение-review-и-снятие-потолка-решения-владельца-2026-08-02); поправка записана в самом правиле 19; преамбула раздела «Темп разработки» называет **28** пятым ослабляющим ADR правилом («2, 5, 7, 24 и 28»). **Отклонено с названной ценой:** «ревьюер сам правит находки» — правящий reviewer становится автором части PR, и независимого взгляда на эту часть не остаётся ни у кого (та же формулировка отклонила «координатор чинит сам» на паке существ v2, строка 2026-08-01). **Отложено, не отменено:** spike по внешней дешёвой модели под review — правило 19 прожило один блок, и менять класс модели поверх неизмеренного рычага значит потерять оба измерения; вернуться после того, как снято число экономии от правила 19, и через [`TECH_RADAR.md`](../engineering/TECH_RADAR.md). **Названная цена принятого:** база review платится трижды, если в блоке идут все три области. Правило 28 **ослабляет** [ADR 0017](../decisions/0017-raise-parallel-writing-ceiling.md) §5, и ослабление названо в тексте правила — исполнение урока [#183](https://github.com/anshushunov/dungeon-fortress/issues/183). Не отменены: правила 1, 2, 3, партиция по файлам и четыре условия ADR 0017 §3, worktree-изоляция, seed, checksum, replay, оценки токенов, checkpoints. Записано задачей [#173](https://github.com/anshushunov/dungeon-fortress/issues/173) — до её merge решения жили только в теле Issue |
| 2026-08-03 | **Толчея после боя: чинить ли занятость клетки сейчас** ([#186](https://github.com/anshushunov/dungeon-fortress/issues/186), PR #198). Задача-вопрос: измеряла и не чинила, `src/` не тронут. Первое измерение сказало, что после боя не теснее обычного дня, и **независимый review это опроверг** — контрольное окно на `prepared` совпадало тик в тик с окном сбора по тревоге. После поправки наблюдение владельца на playtest подтвердилось | **Занятость клетки ([#76](https://github.com/anshushunov/dungeon-fortress/issues/76)) остаётся на срезе 6; толчея не чинится; следующая фича — читаемый удар, вертикаль [#77](https://github.com/anshushunov/dungeon-fortress/issues/77)** | — | Числа записаны **один раз**, в контракте `docs/design/PROTOTYPE_01_PREPARE_FOR_RAID.md` §4.1 и в `evidence/186-*.json` вместе с командами — оба внесены PR #198, поэтому эта строка верна начиная с его merge; копии здесь нет сознательно. Виновный механизм назван и устоял под двумя поправками подряд — обход достаёт больше, чем уступка, на сопоставимом подмножестве отказов. **Названная цена решения:** затор остаётся и будет виден на каждом запуске; исправление симптома уступками закрывает меньше половины случаев (344 из 782, 44,0 % — единственное число, скопированное сюда намеренно, потому что оно и есть цена), а недостающая часть приходится на существ, стоящих на собственной цели: уступать по правилам они не обязаны. **Что решение не отменяет:** правило подхода [#129](https://github.com/anshushunov/dungeon-fortress/issues/129) не откатывается — измерение показало, что толчея была и до него; вердикт источника о его роли — «усилитель, а не механизм», и количественной оценки усиления здесь нет сознательно: источник допускает два прочтения, и выбирать между ними журналу не место. Вторая половина симптома вынесена отдельным решением строкой ниже |
| 2026-08-03 | **Что делает существо, когда работы нет** — вторая половина того же симптома, к затору отношения не имеющая: больше половины стоящих после боя не заблокированы вовсе, они стоят там, где кончилась драка | **Существо без работы уходит с места боя** — в покои или на пост; выбор места делается один раз и обосновывается | — | Решение принято при названной цене и выгоде: оно снимает **видимую** половину симптома дёшево, не трогая занятость клетки, и попутно расходит толпу — место боя перестаёт удерживать половину группы. Числа, по которым решение принято, записаны один раз в контракте §4.1 и в `evidence/186-*.json`. Исполнитель решения — [#201](https://github.com/anshushunov/dungeon-fortress/issues/201); до её merge решение живёт в теле Issue, и это временно сознательно. **Отдельно не решено и в scope #201 не входит:** пятая часть простоя приходится на «зона недостижима», а не на «работы нет» — это возможный самостоятельный дефект, и путать его с предметом #201 нельзя |
| 2026-08-03 | **Темп: чем ограничена скорость блока** — владелец назвал симптом своими словами: «мы очень медленно движемся, берём тикет на геймплей и рядом море сапорт-тикетов, нужно радикально ускориться». Разобрано по факту дня: смержено 12 PR, из них фич четыре; почти каждый PR прошёл по два круга review, два — по три, и **три возврата из четырёх были про утверждения, а не про код** | Три решения владельца: **правило 29** — три фичи могут писать в один файл одновременно, порядок merge и rebase держит координатор; **правило 30** — блок берёт из долга только блокеры, остальное живёт `ready` вне счёта блока; **правило 31** — числа тела PR прогоняются инструментом `check-pr-claimed-output.ps1` до review | — | Полные формулировки, риски и условия отката — раздел [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md), правила 29–31; копии здесь нет сознательно. **Что этими решениями НЕ отменено, названо прямо в правилах:** правило 2 (не более двух gameplay-слайсов, балансные не параллелятся) — оно про playtest как измерительный прибор, а не про конфликты в дереве; правило 1 (в блоке обязателен gameplay-слайс); правило 12 (потолок кругов и подтверждающий круг) — сегодня именно подтверждающие круги нашли два настоящих дефекта. **Названная цена правила 30:** долг копится и однажды потребует блока целиком; владелец это принял |
| 2026-08-03 | **Чем тело говорит, на чьей оно стороне** — playtest после [#177](https://github.com/anshushunov/dungeon-fortress/issues/177): «круги загромождают экран и делают только хуже». #177 чинил видимость индикатора и заплатил за неё радиусом: при клетке 40 px кольцо имеет диаметр 98.18 px против 40 px клетки, то есть около шести клеток площади на одно тело, и девять тел в куче дают наложенные дуги вместо тел | **Кольцо снято, принадлежность несёт контур по силуэту, окрашенный по отношению к игроку, а не по фракции.** Свой teal `#14b8a6` шириной 1.2 опорных px, чужой red `#dc2626` шириной 2.0 — громкий канал достаётся редкому событию. Прозрачность не вводится, `Neutral` не заводится до появления производителя | — | Причина «отношение, а не фракция» названа владельцем: **пак рас будет общим**, поэтому возможен гоблин-свой против гоблина-чужого, и различать стороны нужно независимо от расы — тот случай, который в жанре известен с *Warcraft: Orcs & Humans*. Добавление нейтрала или фракции — строка в таблице отношений, это и было требованием владельца «чтобы потом можно было подкрутить». Решение записано спекой [`SIDE_INDICATOR.md`](../design/SIDE_INDICATOR.md) — **ADR не заводился**: продуктовое обещание описано там же, где живёт соседний UI-пас `UI_CONTROL_PASS.md`, и третья копия одного решения этому репозиторию уже дорого обошлась. **Отвергнуты с названной причиной:** тинт тела (умножается на палитру расы, то есть сигнал зависит ровно от того, от чего не должен) и маркер под ногами (структурно закрывается телом переднего ряда — ломается именно в толпе, ради которой затевался). **Известные ограничения, не закрытые решением:** пара teal/красный при протанопии, слипание контуров соседних тел одной стороны, восемь draw-вызовов на тело и просвет копий сквозь внутренние дыры силуэта; шейдерный контур снимает три последних и назван дальнейшим шагом. Исполнитель — [#208](https://github.com/anshushunov/dungeon-fortress/issues/208), доказательство в `evidence/208-side-indicator.json` |
| 2026-08-03 | **Скоуп блока #201/#210** — согласован владельцем до планирования, по его прямому требованию | Три ответа: тяжёлую фичу берёт основная сессия — [#201](https://github.com/anshushunov/dungeon-fortress/issues/201) «существо без работы уходит с места боя»; ширина блока средняя — фича плюс 4–6 задач долга; **балансный долг симуляции ([#168](https://github.com/anshushunov/dungeon-fortress/issues/168), [#170](https://github.com/anshushunov/dungeon-fortress/issues/170), [#116](https://github.com/anshushunov/dungeon-fortress/issues/116)) в блок не входит** | — | Состав и партиция — `NOW`. Причина отвода балансного долга названа при выборе: все трое пишут в `src/DungeonFortress.Simulation/**`, то есть в файлы #201; параллельно невозможны по партиции, последовательно растянули бы блок. Фича блока — [#210](https://github.com/anshushunov/dungeon-fortress/issues/210), вертикаль 3 задачи #77; её границу («обратная связь удара», без процедурной анимации) назвал координатор, причина — размер PR и потолок кругов review |
| 2026-08-03 | **Реестр долга: правило трёх gate-решений сработало впервые** — и сразу для 37 записей из 44. Решение координатора, записано строкой как условие полномочия, а не как развилка владельца | **37 записей удалены с причиной, ни одна не повышена и ни одна не переформулирована**; в реестре осталось 7, плюс две добавлены тем же набором изменений по предложению review PR #213 и #214, поэтому команда подсчёта на сейчас даёт 9 | — | Правило даёт два исхода, и «остаётся» среди них нет — этот третий исход отклонён 2026-08-01 review PR #155 как сброс счётчика. Переформулировка не применима: условия удалённых записей наблюдаемы, большинство уже переформулированы ради наблюдаемости 2026-08-01. Проверка **срабатывания** велась отдельно от проверки возраста — исполнение урока протокола 2026-08-02, где сработавшее условие пропустили именно из-за этого. Команды, перечень удалённого и разбор — протокол 2026-08-03 в [`DEBT_LEDGER.md`](../engineering/DEBT_LEDGER.md), копии здесь нет. **Наблюдение на будущее:** механизм, срубающий за раз 84 % реестра, стоит проверить на следующем сроке — если тот же эффект повторится, вопрос не в записях, а в том, что реестр принимает находки быстрее, чем gate успевает случиться; предмет — [#159](https://github.com/anshushunov/dungeon-fortress/issues/159) |
| 2026-08-04 | **Срез «существо без работы уходит с места боя»** ([#201](https://github.com/anshushunov/dungeon-fortress/issues/201), PR #217), gameplay-слайс. Владелец играл на `main` после merge всех PR блока; дерево — коммит `41bccf0` (`git log --oneline origin/main -1`), соответствие PR ↔ коммит получено `gh pr list --state merged --json number,mergeCommit` | **`CONTINUE`** | 0 | Ответ владельца целиком: «разошлись быстрее, все встали в больницу и не могли оттуда выйти, возможно изза того что поле боя блокировало». **Вопрос слайса закрыт первой половиной ответа**: группа перестала стоять там, где кончилась драка, — то самое, что задача обещала, и что измерено долей когорты «стоит без шага и без отказа» 29,7 % → 17,1 % (контракт §4.1). **Вторая половина вопросом слайса не является и потому не даёт `ADJUST`**: «больница» — это `Quarters` (`PrototypeTypes.cs:31`), то есть место, **куда** группа пришла, а не место боя; картина «стоят и не выходят» совместима с тремя разными механизмами (работы нет / выход заперт телами / зона недостижима), и который из них виновен — неизвестно. Прецедент есть, и он описан здесь **по строкам журнала, а не по памяти**: playtest 2026-08-02 дал `CONTINUE` по [#171](https://github.com/anshushunov/dungeon-fortress/issues/171) (строка «Срез: цена памяти о месте»), а новое наблюдение владельца того же дня стало [#186](https://github.com/anshushunov/dungeon-fortress/issues/186) — заведена соседней строкой «Масштаб тела 170 % и пак существ v2 в игре», а не строкой gate. Первое измерение #186 утверждало, что толчеи нет, и это опроверг независимый review PR #198 — уже 2026-08-03, строка «Толчея после боя». Три эпизода принадлежат трём разным записям, и первая редакция этой строки склеивала их в один; счёт исправлен независимым review PR #230. Общее у них ровно то, ради чего прецедент назван: **вопрос слайса закрывается, а новое наблюдение уходит отдельной задачей и там же проверяется**. Заведена [#228](https://github.com/anshushunov/dungeon-fortress/issues/228) — измерить и назвать механизм. **Условие, при котором эта строка пересматривается, названо здесь, а не в отчёте:** если #228 покажет, что держит группу само правило ухода, gate по #201 получает продолжение строкой ниже, а не задним числом. Цена правила названа заранее и владельцем видна: отказов в шаге на окно 60,2 → 75,1, и контракт §4.1 говорит, что это [#76](https://github.com/anshushunov/dungeon-fortress/issues/76), оставленный на срезе 6 |
| 2026-08-04 | **Потолок кругов review пробит пятый раз** — на PR #230 (состояние блока, gate, реестр). Разбор вернул три находки; подтверждающий круг вернул PR второй раз, и оба его дефекта внесла **сама правка круга доработки**, а не исходная работа. Находка ломала критерий готовности 5 Issue #229, то есть по правилу 4 не могла стать ни Issue, ни записью реестра | **Разовый третий круг**; правило 12 не меняется | — | Послабление **пятое**, и число получено перечислением прежних, а не по памяти: [#139](https://github.com/anshushunov/dungeon-fortress/issues/139) (2026-08-01), пак существ v2 (2026-08-01), PR #174 (2026-08-02), PR #181 (2026-08-02). Третий круг дал `ЗЕЛЁНЫЙ С ЗАМЕЧАНИЕМ`. **Измеренная причина двух возвратов подряд названа здесь, потому что она одна и та же:** критерий готовности был сформулирован как прогон команды по файлу, а исполнен как взгляд на собственный diff — первый круг так пропустил таблицу «План сессий вперёд», второй строку «Очередь» и абзац `NOW`; команда из критерия ловила оба случая мгновенно и просто не была запущена. Это факт об исполнении критерия, **новым процессным правилом не становится**: мораторий (правило 11) не отменяется, правило 12 не меняется, потолок остаётся один разбор плюс один подтверждающий. **Что решение не отменяет:** независимый review каждого PR, исход находки за review, квалификацию под правило 7 за review. **Наблюдение на будущее, а не правило:** из пяти пробоев потолка **три** вызваны правкой предыдущего круга (пак существ v2, PR #174, этот) — если сработает четвёртый такой, разбирать надо не находку и не правило 12, а то, чем проверяется правка круга доработки |
| 2026-08-04 | **Два правила, выведенных блоком #221/#228** — оба нашлись как измеренная цена, а не как соображение: гард, довольный тем, что тело поворачивается не в ту сторону (PR #235), и три возврата за день по одной и той же форме — правится ячейка статуса, соседний текст строки остаётся из прежней редакции (PR #230 дважды, PR #239) | **Правило 32** — мутант назначается на каждое **утверждение**, которое проверка делает, а не на половину изменения; **правило 33** — состояние задачи проверяется перечислением вхождений её номера, а не меткой статуса | — | Полные формулировки, измеренная цена и условия отката — раздел [«Темп разработки»](../engineering/DEVELOPMENT_PACE.md), правила 32 и 33; копии здесь нет сознательно. **Мораторий правила 11 этим не отменён** — оба правила приняты его собственной предусмотренной формой, явным решением владельца, и текст моратория остаётся. Правило 32 **уточняет** правило 21, правило 33 — способ проверки правила 8; ни одно из прежних правил не заменено. Цена правила 32 названа числом: два мутанта независимого review дали **592 пройдено, 0 не пройдено** на гарде, который считался закрывающим критерий; подтверждающий круг нашёл ещё **пять** проходящих подстановок того же класса — остаток [#237](https://github.com/anshushunov/dungeon-fortress/issues/237). Цена правила 33: команда, записанная координатором в собственный критерий готовности, не могла поймать дефект **по построению** — она искала метку статуса, а ложь жила в описании рядом |

| 2026-08-04 | **Способ производства анимации тела** — владелец, посмотрев на процедурную анимацию ([#221](https://github.com/anshushunov/dungeon-fortress/issues/221)), назвал следующий шаг сам: увидеть **одного полностью анимированного персонажа**, удар и отдачу, и понять, приятно ли на это смотреть. Вопрос «сколько кадров» вторичен; первично, **из чего собран персонаж**, потому что от этого зависят три уже принятых решения — общий пак рас, снаряжение на срезе 7 и читаемость в толпе | **Скелет из частей, а не покадровая анимация**; проба ставится на одном гоблине, сцена — **один на один, крупно** | — | [ADR 0020](../decisions/0020-body-animation-cutout-rig.md); принят при действующем моратории по его собственному исключению для труднообратимых решений. Референсы названы в ADR: принятому варианту — **Battle Brothers**, **Oxygen Not Included**, **Don't Starve**; отклонённому — **Dead Cells**, **Hyper Light Drifter**, **Factorio**. **Отклонена не цена вообще, а то, что покадровая анимация делает дороже именно уже решённое**: кадры множатся на действия, направления и расы, а снаряжение слоями требует перерисовки снаряжённого тела в каждом кадре. Третий вариант — «только код, без нового арта» — отклонён как самостоятельный путь, но **его содержание не выброшено**: отдача обеих сторон и эффект контакта входят в пробу, потому что без них удар не читается независимо от способа рисования тела. **Названный риск, ради которого решение записано:** скелет из частей на 68 px может выглядеть **хуже** склеенной позы — стыки видны, поворот руки вокруг сустава читается как поломка; поэтому проба на одном персонаже, а не сразу на паке. Условие пересмотра — в ADR |
| 2026-08-04 | **Потолок кругов review пробит шестой раз** — на PR #246 (документы после ADR 0020). Разбор вернул шесть находок, подтверждающий круг вернул PR второй раз, и **все три его находки внесла сама правка круга доработки**: разрыв дат назван «сутками» вместо 3 ч 16 мин, абзац отнесён не к тому разделу, и обоснование построено на времени написания, которое даёт обратное. Решение координатора по правилу 24 — не блокер, не архитектура, не геймплей, — и потому записано строкой сюда, а не отдано владельцу | **Разовый третий круг**, шестое послабление потолка правила 12; само правило не меняется. Основание — измеренная история: пробои потолка в большинстве своём вызваны правкой предыдущего круга, а не исходной работой, то есть вероятность ошибки именно в правке высока, а круг на возобновлённом агенте стоит доли базы | — | **Условие, записанное после пятого пробоя, этим случаем сработало, а не приблизилось.** Оно звучало «если сработает четвёртый такой» (строка выше, 2026-08-04, PR #230); PR #246 и есть четвёртый: счёт стал **4 из 6** — пак существ v2, PR #174, PR #230, PR #246; не относятся к этому роду #139 (блокирующая находка второго круга существовала независимо от правки) и PR #181. Значит разбирать надо не находку и не правило 12, а **чем проверяется правка круга доработки**, и исполнено это [#248](https://github.com/anshushunov/dungeon-fortress/issues/248), а не обещанием в этой ячейке. Формулировка «повторится ещё раз» стояла здесь в первой редакции и была ошибкой того же рода, что уже случалась в проекте: условие «при третьем срабатывании» однажды прошло мимо, потому что запись назвала наступившее предстоящим |
| 2026-08-04 | **Проба [ADR 0020](../decisions/0020-body-animation-cutout-rig.md): скелет из частей на одном гоблине** ([#243](https://github.com/anshushunov/dungeon-fortress/issues/243), PR #249; [#244](https://github.com/anshushunov/dungeon-fortress/issues/244), PR #256). Владелец смотрел дуэльную сцену и её A/B на плоском паке и ответил на вопрос, который сам же и поставил — «приятно ли на это смотреть» | **`CONTINUE`** | 0 | Ответ владельца дословно: «на демке плохо видно но движения в целом ок — виден переход между позами. Правда левый стоит задом почему то». Первая половина закрывает вопрос пробы: способ производства анимации принят, ADR 0020 пересмотру не подлежит, условие его замены не наступило. Вторая половина — **дефект направления тела**, вынесен в [#259](https://github.com/anshushunov/dungeon-fortress/issues/259) и не смешивается с gate: gate отвечает на «продолжаем ли», дефект — на «что чинить». Следующий кусок назван владельцем тем же сообщением: перевести на риг **всех**, проверить, что тела смотрят в верную сторону, и посмотреть **массовый бой** — [#260](https://github.com/anshushunov/dungeon-fortress/issues/260); дуэльная сцена была ограничением пробы, и оно снимается сознательно. **Третья часть ответа — «на демке плохо видно» — тоже получила исход, а не была поглощена словом «но»:** это подтверждение того единственного «хуже», которое назвал независимый review PR #256 («размах руки читается, силуэт тела почти нет»), и оно вынесено в [#263](https://github.com/anshushunov/dungeon-fortress/issues/263). Амплитуду при этом нельзя просто увеличить — она упирается в измеренные щели, и выбор выхода входит в задачу |
| 2026-08-04 | **Чем занята разработка дальше** — владелец назвал приоритет сразу после gate по пробе ADR 0020, отвечая на вопрос о кастомизации | **Визуальная проработка боя — ведущее направление.** Формулировка владельца дословно: «**Бой — важная часть игры, хочется её в начале проработать так чтобы было симпатично, без этого дальнейшая разработка не имеет смысла**» | — | Направление разложено по уже заведённым задачам в разделе «Кандидаты после текущего блока»: [#259](https://github.com/anshushunov/dungeon-fortress/issues/259) → [#263](https://github.com/anshushunov/dungeon-fortress/issues/263) и [#260](https://github.com/anshushunov/dungeon-fortress/issues/260) → [#262](https://github.com/anshushunov/dungeon-fortress/issues/262). Остальные кандидаты — срез 3 «момент истины», занятость клетки, экономический долг — из списка **не убраны и не переупорядочены между собой**; они идут после направления. Сроков владелец не называл, и здесь их нет |
| 2026-08-06 | **Потолок боя** — владелец сыграл дуэльную сцену на `main` после merge PR #268 (коммит `8850209`) и следом усомнился в самом направлении: «может я неправильно расставляю приоритеты? бой все равно не получится в текущей архитектуре сделать красиво, может вернуться в 150% и сосредоточиться на симуляции? а идею с боем оставить для других игр» | **Бою ставится потолок: читаемый, а не красивый.** Направление «визуальная проработка боя» свёрнуто; следующий блок — **срез 3 «момент истины»** | — | Ответ владельца на playtest дословно: «ну работает как нужно но визуально прямо не очень. Нет плавности движения, очень быстые удары даже на скорости 0.5 (должны быть сильно медленее) не хватает кадров как будто, просто две позы. Сейчас прямо не нравится, нужно сделать чтобы прямо приятно было смотреть». **Источник обеих цитат — сообщения владельца координационной сессии 2026-08-06; переводы строк заменены точками, потому что ячейка таблицы их не несёт, а точная форма с разбивкой лежит в [#272](https://github.com/anshushunov/dungeon-fortress/issues/272).** Первая фраза закрывает [#259](https://github.com/anshushunov/dungeon-fortress/issues/259). **Посылка «в текущей архитектуре не сделать» проверена и оказалась неверной; решение принято при исправленной посылке.** Рендер уже отвязан от тика и интерполируется по alpha (Presentation pass A), события лежат в каноническом журнале, а узкое место — длительность: `TicksPerSecond = 6.0` (`src/DungeonFortress.Game/Main.cs:17`) даёт тик 167 мс и 333 мс на скорости 0.5, тогда как весь размен живёт **внутри одного тика** при индустриальной норме ощущаемого удара 500–800 мс. Дешёвый слой обратной связи уже снят [#210](https://github.com/anshushunov/dungeon-fortress/issues/210): `BlowEffects.cs` несёт hit-stop `HitStopShare = 0.35` тика, вспышку, восемь лучей искр и всплывающие числа урона. Лечение известно — **отставание представления** на тик-два (приём Source `cl_interp`, где клиент рисует на 100 мс в прошлом): оно затрагивает только слой представления и не меняет ни снапшот, ни checksum. Владелец его **отложил, а не отверг** — строка в «Кандидатах слоя отрисовки». Основание потолка — собственный питч: [`PITCH.md`](PITCH.md) §3 «бой — проверка подготовки, а не место, где игрок проводит время» и §7 «приоритет у работы, усиливающей столкновение плана и людей». Возврат к масштабу 150 % **отклонён**: он уменьшает изъяны, а не лечит их, а 170 % выбран playtest владельца на живой сцене ([#142](https://github.com/anshushunov/dungeon-fortress/issues/142)). Что вышло: [#263](https://github.com/anshushunov/dungeon-fortress/issues/263) сужена до замера и закрывается без правки; [#260](https://github.com/anshushunov/dungeon-fortress/issues/260) и [#262](https://github.com/anshushunov/dungeon-fortress/issues/262) переведены в кандидаты; следующий блок — срез 3 |
| 2026-08-07 | **Merge при сбое GitHub Actions** — с 2026-08-06 09:32 UTC в репозитории не запускался ни один workflow, `main` защищён обязательной проверкой `build-and-test` при `enforce_admins: true`, поэтому merge заблокирован технически, а не по правилам; попытка перевызвать событие (close/reopen PR #277) прогона не создала | Перед merge снимать `contexts` у `required_status_checks`, мержить при **зелёном `verify.ps1` и зелёном независимом review**, сразу возвращать защиту; прочие условия merge не ослаблены | — | Названная владельцем цена: локальный `verify.ps1` не покрывает ровно то же, что CI (в CI — restore с `--locked-mode` на чистой машине). За сессию так смержено семь PR: PR #277, PR #278, PR #288, PR #289, PR #290, PR #291, PR #292 (`gh pr list --state merged --search "merged:>=2026-08-06T09:32:00Z"` даёт ровно эти семь); каждое окно открывалось и закрывалось отдельно |
| 2026-08-07 | **Класс модели по расходу токенов** ([#283](https://github.com/anshushunov/dungeon-fortress/issues/283)) — замер 90,5 % вызовов на `claude-opus-5` (31 168 из 34 436), 8,8 % на `claude-sonnet-5`, 0,7 % на `claude-fable-5` | Класс модели по метке `tier:*`; младшая модель (Haiku 4.5) не используется — «слишком слабая, вместо неё Sonnet» | — | `tier:fast` и `tier:standard` → средняя (Sonnet 5), `tier:deep` и `tier:critical` → старшая (Opus 5), независимый review — не ниже класса исполнителя. Записано правилом 34 (PR #291, `9457d65`). Следствие: «дешёвая модель reviewer» правила 27 теперь означает среднюю |
| 2026-08-07 | **Поправка критерия готовности [#286](https://github.com/anshushunov/dungeon-fortress/issues/286)** (категория правила 24: риск безопасности) — абсолютная форма «ни одна запись allowlist не даёт права на запись, сеть или удаление» недостижима: независимый review PR #288 показал эмпирически, что `git diff`, `git log` и `git show` принимают `--output=<путь>` и создают файл по произвольному пути (`git diff --output=/tmp/z.txt HEAD~1 HEAD` → 503 767 B вне репозитория); тот же класс — `rg --pre`, `rg --hostname-bin`, `fd -x` | Критерий заменён на достижимый: запись не даёт таких прав при употреблении команды по назначению; остаточный класс назван строкой в [`DEBT_LEDGER.md`](../engineering/DEBT_LEDGER.md), deny-записи на известные флаги добавлены и помечены как неполные | — | Общая причина названа прямо: префиксный allowlist Bash в принципе не выражает «read-only» — наивное совпадение по началу команды без семантики флагов. Верное место защиты — `PreToolUse` hook, вне рамок Issue |
| 2026-08-07 | **Разовый третий круг review на PR #288** (категория правила 24: процессное, отступление от правила 12) — потолок один разбор плюс один подтверждающий; подтверждающий круг нашёл `rg --hostname-bin`, второй независимый флаг произвольного исполнения, не покрытый записью про `--pre` | Третий круг назначен разово и узко — две находки, ничего сверх | — | Находка ломала сам критерий готовности, а не лежала рядом, поэтому не могла уйти в реестр по правилу 12. Прецедент разового третьего круга в проекте есть (коммит `5b82944`) |
| 2026-08-07 | **Потолок длины прогона агента 300 k** ([#279](https://github.com/anshushunov/dungeon-fortress/issues/279), PR #293) — постановка содержала дефект: агент не наблюдает размер собственного контекста, поэтому правило «достигнув 300 k, завершись» неисполнимо | **Не принятое решение — решение, ждущее санкции владельца.** Координатор записал правило 35 в `DEVELOPMENT_PACE.md` (PR #293), приписав его правилу 24 категорией «процессное», но правило 24 прямо оговаривает: «новое решение» распадается по тем же трём категориям, а **его процессную часть держит мораторий (правило 11)**, снимаемый только явным решением владельца (`DEVELOPMENT_PACE.md:409-411`). Координатор превысил полномочия, опираясь на неполную цитату собственного же правила 24 в брифе | — | Ошибку нашёл независимый review PR #293. Вопрос вынесен владельцу точечно: **подтвердить правило 35 задним числом либо вернуть PR на пересмотр. PR #293 до ответа не мержится.** Предложенные правилом два наблюдаемых признака — авто-компакция как конец прогона и калиброванный потолок шагов — остаются предложением, не действующим правилом: исполнитель PR #293 показал, что признака компакции в 181 транскрипте проекта (65 973 записи) не нашлось ни разу — он наблюдаем агентом вживую, но непроверяем задним числом. **Это не единичный случай**: тот же дефект брифа — неполная цитата правила 24 — повторился в [#287](https://github.com/anshushunov/dungeon-fortress/issues/287), запись строкой ниже разбирает оба случая вместе |
| 2026-08-07 | **Обязательное чтение брифа** ([#282](https://github.com/anshushunov/dungeon-fortress/issues/282), категория правила 24: процессное) — типовой бриф требовал порядка десяти документов до первой строки работы | Обязательны только `AGENTS.md` (подтягивается клиентом) и `docs/engineering/AGENT_ENTRY.md`; остальное подтягивается по признаку задачи, за полноту отвечает бриф координатора | — | Основание — уже действующее правило 13 («бриф самодостаточен: неси выдержки, а не ссылки»). Цена названа прямо: ответственность за непрочитанное переносится с исполнителя на координатора; нарушение критерия «ни одна задача не провалилась из-за непрочитанного» — дефект брифа, а не исполнителя. **Проверено дважды тем же взглядом, что дважды вскрыл ошибку Р5, и мораторий здесь не задет**: партиция #282 — `AGENT_ENTRY.md`, `take-task.ps1`/тест, шаблон брифа `MULTI_AGENT_WORKFLOW.md`; ни один новый нумерованный пункт в `DEVELOPMENT_PACE.md` не заводится, задача только приводит фактический пакет чтения в соответствие с уже действующим правилом 13. Issue #282 после первого случая #279 получила от координатора собственный раздел «Проверка на мораторий правила 11» с этим же выводом (`gh issue view 282 --json body`); третьего случая в сессии не нашлось |
| 2026-08-07 | **Разрез `Main.cs` и `PrototypeWorld.cs`** ([#281](https://github.com/anshushunov/dungeon-fortress/issues/281), PR #292; категория правила 24: архитектурная развилка, решённая в пользу неархитектурного варианта) | Разрез идёт через `partial`, ADR не заводится — `Main` уже был объявлен `public partial class Main`, механизм в деле, разрез не вводит новой архитектуры | — | Выделение настоящих типов по ответственности — отдельное архитектурное решение, требует ADR и в задачу не входило. Цена признана прямо: связность классов разрез не уменьшает, уменьшает только стоимость чтения; исполнитель PR #292 измерил её числом — 72,1 % ссылок внутри класса `PrototypeWorld` пересекают границу файла |
| 2026-08-07 | **Поправка критерия готовности [#285](https://github.com/anshushunov/dungeon-fortress/issues/285)** (категория правила 24: процессное) — критерий «`-Stage scripts,build,tests` проходит без Godot» недостижим без ослабления изоляции NuGet, которое сама Issue запрещает: `build`, `tests`, `mcp` идут через `Initialize-SolutionBuild` → `Initialize-SolutionRestore`, `sim` и `load` — через `Initialize-ScenarioAssembly` к тому же restore решения, а NuGet-профиль пишется из бандла движка | Критерий заменён: свободна ровно одна стадия — `scripts` | — | Подтверждено дважды независимо — исполнителем эмпирически и review по коду. Реализация — PR #289 |
| 2026-08-07 | **Объём сессии 2026-08-07** — изначально владелец ограничил сессию пятью задачами | Владелец снял ограничение по ходу: «домержи таким же способом два зависших мра и добей в этой сессии все оптимизационные задачки» | — | В работу вошли [#275](https://github.com/anshushunov/dungeon-fortress/issues/275), [#276](https://github.com/anshushunov/dungeon-fortress/issues/276), [#279](https://github.com/anshushunov/dungeon-fortress/issues/279), [#280](https://github.com/anshushunov/dungeon-fortress/issues/280), [#281](https://github.com/anshushunov/dungeon-fortress/issues/281), [#282](https://github.com/anshushunov/dungeon-fortress/issues/282), [#283](https://github.com/anshushunov/dungeon-fortress/issues/283), [#284](https://github.com/anshushunov/dungeon-fortress/issues/284), [#285](https://github.com/anshushunov/dungeon-fortress/issues/285), [#286](https://github.com/anshushunov/dungeon-fortress/issues/286), [#287](https://github.com/anshushunov/dungeon-fortress/issues/287) |
| 2026-08-07 | **Координатор дважды заложил новое процессное правило в постановку задачи без санкции владельца по правилу 11** (категория: превышение полномочий, обнаруженное review) — [#279](https://github.com/anshushunov/dungeon-fortress/issues/279) (правило 35, потолок прогона, PR #293) и [#287](https://github.com/anshushunov/dungeon-fortress/issues/287) (уборка worktree, абзац `MULTI_AGENT_WORKFLOW.md`, PR #297). Оба раза обнаружил независимый review (PR #293, PR #297), не координатор | Второй случай уже закрыт без участия владельца: абзац правила в PR #297 изъят целиком, PR смержен (`d5c3cf5`) без него, а критерий готовности #287 «правило записано» снят координатором как дефект постановки — Issue закрыта. **К владельцу уходит один вопрос, а не два**: снимает ли он мораторий правила 11 для правила 35, или PR #293 возвращается на пересмотр | — | Причина одна для обоих случаев: цитата правила 24 в брифе координатора неполная, без оговорки `DEVELOPMENT_PACE.md:410-411` о том, что процессную часть нового решения держит мораторий 11 — сведение эскалации к трём категориям правила 24 мораторий не отменяет. Сверка нового пункта документа с правилом 11 текстовая, а не терминологическая, и поиском по ключевым словам не находится — названо отдельно в PR #293. Третьего случая за сессию не найдено: [#282](https://github.com/anshushunov/dungeon-fortress/issues/282) (запись выше, Р6) получила явную оговорку от координатора после первого случая и партиции `DEVELOPMENT_PACE.md` не касается |
| 2026-08-07 | **Мораторий правила 11 снят для правила 35** (потолок длины прогона агента) — вопрос, оставшийся от записи выше. Владельцу предъявлены: измерение ([`TOKEN_BUDGET.md`](../engineering/TOKEN_BUDGET.md) — 451 M cache read за 778 вызовов, 9 % расхода субагентов на одну задачу при медиане 18 M), содержание правила и **возражение координатора против собственной же калибровки**: предложенный потолок 90 вызовов отсекал бы половину успешных прогонов сессии (97, 122, 124, 127, 149, 157, 192, 212 tool use), тогда как измеренная проблема — единственный выброс, а не медиана | Владелец: «принимаю с потолком **250**». Правило 35 вступает в силу; величина потолка изменена с предложенных 90 на 250. Это **санкция задним числом**: правило было записано координатором до неё, и порядок был нарушен — см. запись выше. **Правило уборки worktree этой санкцией не покрыто**: абзац остаётся изъятым из репозитория, и его возврат требует отдельного решения | — | Пересмотр **величины** потолка после замера скриптом [#303](https://github.com/anshushunov/dungeon-fortress/issues/303) идёт к владельцу тем же способом, каким получено 250. Координатор записал было обратное — что величину вправе менять сам, раз она «откалибрована, а не принята разменом», — и независимый review это снял как **третье за сессию** повторение того же паттерна: формула давала 90–106, а 250 выбрано против формулы по качественному доводу, то есть в величине зашито суждение владельца, а не только аппроксимация. Отдельная находка процесса, названная в этом же обмене: **координатор структурно не может провести решение владельца в журнал через реле** — предохранитель харнесса распознал пересказ санкции как непроверяемое утверждение и заблокировал правку и агенту, и координатору; запись сделана только после прямой формулировки владельца. Ограничение уходит в [`DEBT_LEDGER.md`](../engineering/DEBT_LEDGER.md) |
