# Реестр долга

Статус: действует  
Дата обновления: 2026-08-02

## Что здесь лежит

Находки независимого review, у которых **нет наблюдаемого последствия** — ни для
игрока, ни для реально гоняемой проверки. Правило принято владельцем 2026-07-31 и
записано как правило 4 раздела
[«Темп разработки»](../product/ROADMAP.md#темп-разработки) в
[`ROADMAP.md`](../product/ROADMAP.md).

Находка с наблюдаемым последствием становится Issue и получает место в очереди.
Находка без последствия попадает сюда и слота в очереди не получает.

## Зачем реестр существует

У review-порождённых задач **нет продуктового сигнала приоритета**: их некому
ранжировать по ценности для игрока. Playtest-порождённая задача приходит с
ответом на вопрос «что мешало владельцу играть»; review-порождённая приходит с
ответом на вопрос «что неточно в коде», и обе попадают в одну очередь, где вторая
конкурирует с фичей на равных, не имея того, чем эта конкуренция решается.

Реестр разделяет два действия, которые раньше были одним: **зафиксировать
находку** и **дать ей слот в очереди**. Первое остаётся обязательным, второе
становится решением с названным условием.

Чего реестр **не** делает: он не заменяет review и не сокращает его. Он не
опирается на утверждение, что очередь инфраструктурных задач замкнута сама на
себя, — это утверждение проверялось по репозиторию 2026-07-31 и не
подтвердилось: обеспечивающие задачи порождает и playtest (#99, #86), и
исполнитель задачи (#42).

Это **не** список «починим потом». Список без условия выхода — это удалённая
находка с лишним шагом, поэтому у каждой записи обязательны и условие повышения,
и срок следующего перечитывания.

## Формат записи

| Дата | Откуда | Что | Почему без последствия | Что сделает её задачей |
|---|---|---|---|---|

- **Дата** — когда находка попала в реестр.
- **Откуда** — review какого PR её нашёл. Без источника запись нельзя перечитать.
- **Что** — сама находка, с файлом или местом, а не «неточность в снапшоте».
- **Почему без последствия** — объяснение, почему сегодня ни игрок, ни
  запускаемая проверка от неё не страдают. Это то, что перечитывание проверяет
  заново.
- **Что сделает её задачей** — наблюдаемое событие, при котором запись
  повышается до Issue. Запись без названного условия неотличима от забытой и в
  реестр не принимается.

## Кто решает, что последствия нет

**Исход по каждой находке предлагает сам review** — возврат исполнителю, Issue
или запись сюда. Координатор соглашается либо повышает находку до Issue; понизить
её до реестра против предложения review он не может.

Причина в том, кому достаётся выгода от ответа: «наблюдаемого последствия нет»
разблокирует merge, а сторона, чей merge разблокируется, не выдаёт этот ответ
себе сама. Повышение работает в одну сторону свободно, понижение — только по
предложению проверяющего.

## Когда запись повышается до Issue

Запись уходит из реестра в очередь по одному из трёх путей:

1. **Сработало названное условие** — произошло событие из колонки «Что сделает её
   задачей». Тогда координатор заводит Issue и удаляет запись отсюда, ссылаясь на
   неё в теле Issue.
2. **Находка получила наблюдаемое последствие иначе** — например, тот же дефект
   пришёл с playtest. Тогда приоритет задаёт playtest, а запись реестра
   приводится в Issue как уже известное описание.
3. **Владелец решил разобрать накопленное** при свободной очереди.

Обратного пути «поживём — увидим» нет: запись либо повышается, либо остаётся с
тем же условием, либо удаляется с записанной причиной удаления.

## Кто и когда перечитывает

- **Каждая координационная сессия** перечитывает реестр вместе с coordination
  inbox [#33](https://github.com/anshushunov/dungeon-fortress/issues/33) — это
  шаг разбора входящих в `.claude/commands/coord.md`. Результат называется
  владельцу одной строкой блок-отчёта: сколько записей, какие условия сработали,
  что повышено и что удалено. «Реестр пуст» — тоже результат и тоже называется.
- **Запись, пережившая три gate-решения** без изменения условия, разбирается
  явно: либо условие переформулировано, потому что оно ненаблюдаемо, либо запись
  удаляется с причиной. Условие, которое невозможно заметить, — не условие.

  **Единица — gate-решение, а не координационная сессия.** Прежняя редакция
  мерила возраст в сессиях и писалась, когда сессия была примерно днём; 2026-07-31
  и 2026-08-01 сессий было по три за день, и одиннадцать записей «состарились» за
  сутки, будучи по смыслу правила молодыми. Gate-решение — единица, которую нельзя
  нащёлкать: оно требует, чтобы владелец сыграл. Запись, пережившая три его захода
  в игру и ни разу не понадобившаяся, мертва по существу, а не по календарю.

  **Перечитывание счётчик не сбрасывает.** Реестр перечитывает каждая сессия, и
  если бы перечитывание обнуляло срок, ни одна запись не дожила бы до разбора
  никогда. Срок считается от последнего изменения **условия**; пометка о том, что
  запись перечитана и условие подтверждено, изменением условия не является.

  **Известная дыра, задача заведена.** Счётчику сегодня негде записаться: ни
  формат записи, ни таблица перечитываний не несут числа переживших gate-решений,
  и по дате запись против gate не упорядочивается — 2026-08-01 их было три за
  день. Кроме того, число переформулировок условия ничем не ограничено, а значит
  запись можно держать вечно, трогая её условие раз в три gate. Найдено
  независимым review PR #158, заведено
  [#159](https://github.com/anshushunov/dungeon-fortress/issues/159). Отличие от
  прежней поломки существенно: вечная жизнь записи теперь требует действия каждый
  цикл, а не наступает сама.

  **Переходная оговорка — отдельное решение владельца 2026-08-01, а не следствие
  выбора единицы.** Различие названо независимым review PR #158 и принято: выбор
  единицы решал, чем мерить возраст, а оговорка решает судьбу девяти записей,
  просроченных по букве нового правила уже в день его введения, и для них её
  эффект тождественен отклонённому третьему исходу. Поэтому она вынесена владельцу
  отдельно и получила его «да». Отсчёт для записей, существовавших на 2026-08-01,
  начинается с gate-решения `PROMOTE` по срезу 2. Буквальный пересчёт прежнего
  возраста в новой единице дал бы срабатывание в день введения правила: две
  старейшие записи приехали вместе с самим реестром в коммите `89ade73`
  (`git log --diff-filter=A -- docs/engineering/DEBT_LEDGER.md`), а он входил в
  дерево пакетного playtest с решением `CONTINUE` 2026-07-31 — значит они пережили
  **четыре** gate-решения, а не три: `CONTINUE`, два `ADJUST` и этот `PROMOTE`.
- **Каждый gameplay gate** — реестр просматривается на предмет записей, которые
  касаются той же области, что закрываемый слайс: дешевле исправить в уже
  поднятом контексте, чем отдельной задачей.

Реестр, который никто не открывает, эквивалентен удалённой находке, поэтому
правило перечитывания является частью правила, а не пожеланием.

## Записи

| Дата | Откуда | Что | Почему без последствия | Что сделает её задачей |
|---|---|---|---|---|
| 2026-07-31 | review PR #105 | Правило версионирования снапшота (`PROTOTYPE_HEADLESS.md`, «Что аддитивно, а что ломающе») перечисляет четыре рода ломающего изменения, а `PrototypeSnapshotShapeTests` закрывает **один** — удаление и переименование, потому что закреплён набор имён полей по секциям. Смена типа и смена правила сортировки сдвигают канонический JSON и потому checksum трёх golden UI-кадров, но сдвиг checksum сам по себе не отличает ломающее изменение от tuning'а и штатно снимается регенерацией эталонов. Род «поле сохранило имя, но стало отвечать на другой вопрос» не закрыт **вовсе** | Отказ закреплять типы и значения — осознанный и записан в XML-docstring теста (`PrototypeSnapshotShapeTests.cs:25-27`): shape, несущий типы, двигался бы вместе с состоянием прогона, а не вместе с решением. Ни игрок, ни запускаемая проверка сегодня не вводятся в заблуждение | **Условие переформулировано на третьем перечитывании 2026-08-01.** Было: «смена типа поля, порядка сортировки или смысла поля, прошедшая в `main` незамеченной» — заметить срабатывание такого условия невозможно по его же формулировке, а незамечаемое условие условием не является. Стало: **первая правка, меняющая тип поля снапшота, правило его сортировки или вопрос, на который поле отвечает** — она наблюдаема на review в момент, когда её делают, и именно тогда решается, нужен ли род проверки, которого нет |
| 2026-07-31 | review PR #105 | `includeEvents` в `PrototypeCanonical.WriteState` имеет единственный call-site со значением `true`; ветка `false` недостижима. Будь она живой, она дала бы второе условно присутствующее поле, которое shape-тест не сэмплирует | Мёртвый код, существовавший до PR #105; сегодня недостижим ни из одной точки входа | Появление второго call-site у `WriteState` |
| 2026-07-31 | review PR #121 | Вывод 9 калибровки (перенесён 2026-07-31 в `docs/engineering/archive/CALIBRATION_HISTORY.md` при сжатии калибровки, Issue #122) утверждает «ни один агент этого блока разведчика не поднимал», не называя источника. Для review PR #121 это сказано самим review; для трёх review блока 2026-07-31 — ничем | Доля 42 % считается от чисел таблицы и от наличия разведчиков не зависит: будь они, доля была бы **выше**, то есть вывод «review дороже 11 %» только усилился бы. Утверждение избыточно для собственного вывода | Появление строки калибровки с вложенным субагентом и попытка сравнить её со строками этого блока. **Проверено третьим перечитыванием 2026-08-01: условие наблюдаемо и не сработало** — все пять агентов сессии назвали отсутствие вложенных агентов явно. Запись ошибочно удалялась этой же сессией по ложной посылке «условие про оценки по выводу»; восстановлена по находке независимого review PR #155 |
| 2026-07-31 | review PR #121, круг 2 | Вывод 11 калибровки (перенесён 2026-07-31 в `docs/engineering/archive/CALIBRATION_HISTORY.md` при сжатии калибровки, Issue #122) стоит на одном измерении одной задачи: 242k против оценки 350–550k. Механизм («возобновление не оплачивает чтение второй раз») в тексте помечен гипотезой, но само наблюдение остаётся единичным, и «оборванный прогон стоит почти ничего» — обобщение с одного случая | Практическое следствие вывода — «надбавку за обрыв в оценку не закладывают» — держится на самом числе, а не на механизме и не на объёме выборки. Ошибка в сторону завышения оценки ничего не ломает: ни игрок, ни запускаемая проверка от неё не страдают | Оборванный прогон, стоивший заметно дороже, — или оценка, построенная по формулировке «стоит почти ничего» и промахнувшаяся в занижение. **Проверено третьим перечитыванием 2026-08-01: первая половина условия от выводов калибровки не зависит вовсе и наблюдаема.** Запись ошибочно удалялась этой же сессией; восстановлена по находке независимого review PR #155 |
| 2026-07-31 | review PR #123 | Правило 7 (`ROADMAP.md`) называет одно из двух ослаблений [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md) §5/§7 — «класс задачи перестаёт быть условием» — но не второе: что «всё остальное мержится только по `MERGE #N`» перестаёт действовать там, где решение уже принято владельцем | Второе ослабление выводимо из самого текста правила 7 — «PR, только исполняющий уже принятое владельцем решение, мержится без второй просьбы... независимо от класса задачи» — оно описано практически, просто не поименовано отдельно. Ни один PR не мержится иначе, чем предписывает эта формулировка | Спор о том, требует ли конкретный PR команды `MERGE #N`, который решается по-разному в зависимости от того, названо ли второе ослабление явно |
| 2026-07-31 | review PR #123 | Из раздела «Темп разработки» (`ROADMAP.md`) пропала фраза «условия отката, названные при каждом правиле, остаются рабочими условиями координатора и не являются отдельными решениями владельца» | Каждое условие отката сформулировано в тексте своего правила как проверяемый факт («если... → правило возвращается/отменяется, запись в журнал»), а не как открытый вопрос: координатор и без общей оговорки применяет его сам и пишет строку в журнал | Координатор откатывает правило по названному условию, и владелец оспаривает это как самовольное решение — тогда общая оговорка нужна явно, в тексте раздела |
| 2026-07-31 | review PR #124 | Мёртвый рукав `refused_injured` в `EventNarration.Sentence` (`EventNarration.cs`): предложение написано для reason code, который симуляция не эмитит ни в одном прогоне матрицы. `Every_reason_code_the_matrix_produces_has_a_sentence` его не покрывает — он проверяет обратное включение | Лишнее предложение не может быть показано: показать его может только событие, которого нет. Отсутствие рукава было бы хуже — падение вместо строки, — а его наличие не стоит ничего | Появление `refused_injured` в каноническом журнале любого сценария: тогда рукав становится живым и обязан попасть под guard полноты |
| 2026-08-01 | review ветки `agent/127-tooltip-readability` (Issue #127) | Задача писала в `src/DungeonFortress.Game/Main.cs` **вне своего именованного региона** — разбор CLI-аргументов, поля класса, `_Ready`, `LayoutHud`, `InjectHudTooltipReadabilityRegression`. Это члены владельца остатка по [ADR 0017](../decisions/0017-raise-parallel-writing-ceiling.md) §3, а порядок merge и rebase координатором для такого пересечения в брифе не назначались. Расширение оправдано по существу — CLI-флаг обязан разбираться там, где разбираются флаги, а проброс масштаба в `LayoutHud` есть сама суть правки, — и исполнитель назвал его сам | Названный владелец остатка `agent/52-room-readability` не трогает `Main.cs` вовсе; `git merge-tree --write-tree` сливает ветку #127 и с `agent/52-room-readability`, и с `agent/128-125-creature-decision-history` без конфликта — проверено review собственным прогоном | Появление второй одновременной задачи, реально пишущей в `Main.cs`, либо конфликт rebase на этом файле: тогда ADR 0017 §3 придётся применять с назначенным порядком merge |
| 2026-08-01 | review `agent/127-tooltip-readability`, подтверждающий круг (Issue #127) | `HudButton.BuildTooltip` можно заставить игнорировать свой параметр `scale` — `_ = scale;` плюс `ScaledSize(…, 1.0)` во всех трёх местах внутри метода: `dotnet test` остаётся **601/601**, headless-smoke Godot отдаёт exit 0, а дефект #127 возвращается целиком. Два теста, добавленные кругом доработки, доказывают, что оба **места вызова** передают живой масштаб; что параметр **потребляется** внутри, не доказывает ничто | Сегодня код верен, и оба отката, названные первым кругом review, закрыты проверками; мутант гипотетический. Ни игрок, ни запускаемая проверка сегодня не страдают | Любая правка `BuildTooltip`, появление третьего вызывающего либо переход на настоящий замер попапа — тогда цепочку текстовых утверждений можно заменить одним поведенческим |
| 2026-08-01 | review `agent/127-tooltip-readability`, подтверждающий круг (Issue #127) | `scripts/verify.ps1` присваивает `$hudTooltipReadabilityFailure` и больше нигде на неё не ссылается, тогда как каждый соседний негативный прогон кладёт свой `ExitCode` в `verification_result` (`hudReadabilityRegressionExitCode`, `hudGuardRegressionExitCode`, `cameraTransformRegressionExitCode`). В машинном итоге полного прогона поля про подсказку нет — форма та же, что у [#49](https://github.com/anshushunov/dungeon-fortress/issues/49): значение получено и выброшено | Сам gate работает: `Invoke-GodotExpectedFailure` бросает, если прогон перестал падать, и событие `godot_expected_failure_guard` присутствует в потоке — проверено review собственным прогоном стадии `godot` | Появление автоматического разбора `verification_result` — CI-бот, дашборд, координаторская сводка, — которому нужно подтверждение по полям, а не по событиям потока |
| 2026-08-01 | review `agent/52-room-readability` (Issue #52) | В контракте §12.1/§4.4 поле `Room.perimeter` не описано по составу: оно несёт **все покрашенные клетки участка** (42 у `farm@1,1`), а не обводку, и расходится с обиходным значением слова. Единственное место, где состав назван, — docstring `PrototypeTypes.cs`. Источник формулировки — сам [ADR 0013](../decisions/0013-what-is-a-room.md) («периметр покрашенных клеток»), реализация ему соответствует | Ни один потребитель не читает контракт как спецификацию для кода — снапшот нигде не десериализуется; обводку строит `RoomGeometry` из того же списка клеток и покрыта шестью мутациями. Ни игрок, ни запускаемая проверка не страдают | Появление потребителя, читающего снапшот по контракту (сохранение партии, внешний инструмент, JSON Schema), либо первая правка, где `perimeter` поняли как границу |
| 2026-08-01 | review `agent/52-room-readability` (Issue #52) | Пара чисел «16 строк нужно / 15 помещается на 2048×1440 при UI scale 2» в docstring `InspectorText.DescribeRooms` и в коммите `038b884` не имеет ни названной команды, ни единственного места записи. Производитель у них есть — сообщение HUD-guard'а `needs {n} lines but only {m} fit`, — но **измерение больше не воспроизводится**: правка убрала переполнение, и guard молчит | Число обосновывает уже принятое решение, ничего не настраивает и никуда не читается; сам guard остаётся исполняемой проверкой и отвергает переполнение независимо от того, записано число или нет | Попытка сослаться на этот бюджет при следующей правке инспектора — то есть момент, когда невоспроизводимое число начнёт обосновывать новое решение |
| 2026-08-01 | review `agent/52-room-readability` (Issue #52) | `evidence/52-mutations.json` и коммит `73a1ac0` заявляют «31 мутация, по одной на половину», но проход не касается `RoomReadings.cs` (259 новых строк) и `InspectorText.DescribeRooms`, где покраснимые половины есть — каждая ветвь `RoomLabels.Caption`, свёртка `IsInZone` в `RoomObjects.Consider` | Непокрытый мутациями код покрыт обычными тестами по каждой ветви (`RoomReadingTests`, 340 строк, включая пустую ветвь `DescribeRooms` и прогон более тысячи проверок принадлежности); дыры вида «тест зелен по неверной причине» review не обнаружил. Расхождение — в широте заявления, а не в качестве проверки | Первая правка `RoomLabels`/`RoomObjects`, прошедшая с зелёными тестами и сломавшая читаемость, — либо решение сделать мутационный проход обязательным для слоя представления целиком |
| 2026-08-01 | review `agent/128-125-creature-decision-history` (#128/#125) | В `evidence/125-false-refusals.json` стадия «две половины» (159 отказов / 87 ложных) — единственная из трёх без разбивки по партиям и без поля `takenOn`. Число «20 из 20 на `prepared/20260726`», которым обосновано третье правило, записано только для стадии «before» | Третье правило несут красный мутант 4 и величины 80/11, воспроизведённые review собственным прогоном; 159/87 — подтверждающее число, а не опора | Понадобится оспорить или перемерить третье правило, и стадию двух половин нельзя будет восстановить |
| 2026-08-01 | там же | Контракт 15.5 дописал в порядок условий пары оговорку «счёт не ниже `T.score_floor`», но мутант 5 остаётся зелёным — матрица эту оговорку не упражняет | Измерено: под мутацией счётчики отказов идентичны (80/69/2/9/11), ни одна пара с попаданием памяти не отсекается полом счёта | Появится сценарий, где единственная работа существа у запомненного места набирает счёт ниже пола |
| 2026-08-01 | там же | Раннеры мутационных прогонов (`.artifacts/mutate.sh`, `mN.py`, `mutate-pres.sh`, `pN.py`) не отгружены — `.artifacts` в `.gitignore`, поэтому перегнать мутации не может ни исполнитель, ни проверяющий | Результаты записаны поимённо в `evidence/125-mutations.json` и `evidence/128-history.json`; практика уже принята на `evidence/117-mutations.json` | Мутационный результат будет оспорен либо потребуется перегнать его на другом дереве |
| 2026-08-01 | там же | `MemoryFreeWinnerOfRefusedTile` берёт `free.Values.FirstOrDefault(… InitialTarget …)`, а исключительность тайла в `ResolveMatching` действует только для тайлов кладовой: два не-кладовых назначения могут делить тайл, и ответ зависит от порядка обхода `Dictionary` | Поле диагностическое, в канонический снапшот не входит; единственное утверждение о нём (`winner is not null`) от порядка обхода не зависит | Появится проверка или evidence, читающая конкретное значение этого поля |
| 2026-08-01 | там же | Контрфактический проб гоняется на 6 партиях (`baseline` и `prepared` × 3 seed), тогда как `MatrixScenarios` объявляет 9 | `neglected` до волны не доживает и отказов по памяти не даёт вовсе — проверять там нечего | `neglected` начнёт доживать до волны либо в матрицу войдёт сценарий, дающий отказы вне `baseline`/`prepared` |
| 2026-08-01 | там же | `CreatureStoryTests.Reading_a_creature_s_story_needs_no_tick_to_run` сравнивает `HudText.Feedback` с `HudText.CreatureStory` — то есть проекцию с собой; «прогон тика не нужен» доказано комментарием, а не утверждением | Границу реально держит `The_story_a_creature_shows_is_the_journal_ranked_by_what_it_meant` (до #140 — `…is_its_own_tail_of_the_canonical_journal`), независимо пересчитывающий выборку из `state.Events` | Первый тест ослабнет или будет удалён, оставив границу «проекция против мира» без утверждения |
| 2026-08-01 | там же | `CreatureStoryPanels()` строит «дополненный худший случай» из самой широкой строки **текущей** сессии на **текущем** тике, поэтому на smoke-тике 1 guard меряет пол бюджета, а не потолок | Настоящий потолок проверен ручным прогоном с `CreatureStoryLines = 6`, поимённо отвергшим две истории; #140 повторил это на пяти строках — `verify.ps1 -Stage godot` краснеет на дополненном худшем случае (`evidence/140-mutations.json`), — и отдельно измерил настоящую позднюю панель: 5 строк текста → 6 отрисованных при 6 помещающихся на 1280×720 (`evidence/140-after.json`). На входе `capture-evidence` тик поздний и строка настоящая | Smoke станет единственным входом guard'а либо ручной прогон с лишней строкой перестанет воспроизводиться |
| 2026-08-01 | review PR #124, подтверждающий круг | У переизданных «воспроизводимых» кадров #83 (`PROTOTYPE_GRAYBOX.md`, раздел про объём стены) проверена только половина утверждения. Что `(10,5)` и `(13,5)` — камень, а `(10,4)` и `(13,6)` — пол, следует из `PrototypeLayout.Rows` и падает вместе с раскладкой. Что существо #2 стоит на `(10,4)` на тике 40, а существо #0 на `(13,6)` на тике 80, не проверяется ничем: кадры помечены ignored, `verify.ps1` их не снимает | Кадры справочные: ни одна запускаемая проверка от них не зависит, и игрок их не видит. Позиции выведены прогоном шипованного журнала `baseline`, а не памятью, поэтому сегодня они верны — не покрыто именно их **сохранение** | Кадр снят по команде из документа, и существо оказалось не на названном тайле |

| 2026-08-01 | review `agent/142-scale-spike` (Issue #142) | Ветка вводит каталог верхнего уровня `spikes/`, и ни один документ не говорит, что это такое: для чего каталог, чистятся ли записи, освобождены ли они от `verify.ps1`. На момент находки `rg -n "spikes/" docs/ README.md` не давал ничего; сейчас команда возвращает саму эту запись, поэтому проверять надо наличие **описания** каталога, а не упоминания | Спайк ровно один; он озаглавлен «NOT production» в `project.godot` и в обоих `.gd`, не содержит `.csproj`, отсутствует в `DungeonFortress.sln` (8 проектов, все под `src/`, `tests/`, `tools/`) и не упомянут в `verify.ps1`. Собраться или проверяться против него молча ничто не может | Появление второго спайка — либо первый вопрос «этот спайк живой, протухший или обязан оставаться зелёным» |

| 2026-08-01, текст переписан после merge #147 | review `agent/139-room-border-facade` (Issue #139), перемерено review `agent/147-room-border-vs-wall-edge` | Свойство #52 «две перекрывающиеся комнаты дают две линии» вырождается в смешанном случае: пара «комната у стены» и «комната не у стены» может получить одинаковый отступ и слиться в одну линию. Прежний текст записи описывал две базы (`2,0 + шаг` и `5,0 + шаг`) и предикат `BordersWallToNorth`, которых после #147 не существует: баз стало три (2,0 без стен, 2,625 при породе в соседней колонке, 5,625 при породе прямо к северу), а решение принимает `RoomGeometry.BorderInsetFor` по сторонам, упирающимся в породу. Счёт вырождения review перемерил **своим** проходом по кросс-произведению профилей и назначений, не тестом задачи: до #147 — 6 упорядоченных пар на ступени 5,0 (`2,0 + 3,0 == 5,0 + 0`), после — 6 пар на 5,625 (`2,625 + 3,0 == 5,625 + 0`) | Пара, названная прежней редакцией записи, **исчезла**: комнату без стен больше нельзя спутать с комнатой под стеной. Оставшееся вырождение — совпадение свеса фасада (`FacadeReferenceOverhang`, #83) с самым широким шагом лестницы назначений (#52); обе константы старше #147 и им не вводились. Стартовая карта рисует четыре непересекающиеся комнаты, перекрывающихся пар не существует вовсе | Кадр playtest или снятый кадр, на котором две перекрывающиеся комнаты, из которых у стены только одна, показывают одну линию вместо двух |

| 2026-08-01 | review `agent/142-scale-spike`, подтверждающий круг (Issue #142) | Поле `parentCommit` в `evidence/142-scale-spike.json` указывает на коммит `bb64cf2`, то есть на дерево **до** правки, где `WORLD_MAGNIFICATION` ещё 1.5 и захват даёт другие байты, чем перечисленные в том же файле SHA-256. Соседнее поле `parentCommitNote` называет верное дерево словами, но имя поля уводит механического читателя не туда. Причина — курица и яйцо: файл не может содержать хеш собственного коммита; штатный `capture-evidence.ps1` обходит это тем, что снимает bundle **после** коммита и пишет `commit` плюс `sourceDirty` | Примечание рядом с полем прямо называет верное дерево, а все три SHA-256 воспроизведены review побайтно с HEAD: читатель, читающий файл целиком, получает правильные кадры | Второй манифест, написанный по этому образцу, либо первый потребитель, разбирающий `parentCommit` программно вместо чтения примечания |

| 2026-08-01 | там же | Дополненный худший случай бюджета панели истории стоит **ровно на потолке**: из воспроизведённого мутанта видно, что при 5 строках нужно 12 при 10 помещающихся на 2048×1440 UI 2, значит при штатных 4 нужно ровно 10 при 10 помещающихся — запас нулевой. Любое изменение формулировок в `EventNarration`, удлиняющее самую широкую фразу на один перенос, покраснит `verify.ps1 -Stage godot` в задаче, которая `HudText.cs` не трогала | Полный `verify.ps1` зелёный, запас неотрицательный. Ни игрок, ни запускаемая проверка сегодня не страдают | Красная стадия `godot` в задаче, не трогавшей `HudText.cs`, либо появление третьего вызывающего у формулировок панели |

| 2026-08-01 | review `agent/139-room-border-facade`, подтверждающий круг (Issue #139) | У комнаты **высотой в одну клетку**, примыкающей к стене с севера, новое положение подписи создаёт вторую коллизию: нижняя граница комнаты режет иконку, а базовая линия подписи оказывается **ниже собственной обводки комнаты** — подпись рисуется снаружи своего контура. **Числа перемерены после #147 и выросли, охват расширился на все назначения** — прежняя редакция записи утверждала обратное вслед за телом PR #151, и это утверждение review опроверг собственным счётом. Переполнение подписи за собственную нижнюю обводку, референсные px, до #147 → после #147: Larder и Watch 5,0 → **6,25**; Kitchen, TrainingGround, MaterialStockpile 2,0 → **3,25**; Farm, Quarters, Forbidden −1,0, то есть конфликта не было вовсе, → **+0,25, конфликт появился**. Числа «до» воспроизводят прежнюю редакцию дословно, чем метод и подтверждён. Итог: #147 углубила находку на 1,25 реф. px и расширила её с пяти назначений из восьми на все восемь | На стартовой карте однотайловых комнат нет: farm 7 клеток высотой, quarters 4, kitchen и larder по 3. `The_border_of_every_room_clears_its_own_caption_and_icon` гоняется по этой же карте и потому случай не видит — но и игрок его сегодня увидеть не может | Первая покрашенная игроком однотайловая зона под стеной — либо кадр, на котором подпись комнаты оказалась снаружи её обводки |


| 2026-08-01 | review `agent/147-room-border-vs-wall-edge` (Issue #147) | `RoomWallClearanceTests.Measure` исключает стену, стоящую прямо к югу от **любой** клетки комнаты, из проверки **всех** её граней, тогда как объявленное исключение — «стена прямо к югу от этой клетки». На стартовой карте это прячет три настоящих пересечения южной грани с вертикальным швом диагонально-южной стены: quarters (21,5)×(20,6) −0,625 реф. px, kitchen (10,8)×(9,9) −0,625, larder (15,8)×(14,9) −0,375. Измерено review собственным прогоном с суженным исключением, не тестом задачи | Все три пересечения необходимы по существу самого исключения: поднятый верх стены заходит в клетку на 8,0 реф. px, и обойти его нельзя ни одним отступом. Северный случай прикрыт отдельно — неисключающим прогоном внутри `A_wall_in_front_of_a_room_cannot_be_cleared_by_any_inset` | Форма комнаты, у которой стена одновременно к югу от одной клетки и к востоку или западу от другой, с **обходимым** пересечением, — либо перевод исключения на уровень клетки |
| 2026-08-01 | там же | Потолок лестницы отступов заменён с фактических 8,0 (`32 / 4.0`, единица которых была неверна) на `MaximumBorderInset = 10,0`, выведенный геометрически и корректно. Но между глубочайшей ступенью 8,625 и потолком 10,0 больше нет проверки, которая отвергла бы углубление; прежний потолок такое углубление отвергал. Проверка «подпись помещается в клетку» даёт границу ещё слабее — 18,625 из 22,0 | Обе базы отступа — `const`-выражения, выведенные из `WallRenderGeometry` и `BorderStrokeHalfWidth`, и каждый их вход покрыт мутантом (a-i, a-ii, d-i, d-ii плюс собственный мутант review `WallVisibleGap → 0`). Углубить лестницу «от руки», не тронув одну из этих констант, технически нельзя | Первая база отступа, записанная числом, а не выведенная из геометрии стены; либо ступень глубже 8,625, прошедшая с зелёными тестами |
| 2026-08-01 | review `agent/147-room-border-vs-wall-edge`, ретроспективно к Issue #139 | Критерий готовности #139 был закрыт не полностью: `The_border_of_every_room_clears_a_neighbouring_walls_facade` меряет против **прямоугольника** фасада и не видит шва `FacadeBottom`, который рисуется по его нижней кромке и уходит полосой на 0,625 реф. px ниже. У `farm@1,1` после merge #139 оставалось 0,375 реф. px (0,68 экранных при tileSize 40, 0,55 при 32) — ровно та теснота `quarters`, ради которой заведён #147. Evidence и тело PR #148 описывают этот замер как «граница чиста от фасада» | Геометрическое последствие снято merge #147: farm North теперь 1,0 реф. px, и общий проход `RoomWallClearanceTests` меряет полосы, а не прямоугольники. Узкий тест #139 сохранён рядом как исторический и вреда не наносит | Ссылка на evidence #139 как на доказательство чистоты границы в новой задаче — либо решение свернуть узкий тест #139 в общий проход |
| 2026-08-01 | review PR #152 (Issue #145) | У ленты владения нет «дополненного худшего случая»: `Main.CreatureStoryPanels()` строит его для панели истории существа, а лента меряется только как **живой** label `feedback` на тике входа. При нулевом запасе её потолок утверждается двумя кадрами, которые конвейер снимает попутно, — `screenshots`/prepared t1670 и внешний `capture-evidence` на baseline t2400. Docstring `DomainFeedLines` подаёт это как достоинство, тогда как живой label на входном тике есть пол проверки, а не потолок — тот же дефект, что записан 2026-08-01 про `CreatureStoryPanels()`, только зеркальный | Ни один прогон сегодня не переполняется: сверх авторских кадров review снял ещё девять (baseline 1400/1800/2200/2400, prepared 1670/2000/2400, neglected 1400/2400), каждый меряет все восемь кадров HUD-guard'а, включая 2048×1440 при UI scale 2 — все зелёные. Отказ при переполнении громкий, а не потеря строки молча. Дополнено подтверждающим кругом: у ленты **три** места замера, а не два — всю-рутинную ленту на её ширине меряет ещё и стадия `ui`, снимающая эталонные кадры t190/t336/t950 через движок, и её не называет ни контракт, ни docstring `DomainFeedLines` | Партия, в которой строка ленты длиннее снятых — больше повторов в `(xN)` либо более длинное предложение отказа, — либо `-Stage screenshots` перестанет снимать prepared/t1670 |
| 2026-08-01 | review PR #152 (Issue #145), подтверждающий круг | `evidence/145-bound.json → answerFromTheFrame.whatActuallyBinds` несёт измерение «заголовок сокращали до 39 и 28 символов, панели каждый раз нужны те же 11 строк» без команды и без единственного места записи: разовый ручной опыт, который правка ленты сделала невоспроизводимым. Остальные три пункта той же находки первого круга закрыты кругом доработки — команда заменена на воспроизводимый прогон, «50 тактов» и «11 видов» удалены, а не подновлены | Число обосновывает уже принятое решение — связывают предложения, а не заголовок — и никуда не читается; сам вывод перепроверен другим путём, замером стадий, у которого команда есть | Ссылка на этот замер заголовка при следующей правке ленты или шапки — момент, когда невоспроизводимое число начнёт обосновывать новое решение |
| 2026-08-01 | там же | `docs/engineering/PROTOTYPE_GRAYBOX.md` объясняет, почему метки памяти живут в инспекторе, словами «потому что **лента прокручивается**». После #145 лента — дайджест, а не тикер: её верх может стоять сотни тиков. Исполнитель называет это в рисках PR, но GRAYBOX не обновлён | Обоснование остаётся верным по сути — вопрос «почему это существо стоит» задают сильно позже события, — ни одна запускаемая проверка от фразы не зависит, и игрок её не видит | Следующая правка панели памяти или ленты сошлётся на «лента прокручивается» как на действующее свойство |
| 2026-08-01 | review PR #166 (Issue #156), подтверждающий круг | Соседние комнаты в одном кадре по-разному толсты без объяснения в мире: южная кромка larder на клетках 13,8 и 14,8 идёт полутолщиной, ~4,4 экранных px против ~8,7 при tile 48 и zoom 2. Рядом соседняя kitchen над своей стеной сохраняет полную толщину, потому что её отступ мельче, — две соседние комнаты в одном кадре ведут себя по-разному, и объяснения в мире для этого нет | **Полутолщина сама по себе игроку видна — в реестр кладётся не она, а необъяснённость разницы между комнатами.** Разница читается как перекрытие стеной, а не как дефект: ступенька совпадает с концом стены, линия не прерывается, и ни один вопрос игрока она сегодня не порождает. Цена названа в теле PR, в `PROTOTYPE_GRAYBOX.md` и пришпилена именем в `The_shipped_map_pays_for_the_exception_in_two_cells_of_the_larder` | Кадр playtest, на котором владелец назовёт линию оборванной, — либо вторая комната с той же глубиной лестницы отступов |
| 2026-08-01 | там же | `evidence/156-mutations.json` держит `imageSha256` кадра `156-kitchen-larder-bottom-row` от первого круга, а голова ветки по той же записанной команде даёт другой хеш; соседнее поле `note` честно объясняет, что это снимок первого круга. Отдельно: угловые кадры второго круга записаны **без единого SHA-256** и без `byteForByteRepeat` — взят `run-game.ps1` на detached HEAD вместо `capture-evidence.ps1`, причина названа | Оба хеша измерены review, расхождение объяснено в самом файле, а решающий факт установлен независимо: кадр угла на голове ветки побайтово совпал с кадром на `main`. Тот же класс, что уже лежит в реестре про `parentCommit` в `evidence/142-scale-spike.json` | Первая ссылка на `156-mutations.json` как на источник хеша кадра |
| 2026-08-01 | там же | `PROTOTYPE_GRAYBOX.md` и docstring `Main.DrawRoomBordersOverWalls` описывают часть боковой кромки, которая «за стеной спереди, выше хвоста, замыкающего угол, больше не рисуется по фасаду». Такой части не существует: полоса вертикального штриха стоит на `inset ± 1,0` реф. px от границы клетки, `inset ≥ 2,625`, поэтому по x она всегда строго внутри клетки, а верх стены и её светлый шов идут во всю ширину — любая точка вертикали внутри краски стены покрыта целиком и уходит наверх | Утверждение пустое, а не ложное по последствию: описанного остатка нет, поэтому и поведения, которое оно описывает, нет. Подтверждено побайтовым совпадением кадра угла с `main` | Первая ссылка на эту фразу как на описанное поведение — например, при попытке объяснить ею новый кадр |
| 2026-08-01 | там же | Ориентация сегмента границы выводится дважды разными выражениями: `StrokeBand` берёт её из экстентов сегмента (`right-left >= bottom-top`), `Split` — из экстентов полосы (`stroke.Width >= stroke.Height`). Совпадают, только пока длина сегмента не меньше ширины штриха | Самый тесный запас сегодня — larder при tile 32: 6,9 мировых px против 2,9. Выражения сходятся впритык ровно на `MaximumBorderInset` (отступ 10,0 → длина сегмента 2,0 реф. px = ширине штриха) и расходятся за ним, а потолок держит `RoomWallClearanceTests` | Первый отступ, поднятый выше 8,625 |
| 2026-08-01 | там же | `HudTextTests.The_event_panel_is_a_header_three_lines_and_the_diagnostics_count` заменил независимое утверждение «первая строка — самая свежая запись журнала» на пересказ кода: `lines[1]` сверяется с `HudText.DomainSelection(...)[0]`. Там же `CreatureStoryTests.Nothing_selected_…` ослабил `Equal("EVENT FEEDBACK")` до `StartsWith("EVENT FEEDBACK")` без разделителя | Оба свойства держатся утверждениями в другом месте: `DomainFeedTests.The_feed_a_domain_shows_is_the_journal_ranked_by_what_it_meant` независимо пересчитывает выборку из `state.Events` на 3 seed × 2 фикстуры × 4 тика, а счётчики шапки посимвольно сверяет `The_feed_is_bounded_and_the_header_says_what_is_off_the_panel` | Один из этих двух тестов `DomainFeedTests` ослабнет или будет удалён — форма панели останется без независимого утверждения |
| 2026-08-02 | review PR #175 (Issue #165) | Перегенерация позы `flinch` edit-режимом сдвинула её палитру относительно остальных пяти поз пака v2. **Числа сняты независимым review PR #175 и здесь не перепроверялись** — воспроизвести их можно только тем же проходом по PNG. Доминирующий средний тон кожи у пяти поз и у прежнего `flinch` — квантованный `(144,144,48)`; у нового `flinch` он выпал из четвёрки самых частых, доминирует `(160,144,48)`. Блик кожи: пак `(194–198, 185–188, 75–78)`, новый `flinch` `(204,193,93)` — синий канал выше потолка пака на 15. Доля тёмного (`max(RGB) < 40`): пак 24,2–28,3 %, новый `flinch` 23,0 % | Новых цветов не введено: сместилось распределение по уже существующим тонам, то есть освещённость, а не палитра. На контактном листе при высоте холста 62 px review разницу не различает. Рантайм грузит v1 — `Main.cs:2107` собирает путь `goblin_{state}_v1.png`, подключение пака v2 входит в [#77](https://github.com/anshushunov/dungeon-fortress/issues/77). Ни одна запускаемая проверка на палитру не смотрит | Кадр playtest или контактный лист, на котором владелец назовёт `flinch` светлее остальных поз, — либо вторая перегенерация любой позы этого пака edit-режимом: тогда у дрейфа появляется вторая точка и он становится свойством режима, а не одного файла |
| 2026-08-02 | review PR #178 (Issue #171) | `MemoryYieldsSatiety = 30` (`PrototypeTuning.cs:255`) и `EatThreshold = 30` (там же, `:299`) — два независимых литерала. Всё обоснование значения держится на их равенстве, и утверждается это равенство только прозой: комментарий над самой константой («Thirty is EatThreshold…», `PrototypeTuning.cs:242`), docstring `PrototypeWorld.AvoidedPlace`, контракт, тело PR и `evidence/171-after.json`. Исполняемого утверждения нет: `rg -n 'MemoryYieldsSatiety' -g '*.cs'` даёт 7 строк — объявление и шесть ссылок (три сравнения сытости существа в коде, две docstring-ссылки, одно сообщение теста), ни одна не упоминает `EatThreshold`; обратная команда `rg -n 'EatThreshold' -g '*.cs'` даёт 6 строк, и ни одна не упоминает `MemoryYieldsSatiety`. **Асимметрия называется сама собой:** вторую половину обоснования автор закрепил тестом — `A_memory_stops_refusing_work_before_the_next_wave_arrives` начинается с `Assert.True(MemoryAvoidTicks < WaveIntervalTicks)` (`PrototypeMemoryCostTests.cs:196`) | Сегодня обе величины равны 30, проверки зелёные, поведение верное. Прецедент в проекте свой: запись про молчаливую копию `BorderStrokeHalfWidth` закрыта не задачей, а тем, что константу сделали публичной и стали на неё ссылаться (протокол перечитывания 2026-08-01 ниже) | Первая правка `EatThreshold` или `MemoryYieldsSatiety`, после которой они разойдутся |
| 2026-08-02 | там же | Две фикстуры, которые правка #171 сдвинула, лежат вне её собственных границ. `A_party_that_wins_its_fights_does_not_end_it_starving` и `No_creature_is_taken_out_of_the_domain_by_what_it_remembers` гоняются по `SixParties()` — `baseline` и `prepared` × 3 seed (`PrototypeMemoryCostTests.cs:38`, `:307`); `prepared-ration-zero` и `prepared-watch-zero`, это шесть прогонов, не проверяются ни на исход, ни на длину серии отказов, хотя `prepared-ration-zero/20260728` от правки изменилась — 902 → 947, измерено независимым review PR #178. Исключение `neglected` законно и названо в docstring `SixParties()`: партия не доживает до первой волны и памятей не пишет | Все 15 прогонов матрицы 13.4 гоняются `--evaluate-prototype`, инварианты зелёные, и единственная сдвинувшаяся ячейка вне выборки улучшилась | Первый прогон `prepared-ration-zero` или `prepared-watch-zero`, приходящий `fallen` при выигранном бое, при зелёном `PrototypeMemoryCostTests` — он виден в батч-отчёте, который следующая балансная задача снимает штатно |
| 2026-08-02 | review PR #193 (Issue #179) | Мутант, назначенный **самой постановкой** (Issue #179, раздел «Мутанты»: убрать из provenance контрольную сумму или пометку о неисполнимости шага), не отчитан в теле PR. По правилу 21 для документации мутант обязателен именно тогда, когда назначен брифом, — здесь был назначен | Критерий готовности 2 закрыт review сильнее, чем требовал мутант: все три шага, помеченные `executable`, исполнены на реальных источниках и воспроизвели заявленные числа; пометки на месте (`rg -c "executable"` → по 2 в каждом из трёх provenance) | Второй PR подряд, не отчитавший назначенного брифом мутанта — тогда правило 21 требует явного поля «мутант» в теле PR, а не полагается на добросовестность |
| 2026-08-02 | review PR #199 (Issue #184) | Тело PR называет базой прогонов `19013ae`, фактическая база ветки — `431887f` (`git merge-base HEAD origin/main`). Три заявленных прогона сняты на другой базе | Разница между базами — только markdown; независимый review перегнал полный `verify.ps1` **дважды** на фактическом финальном состоянии, оба зелёные. Ни игрок, ни запускаемая проверка не страдают | Второй PR подряд называет базу, на которой заявленных прогонов уже нет — тогда в тело PR вводится обязательное поле «база прогона», получаемое `git rev-parse HEAD`, а не называемое по памяти |
| 2026-08-02 | review PR #198 (Issue #186) | Модель `CanYield` в `PrototypePostCombatDispersalTests` не воспроизводит клаузу `TrafficTarget is null`: поля нет в `PrototypeCreatureSnapshot` (`PrototypeTypes.cs:138-174`), поэтому harness его прочитать не может. Смещение направлено **вверх** — существо, уже получившее приказ уступить, harness считает пригодным к уступке | Клауза срабатывает только на существах, которым в тот же тик уже выдан `chosen_traffic_yield`; на 680 из 782 тиков (87,0 %) уступка не планировалась нигде в мире, то есть затронуто не более 13 % выборки. Вывод задачи от этого не переворачивается: он опирается на превышение обхода над уступкой, а смещение работает против вывода, а не за него | Публикация `TrafficTarget` в каноническом снапшоте — например, ради [#120](https://github.com/anshushunov/dungeon-fortress/issues/120) «показать, куда шёл вставший с `blocked_by_others`»: тогда клауза станет проверяемой, и модель обязана её взять |

## Перечитывание и удалённые записи

Перечитывание фиксируется здесь, а не в отчёте: правило «запись, пережившая три
gate-решения без изменения условия, разбирается явно» без записанного следа не
срабатывает. До 2026-08-01 то же правило мерило возраст координационными
сессиями; строки таблицы ниже, датированные тем днём и раньше, писались в той
единице.

| Дата | Записей до | Повышено | Удалено | Осталось |
|---|---|---|---|---|
| 2026-08-01 | 12 | 0 | 1 | 11 |
| 2026-08-01, сессия по бою и масштабу | 24 | 0 | 0 | 24 |
| 2026-08-01, сессия ленты владения и кромки стены | 33 | 2 | 3 | 28 |
| 2026-08-01, блок боя | 35 | 1 | 0 | 34 |
| 2026-08-02, блок #171/#77/#165 | 39 | 1 | 0 | 38 |
| 2026-08-02, третий круг PR #181 | 41 | 1 | 0 | 40 |

**Перечитывание 2026-08-02, третий круг PR #181.** Записей на момент начала —
**41**, той же командой, что ниже, на `0016fe1`; после этой правки она даёт
**40**.

**Повышена одна запись — и повышена не по возрасту, а по сработавшему условию.**
Запись от 2026-07-31 (review PR #123) о преамбуле раздела «Темп разработки»,
которая называет ослабляющие ADR правила поимённо и не называет общего правила
приоритета. Условие звучало: «появление четвёртого ослабляющего правила, текст
которого не называет явно, чья буква действует при расхождении с ADR». Оно
сработало **дословно**: правило 24 забирает у [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md)
§7 два основания эскалации и в первой своей редакции ADR 0014 не упоминало
вовсе, а преамбула продолжала перечислять «2, 5 и 7». Заведена
[#183](https://github.com/anshushunov/dungeon-fortress/issues/183) (`gh issue
view 183 --json state` — `OPEN`); запись удалена по пути 1, её условие и
измерение живут в теле задачи. Правило 24 и преамбула исправлены тем же набором
изменений — это лечение случая, а не механизма, механизм и есть предмет #183.

**Перечитывание 2026-08-02 по блоку это условие пропустило, и причина
называется.** Оно смотрело на **возраст** записей — сколько gate-решений запись
пережила — и на этом основании заключило, что правило трёх gate не сработало ни
для одной. Вопрос «не сработало ли у какой-нибудь записи её собственное
условие» отдельно не задавался ни по одной из 39, кроме той, о которой уже
знал review. Условие записи #123 сработало **в том же наборе изменений**, что и
перечитывание, и осталось незамеченным до подтверждающего круга review. Возраст
и срабатывание — две разные проверки, и раздел «Кто и когда перечитывает»
требует обе; выполнена была одна.

**Перечитывание 2026-08-02, блок #171/#77/#165.** Записей на момент начала —
**39**; число получено
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на `origin/main` (коммит `949ca50`). Той же командой считалось состояние после
той правки — **41**, и это намеренно больше: колонка «Осталось» считает
состояние на момент перечитывания (39 − 1 = 38), а команда — на тот момент,
то есть вместе с тремя добавленными. Текущее число — в протоколе третьего круга
выше.

**Повышена одна запись.** Невоспроизводимая половина происхождения пака существ
v2 (2026-08-01, review PR #162) — сработало названное условие дословно:
независимый review PR #175 столкнулся ровно с тем, что оно описывало, — пак
перегенерирован, а ни исходного листа, ни `remove_chroma_key.py` в репозитории
нет, поэтому доказать, что конкретный PNG получен описанной обработкой,
невозможно. Заведена
[#179](https://github.com/anshushunov/dungeon-fortress/issues/179) (`gh issue
view 179 --json state` — `OPEN`); запись удалена отсюда по пути 1 раздела
«Когда запись повышается до Issue», её условие и измерения живут в теле задачи.

**Добавлено три записи**, все по предложению независимого review: дрейф палитры
позы `flinch` (PR #175); молчаливая копия `EatThreshold` в
`MemoryYieldsSatiety` и две фикстуры вне границ правки #171 (обе — PR #178).
Добавление перечитыванием не является и в таблицу выше не входит.

**Правило трёх gate-решений не сработало ни для одной записи.** С начала отсчёта
— gate `PROMOTE` по срезу 2, переходная оговорка 2026-08-01 — прошло **два**
gate-решения: сам `PROMOTE` и `CONTINUE` по блоку боя 2026-08-02. Проверено по
журналу, а не принято на слово — команда даёт ровно эти два:

```bash
awk '/^\| 2026-08-01 \| \*\*Срез 2 — история влияет/,/^## Правила движения/' \
  docs/product/ROADMAP.md \
  | rg -o '\*\*`(CONTINUE|ADJUST|PROMOTE|DEFER|STOP)`\*\*'
```

Остальные строки журнала того же периода — решения владельца, а не заходы в
игру, и единицей возраста не являются.

**Перечитывание 2026-08-01, блок боя.** Записей на момент начала сессии — **35**;
число получено
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на `74af087`, той же командой считается и текущее состояние — **на момент этой
записи** она давала **39**, и это намеренно больше: колонка «Осталось» в таблице
выше считает состояние на момент перечитывания (35 − 1 = 34), а команда — на
тогдашнее «сейчас», то есть вместе с пятью добавленными. Текущее число — в
протоколе 2026-08-02 выше.

**Правило трёх gate-решений не сработало ни для одной записи, и это следствие
переходной оговорки, а не недосмотра.** Отсчёт для всех записей, существовавших на
2026-08-01, начинается с gate `PROMOTE` по срезу 2, а новых gate-решений на момент
той сессии не было: `PROMOTE` оставался последним. Ближайшая к срабатыванию по **условию**, а
не по возрасту — запись про выход задачи #127 за именованный регион `Main.cs`: её
условие требует второй одновременной задачи, реально пишущей в этот файл. В блоке
боя `Main.cs` держала единственная задача (#156), и это проверено при раздаче
партиции чтением `DrawMap`, а не предположено.

**Повышена одна запись.** Планка «бегство — это ходьба» (2026-07-31, review
PR #124) — сработало названное условие «доля ниже 80 % на любой фикстуре»:
независимый review PR #164 измерил 104 из 123 (84,5 %) против 76 из 108 (70,4 %),
ниже 80 % оказались пять ячеек из шести. Заведена
[#168](https://github.com/anshushunov/dungeon-fortress/issues/168); запись удалена
отсюда, её условие и оба измерения перенесены в тело задачи. Там же исправлено
само условие: «на любой фикстуре» читается по ячейкам, а по ячейкам оно было
истинным уже в момент написания (худшая 11 из 16 = 68,75 %) — незапускаемое
условие условием не является.

**Добавлено пять записей**, все по предложению независимого review: одна от
PR #162 — невоспроизводимая половина происхождения пака v2; четыре от PR #166 —
видимая игроку цена larder, устаревшие хеши кадров в evidence, пустое утверждение
про боковую кромку и двойной вывод ориентации сегмента. Добавление перечитыванием
не является и в таблицу выше не входит.

**Наблюдение, которое стоит проверить следующим перечитыванием.** Обе смены
единицы измерения в #156 — «объединение полос вместо одной» и «кусок кромки вместо
кромки» — поймал не тест и не мутант, а кадр: review оба раза смотрел на картинку.
Записей о геометрии представления в реестре уже пять, и ни одна не имеет условия,
которое срабатывает без человеческого взгляда. Если это подтвердится на четвёртой
точке, вывод не про реестр, а про то, чем проверяется слой представления.

Ниже — протокол предыдущей сессии.

В ту же сессию **добавлено тринадцать** записей, все по предложению независимого
review: три от ветки #127 — выход за именованный регион `Main.cs` (первый круг),
необязательность параметра `scale` внутри `BuildTooltip` и мёртвое присваивание в
`verify.ps1` (подтверждающий круг); три от ветки #52 — состав `perimeter` в
контракте, невоспроизводимые «16 / 15» и узость мутационного прохода; семь от
ветки #128/#125. Добавление перечитыванием не является и в таблицу выше не
входит.

**Перечитывание 2026-08-01, сессия по бою и масштабу.** Записей на момент
перечитывания — **24**, до добавления девяти этой же сессией; текущее число даёт
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md |
rg -c '^\| 2026-'`, и оно намеренно больше: команда считает состояние на сейчас, а
не на момент перечитывания. Ни одно условие повышения не сработало, удалять
нечего.
Три координационные сессии не пролежала ни одна запись: одиннадцать записей от
2026-07-31 проходят **второе** перечитывание, тринадцать от 2026-08-01 —
**первое**. Ближайшая к срабатыванию — запись про выход задачи #127 за
именованный регион `Main.cs`: её условие требует второй одновременной задачи,
реально пишущей в этот файл. В блоке, запущенном этой сессией, `Main.cs` держит
единственная задача (#139), поэтому условие не сработало; это проверено при
раздаче партиции, а не предположено.

В ту же сессию **добавлены девять** записей, все по предложению независимого review:
недокументированный каталог `spikes/` и `parentCommit` манифеста (ветка #142); вырождение свойства #52 в
смешанном случае и молчаливая копия половины толщины штриха (ветка #139); незащищённый порядок уровней значимости и нулевой запас бюджета панели (ветка #140); подпись однотайловой комнаты снаружи своей обводки, третье незащищённое звено проводки подписи и два сравнения референсной величины с экранным потолком (#139, круги 2–3).
Добавление перечитыванием не является и в таблицу выше не входит.

Тринадцать записей за одну сессию против одиннадцати, накопленных за всё
предыдущее время, — это само по себе наблюдение, а не бухгалтерия. Оно означает
одно из двух: либо review стал находить больше, либо реестр начал принимать то,
что раньше становилось Issue. Разбирать это надо на следующем перечитывании, с
числами, а не сейчас.

**Третье перечитывание 2026-08-01, сессия ленты владения и кромки стены.** Записей
на момент начала сессии — **33**; число получено
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на `f03f41f`, той же командой считается и текущее состояние.

Это первая сессия, на которой сработало правило трёх перечитываний: одиннадцать
записей эпохи 2026-07-31 (десять с этой датой плюс запись по подтверждающему кругу
PR #124, датированная следующим днём) дожили до **третьего** разбора. Разобраны
поимённо:

- **Правило версионирования снапшота (#105)** — условие переформулировано, потому
  что было ненаблюдаемым по собственной формулировке: «изменение, прошедшее в
  `main` **незамеченным**», заметить нельзя. Новая формулировка ловит момент
  правки, а не её последствие.
- **`includeEvents` (#105)** — условие наблюдаемо и проверено командой:
  `rg -n 'WriteState\(' -g '*.cs'` даёт единственный вызов. Запись остаётся.
- **Запись о выводе 8 калибровки (#121)** — **удалена**, причина своя, не общая:
  её условие звучит «оценка блока, построенная **по этому выводу**, снова
  промахнётся», а вывод 8 переехал в `archive/CALIBRATION_HISTORY.md`, тогда как
  оценки строятся по трём выводам, оставленным в `MULTI_AGENT_WORKFLOW.md`.
  Сработать это условие больше не может.
- **Записи о выводах 9 и 11 калибровки (#121)** — **остаются**, и это исправление
  ошибки той же сессии. Первая редакция этого разбора удалила все три по общей
  посылке «условие каждой начинается словами „оценка, построенная по этому
  выводу“». Независимый review PR #155 показал, что посылка верна только для
  вывода 8: условие записи 9 — «появление строки калибровки с вложенным
  субагентом», условие записи 11 наполовину — «оборванный прогон, стоивший
  заметно дороже», и ни то, ни другое от выводов калибровки не зависит. Обе
  восстановлены дословно. Отдельно опровергнута и вторая половина посылки:
  «в архиве — значит ни на что не опирается» неверно, `MULTI_AGENT_WORKFLOW.md`
  цитирует архивный вывод 14, а оговорка про вложенного субагента прямо помечена
  там как не подлежащая архивации.
- **Три записи о разделе «Темп разработки» (#123)** — остаются, условия
  наблюдаемы и ни одно не сработало. Ближайшая к срабатыванию — запись про
  правило 7: её условие требует **спора** о том, нужна ли команда `MERGE #N`, и в
  этой сессии вопрос действительно вставал на PR #150, но спором не стал —
  `.claude/commands/coord.md` отвечает на него прямо, и обе стороны прочитали
  ответ одинаково. Запись про четвёртое ослабляющее правило проверена отдельно:
  PR #150 нового ослабления не вводит, он задаёт значение параметра правила 14.
- **Планка бегства 80 % и мёртвый рукав `refused_injured` (#124)** — остаются, не
  сработали. У второй появился названный адресат:
  [#154](https://github.com/anshushunov/dungeon-fortress/issues/154) включает
  `refused_injured` в свой scope, и если задача его закроет, запись уйдёт вместе
  с ней.
- **Кадры #83 в `PROTOTYPE_GRAYBOX.md` (#124, подтверждающий круг)** — остаётся,
  условие наблюдаемо: кадр снимается по команде из документа, и расхождение
  видно сразу.

**Порядок применения правила: предложение координатора отклонено, вместо него
изменена единица возраста.** Координатор предлагал добавить правилу третий исход
— «разобрана, условие подтверждено, остаётся». Независимый review PR #155
показал, что предложение несло в себе сброс счётчика, а он делает храповик
недостижимым по устройству: реестр перечитывает каждая сессия, поэтому «три
сессии без изменения» не набрала бы ни одна запись никогда. Отсюда выбор в пользу
другого лечения — исходов по-прежнему **два**, а изменилась единица, в которой
считается возраст: gate-решения вместо сессий, см. раздел «Кто и когда
перечитывает».

Записи, разобранные 2026-08-01 и оставленные с наблюдаемым несработавшим
условием, начинают отсчёт заново не потому, что их перечитали, а потому, что
переходная оговорка нового правила назначает началом отсчёта gate `PROMOTE` по
срезу 2.

Ниже, **до конца этого абзаца**, — прежний текст разбора, сохранённый как
протокол; абзацы после него («Повышено две записи», «Добавлено семь записей»,
«Удалены сверх разбора», «Наблюдение прошлой сессии») — действующий протокол
сессии, а не отклонённое предложение. Правило предлагает два исхода —
переформулировать условие или удалить запись. Оба применены там, где условие
оказалось дефектным. Записи с наблюдаемым и несработавшим условием оставлены, и
сам этот разбор был объявлен для них исходом. **Это и есть отклонённое
предложение**; действующий порядок — абзацем выше.

**Повышено две записи.** Третье незащищённое звено проводки подписи (#139, круг 3)
— сработало названное условие «любая правка `DrawRoomLabel`»: правку внесла #147,
и review её ветки перепроверил мутанта на новом дереве, получив 451/451 зелёных.
Заведена [#153](https://github.com/anshushunov/dungeon-fortress/issues/153).
Незащищённый порядок уровней `StoryWeight` (#140) — получил наблюдаемое
последствие иначе, чем предполагало его условие: #145 вывела ту же шкалу на экран
по умолчанию, и на фикстуре `neglected` шапка ленты сообщает «0 значащих» о
партии, вымирающей от истощения. Заведена
[#154](https://github.com/anshushunov/dungeon-fortress/issues/154); запись удалена
как техдолг, у которого появился исполнитель.

**Добавлено семь записей**, все по предложению независимого review: **три** от
ветки #147 — широкое исключение юга в `RoomWallClearanceTests`, ослабленный
потолок лестницы и ретроспективная запись о неполноте #139; **четыре** от PR #152
— отсутствие дополненного худшего случая у ленты, невоспроизводимый замер
заголовка в `evidence/145-bound.json`, устаревшее «лента прокручивается» в
`PROTOTYPE_GRAYBOX.md` и два ослабленных утверждения в `HudTextTests`. Разбивка
исправлена по находке review PR #155: прежняя редакция называла 4 и 3 и числила
среди добавленных запись про формулировки 10,0, которая была добавлена и удалена
внутри этой же сессии — её закрыл круг доработки #147, — а фактически добавленную
запись про замер заголовка не называла. Добавление перечитыванием не является и в
таблицу выше не входит.

**Удалены сверх разбора одиннадцати, причина на каждую.** Запись про молчаливую
копию `BorderStrokeHalfWidth` — предмет исчез: `RoomGeometry.BorderStrokeWidth`
стала публичной, адаптер читает её вместо литерала, связь закреплена проверкой и
мутантом (#147, PR #151), и это подтвердил независимый review ветки. Запись про
два сравнения `32 / 4.0` в `RoomGeometryTests.cs` — предмет исчез там же: оба
сравнения заменены на `RoomGeometry.MaximumBorderInset`, величина референсная и
привязана к `CameraView` исполняемо. Обе удалялись первой редакцией без
записанной причины, чего требует раздел «Когда запись повышается до Issue»; на
обе ссылались docstring в `RoomGeometry.cs`, и эти ссылки поправлены тем же
набором изменений, иначе после merge код утверждал бы о реестре неправду.

**Наблюдение прошлой сессии проверено числом.** Прошлое перечитывание отметило,
что тринадцать записей за одну сессию против одиннадцати за всё предыдущее время
означают одно из двух: либо review стал находить больше, либо реестр начал
принимать то, что раньше становилось Issue. Эта сессия даёт данные против второй
версии: при семи новых записях два review той же сессии предложили и **два
повышения до Issue**, и **пять возвратов исполнителю** — то есть разделение
исходов работает, а не сползает в реестр. Считать тенденцию по трём точкам рано;
следующее перечитывание получит четвёртую.

**Удалено 2026-08-01** — запись от 2026-07-31 (review PR #121) о том, что раздел
`NOW` обосновывает последовательность #117 → #115 ссылкой на ADR 0017 §3 вместо
базового файлового правила. Причина удаления: **предмет исчез**. #117 закрыт
(PR #124), раздел `NOW` переписан в этой же сессии, и обоснования, к которому
относилась запись, в документе больше нет. Ни условие повышения, ни сама находка
к новому тексту `NOW` не применимы.

## Что сюда не кладётся

- находка, из-за которой не выполнен критерий готовности своей Issue, — она
  возвращается исполнителю кругом доработки;
- находка с наблюдаемым последствием — она становится Issue;
- **утверждение документа о репозитории, оказавшееся ложным.** Оно не проходит
  проверку на «нет наблюдаемого последствия» по букве — игрок его не видит, ни
  одна проверка от него не краснеет, — и именно поэтому названо здесь отдельно:
  это дефект самого документа, он правится в документе тем же набором изменений.
  Отложить его сюда значит оставить в репозитории утверждение, которое читатель
  примет за факт. Ровно на таком утверждении закрыт **черновик ADR 0018**
  (PR #111) — не следует путать с действующим
  [ADR 0018](../decisions/0018-creature-memory-of-place.md), который получил этот
  номер 2026-07-31 и решает другой вопрос. Как не создавать такую копию —
  правило 8 раздела [«Темп разработки»](../product/ROADMAP.md#темп-разработки);
- продуктовая идея, вопрос или наблюдение владельца — они идут в coordination
  inbox [#33](https://github.com/anshushunov/dungeon-fortress/issues/33);
- незакрытый вопрос, требующий решения, — он идёт в
  [`OPEN_QUESTIONS.md`](../OPEN_QUESTIONS.md);
- технический долг, который кто-то намерен исправить в ближайшем блоке, — у него
  есть исполнитель, значит есть и Issue.
