# Реестр долга

Статус: действует  
Дата обновления: 2026-08-01

## Что здесь лежит

Находки независимого review, у которых **нет наблюдаемого последствия** — ни для
игрока, ни для реально гоняемой проверки. Правило принято владельцем 2026-07-31 и
записано как правило 4 раздела
[«Темп разработки»](../product/ROADMAP.md#темп-разработки) в
[`ROADMAP.md`](../product/ROADMAP.md).

Находка с наблюдаемым последствием становится Issue и получает место в очереди.
Находка без последствия попадает сюда и слота в очереди не получает.

## Зачем реестр существует

У review-порождённых задач **нет продуктового сигнала приоритета**: их некому
ранжировать по ценности для игрока. Playtest-порождённая задача приходит с
ответом на вопрос «что мешало владельцу играть»; review-порождённая приходит с
ответом на вопрос «что неточно в коде», и обе попадают в одну очередь, где вторая
конкурирует с фичей на равных, не имея того, чем эта конкуренция решается.

Реестр разделяет два действия, которые раньше были одним: **зафиксировать
находку** и **дать ей слот в очереди**. Первое остаётся обязательным, второе
становится решением с названным условием.

Чего реестр **не** делает: он не заменяет review и не сокращает его. Он не
опирается на утверждение, что очередь инфраструктурных задач замкнута сама на
себя, — это утверждение проверялось по репозиторию 2026-07-31 и не
подтвердилось: обеспечивающие задачи порождает и playtest (#99, #86), и
исполнитель задачи (#42).

Это **не** список «починим потом». Список без условия выхода — это удалённая
находка с лишним шагом, поэтому у каждой записи обязательны и условие повышения,
и срок следующего перечитывания.

## Формат записи

| Дата | Откуда | Что | Почему без последствия | Что сделает её задачей |
|---|---|---|---|---|

- **Дата** — когда находка попала в реестр.
- **Откуда** — review какого PR её нашёл. Без источника запись нельзя перечитать.
- **Что** — сама находка, с файлом или местом, а не «неточность в снапшоте».
- **Почему без последствия** — объяснение, почему сегодня ни игрок, ни
  запускаемая проверка от неё не страдают. Это то, что перечитывание проверяет
  заново.
- **Что сделает её задачей** — наблюдаемое событие, при котором запись
  повышается до Issue. Запись без названного условия неотличима от забытой и в
  реестр не принимается.

## Кто решает, что последствия нет

**Исход по каждой находке предлагает сам review** — возврат исполнителю, Issue
или запись сюда. Координатор соглашается либо повышает находку до Issue; понизить
её до реестра против предложения review он не может.

Причина в том, кому достаётся выгода от ответа: «наблюдаемого последствия нет»
разблокирует merge, а сторона, чей merge разблокируется, не выдаёт этот ответ
себе сама. Повышение работает в одну сторону свободно, понижение — только по
предложению проверяющего.

## Когда запись повышается до Issue

Запись уходит из реестра в очередь по одному из трёх путей:

1. **Сработало названное условие** — произошло событие из колонки «Что сделает её
   задачей». Тогда координатор заводит Issue и удаляет запись отсюда, ссылаясь на
   неё в теле Issue.
2. **Находка получила наблюдаемое последствие иначе** — например, тот же дефект
   пришёл с playtest. Тогда приоритет задаёт playtest, а запись реестра
   приводится в Issue как уже известное описание.
3. **Владелец решил разобрать накопленное** при свободной очереди.

Обратного пути «поживём — увидим» нет: запись либо повышается, либо остаётся с
тем же условием, либо удаляется с записанной причиной удаления.

## Кто и когда перечитывает

- **Каждая координационная сессия** перечитывает реестр вместе с coordination
  inbox [#33](https://github.com/anshushunov/dungeon-fortress/issues/33) — это
  шаг разбора входящих в `.claude/commands/coord.md`. Результат называется
  владельцу одной строкой блок-отчёта: сколько записей, какие условия сработали,
  что повышено и что удалено. «Реестр пуст» — тоже результат и тоже называется.
- **Запись, пролежавшая три координационные сессии** без изменения, разбирается
  явно: либо условие переформулировано, потому что оно ненаблюдаемо, либо запись
  удаляется с причиной. Условие, которое невозможно заметить, — не условие.
- **Каждый gameplay gate** — реестр просматривается на предмет записей, которые
  касаются той же области, что закрываемый слайс: дешевле исправить в уже
  поднятом контексте, чем отдельной задачей.

Реестр, который никто не открывает, эквивалентен удалённой находке, поэтому
правило перечитывания является частью правила, а не пожеланием.

## Записи

| Дата | Откуда | Что | Почему без последствия | Что сделает её задачей |
|---|---|---|---|---|
| 2026-07-31 | review PR #105 | Правило версионирования снапшота (`PROTOTYPE_HEADLESS.md`, «Что аддитивно, а что ломающе») перечисляет четыре рода ломающего изменения, а `PrototypeSnapshotShapeTests` закрывает **один** — удаление и переименование, потому что закреплён набор имён полей по секциям. Смена типа и смена правила сортировки сдвигают канонический JSON и потому checksum трёх golden UI-кадров, но сдвиг checksum сам по себе не отличает ломающее изменение от tuning'а и штатно снимается регенерацией эталонов. Род «поле сохранило имя, но стало отвечать на другой вопрос» не закрыт **вовсе** | Отказ закреплять типы и значения — осознанный и записан в XML-docstring теста (`PrototypeSnapshotShapeTests.cs:25-27`): shape, несущий типы, двигался бы вместе с состоянием прогона, а не вместе с решением. Ни игрок, ни запускаемая проверка сегодня не вводятся в заблуждение | Смена типа поля, порядка сортировки или смысла поля, прошедшая в `main` незамеченной |
| 2026-07-31 | review PR #105 | `includeEvents` в `PrototypeCanonical.WriteState` имеет единственный call-site со значением `true`; ветка `false` недостижима. Будь она живой, она дала бы второе условно присутствующее поле, которое shape-тест не сэмплирует | Мёртвый код, существовавший до PR #105; сегодня недостижим ни из одной точки входа | Появление второго call-site у `WriteState` |
| 2026-07-31 | review PR #121 | Вывод 8 калибровки (перенесён 2026-07-31 в `docs/engineering/archive/CALIBRATION_HISTORY.md` при сжатии калибровки, Issue #122) считает «круги» двумя разными способами в одном абзаце: девять — это круги сверх первого у **шести работ вместе**, включая review, а «четыре на PR #85, три на PR #98» — круги **review одной работы**. Планирующий блок может сложить несопоставимое | Обе величины выводятся из таблицы там же, а число 2 025k, ради которого вывод написан, от смешения не зависит. Ни игрок, ни запускаемая проверка от него не страдают | Оценка блока, построенная по этому выводу, снова промахнётся, и разбор покажет, что промах дал подсчёт кругов |
| 2026-07-31 | review PR #121 | Вывод 9 калибровки (перенесён 2026-07-31 в `docs/engineering/archive/CALIBRATION_HISTORY.md` при сжатии калибровки, Issue #122) утверждает «ни один агент этого блока разведчика не поднимал», не называя источника. Для review PR #121 это сказано самим review; для трёх review блока 2026-07-31 — ничем | Доля 42 % считается от чисел таблицы и от наличия разведчиков не зависит: будь они, доля была бы **выше**, то есть вывод «review дороже 11 %» только усилился бы. Утверждение избыточно для собственного вывода | Появление строки калибровки с вложенным субагентом и попытка сравнить её со строками этого блока |
| 2026-07-31 | review PR #121, круг 2 | Вывод 11 калибровки (перенесён 2026-07-31 в `docs/engineering/archive/CALIBRATION_HISTORY.md` при сжатии калибровки, Issue #122) стоит на одном измерении одной задачи: 242k против оценки 350–550k. Механизм («возобновление не оплачивает чтение второй раз») в тексте помечен гипотезой, но само наблюдение остаётся единичным, и «оборванный прогон стоит почти ничего» — обобщение с одного случая | Практическое следствие вывода — «надбавку за обрыв в оценку не закладывают» — держится на самом числе, а не на механизме и не на объёме выборки. Ошибка в сторону завышения оценки ничего не ломает: ни игрок, ни запускаемая проверка от неё не страдают | Оборванный прогон, стоивший заметно дороже, — или оценка, построенная по формулировке «стоит почти ничего» и промахнувшаяся в занижение |
| 2026-07-31 | review PR #123 | Преамбула раздела «Темп разработки» (`ROADMAP.md`) после сжатия называет, какие правила (2, 5, 7) ослабляют ADR 0014/0017, но больше не называет общее правило приоритета — «там, где раздел расходится с буквой ADR, действует раздел». Осталось только «ослабление названо в тексте правила» | Расхождение с ADR и то, что именно ослабляется, названо поимённо в тексте каждого из правил 2, 5 и 7 — координатор читает конкретное правило, а не общую преамбулу. Ни игрок, ни запускаемая проверка от отсутствия общей фразы не страдают | Появление четвёртого ослабляющего правила, текст которого не называет явно, чья буква действует при расхождении с ADR |
| 2026-07-31 | review PR #123 | Правило 7 (`ROADMAP.md`) называет одно из двух ослаблений [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md) §5/§7 — «класс задачи перестаёт быть условием» — но не второе: что «всё остальное мержится только по `MERGE #N`» перестаёт действовать там, где решение уже принято владельцем | Второе ослабление выводимо из самого текста правила 7 — «PR, только исполняющий уже принятое владельцем решение, мержится без второй просьбы... независимо от класса задачи» — оно описано практически, просто не поименовано отдельно. Ни один PR не мержится иначе, чем предписывает эта формулировка | Спор о том, требует ли конкретный PR команды `MERGE #N`, который решается по-разному в зависимости от того, названо ли второе ослабление явно |
| 2026-07-31 | review PR #123 | Из раздела «Темп разработки» (`ROADMAP.md`) пропала фраза «условия отката, названные при каждом правиле, остаются рабочими условиями координатора и не являются отдельными решениями владельца» | Каждое условие отката сформулировано в тексте своего правила как проверяемый факт («если... → правило возвращается/отменяется, запись в журнал»), а не как открытый вопрос: координатор и без общей оговорки применяет его сам и пишет строку в журнал | Координатор откатывает правило по названному условию, и владелец оспаривает это как самовольное решение — тогда общая оговорка нужна явно, в тексте раздела |
| 2026-07-31 | review PR #124 | Планка «бегство — это ходьба» из #101 ослаблена с 85 % до 80 % (`PrototypeMoraleTests.cs`, `Most_broken_defenders_actually_leave_the_tile_they_broke_on`). Измерение на подземелье: 104 из 123 по матрице, то есть 84,5 %; худшая ячейка 11 из 16. Причина названа в тесте: беглец теперь ждёт в двери, а тик ожидания — это тик, в который он не сдвинулся | Обещание #101 — «бегство видно как перемещение» — держится: доля сдвинувшихся бегств упала на 3,5 пункта, а не на порядок, и вторая половина планки, ≥ 50 % на каждой ячейке, не тронута вовсе. Ни игрок, ни запускаемая проверка сегодня не видят бегство как телепорт | Доля ниже 80 % на любой фикстуре, либо прогон, в котором хотя бы одно бегство не сдвинуло существо ни разу за всю свою длительность |
| 2026-07-31 | review PR #124 | Мёртвый рукав `refused_injured` в `EventNarration.Sentence` (`EventNarration.cs`): предложение написано для reason code, который симуляция не эмитит ни в одном прогоне матрицы. `Every_reason_code_the_matrix_produces_has_a_sentence` его не покрывает — он проверяет обратное включение | Лишнее предложение не может быть показано: показать его может только событие, которого нет. Отсутствие рукава было бы хуже — падение вместо строки, — а его наличие не стоит ничего | Появление `refused_injured` в каноническом журнале любого сценария: тогда рукав становится живым и обязан попасть под guard полноты |
| 2026-08-01 | review ветки `agent/127-tooltip-readability` (Issue #127) | Задача писала в `src/DungeonFortress.Game/Main.cs` **вне своего именованного региона** — разбор CLI-аргументов, поля класса, `_Ready`, `LayoutHud`, `InjectHudTooltipReadabilityRegression`. Это члены владельца остатка по [ADR 0017](../decisions/0017-raise-parallel-writing-ceiling.md) §3, а порядок merge и rebase координатором для такого пересечения в брифе не назначались. Расширение оправдано по существу — CLI-флаг обязан разбираться там, где разбираются флаги, а проброс масштаба в `LayoutHud` есть сама суть правки, — и исполнитель назвал его сам | Названный владелец остатка `agent/52-room-readability` не трогает `Main.cs` вовсе; `git merge-tree --write-tree` сливает ветку #127 и с `agent/52-room-readability`, и с `agent/128-125-creature-decision-history` без конфликта — проверено review собственным прогоном | Появление второй одновременной задачи, реально пишущей в `Main.cs`, либо конфликт rebase на этом файле: тогда ADR 0017 §3 придётся применять с назначенным порядком merge |
| 2026-08-01 | review `agent/127-tooltip-readability`, подтверждающий круг (Issue #127) | `HudButton.BuildTooltip` можно заставить игнорировать свой параметр `scale` — `_ = scale;` плюс `ScaledSize(…, 1.0)` во всех трёх местах внутри метода: `dotnet test` остаётся **601/601**, headless-smoke Godot отдаёт exit 0, а дефект #127 возвращается целиком. Два теста, добавленные кругом доработки, доказывают, что оба **места вызова** передают живой масштаб; что параметр **потребляется** внутри, не доказывает ничто | Сегодня код верен, и оба отката, названные первым кругом review, закрыты проверками; мутант гипотетический. Ни игрок, ни запускаемая проверка сегодня не страдают | Любая правка `BuildTooltip`, появление третьего вызывающего либо переход на настоящий замер попапа — тогда цепочку текстовых утверждений можно заменить одним поведенческим |
| 2026-08-01 | review `agent/127-tooltip-readability`, подтверждающий круг (Issue #127) | `scripts/verify.ps1` присваивает `$hudTooltipReadabilityFailure` и больше нигде на неё не ссылается, тогда как каждый соседний негативный прогон кладёт свой `ExitCode` в `verification_result` (`hudReadabilityRegressionExitCode`, `hudGuardRegressionExitCode`, `cameraTransformRegressionExitCode`). В машинном итоге полного прогона поля про подсказку нет — форма та же, что у [#49](https://github.com/anshushunov/dungeon-fortress/issues/49): значение получено и выброшено | Сам gate работает: `Invoke-GodotExpectedFailure` бросает, если прогон перестал падать, и событие `godot_expected_failure_guard` присутствует в потоке — проверено review собственным прогоном стадии `godot` | Появление автоматического разбора `verification_result` — CI-бот, дашборд, координаторская сводка, — которому нужно подтверждение по полям, а не по событиям потока |
| 2026-08-01 | review `agent/52-room-readability` (Issue #52) | В контракте §12.1/§4.4 поле `Room.perimeter` не описано по составу: оно несёт **все покрашенные клетки участка** (42 у `farm@1,1`), а не обводку, и расходится с обиходным значением слова. Единственное место, где состав назван, — docstring `PrototypeTypes.cs`. Источник формулировки — сам [ADR 0013](../decisions/0013-what-is-a-room.md) («периметр покрашенных клеток»), реализация ему соответствует | Ни один потребитель не читает контракт как спецификацию для кода — снапшот нигде не десериализуется; обводку строит `RoomGeometry` из того же списка клеток и покрыта шестью мутациями. Ни игрок, ни запускаемая проверка не страдают | Появление потребителя, читающего снапшот по контракту (сохранение партии, внешний инструмент, JSON Schema), либо первая правка, где `perimeter` поняли как границу |
| 2026-08-01 | review `agent/52-room-readability` (Issue #52) | Пара чисел «16 строк нужно / 15 помещается на 2048×1440 при UI scale 2» в docstring `InspectorText.DescribeRooms` и в коммите `038b884` не имеет ни названной команды, ни единственного места записи. Производитель у них есть — сообщение HUD-guard'а `needs {n} lines but only {m} fit`, — но **измерение больше не воспроизводится**: правка убрала переполнение, и guard молчит | Число обосновывает уже принятое решение, ничего не настраивает и никуда не читается; сам guard остаётся исполняемой проверкой и отвергает переполнение независимо от того, записано число или нет | Попытка сослаться на этот бюджет при следующей правке инспектора — то есть момент, когда невоспроизводимое число начнёт обосновывать новое решение |
| 2026-08-01 | review `agent/52-room-readability` (Issue #52) | `evidence/52-mutations.json` и коммит `73a1ac0` заявляют «31 мутация, по одной на половину», но проход не касается `RoomReadings.cs` (259 новых строк) и `InspectorText.DescribeRooms`, где покраснимые половины есть — каждая ветвь `RoomLabels.Caption`, свёртка `IsInZone` в `RoomObjects.Consider` | Непокрытый мутациями код покрыт обычными тестами по каждой ветви (`RoomReadingTests`, 340 строк, включая пустую ветвь `DescribeRooms` и прогон более тысячи проверок принадлежности); дыры вида «тест зелен по неверной причине» review не обнаружил. Расхождение — в широте заявления, а не в качестве проверки | Первая правка `RoomLabels`/`RoomObjects`, прошедшая с зелёными тестами и сломавшая читаемость, — либо решение сделать мутационный проход обязательным для слоя представления целиком |
| 2026-08-01 | review PR #124, подтверждающий круг | У переизданных «воспроизводимых» кадров #83 (`PROTOTYPE_GRAYBOX.md`, раздел про объём стены) проверена только половина утверждения. Что `(10,5)` и `(13,5)` — камень, а `(10,4)` и `(13,6)` — пол, следует из `PrototypeLayout.Rows` и падает вместе с раскладкой. Что существо #2 стоит на `(10,4)` на тике 40, а существо #0 на `(13,6)` на тике 80, не проверяется ничем: кадры помечены ignored, `verify.ps1` их не снимает | Кадры справочные: ни одна запускаемая проверка от них не зависит, и игрок их не видит. Позиции выведены прогоном шипованного журнала `baseline`, а не памятью, поэтому сегодня они верны — не покрыто именно их **сохранение** | Кадр снят по команде из документа, и существо оказалось не на названном тайле |

## Перечитывание и удалённые записи

Перечитывание фиксируется здесь, а не в отчёте: правило «запись, пролежавшая три
координационные сессии без изменения, разбирается явно» без даты не срабатывает.

| Дата | Записей до | Повышено | Удалено | Осталось |
|---|---|---|---|---|
| 2026-08-01 | 12 | 0 | 1 | 11 |

В ту же сессию **добавлено шесть** записей, все по предложению независимого
review: три от ветки #127 — выход за именованный регион `Main.cs` (первый круг),
необязательность параметра `scale` внутри `BuildTooltip` и мёртвое присваивание в
`verify.ps1` (подтверждающий круг); три от ветки #52 — состав `perimeter` в
контракте, невоспроизводимые «16 / 15» и узость мутационного прохода. Добавление
перечитыванием не является и в таблицу выше не входит.

**Удалено 2026-08-01** — запись от 2026-07-31 (review PR #121) о том, что раздел
`NOW` обосновывает последовательность #117 → #115 ссылкой на ADR 0017 §3 вместо
базового файлового правила. Причина удаления: **предмет исчез**. #117 закрыт
(PR #124), раздел `NOW` переписан в этой же сессии, и обоснования, к которому
относилась запись, в документе больше нет. Ни условие повышения, ни сама находка
к новому тексту `NOW` не применимы.

## Что сюда не кладётся

- находка, из-за которой не выполнен критерий готовности своей Issue, — она
  возвращается исполнителю кругом доработки;
- находка с наблюдаемым последствием — она становится Issue;
- **утверждение документа о репозитории, оказавшееся ложным.** Оно не проходит
  проверку на «нет наблюдаемого последствия» по букве — игрок его не видит, ни
  одна проверка от него не краснеет, — и именно поэтому названо здесь отдельно:
  это дефект самого документа, он правится в документе тем же набором изменений.
  Отложить его сюда значит оставить в репозитории утверждение, которое читатель
  примет за факт. Ровно на таком утверждении закрыт **черновик ADR 0018**
  (PR #111) — не следует путать с действующим
  [ADR 0018](../decisions/0018-creature-memory-of-place.md), который получил этот
  номер 2026-07-31 и решает другой вопрос. Как не создавать такую копию —
  правило 8 раздела [«Темп разработки»](../product/ROADMAP.md#темп-разработки);
- продуктовая идея, вопрос или наблюдение владельца — они идут в coordination
  inbox [#33](https://github.com/anshushunov/dungeon-fortress/issues/33);
- незакрытый вопрос, требующий решения, — он идёт в
  [`OPEN_QUESTIONS.md`](../OPEN_QUESTIONS.md);
- технический долг, который кто-то намерен исправить в ближайшем блоке, — у него
  есть исполнитель, значит есть и Issue.
