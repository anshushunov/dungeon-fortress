# Реестр долга

Статус: действует  
Дата обновления: 2026-08-03

## Что здесь лежит

Находки независимого review, у которых **нет наблюдаемого последствия** — ни для
игрока, ни для реально гоняемой проверки. Правило принято владельцем 2026-07-31 и
записано как правило 4 раздела
[«Темп разработки»](../product/ROADMAP.md#темп-разработки) в
[`ROADMAP.md`](../product/ROADMAP.md).

Находка с наблюдаемым последствием становится Issue и получает место в очереди.
Находка без последствия попадает сюда и слота в очереди не получает.

## Зачем реестр существует

У review-порождённых задач **нет продуктового сигнала приоритета**: их некому
ранжировать по ценности для игрока. Playtest-порождённая задача приходит с
ответом на вопрос «что мешало владельцу играть»; review-порождённая приходит с
ответом на вопрос «что неточно в коде», и обе попадают в одну очередь, где вторая
конкурирует с фичей на равных, не имея того, чем эта конкуренция решается.

Реестр разделяет два действия, которые раньше были одним: **зафиксировать
находку** и **дать ей слот в очереди**. Первое остаётся обязательным, второе
становится решением с названным условием.

Чего реестр **не** делает: он не заменяет review и не сокращает его. Он не
опирается на утверждение, что очередь инфраструктурных задач замкнута сама на
себя, — это утверждение проверялось по репозиторию 2026-07-31 и не
подтвердилось: обеспечивающие задачи порождает и playtest (#99, #86), и
исполнитель задачи (#42).

Это **не** список «починим потом». Список без условия выхода — это удалённая
находка с лишним шагом, поэтому у каждой записи обязательны и условие повышения,
и срок следующего перечитывания.

## Формат записи

| Дата | Откуда | Что | Почему без последствия | Что сделает её задачей |
|---|---|---|---|---|

- **Дата** — когда находка попала в реестр.
- **Откуда** — review какого PR её нашёл. Без источника запись нельзя перечитать.
- **Что** — сама находка, с файлом или местом, а не «неточность в снапшоте».
- **Почему без последствия** — объяснение, почему сегодня ни игрок, ни
  запускаемая проверка от неё не страдают. Это то, что перечитывание проверяет
  заново.
- **Что сделает её задачей** — наблюдаемое событие, при котором запись
  повышается до Issue. Запись без названного условия неотличима от забытой и в
  реестр не принимается.

## Кто решает, что последствия нет

**Исход по каждой находке предлагает сам review** — возврат исполнителю, Issue
или запись сюда. Координатор соглашается либо повышает находку до Issue; понизить
её до реестра против предложения review он не может.

Причина в том, кому достаётся выгода от ответа: «наблюдаемого последствия нет»
разблокирует merge, а сторона, чей merge разблокируется, не выдаёт этот ответ
себе сама. Повышение работает в одну сторону свободно, понижение — только по
предложению проверяющего.

## Когда запись повышается до Issue

Запись уходит из реестра в очередь по одному из трёх путей:

1. **Сработало названное условие** — произошло событие из колонки «Что сделает её
   задачей». Тогда координатор заводит Issue и удаляет запись отсюда, ссылаясь на
   неё в теле Issue.
2. **Находка получила наблюдаемое последствие иначе** — например, тот же дефект
   пришёл с playtest. Тогда приоритет задаёт playtest, а запись реестра
   приводится в Issue как уже известное описание.
3. **Владелец решил разобрать накопленное** при свободной очереди.

Обратного пути «поживём — увидим» нет: запись либо повышается, либо остаётся с
тем же условием, либо удаляется с записанной причиной удаления.

## Кто и когда перечитывает

- **Каждая координационная сессия** перечитывает реестр вместе с coordination
  inbox [#33](https://github.com/anshushunov/dungeon-fortress/issues/33) — это
  шаг разбора входящих в `.claude/commands/coord.md`. Результат называется
  владельцу одной строкой блок-отчёта: сколько записей, какие условия сработали,
  что повышено и что удалено. «Реестр пуст» — тоже результат и тоже называется.
- **Запись, пережившая три gate-решения** без изменения условия, разбирается
  явно: либо условие переформулировано, потому что оно ненаблюдаемо, либо запись
  удаляется с причиной. Условие, которое невозможно заметить, — не условие.

  **Единица — gate-решение, а не координационная сессия.** Прежняя редакция
  мерила возраст в сессиях и писалась, когда сессия была примерно днём; 2026-07-31
  и 2026-08-01 сессий было по три за день, и одиннадцать записей «состарились» за
  сутки, будучи по смыслу правила молодыми. Gate-решение — единица, которую нельзя
  нащёлкать: оно требует, чтобы владелец сыграл. Запись, пережившая три его захода
  в игру и ни разу не понадобившаяся, мертва по существу, а не по календарю.

  **Перечитывание счётчик не сбрасывает.** Реестр перечитывает каждая сессия, и
  если бы перечитывание обнуляло срок, ни одна запись не дожила бы до разбора
  никогда. Срок считается от последнего изменения **условия**; пометка о том, что
  запись перечитана и условие подтверждено, изменением условия не является.

  **Известная дыра, задача заведена.** Счётчику сегодня негде записаться: ни
  формат записи, ни таблица перечитываний не несут числа переживших gate-решений,
  и по дате запись против gate не упорядочивается — 2026-08-01 их было три за
  день. Кроме того, число переформулировок условия ничем не ограничено, а значит
  запись можно держать вечно, трогая её условие раз в три gate. Найдено
  независимым review PR #158, заведено
  [#159](https://github.com/anshushunov/dungeon-fortress/issues/159). Отличие от
  прежней поломки существенно: вечная жизнь записи теперь требует действия каждый
  цикл, а не наступает сама.

  **Переходная оговорка — отдельное решение владельца 2026-08-01, а не следствие
  выбора единицы.** Различие названо независимым review PR #158 и принято: выбор
  единицы решал, чем мерить возраст, а оговорка решает судьбу девяти записей,
  просроченных по букве нового правила уже в день его введения, и для них её
  эффект тождественен отклонённому третьему исходу. Поэтому она вынесена владельцу
  отдельно и получила его «да». Отсчёт для записей, существовавших на 2026-08-01,
  начинается с gate-решения `PROMOTE` по срезу 2. Буквальный пересчёт прежнего
  возраста в новой единице дал бы срабатывание в день введения правила: две
  старейшие записи приехали вместе с самим реестром в коммите `89ade73`
  (`git log --diff-filter=A -- docs/engineering/DEBT_LEDGER.md`), а он входил в
  дерево пакетного playtest с решением `CONTINUE` 2026-07-31 — значит они пережили
  **четыре** gate-решения, а не три: `CONTINUE`, два `ADJUST` и этот `PROMOTE`.
- **Каждый gameplay gate** — реестр просматривается на предмет записей, которые
  касаются той же области, что закрываемый слайс: дешевле исправить в уже
  поднятом контексте, чем отдельной задачей.

Реестр, который никто не открывает, эквивалентен удалённой находке, поэтому
правило перечитывания является частью правила, а не пожеланием.

## Записи

| Дата | Откуда | Что | Почему без последствия | Что сделает её задачей |
|---|---|---|---|---|
| 2026-08-03 | review PR #214 (Issue #207) | Docstring `scripts/agent/test-take-task.ps1:31-33` утверждает, что тест **не** подключён ни к одной стадии `verify.ps1` и что подключение — решение координатора. После merge PR #214 (коммит `fe08076`) это ложно: тест гоняется стадией `scripts`, событие `take_task_test` видно в её выводе | Утверждение живёт в комментарии скрипта, который сам себя не читает; ни один прогон от него не зависит, и поведение теста верное. Исход назвал сам review, координатор согласился | Первый агент, который сошлётся на этот комментарий как на действующее описание — например, решит, что подключение ещё предстоит сделать |
| 2026-08-03 | review PR #213 (Issue #202) | Работа по #202 велась и коммитилась **в корневой рабочей копии**: `git reflog` корня показывает `checkout: moving from main to agent/202-worktree-command`, затем `commit: … (#202)`. Это нарушение правила 17 «Темпа разработки» — того самого правила, чей дубликат команды PR и убирал | Содержание PR от этого не пострадало, и проверка review не подорвана: содержимое сверялось через `git show <branch>:<path>` и `gh pr diff`, а не через состояние рабочей копии. Корень возвращён на `main` координатором в тот же день (`git status --short --branch` → `## main...origin/main`). Ни игрок, ни запускаемая проверка не страдают | **Второй** случай записи в корневую копию внешним исполнителем. Первый — art-задача 2026-08-01, ради которой правило 17 и написано; на втором подряд правило перестаёт работать как правило, и вопрос становится не про исполнителя, а про то, чем оно обеспечено технически |
| 2026-08-02 | review PR #175 (Issue #165) | Перегенерация позы `flinch` edit-режимом сдвинула её палитру относительно остальных пяти поз пака v2. **Числа сняты независимым review PR #175 и здесь не перепроверялись** — воспроизвести их можно только тем же проходом по PNG. Доминирующий средний тон кожи у пяти поз и у прежнего `flinch` — квантованный `(144,144,48)`; у нового `flinch` он выпал из четвёрки самых частых, доминирует `(160,144,48)`. Блик кожи: пак `(194–198, 185–188, 75–78)`, новый `flinch` `(204,193,93)` — синий канал выше потолка пака на 15. Доля тёмного (`max(RGB) < 40`): пак 24,2–28,3 %, новый `flinch` 23,0 % | Новых цветов не введено: сместилось распределение по уже существующим тонам, то есть освещённость, а не палитра. На контактном листе при высоте холста 62 px review разницу не различает. **Фактура обновлена 2026-08-03: прежний текст говорил «рантайм грузит v1», после merge PR #185 это неверно** — `BodySprites.PackVersion` = `"v2"`, путь строится через `BodySprites.FileName` (`Main.cs:2112`). Отсутствие последствия держится теперь на другом и более узком факте: поза `flinch` не рисуется **вовсе**, потому что оба вызова адаптера передают `BodyActionPhase.None` (`Main.cs:5503`, `Main.cs:5506`), а достижимой её делает [#210](https://github.com/anshushunov/dungeon-fortress/issues/210). Ни одна запускаемая проверка на палитру не смотрит | Кадр playtest или контактный лист, на котором владелец назовёт `flinch` светлее остальных поз, — либо вторая перегенерация любой позы этого пака edit-режимом: тогда у дрейфа появляется вторая точка и он становится свойством режима, а не одного файла |
| 2026-08-02 | review PR #178 (Issue #171) | `MemoryYieldsSatiety = 30` (`PrototypeTuning.cs:255`) и `EatThreshold = 30` (там же, `:299`) — два независимых литерала. Всё обоснование значения держится на их равенстве, и утверждается это равенство только прозой: комментарий над самой константой («Thirty is EatThreshold…», `PrototypeTuning.cs:242`), docstring `PrototypeWorld.AvoidedPlace`, контракт, тело PR и `evidence/171-after.json`. Исполняемого утверждения нет: `rg -n 'MemoryYieldsSatiety' -g '*.cs'` даёт 7 строк — объявление и шесть ссылок (три сравнения сытости существа в коде, две docstring-ссылки, одно сообщение теста), ни одна не упоминает `EatThreshold`; обратная команда `rg -n 'EatThreshold' -g '*.cs'` даёт 6 строк, и ни одна не упоминает `MemoryYieldsSatiety`. **Асимметрия называется сама собой:** вторую половину обоснования автор закрепил тестом — `A_memory_stops_refusing_work_before_the_next_wave_arrives` начинается с `Assert.True(MemoryAvoidTicks < WaveIntervalTicks)` (`PrototypeMemoryCostTests.cs:196`) | Сегодня обе величины равны 30, проверки зелёные, поведение верное. Прецедент в проекте свой: запись про молчаливую копию `BorderStrokeHalfWidth` закрыта не задачей, а тем, что константу сделали публичной и стали на неё ссылаться (протокол перечитывания 2026-08-01 ниже) | Первая правка `EatThreshold` или `MemoryYieldsSatiety`, после которой они разойдутся |
| 2026-08-02 | там же | Две фикстуры, которые правка #171 сдвинула, лежат вне её собственных границ. `A_party_that_wins_its_fights_does_not_end_it_starving` и `No_creature_is_taken_out_of_the_domain_by_what_it_remembers` гоняются по `SixParties()` — `baseline` и `prepared` × 3 seed (`PrototypeMemoryCostTests.cs:38`, `:307`); `prepared-ration-zero` и `prepared-watch-zero`, это шесть прогонов, не проверяются ни на исход, ни на длину серии отказов, хотя `prepared-ration-zero/20260728` от правки изменилась — 902 → 947, измерено независимым review PR #178. Исключение `neglected` законно и названо в docstring `SixParties()`: партия не доживает до первой волны и памятей не пишет | Все 15 прогонов матрицы 13.4 гоняются `--evaluate-prototype`, инварианты зелёные, и единственная сдвинувшаяся ячейка вне выборки улучшилась | Первый прогон `prepared-ration-zero` или `prepared-watch-zero`, приходящий `fallen` при выигранном бое, при зелёном `PrototypeMemoryCostTests` — он виден в батч-отчёте, который следующая балансная задача снимает штатно |
| 2026-08-02 | review PR #193 (Issue #179) | Мутант, назначенный **самой постановкой** (Issue #179, раздел «Мутанты»: убрать из provenance контрольную сумму или пометку о неисполнимости шага), не отчитан в теле PR. По правилу 21 для документации мутант обязателен именно тогда, когда назначен брифом, — здесь был назначен | Критерий готовности 2 закрыт review сильнее, чем требовал мутант: все три шага, помеченные `executable`, исполнены на реальных источниках и воспроизвели заявленные числа; пометки на месте (`rg -c "executable"` → по 2 в каждом из трёх provenance) | Второй PR подряд, не отчитавший назначенного брифом мутанта — тогда правило 21 требует явного поля «мутант» в теле PR, а не полагается на добросовестность |
| 2026-08-02 | review PR #199 (Issue #184) | Тело PR называет базой прогонов `19013ae`, фактическая база ветки — `431887f` (`git merge-base HEAD origin/main`). Три заявленных прогона сняты на другой базе | Разница между базами — только markdown; независимый review перегнал полный `verify.ps1` **дважды** на фактическом финальном состоянии, оба зелёные. Ни игрок, ни запускаемая проверка не страдают | Второй PR подряд называет базу, на которой заявленных прогонов уже нет — тогда в тело PR вводится обязательное поле «база прогона», получаемое `git rev-parse HEAD`, а не называемое по памяти |
| 2026-08-02 | review PR #198 (Issue #186) | Модель `CanYield` в `PrototypePostCombatDispersalTests` не воспроизводит клаузу `TrafficTarget is null`: поля нет в `PrototypeCreatureSnapshot` (`PrototypeTypes.cs:138-174`), поэтому harness его прочитать не может. Смещение направлено **вверх** — существо, уже получившее приказ уступить, harness считает пригодным к уступке | Клауза срабатывает только на существах, которым в тот же тик уже выдан `chosen_traffic_yield`; на 680 из 782 тиков (87,0 %) уступка не планировалась нигде в мире, то есть затронуто не более 13 % выборки. Вывод задачи от этого не переворачивается: он опирается на превышение обхода над уступкой, а смещение работает против вывода, а не за него | Публикация `TrafficTarget` в каноническом снапшоте — например, ради [#120](https://github.com/anshushunov/dungeon-fortress/issues/120) «показать, куда шёл вставший с `blocked_by_others`»: тогда клауза станет проверяемой, и модель обязана её взять |
| 2026-08-03 | review PR #198, круг 3 (Issue #186) | Мутационные подстановки живут в `.artifacts/mutate-186.py` под `.gitignore`; в `evidence/186-mutations.json` записана только проза. Для мутанта `R2` проза, исполненная буквально (`if (true)`), **не собирается** — `CS0219`, переменной присвоено значение и ни разу не использовано; рабочая подстановка `if (!nextTileWasFree \|\| nextTileWasFree)` в дереве не записана нигде (`rg "nextTileWasFree \\\|\\\| nextTileWasFree"` по `evidence/` и `docs/` — пусто) | Половина покрыта проверяемо: независимый review восстановил откат этой половины **двумя разными способами**, оба красные по одному и тому же утверждению. Проза называет свойство верно и промахивается только в букве; чтобы получить красный, проверяющему пришлось самому сохранить чтение переменной | Мутант, который восстанавливается с дерева **неоднозначно** и даёт разный результат в зависимости от прочтения; либо решение хранить подстановки в дереве — поле `replacement` рядом с `edit` |

## Перечитывание и удалённые записи

Перечитывание фиксируется здесь, а не в отчёте: правило «запись, пережившая три
gate-решения без изменения условия, разбирается явно» без записанного следа не
срабатывает. До 2026-08-01 то же правило мерило возраст координационными
сессиями; строки таблицы ниже, датированные тем днём и раньше, писались в той
единице.

| Дата | Записей до | Повышено | Удалено | Осталось |
|---|---|---|---|---|
| 2026-08-01 | 12 | 0 | 1 | 11 |
| 2026-08-01, сессия по бою и масштабу | 24 | 0 | 0 | 24 |
| 2026-08-01, сессия ленты владения и кромки стены | 33 | 2 | 3 | 28 |
| 2026-08-01, блок боя | 35 | 1 | 0 | 34 |
| 2026-08-02, блок #171/#77/#165 | 39 | 1 | 0 | 38 |
| 2026-08-02, третий круг PR #181 | 41 | 1 | 0 | 40 |
| 2026-08-03, блок #201/#210 | 44 | 0 | 37 | 7 |

**Перечитывание 2026-08-03, блок #201/#210.** Записей на момент начала — **44**,
той же командой, что и прежние протоколы:

```bash
awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'
```

После этой правки она даёт **7**.

**Правило трёх gate-решений сработало впервые, и сразу для 37 записей из 44.**
Возраст считается в gate-решениях с начала отсчёта, назначенного переходной
оговоркой 2026-08-01, — gate `PROMOTE` по срезу 2. Прошедшие gate получены той же
командой, что и в протоколе 2026-08-02, и их стало **три**:

```bash
awk '/^\| 2026-08-01 \| \*\*Срез 2 — история влияет/,/^## Правила движения/' \
  docs/product/ROADMAP.md \
  | rg -o '\*\*`(CONTINUE|ADJUST|PROMOTE|DEFER|STOP)`\*\*'
```

→ `PROMOTE` (срез 2), `CONTINUE` (блок боя), `CONTINUE` (срез «цена памяти о
месте»). Третий gate появился после протокола 2026-08-02, который считал два.
Под разбор попадает вся эпоха до 2026-08-02 включительно по дате записи: **7**
записей от 2026-07-31 и **30** от 2026-08-01 (в том числе одна с датой
«2026-08-01, текст переписан после merge #147»).

**Проверка срабатывания велась отдельно от проверки возраста** — это исполнение
урока, записанного протоколом 2026-08-02: тогда перечитывание смотрело только на
возраст и пропустило сработавшее условие записи #123. Проверяемые командой
условия проверены командой, а не суждением:

| Условие записи | Команда | Результат |
|---|---|---|
| Второй call-site у `WriteState` | `rg -n 'WriteState\(' -g '*.cs'` | один вызов (`PrototypeCanonical.cs:20`) — не сработало |
| `refused_injured` в каноническом журнале | `rg -n 'refused_injured' -g '*.cs'` | симуляция эмитит только `combat_refused_injured` (`PrototypeWorld.cs:2508`); рукав без префикса остаётся мёртвым — не сработало |
| Правка `BuildTooltip`, третий вызывающий | `rg -n 'BuildTooltip' -g '*.cs'` | два вызывающих, как и было — не сработало |
| `EatThreshold` и `MemoryYieldsSatiety` разошлись | `rg -n 'MemoryYieldsSatiety = \|EatThreshold = ' src/DungeonFortress.Simulation/PrototypeTuning.cs` | обе `= 30` — не сработало (запись молодая, остаётся) |

Остальные условия эпохи разбора — про кадр playtest, про появление второго
спайка, про потребителя снапшота, про отступ глубже 8,625 — за три gate не
наступили ни разу.

**Исход: 37 записей удалены, ни одна не повышена, ни одна не переформулирована.**
Правило даёт ровно два исхода — переформулировать условие, **потому что оно
ненаблюдаемо**, либо удалить с причиной; исход «разобрана и остаётся» отклонён
2026-08-01 независимым review PR #155 как сброс счётчика, делающий храповик
недостижимым. Переформулировка здесь не применима: условия этих записей
наблюдаемы — большинство из них уже переформулированы ради наблюдаемости на
перечитывании 2026-08-01. Значит применён второй исход, и **общая причина одна:
условие наблюдаемо, три захода владельца в игру запись пережила и ни разу не
понадобилась.**

Что это значит по существу, а не по бухгалтерии: реестр — очередь с храповиком, а
не архив находок. Находка, которая за три gate не превратилась ни в дефект, ни в
вопрос, ни в помеху проверке, по устройству правила считается не нужной. Текст
удалённых записей остаётся в истории git (`git log -p docs/engineering/DEBT_LEDGER.md`),
а их источник — review конкретного PR — назван в перечне ниже, поэтому след не
теряется.

**Перечень удалённого.** Порядок — как в таблице записей; «источник» позволяет
поднять исходный вердикт review.

| Источник | Что было записано |
|---|---|
| review PR #105 | Правило версионирования снапшота закрыто тестом на один род ломающих изменений из четырёх |
| review PR #105 | `includeEvents` в `WriteState`: недостижимая ветка `false` |
| review PR #121 | Вывод 9 калибровки утверждает про разведчиков без источника |
| review PR #121, круг 2 | Вывод 11 калибровки стоит на одном измерении одной задачи |
| review PR #123 | Правило 7 называет одно из двух ослаблений ADR 0014 |
| review PR #123 | Пропала фраза про условия отката как рабочие условия координатора |
| review PR #124 | Мёртвый рукав `refused_injured` в `EventNarration.Sentence` |
| review #127 | Задача писала в `Main.cs` вне своего именованного региона |
| review #127, круг 2 | `BuildTooltip` можно заставить игнорировать параметр `scale` |
| review #127, круг 2 | `$hudTooltipReadabilityFailure` присвоена и не прочитана |
| review #52 | Состав `Room.perimeter` не описан в контракте |
| review #52 | Невоспроизводимые «16 строк нужно / 15 помещается» |
| review #52 | Мутационный проход не касается `RoomReadings.cs` и `DescribeRooms` |
| review #128/#125 | Стадия «две половины» в `evidence/125-false-refusals.json` без разбивки |
| review #128/#125 | Оговорка «счёт не ниже `score_floor`» не упражняется матрицей |
| review #128/#125 | Раннеры мутационных прогонов не отгружены (`.artifacts` в `.gitignore`) |
| review #128/#125 | `MemoryFreeWinnerOfRefusedTile` зависит от порядка обхода `Dictionary` |
| review #128/#125 | Контрфактический проб на 6 партиях против 9 в `MatrixScenarios` |
| review #128/#125 | `Reading_a_creature_s_story_needs_no_tick_to_run` сравнивает проекцию с собой |
| review #128/#125 | `CreatureStoryPanels()` на smoke-тике меряет пол бюджета, а не потолок |
| review PR #124, круг 2 | Кадры #83 в `PROTOTYPE_GRAYBOX.md` проверены наполовину |
| review #142 | Каталог `spikes/` не описан ни одним документом |
| review #139/#147 | Вырождение свойства #52 в смешанном случае |
| review #142, круг 2 | `parentCommit` в `evidence/142-scale-spike.json` указывает на дерево до правки |
| review #142, круг 2 | Нулевой запас бюджета панели истории |
| review #139, круг 2 | Подпись однотайловой комнаты под стеной выходит за свою обводку |
| review #147 | `RoomWallClearanceTests.Measure` исключает юг слишком широко |
| review #147 | Между ступенью 8,625 и потолком 10,0 нет проверки |
| review #147, ретроспективно к #139 | Критерий #139 закрыт против прямоугольника фасада, а не полосы |
| review PR #152 | У ленты владения нет дополненного худшего случая |
| review PR #152, круг 2 | Невоспроизводимый замер заголовка в `evidence/145-bound.json` |
| review PR #152, круг 2 | «Лента прокручивается» в `PROTOTYPE_GRAYBOX.md` после #145 неверно |
| review PR #166, круг 2 | Необъяснённая разница толщины кромок larder и kitchen |
| review PR #166, круг 2 | Устаревший `imageSha256` в `evidence/156-mutations.json` |
| review PR #166, круг 2 | Пустое утверждение про боковую кромку за стеной |
| review PR #166, круг 2 | Ориентация сегмента границы выводится дважды разными выражениями |
| review PR #152, круг 2 | Два ослабленных утверждения в `HudTextTests` |

**Отдельно: у оставшейся записи про палитру `flinch` исправлена фактура, а не
условие.** Запись объясняла отсутствие последствия в том числе тем, что «рантайм
грузит v1». После merge PR #185 это неверно: `BodySprites.PackVersion` = `"v2"`,
и путь строится через `BodySprites.FileName` (`Main.cs:2112`). Ложное утверждение
документа о репозитории в реестре не отлёживается — оно правится в документе, —
поэтому текст записи приведён к факту. Условие не менялось, счётчик возраста не
сбрасывается. Практическое следствие названо там же: поза `flinch` сегодня не
рисуется вовсе, потому что оба вызова адаптера передают `BodyActionPhase.None`;
[#210](https://github.com/anshushunov/dungeon-fortress/issues/210) делает её
достижимой, и с её merge условие записи впервые становится проверяемым на кадре.

**Перечитывание 2026-08-02, третий круг PR #181.** Записей на момент начала —
**41**, той же командой, что ниже, на `0016fe1`; после этой правки она даёт
**40**.

**Повышена одна запись — и повышена не по возрасту, а по сработавшему условию.**
Запись от 2026-07-31 (review PR #123) о преамбуле раздела «Темп разработки»,
которая называет ослабляющие ADR правила поимённо и не называет общего правила
приоритета. Условие звучало: «появление четвёртого ослабляющего правила, текст
которого не называет явно, чья буква действует при расхождении с ADR». Оно
сработало **дословно**: правило 24 забирает у [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md)
§7 два основания эскалации и в первой своей редакции ADR 0014 не упоминало
вовсе, а преамбула продолжала перечислять «2, 5 и 7». Заведена
[#183](https://github.com/anshushunov/dungeon-fortress/issues/183) (`gh issue
view 183 --json state` — `OPEN`); запись удалена по пути 1, её условие и
измерение живут в теле задачи. Правило 24 и преамбула исправлены тем же набором
изменений — это лечение случая, а не механизма, механизм и есть предмет #183.

**Перечитывание 2026-08-02 по блоку это условие пропустило, и причина
называется.** Оно смотрело на **возраст** записей — сколько gate-решений запись
пережила — и на этом основании заключило, что правило трёх gate не сработало ни
для одной. Вопрос «не сработало ли у какой-нибудь записи её собственное
условие» отдельно не задавался ни по одной из 39, кроме той, о которой уже
знал review. Условие записи #123 сработало **в том же наборе изменений**, что и
перечитывание, и осталось незамеченным до подтверждающего круга review. Возраст
и срабатывание — две разные проверки, и раздел «Кто и когда перечитывает»
требует обе; выполнена была одна.

**Перечитывание 2026-08-02, блок #171/#77/#165.** Записей на момент начала —
**39**; число получено
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на `origin/main` (коммит `949ca50`). Той же командой считалось состояние после
той правки — **41**, и это намеренно больше: колонка «Осталось» считает
состояние на момент перечитывания (39 − 1 = 38), а команда — на тот момент,
то есть вместе с тремя добавленными. Текущее число — в протоколе третьего круга
выше.

**Повышена одна запись.** Невоспроизводимая половина происхождения пака существ
v2 (2026-08-01, review PR #162) — сработало названное условие дословно:
независимый review PR #175 столкнулся ровно с тем, что оно описывало, — пак
перегенерирован, а ни исходного листа, ни `remove_chroma_key.py` в репозитории
нет, поэтому доказать, что конкретный PNG получен описанной обработкой,
невозможно. Заведена
[#179](https://github.com/anshushunov/dungeon-fortress/issues/179) (`gh issue
view 179 --json state` — `OPEN`); запись удалена отсюда по пути 1 раздела
«Когда запись повышается до Issue», её условие и измерения живут в теле задачи.

**Добавлено три записи**, все по предложению независимого review: дрейф палитры
позы `flinch` (PR #175); молчаливая копия `EatThreshold` в
`MemoryYieldsSatiety` и две фикстуры вне границ правки #171 (обе — PR #178).
Добавление перечитыванием не является и в таблицу выше не входит.

**Правило трёх gate-решений не сработало ни для одной записи.** С начала отсчёта
— gate `PROMOTE` по срезу 2, переходная оговорка 2026-08-01 — прошло **два**
gate-решения: сам `PROMOTE` и `CONTINUE` по блоку боя 2026-08-02. Проверено по
журналу, а не принято на слово — команда даёт ровно эти два:

```bash
awk '/^\| 2026-08-01 \| \*\*Срез 2 — история влияет/,/^## Правила движения/' \
  docs/product/ROADMAP.md \
  | rg -o '\*\*`(CONTINUE|ADJUST|PROMOTE|DEFER|STOP)`\*\*'
```

Остальные строки журнала того же периода — решения владельца, а не заходы в
игру, и единицей возраста не являются.

**Перечитывание 2026-08-01, блок боя.** Записей на момент начала сессии — **35**;
число получено
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на `74af087`, той же командой считается и текущее состояние — **на момент этой
записи** она давала **39**, и это намеренно больше: колонка «Осталось» в таблице
выше считает состояние на момент перечитывания (35 − 1 = 34), а команда — на
тогдашнее «сейчас», то есть вместе с пятью добавленными. Текущее число — в
протоколе 2026-08-02 выше.

**Правило трёх gate-решений не сработало ни для одной записи, и это следствие
переходной оговорки, а не недосмотра.** Отсчёт для всех записей, существовавших на
2026-08-01, начинается с gate `PROMOTE` по срезу 2, а новых gate-решений на момент
той сессии не было: `PROMOTE` оставался последним. Ближайшая к срабатыванию по **условию**, а
не по возрасту — запись про выход задачи #127 за именованный регион `Main.cs`: её
условие требует второй одновременной задачи, реально пишущей в этот файл. В блоке
боя `Main.cs` держала единственная задача (#156), и это проверено при раздаче
партиции чтением `DrawMap`, а не предположено.

**Повышена одна запись.** Планка «бегство — это ходьба» (2026-07-31, review
PR #124) — сработало названное условие «доля ниже 80 % на любой фикстуре»:
независимый review PR #164 измерил 104 из 123 (84,5 %) против 76 из 108 (70,4 %),
ниже 80 % оказались пять ячеек из шести. Заведена
[#168](https://github.com/anshushunov/dungeon-fortress/issues/168); запись удалена
отсюда, её условие и оба измерения перенесены в тело задачи. Там же исправлено
само условие: «на любой фикстуре» читается по ячейкам, а по ячейкам оно было
истинным уже в момент написания (худшая 11 из 16 = 68,75 %) — незапускаемое
условие условием не является.

**Добавлено пять записей**, все по предложению независимого review: одна от
PR #162 — невоспроизводимая половина происхождения пака v2; четыре от PR #166 —
видимая игроку цена larder, устаревшие хеши кадров в evidence, пустое утверждение
про боковую кромку и двойной вывод ориентации сегмента. Добавление перечитыванием
не является и в таблицу выше не входит.

**Наблюдение, которое стоит проверить следующим перечитыванием.** Обе смены
единицы измерения в #156 — «объединение полос вместо одной» и «кусок кромки вместо
кромки» — поймал не тест и не мутант, а кадр: review оба раза смотрел на картинку.
Записей о геометрии представления в реестре уже пять, и ни одна не имеет условия,
которое срабатывает без человеческого взгляда. Если это подтвердится на четвёртой
точке, вывод не про реестр, а про то, чем проверяется слой представления.

Ниже — протокол предыдущей сессии.

В ту же сессию **добавлено тринадцать** записей, все по предложению независимого
review: три от ветки #127 — выход за именованный регион `Main.cs` (первый круг),
необязательность параметра `scale` внутри `BuildTooltip` и мёртвое присваивание в
`verify.ps1` (подтверждающий круг); три от ветки #52 — состав `perimeter` в
контракте, невоспроизводимые «16 / 15» и узость мутационного прохода; семь от
ветки #128/#125. Добавление перечитыванием не является и в таблицу выше не
входит.

**Перечитывание 2026-08-01, сессия по бою и масштабу.** Записей на момент
перечитывания — **24**, до добавления девяти этой же сессией; текущее число даёт
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md |
rg -c '^\| 2026-'`, и оно намеренно больше: команда считает состояние на сейчас, а
не на момент перечитывания. Ни одно условие повышения не сработало, удалять
нечего.
Три координационные сессии не пролежала ни одна запись: одиннадцать записей от
2026-07-31 проходят **второе** перечитывание, тринадцать от 2026-08-01 —
**первое**. Ближайшая к срабатыванию — запись про выход задачи #127 за
именованный регион `Main.cs`: её условие требует второй одновременной задачи,
реально пишущей в этот файл. В блоке, запущенном этой сессией, `Main.cs` держит
единственная задача (#139), поэтому условие не сработало; это проверено при
раздаче партиции, а не предположено.

В ту же сессию **добавлены девять** записей, все по предложению независимого review:
недокументированный каталог `spikes/` и `parentCommit` манифеста (ветка #142); вырождение свойства #52 в
смешанном случае и молчаливая копия половины толщины штриха (ветка #139); незащищённый порядок уровней значимости и нулевой запас бюджета панели (ветка #140); подпись однотайловой комнаты снаружи своей обводки, третье незащищённое звено проводки подписи и два сравнения референсной величины с экранным потолком (#139, круги 2–3).
Добавление перечитыванием не является и в таблицу выше не входит.

Тринадцать записей за одну сессию против одиннадцати, накопленных за всё
предыдущее время, — это само по себе наблюдение, а не бухгалтерия. Оно означает
одно из двух: либо review стал находить больше, либо реестр начал принимать то,
что раньше становилось Issue. Разбирать это надо на следующем перечитывании, с
числами, а не сейчас.

**Третье перечитывание 2026-08-01, сессия ленты владения и кромки стены.** Записей
на момент начала сессии — **33**; число получено
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на `f03f41f`, той же командой считается и текущее состояние.

Это первая сессия, на которой сработало правило трёх перечитываний: одиннадцать
записей эпохи 2026-07-31 (десять с этой датой плюс запись по подтверждающему кругу
PR #124, датированная следующим днём) дожили до **третьего** разбора. Разобраны
поимённо:

- **Правило версионирования снапшота (#105)** — условие переформулировано, потому
  что было ненаблюдаемым по собственной формулировке: «изменение, прошедшее в
  `main` **незамеченным**», заметить нельзя. Новая формулировка ловит момент
  правки, а не её последствие.
- **`includeEvents` (#105)** — условие наблюдаемо и проверено командой:
  `rg -n 'WriteState\(' -g '*.cs'` даёт единственный вызов. Запись остаётся.
- **Запись о выводе 8 калибровки (#121)** — **удалена**, причина своя, не общая:
  её условие звучит «оценка блока, построенная **по этому выводу**, снова
  промахнётся», а вывод 8 переехал в `archive/CALIBRATION_HISTORY.md`, тогда как
  оценки строятся по трём выводам, оставленным в `MULTI_AGENT_WORKFLOW.md`.
  Сработать это условие больше не может.
- **Записи о выводах 9 и 11 калибровки (#121)** — **остаются**, и это исправление
  ошибки той же сессии. Первая редакция этого разбора удалила все три по общей
  посылке «условие каждой начинается словами „оценка, построенная по этому
  выводу“». Независимый review PR #155 показал, что посылка верна только для
  вывода 8: условие записи 9 — «появление строки калибровки с вложенным
  субагентом», условие записи 11 наполовину — «оборванный прогон, стоивший
  заметно дороже», и ни то, ни другое от выводов калибровки не зависит. Обе
  восстановлены дословно. Отдельно опровергнута и вторая половина посылки:
  «в архиве — значит ни на что не опирается» неверно, `MULTI_AGENT_WORKFLOW.md`
  цитирует архивный вывод 14, а оговорка про вложенного субагента прямо помечена
  там как не подлежащая архивации.
- **Три записи о разделе «Темп разработки» (#123)** — остаются, условия
  наблюдаемы и ни одно не сработало. Ближайшая к срабатыванию — запись про
  правило 7: её условие требует **спора** о том, нужна ли команда `MERGE #N`, и в
  этой сессии вопрос действительно вставал на PR #150, но спором не стал —
  `.claude/commands/coord.md` отвечает на него прямо, и обе стороны прочитали
  ответ одинаково. Запись про четвёртое ослабляющее правило проверена отдельно:
  PR #150 нового ослабления не вводит, он задаёт значение параметра правила 14.
- **Планка бегства 80 % и мёртвый рукав `refused_injured` (#124)** — остаются, не
  сработали. У второй появился названный адресат:
  [#154](https://github.com/anshushunov/dungeon-fortress/issues/154) включает
  `refused_injured` в свой scope, и если задача его закроет, запись уйдёт вместе
  с ней.
- **Кадры #83 в `PROTOTYPE_GRAYBOX.md` (#124, подтверждающий круг)** — остаётся,
  условие наблюдаемо: кадр снимается по команде из документа, и расхождение
  видно сразу.

**Порядок применения правила: предложение координатора отклонено, вместо него
изменена единица возраста.** Координатор предлагал добавить правилу третий исход
— «разобрана, условие подтверждено, остаётся». Независимый review PR #155
показал, что предложение несло в себе сброс счётчика, а он делает храповик
недостижимым по устройству: реестр перечитывает каждая сессия, поэтому «три
сессии без изменения» не набрала бы ни одна запись никогда. Отсюда выбор в пользу
другого лечения — исходов по-прежнему **два**, а изменилась единица, в которой
считается возраст: gate-решения вместо сессий, см. раздел «Кто и когда
перечитывает».

Записи, разобранные 2026-08-01 и оставленные с наблюдаемым несработавшим
условием, начинают отсчёт заново не потому, что их перечитали, а потому, что
переходная оговорка нового правила назначает началом отсчёта gate `PROMOTE` по
срезу 2.

Ниже, **до конца этого абзаца**, — прежний текст разбора, сохранённый как
протокол; абзацы после него («Повышено две записи», «Добавлено семь записей»,
«Удалены сверх разбора», «Наблюдение прошлой сессии») — действующий протокол
сессии, а не отклонённое предложение. Правило предлагает два исхода —
переформулировать условие или удалить запись. Оба применены там, где условие
оказалось дефектным. Записи с наблюдаемым и несработавшим условием оставлены, и
сам этот разбор был объявлен для них исходом. **Это и есть отклонённое
предложение**; действующий порядок — абзацем выше.

**Повышено две записи.** Третье незащищённое звено проводки подписи (#139, круг 3)
— сработало названное условие «любая правка `DrawRoomLabel`»: правку внесла #147,
и review её ветки перепроверил мутанта на новом дереве, получив 451/451 зелёных.
Заведена [#153](https://github.com/anshushunov/dungeon-fortress/issues/153).
Незащищённый порядок уровней `StoryWeight` (#140) — получил наблюдаемое
последствие иначе, чем предполагало его условие: #145 вывела ту же шкалу на экран
по умолчанию, и на фикстуре `neglected` шапка ленты сообщает «0 значащих» о
партии, вымирающей от истощения. Заведена
[#154](https://github.com/anshushunov/dungeon-fortress/issues/154); запись удалена
как техдолг, у которого появился исполнитель.

**Добавлено семь записей**, все по предложению независимого review: **три** от
ветки #147 — широкое исключение юга в `RoomWallClearanceTests`, ослабленный
потолок лестницы и ретроспективная запись о неполноте #139; **четыре** от PR #152
— отсутствие дополненного худшего случая у ленты, невоспроизводимый замер
заголовка в `evidence/145-bound.json`, устаревшее «лента прокручивается» в
`PROTOTYPE_GRAYBOX.md` и два ослабленных утверждения в `HudTextTests`. Разбивка
исправлена по находке review PR #155: прежняя редакция называла 4 и 3 и числила
среди добавленных запись про формулировки 10,0, которая была добавлена и удалена
внутри этой же сессии — её закрыл круг доработки #147, — а фактически добавленную
запись про замер заголовка не называла. Добавление перечитыванием не является и в
таблицу выше не входит.

**Удалены сверх разбора одиннадцати, причина на каждую.** Запись про молчаливую
копию `BorderStrokeHalfWidth` — предмет исчез: `RoomGeometry.BorderStrokeWidth`
стала публичной, адаптер читает её вместо литерала, связь закреплена проверкой и
мутантом (#147, PR #151), и это подтвердил независимый review ветки. Запись про
два сравнения `32 / 4.0` в `RoomGeometryTests.cs` — предмет исчез там же: оба
сравнения заменены на `RoomGeometry.MaximumBorderInset`, величина референсная и
привязана к `CameraView` исполняемо. Обе удалялись первой редакцией без
записанной причины, чего требует раздел «Когда запись повышается до Issue»; на
обе ссылались docstring в `RoomGeometry.cs`, и эти ссылки поправлены тем же
набором изменений, иначе после merge код утверждал бы о реестре неправду.

**Наблюдение прошлой сессии проверено числом.** Прошлое перечитывание отметило,
что тринадцать записей за одну сессию против одиннадцати за всё предыдущее время
означают одно из двух: либо review стал находить больше, либо реестр начал
принимать то, что раньше становилось Issue. Эта сессия даёт данные против второй
версии: при семи новых записях два review той же сессии предложили и **два
повышения до Issue**, и **пять возвратов исполнителю** — то есть разделение
исходов работает, а не сползает в реестр. Считать тенденцию по трём точкам рано;
следующее перечитывание получит четвёртую.

**Удалено 2026-08-01** — запись от 2026-07-31 (review PR #121) о том, что раздел
`NOW` обосновывает последовательность #117 → #115 ссылкой на ADR 0017 §3 вместо
базового файлового правила. Причина удаления: **предмет исчез**. #117 закрыт
(PR #124), раздел `NOW` переписан в этой же сессии, и обоснования, к которому
относилась запись, в документе больше нет. Ни условие повышения, ни сама находка
к новому тексту `NOW` не применимы.

## Что сюда не кладётся

- находка, из-за которой не выполнен критерий готовности своей Issue, — она
  возвращается исполнителю кругом доработки;
- находка с наблюдаемым последствием — она становится Issue;
- **утверждение документа о репозитории, оказавшееся ложным.** Оно не проходит
  проверку на «нет наблюдаемого последствия» по букве — игрок его не видит, ни
  одна проверка от него не краснеет, — и именно поэтому названо здесь отдельно:
  это дефект самого документа, он правится в документе тем же набором изменений.
  Отложить его сюда значит оставить в репозитории утверждение, которое читатель
  примет за факт. Ровно на таком утверждении закрыт **черновик ADR 0018**
  (PR #111) — не следует путать с действующим
  [ADR 0018](../decisions/0018-creature-memory-of-place.md), который получил этот
  номер 2026-07-31 и решает другой вопрос. Как не создавать такую копию —
  правило 8 раздела [«Темп разработки»](../product/ROADMAP.md#темп-разработки);
- продуктовая идея, вопрос или наблюдение владельца — они идут в coordination
  inbox [#33](https://github.com/anshushunov/dungeon-fortress/issues/33);
- незакрытый вопрос, требующий решения, — он идёт в
  [`OPEN_QUESTIONS.md`](../OPEN_QUESTIONS.md);
- технический долг, который кто-то намерен исправить в ближайшем блоке, — у него
  есть исполнитель, значит есть и Issue.
