# Реестр долга

Статус: действует  
Дата обновления: 2026-08-08

## Что здесь лежит

Находки независимого review, у которых **нет наблюдаемого последствия** — ни для
игрока, ни для реально гоняемой проверки. Правило принято владельцем 2026-07-31 и
записано как правило 4 в
[`DEVELOPMENT_PACE.md`](DEVELOPMENT_PACE.md).

Находка с наблюдаемым последствием становится Issue и получает место в очереди.
Находка без последствия попадает сюда и слота в очереди не получает.

## Зачем реестр существует

У review-порождённых задач **нет продуктового сигнала приоритета**: их некому
ранжировать по ценности для игрока. Playtest-порождённая задача приходит с
ответом на вопрос «что мешало владельцу играть»; review-порождённая приходит с
ответом на вопрос «что неточно в коде», и обе попадают в одну очередь, где вторая
конкурирует с фичей на равных, не имея того, чем эта конкуренция решается.

Реестр разделяет два действия, которые раньше были одним: **зафиксировать
находку** и **дать ей слот в очереди**. Первое остаётся обязательным, второе
становится решением с названным условием.

Чего реестр **не** делает: он не заменяет review и не сокращает его. Он не
опирается на утверждение, что очередь инфраструктурных задач замкнута сама на
себя, — это утверждение проверялось по репозиторию 2026-07-31 и не
подтвердилось: обеспечивающие задачи порождает и playtest (#99, #86), и
исполнитель задачи (#42).

Это **не** список «починим потом». Список без условия выхода — это удалённая
находка с лишним шагом, поэтому у каждой записи обязательны и условие повышения,
и срок следующего перечитывания.

## Формат записи

| Дата | Откуда | Что | Почему без последствия | Что сделает её задачей | Срок с | Переформулировок |
|---|---|---|---|---|---|---|

- **Дата** — когда находка попала в реестр.
- **Откуда** — review какого PR её нашёл. Без источника запись нельзя перечитать.
- **Что** — сама находка, с файлом или местом, а не «неточность в снапшоте».
- **Почему без последствия** — объяснение, почему сегодня ни игрок, ни
  запускаемая проверка от неё не страдают. Это то, что перечитывание проверяет
  заново.
- **Что сделает её задачей** — наблюдаемое событие, при котором запись
  повышается до Issue. Запись без названного условия неотличима от забытой и в
  реестр не принимается.
- **Срок с** — gate-решение, последний на момент добавления записи, либо на
  момент последней переформулировки условия. Возраст записи — число gate-решений
  в журнале [`GATE_DECISIONS.md`](../product/GATE_DECISIONS.md), произошедших **после** этого
  gate. Ответ на вопрос «просрочена ли запись» (три и более) читается из этой
  колонки и журнала, а не восстанавливается по истории git. Механика — раздел
  «Кто и когда перечитывает».
- **Переформулировок** — сколько раз за жизнь записи переформулировано её
  условие (колонка «Что сделает её задачей»): 0 или 1. Вторая переформулировка
  той же записи равносильна удалению — граница раздела «Кто и когда
  перечитывает».

## Кто решает, что последствия нет

**Исход по каждой находке предлагает сам review** — возврат исполнителю, Issue
или запись сюда. Координатор соглашается либо повышает находку до Issue; понизить
её до реестра против предложения review он не может.

Причина в том, кому достаётся выгода от ответа: «наблюдаемого последствия нет»
разблокирует merge, а сторона, чей merge разблокируется, не выдаёт этот ответ
себе сама. Повышение работает в одну сторону свободно, понижение — только по
предложению проверяющего.

## Когда запись повышается до Issue

Запись уходит из реестра в очередь по одному из трёх путей:

1. **Сработало названное условие** — произошло событие из колонки «Что сделает её
   задачей». Тогда координатор заводит Issue и удаляет запись отсюда, ссылаясь на
   неё в теле Issue.
2. **Находка получила наблюдаемое последствие иначе** — например, тот же дефект
   пришёл с playtest. Тогда приоритет задаёт playtest, а запись реестра
   приводится в Issue как уже известное описание.
3. **Владелец решил разобрать накопленное** при свободной очереди.

Обратного пути «поживём — увидим» нет: запись либо повышается, либо остаётся с
тем же условием, либо удаляется с записанной причиной удаления.

## Кто и когда перечитывает

**Правило счёта возраста и граница переформулировок записаны один раз — здесь.**
`.claude/commands/coord.md` на это правило ссылается, а не пересказывает его;
расхождение между этим разделом и командой — дефект документа.

- **Каждая координационная сессия** перечитывает реестр вместе с coordination
  inbox [#33](https://github.com/anshushunov/dungeon-fortress/issues/33) — это
  шаг разбора входящих в `.claude/commands/coord.md`. Результат называется
  владельцу одной строкой блок-отчёта: сколько записей, какие условия сработали,
  что повышено и что удалено. «Реестр пуст» — тоже результат и тоже называется.
- **Запись, пережившая три gate-решения** без изменения условия, разбирается
  явно: либо условие переформулировано, потому что оно ненаблюдаемо, либо запись
  удаляется с причиной. Условие, которое невозможно заметить, — не условие.

  **Единица — gate-решение, а не координационная сессия.** Прежняя редакция
  мерила возраст в сессиях и писалась, когда сессия была примерно днём; 2026-07-31
  и 2026-08-01 сессий было по три за день, и одиннадцать записей «состарились» за
  сутки, будучи по смыслу правила молодыми. Gate-решение — единица, которую нельзя
  нащёлкать: оно требует, чтобы владелец сыграл. Запись, пережившая три его захода
  в игру и ни разу не понадобившаяся, мертва по существу, а не по календарю.

  **Срок записан в самой записи, колонка «Срок с»** (формат записи выше). Она
  называет gate-решение, последний на момент добавления записи либо на момент
  последней переформулировки условия. Пережитыми считаются gate-решения,
  произошедшие **после** названного: само «Срок с» gate произошло до добавления
  записи и пережитым не считается. Возраст записи — число gate-решений в журнале
  [`GATE_DECISIONS.md`](../product/GATE_DECISIONS.md) от gate, следующего за «Срок с», до
  последнего включительно, и «просрочена ли запись» (три и более) читается из
  записи и журнала, а не восстанавливается по истории git. Считается командой по
  журналу, якорь — строка с названным в «Срок с» gate; возраст — число gate в
  выводе минус один (сам «Срок с»). Пример для записи с «Срок с: `CONTINUE`
  (блок боя), 2026-08-02»:

  ```bash
  awk '/^\| 2026-08-02 \| \*\*Блок боя — правило подхода/,0' \
    docs/product/GATE_DECISIONS.md \
    | rg -o '\*\*`(CONTINUE|ADJUST|PROMOTE|DEFER|STOP)`\*\*'
  ```

  → `CONTINUE` (блок боя), `CONTINUE` (срез «цена памяти о месте») — два gate в
  выводе, возраст записи 2 − 1 = 1: пережит только `CONTINUE` (срез «цена памяти
  о месте»), запись не просрочена. Общее число gate-решений с начала отсчёта (от
  «Срез 2» до конца журнала) даёт та же команда с якорем на строку среза 2.

  **Перечитывание счётчик не сбрасывает.** Реестр перечитывает каждая сессия, и
  если бы перечитывание обнуляло срок, ни одна запись не дожила бы до разбора
  никогда. Срок считается от последнего изменения **условия**; пометка о том, что
  запись перечитана и условие подтверждено, изменением условия не является.

  **Переформулировка ограничена одной.** Переформулировать условие записи можно
  не больше одного раза за её жизнь: вторая переформулировка той же записи
  равносильна удалению. Счётчик переформулировок — колонка «Переформулировок»
  (формат записи выше): 0 или 1. При переформулировке колонка «Срок с» переносится
  на текущий gate, а «Переформулировок» увеличивается на 1; запись, у которой
  «Переформулировок» уже 1, при следующем разборе удаляется с причиной, а не
  переформулируется. Так закрыта дыра, из-за которой запись можно было держать
  вечно, трогая её условие раз в три gate (найдена независимым review PR #158,
  заведена [#159](https://github.com/anshushunov/dungeon-fortress/issues/159)).

  **Переходная оговорка 2026-08-01 снимается явно.** Оговорка назначала началом
  отсчёта для записей, существовавших на 2026-08-01, gate `PROMOTE` по срезу 2.
  Её субъекты — девять записей, просроченных по букве нового правила уже в день
  его введения. Все они разобраны перечитыванием 2026-08-03 (37 записей эпохи до
  этого включительно удалены, ни одна не повышена и не переформулирована), новых
  записей той эпохи в реестре нет, и оговорка теряет смысл: каждая запись несёт
  свой «Срок с» в себе, и он виден чтением. Причина снятия, как и причины
  введения, — то же правило трёх gate, и снятие не меняет единицу возраста.
- **Каждый gameplay gate** — реестр просматривается на предмет записей, которые
  касаются той же области, что закрываемый слайс: дешевле исправить в уже
  поднятом контексте, чем отдельной задачей.

Реестр, который никто не открывает, эквивалентен удалённой находке, поэтому
правило перечитывания является частью правила, а не пожеланием.

## Записи

| Дата | Откуда | Что | Почему без последствия | Что сделает её задачей | Срок с | Переформулировок |
|---|---|---|---|---|---|---|
| 2026-08-07 | review PR #299 (Issue #282), находка «minor» | `Get-ReadingPackage` (`scripts/agent/take-task.lib.ps1:233,235`) матчит подстроки `src/DungeonFortress\.Simulation`/`src/DungeonFortress\.Presentation` в теле Issue **в любом контексте**, без учёта секции: Issue #259 пишет в «Чужое, не трогать» — `src/DungeonFortress.Simulation/**` — и получает область `simulation`; Issue #243 пишет в non-goals «не трогать код — ни `Main.cs`, ни `src/DungeonFortress.Presentation/**`» — и получает область `presentation`. Проверено мной чтением обоих тел Issue (`gh issue view 259/243 --json body`) — оба совпадения лежат внутри секций «не трогать» | Направление ошибки безопасное — лишнее чтение, не пропуск нужного документа; критерий готовности #282 «ни одна задача не провалилась из-за непрочитанного» от этого не страдает | Первая задача, чей пакет из-за этого вырос настолько, что съел выигрыш от #282, — либо замер «после» по критерию #282 (см. #303), показавший рост вместо ожидаемого сокращения | «Координатор дважды заложил новое процессное правило», 2026-08-07 | 0 |
| 2026-08-07 | review PR #299 (Issue #282), находка «minor» | Таблица «Что подтягивается по признаку задачи» (`docs/engineering/AGENT_ENTRY.md:43-50`) и `$catalog` в `Get-ReadingPackage` (`scripts/agent/take-task.lib.ps1:205-226`) — два независимых места одного и того же соответствия «область → документы». Сверено мной построчно по всем шести областям (`simulation`, `presentation`, `headless`, `art`, `product`, `tooling-docs`) — сейчас идентичны; механизма, ловящего расхождение при правке одного без другого, нет. Прецедент решения в репозитории есть: маркеры `<!-- stage-table:begin/end -->`, сверяемые `scripts/test-verify-stages.ps1` | Сегодня оба места идентичны (сверено построчно), ни один агент не получал противоречивый пакет чтения | Первое расхождение — агент читает одно, документ обещает другое. **Условие требует человеческого взгляда, и это признаётся прямо: автоматической сверки этих двух мест сегодня нет (в отличие от прецедента `<!-- stage-table -->`), и заметить расхождение при правке одного без другого может только тот, кто читает оба места разом, — исполнитель во время работы или координатор при следующей правке одного из них** | «Координатор дважды заложил новое процессное правило», 2026-08-07 | 0 |
| 2026-08-07 | review PR #299 (Issue #282) | jq-команда в теле PR #299 (раздел «Риски и ограничения», отложенное измерение «после» по #282) считает `Read`-вызовы по документам старого списка плоско по всем `*/subagents/*.jsonl`, без `select` по типу агента (review вперемешку с записывающими) и без деления на их число, хотя тот же абзац называет способ «по recording-субагентам одного типа/`tier`». Проверено мной прямым чтением команды в теле PR — ни фильтра по типу агента, ни нормировки в ней нет | Команда ещё не исполнялась для вывода — измерение «после» отложено до появления транскриптов после мержа #299/#300, вывода по числу ещё не делали | Первая попытка снять «после» по критерию #282 — фильтр по типу агента и нормировка на их число должны быть добавлены в команду **до** того, как число попадёт в вывод, а не после | «Координатор дважды заложил новое процессное правило», 2026-08-07 | 0 |
| 2026-08-07 | review PR #300 (Issue #284) | Раздел «Партиция» Issue #284 называл своими файлами `scripts/verify.ps1`, `scripts/test-verify-stages.ps1`, `evidence/284-*`, `docs/engineering/ENVIRONMENT_SETUP.md` и не называл ни в «своих», ни в «чужих» `scripts/GodotTools.ps1` и `scripts/HudVerification.ps1` — при том что `Write-VerifyDiagnostic`, ядро механизма маршрутизации вывода, определена в `GodotTools.ps1:11` (проверено мной — `rg -n "function Write-VerifyDiagnostic" scripts/GodotTools.ps1` → строка 11). Исполнитель PR #300 сам называет это в разделе о расходе токенов: пришлось «read and reason about... `GodotTools.ps1` and `HudVerification.ps1` end to end», не будучи названными партицией | Коллизии не произошло — ни одна другая задача сессии эти два файла не держала | Вторая Issue, чья секция «Партиция» пропустит файл, без которого работа невыполнима, — тогда это уже не оплошность, а способ писать партиции. **Условие требует человеческого взгляда, и это признаётся прямо: сравнение фактически необходимых файлов с названными в «Партиции» не автоматизировано, и пропуск увидит только координатор, составляющий следующую партицию, либо исполнитель, наткнувшийся на недостающий файл в процессе работы** | «Координатор дважды заложил новое процессное правило», 2026-08-07 | 0 |
| 2026-08-07 | review PR #298 (Issue #294), подтверждающий круг, реплика координатора | Таблица круга доработки PR #298 называла находки A и C «устаревшими к моменту разбора», хотя коммит с исправлением `f7ef36a` датирован 2026-08-07 03:45:55, а последний коммит, запушенный на момент, когда подтверждающий круг читал PR, — `1d778cb`, 03:27:56 (проверено мной — `git log --format='%H %ad' --date=iso-strict -1 f7ef36a` и то же для `1d778cb`, оба значения совпадают с текстом реплики). Находки были верны против фактически опубликованного состояния, а исправление появилось позже, а не раньше | Уточнение касается только формулировки таблицы доработки самого PR #298; содержимого `GATE_DECISIONS.md` и `DEBT_LEDGER.md` не касается — оба проверены отдельно и признаны корректными той же репликой | Второй случай, когда таблица доработки называет находку «устаревшей» вопреки логам проверяющего, — тогда это шаблонная ошибка круга доработки, а не разовая неточность | «Координатор дважды заложил новое процессное правило», 2026-08-07 | 0 |
| 2026-08-07 | координатор, сессия 2026-08-07 (PR #293) — не находка review, исход координатора | **Координатор структурно не может провести решение владельца в журнал через реле.** Исполнитель PR #293 отказался вносить санкцию владельца в `GATE_DECISIONS.md`, потому что единственным её основанием был пересказ координатора, а прямого сообщения владельца в транскрипте агента нет и быть не может — агент получает только реле. Предохранитель харнесса распознал это как непроверяемое утверждение и заблокировал правку сначала агенту, затем координатору. Запись сделана только после прямой формулировки владельца в координационном разговоре | Отказ сработал по адресу и ничего не сломал — наоборот, за ту же сессию координатор дважды ошибся на границе полномочий, и оба раза это ловил не он | Первый случай, когда решение владельца **не удаётся** провести в журнал за разумное время из-за этого ограничения — либо появление в процессе способа предъявить агенту подпись владельца, а не пересказ | «Координатор дважды заложил новое процессное правило», 2026-08-07 | 0 |
| 2026-08-07 | review PR #292 (Issue #281), подтверждающий круг | `AdapterSource.TryFindDeclaration` (`tests/DungeonFortress.Presentation.Tests/AdapterSource.cs:57-64`) сканирует ординальную склейку всех файлов `Main.*.cs` и возвращает **первое** совпадение по имени процедуры, а не по содержимому файла. Ревьюер продемонстрировал: файл-обманка `Main.Aaa.cs` с классом, объявляющим `DrawMap(int rockTiles)`, сортируется раньше настоящего `Main.DrawWorld.cs`, и после переименования всех 29 вхождений `rockTiles` → `stoneTiles` в настоящем файле проверка `Assert.Contains("rockTiles", …)` остаётся **зелёной**; полный набор даёт 618 пройдено / 6 не пройдено | Брешь введена самим разрезом #281 (до него читатель открывал один файл, и подмена была невозможна в принципе), а не регресс правки. Требует, чтобы кто-то положил в каталог проекта посторонний файл `Main.*.cs`, объявляющий уже существующую процедуру. Сегодня такого файла нет | Появление в `src/DungeonFortress.Game/` файла `Main.*.cs`, не принадлежащего классу, — либо первая проверка адаптера, прошедшая зелёной на подменённом теле. Лечение известно и дешёвое: отфильтровать найденные файлы по наличию `partial class Main` | «Объём сессии 2026-08-07», 2026-08-07 | 0 |
| 2026-08-07 | review PR #292 (Issue #281), подтверждающий круг | Doc-комментарий `RequiredAdapterFiles` (`WorldDrawPassGuardTests.cs:103`, «Extra files are allowed on purpose») шире правды: верно только для файлов, чей суффикс начинается с заглавной. `Main.spare.cs` роняет тест — `Assert.Equal() Failure: Expected "Main.cs", Actual "Main.spare.cs"` — и сообщение указывает на порядок, а не на настоящую причину | Конвенция именования C# делает такой файл маловероятным, и сегодня его нет | Первый файл `Main.<строчная>.cs` в каталоге проекта, либо следующее касание этого комментария — тогда фраза правится заодно | «Объём сессии 2026-08-07», 2026-08-07 | 0 |
| 2026-08-07 | review PR #292 (Issue #281), подтверждающий круг | `evidence/281-member-map.json` не содержит поля с коммитом, по которому снят граф, — в отличие от соседнего `evidence/281-split-verification.json`, где стоит `"base": "9457d65"`. Пересъёмка после изменения исходников молча даст другой граф, и сравнить будет не с чем | Для этого PR число застыть не может, входы неизменяемы в истории, а несоответствие сразу выдаст поле `base` соседнего отчёта. Отдельно проверено и не является дефектом: `evidence/281-cohesion.json` полностью воспроизводим — ревьюер прогнал коммиченную подкоманду и получил байт в байт тот же файл | Первая пересъёмка карты членов после изменения `Main.*.cs` или `PrototypeWorld.*.cs`. Лечение: поле `"base"` в карте и проверка в подкоманде `cohesion`, что оба входа названы одной базой | «Объём сессии 2026-08-07», 2026-08-07 | 0 |
| 2026-08-07 | review PR #292 (Issue #281), первый круг, находка F4 | `tests/DungeonFortress.Presentation.Tests/AdapterSource.cs:35` — `RelativePath` остался `"src/DungeonFortress.Game/Main.cs"` и подставляется во все сообщения читателя, включая `"{RelativePath} has an unbalanced '{' at offset {open}"`, где offset после разреза — смещение в склейке из одиннадцати файлов, а не в названном файле | Сообщение появляется только при уже сломанном исходнике, и адрес в нём вводит в заблуждение лишь того, кто станет его читать буквально; ни одна проверка от этого не меняет исход. Исполнитель назвал это сознательно не тронутым, review согласился | Первый разбор поломки по этому сообщению — либо следующее касание файла | «Объём сессии 2026-08-07», 2026-08-07 | 0 |
| 2026-08-07 | review PR #293 (Issue #279), подтверждающий круг | Формулировка «при добавлении нового пункта в `DEVELOPMENT_PACE.md` сверка с правилом 11 обязательна и терминологическим поиском по ключевым словам не заменяется» записана в теле PR #293, а не в самом `DEVELOPMENT_PACE.md` рядом с правилом 11. Следующий исполнитель, добавляющий правило 36, читает документ, а не архив старых PR | Корневая причина исходной ошибки — неполная цитата правила 24 в брифе координатора, а не незнание исполнителя, поэтому ценность правки именно для исполнителей ограничена; ни одно действующее правило от этого не становится неверным | Следующий PR, добавляющий нумерованный пункт в `DEVELOPMENT_PACE.md` без сверки с правилом 11 — либо перенос оговорки под правило 11 в самом документе, после чего запись закрывается | «Объём сессии 2026-08-07», 2026-08-07 | 0 |
| 2026-08-07 | review PR #293 (Issue #279), подтверждающий круг | `git push --force-with-lease` после ребейза собственной ветки агента вызвал разовый отказ на **следующей, не связанной** команде — с формулировкой, что force-push требует прямого человеческого согласия. Сам пуш к тому моменту уже прошёл: ревьюер проверил независимо, `git ls-remote origin refs/heads/agent/279-…` даёт `50cddc2`, что совпадает с `headRefOid` PR. Работа не потеряна. Дефект в том, как классификатор разрешений привязывает обоснование отказа к команде: либо утечка состояния между вызовами, либо неверная атрибуция причины | Случай одноразовый, воспроизводимости нет, на код влияния нет; следующий вызов прошёл нормально | Второй такой же случай — тогда появляется воспроизводимость. Это про инструмент, не про код репозитория, отдельной Issue по решению review не заводится | «Объём сессии 2026-08-07», 2026-08-07 | 0 |
| 2026-08-07 | review PR #290 (Issue #280) | `docs/design/PROTOTYPE_01_PREPARE_FOR_RAID.md:2778` (на коммите `cc224f2`): клауза «Вся таблица переизмерена на подземелье Issue #117, вместе с переписанным планом `prepared` (13.2)» осталась в контракте целиком вместо указателя, как соседняя такая же. ~161 B. Не поймана авторским `rg`, потому что паттерн `решение владельца\|независимый review\|PR #[0-9]+` не покрывает слово «переизмерен» | После разреза 32 842 B это единственный осколок на три порядка меньше прежнего расхождения, и он не меняет ни одного числа контракта | Следующая правка раздела 13.4 — либо первый агент, который сошлётся на эту фразу как на действующий контракт. Готовая правка известна: заменить на указатель по образцу соседней клаузы | «Объём сессии 2026-08-07», 2026-08-07 | 0 |
| 2026-08-07 | review PR #290 (Issue #280) | `evidence/280-round2-cuts.json`, записи `S1`–`S5`: поле `origLines_0beb5a9` указывает 3171–3284 (это текст инварианта 11 и раздела 14), тогда как настоящий источник — 3709–3775 и 3814+. Сам перенос текста корректен и дословен, ревьюер проверил полным диффом; дефект только в метаданных | Текст на месте и верен, ошибка в поле, которое ничего не исполняет | Первая попытка воспроизвести перенос по этим координатам — она даст не тот текст | «Объём сессии 2026-08-07», 2026-08-07 | 0 |
| 2026-08-07 | review PR #291 (Issue #283) | Точка отсчёта для правила 34 снята по срезу, заканчивающемуся вчерашним днём, и такой срез нестабилен: транскрипты продолжают дописываться. Исполнитель заявил 87,99 % Opus (36 666 вызовов), ревьюер тем же срезом через несколько часов получил 86,85 % (32 372 из 37 273). Причина не в методе: изолированный замер за 2026-08-06 даёт 58,14 % Sonnet против 41,86 % Opus — переворот относительно фона, и этот один незакрытый день объясняет весь сдвиг с 90,51 % базовой линии | Направление вывода не меняется — доля Opus далеко за порогом 70 % в любом варианте; число никто не подгонял, методика добросовестная и неполная | Проверка критерия Issue #283 через две недели, снятая по диапазону с запасом менее двух закрытых дней, — либо первое расхождение, меняющее вывод «выше/ниже 70 %» | «Объём сессии 2026-08-07», 2026-08-07 | 0 |
| 2026-08-07 | review PR #288 (Issue #286) | Deny-записи `.claude/settings.json` на опасные флаги (`git diff/log/show --output=<путь>` пишет файл; `rg --pre` и `rg --hostname-bin` — два независимых флага `rg` с произвольным исполнением; `fd -x`/`-X`/`--exec`/`--exec-batch` исполняют произвольную команду) матчат **текст** команды буквально (glob/substring), а не разобранные аргументы — обход через склейку строки (`--out""put=x`) или нестандартную запись флага их не ловит. Находка шире одной записи: префиксный Bash-allowlist в принципе не может выразить «read-only» — один и тот же класс обхода у `rg --pre`/`--hostname-bin`, `fd -x`/`-X`/`--exec` и `git diff/log/show --output`. **Обратная сторона той же неточности:** `fd *-x*`/`fd *-X*` матчат подстроку, а не отдельный флаг, — `fd test-x-file` или `fd '.*-x.*regression'` получают отказ, хотя `-x`/`-X` там не флаг, а часть паттерна/пути | Deny-записи покрывают буквальные формы вызова, которые независимый review проверил эмпирически (`git show --output=...`, `git log --output=...`, `git diff --output=...` создают файл; `rg --hostname-bin=...` проходил под старым `rg *--pre*` как `NO-MATCH` до этой строки), и это ровно то, что наблюдалось в измеренных транскриптах `evidence/286-command-frequency.json`: ни один агент не использовал обходную запись флага. Промах требует намеренного нестандартного написания, которого сегодня не было. Ложный отказ `fd *-x*`/`fd *-X*` — направление ошибки безопасное (лишний отказ, не брешь), и среди 296 измеренных вызовов `fd` в транскриптах ни один не содержал `-x`/`-X` как часть паттерна, а не флага | Появление в проекте `PreToolUse`-hook, проверяющего Bash-команды по существу, а не по тексту префикса; либо первый зафиксированный случай, когда агент реально воспользовался одним из перечисленных флагов и записал файл или исполнил команду в обход подтверждения; либо первый зафиксированный случай, когда `fd *-x*`/`fd *-X*` отказал легитимному вызову агента (переформулировано 2026-08-07: добавлено это третье условие вместе со строкой про ложный отказ) | «Потолок боя: читаемый, не красивый», 2026-08-06 | 1 |
| 2026-08-05 | review PR #268 (Issue #259) | Правило вертикального размена пишет в **память** направления и живёт после удара. `_bodyFacing` (`Main.cs:6504-6505, 6520-6521`) — одно значение на тело, а не ответ на тик, и `BodyMotion.Turn` при шаге без боковой части его сохраняет. Значит тело, шедшее влево и ударившее (или получившее удар) строго вверх/вниз, разворачивается в авторскую сторону (`BodyMotion.cs`, `VerticalExchangeFacing`) и **остаётся** так повёрнутым до следующего шага с боковой частью. Правка меняет этим и поведение **бьющего**, которого Issue #259 менять не просила: до неё удар при `dx == 0` не трогал ни одно из двух тел | Ни одна запускаемая проверка на направление не смотрит, а кадра с вертикальным разменом в репозитории нет **по построению**: сцена `--demo-duel` выбирает боковой удар (`DuelScore` доплачивает за совпадение колонки), и оба кадра #259 сняты на нём. То есть сегодня это не видно ни игроку, ни прогону. Названо в трёх местах текстом — doc-комментарий `VerticalExchangeFacing`, абзац `PROTOTYPE_GRAYBOX.md`, эта строка. Исход предложил сам review (запись реестра как альтернатива именованию), выбор исполнителя назван в теле PR #268 | Первый кадр массового боя ([#260](https://github.com/anshushunov/dungeon-fortress/issues/260)), на котором тело после вертикального размена смотрит не туда, куда шло. Условие требует человеческого взгляда на кадр, и это признаётся прямо: направление детерминировано, и выразить «смотрит не туда, куда шло» прогоном сегодня нечем — в снапшот направление не входит (ADR 0011) | `CONTINUE` (проба ADR 0020), 2026-08-04 | 0 |
| 2026-08-07 | review PR #308 (Issue #305) | `scripts/test-verify-stages.ps1`: временный каталог `$sandbox` (создан строкой 149) убирается единственным `try/finally` в самом конце файла — `Remove-TemporaryItemBestEffort -Path $sandbox` на строках 2510-2514. При раннем падении внутри теста, до этого `finally`, каталог остаётся. Проверено мной — ``rg -n -U 'Remove-TemporaryItemBestEffort `\n\s+-Path \$sandbox' scripts/test-verify-stages.ps1`` (многострочный режим `-U`: сам вызов разбит PowerShell-бэктиком на две строки, однострочный `.*` их не пересекает — прежняя команда без `-U` возвращала пусто) даёт единственное совпадение, строки 2511-2512, внутри `finally` строк 2510-2514 | Независимый review PR #308 лично подтвердил, что остатки **пусты** — без reparse point и без файлов, только каталог, и лежат внутри `.artifacts/`, который и так копит артефакты между прогонами по замыслу. Качественно слабее находки, уже исправленной тем же PR — утечки junction на реальные каталоги Godot вне репозитория | Первый остаток, содержащий не пустой каталог, — либо переработка жизненного цикла `$sandbox` в рамках другой задачи по этому файлу | «Мораторий правила 11 снят для правила 35», 2026-08-07 | 0 |
| 2026-08-07 | review PR #309 (Issue #303) | `scripts/token-budget-report.ps1`: роль субагента определяется по первой строке промпта (`$firstLine = ($text -split "backtick-n", 2)[0]`, строка 203 — разбиение по символу новой строки; матч против `$script:ReviewPattern`, строка 149, на строке 205) — защита от ловушки отрицания («ты НЕ исполнитель») держится на том, что слово `review`/`read-only` сегодня всегда лежит в первой строке промпта. Проверено review PR #309 на 21 реальном review-промпте — 0 расхождений | Сегодня такого промпта, где объявление роли перенесено со первой строки, не существует — 0 из 152 транскриптов. Агент, не подошедший ни под один шаблон, уходит в `unclassified` с видимым предупреждением (`scripts/token-budget-report.ps1:615`), а не примешивается молча | Первый промпт, где объявление роли перенесено со первой строки, — он попадёт в `writer` молча | «Мораторий правила 11 снят для правила 35», 2026-08-07 | 0 |
| 2026-08-08 | review PR #342 (Issue #341), подтверждающий круг | Три круга подряд таблица «находка → правка → команда» несла evidence-команды, которые у проверяющего не воспроизводятся, хотя существо правки верно. Формы разные и одной оговоркой не лечатся: **approxidate** — `--since=2026-08-04` без времени считает от текущего времени суток и в ту же секунду даёт пусто; **фраза-якорь, разорванная переносом строки** — однострочный `rg` по фразе «от начала отсчёта» даёт 0, потому что она разбита между двумя строками файла; **escape перевода строки в многострочном `rg -U`** — шаблон с ним не матчит рабочую копию с CRLF. Проверено review: во всех трёх случаях устойчивый вариант команды подтвердил правку, в дерево неверного не попало | Ни одно неверное утверждение в дерево не попало: расходятся не факты, а способ их предъявить. Круг доработки проходит, потому что подтверждающий круг перепроверяет существо, а не только команду | **Четвёртый случай невоспроизводимой evidence-команды** — тогда это не оплошность, а способ писать таблицы круга, и лечится механизмом (прогоном команд таблицы), а не внимательностью. Исход назвал сам review: строка сюда, при повторении — Issue | «Больше здоровья и разброс урона», 2026-08-08 | 0 |
| 2026-08-07 | review PR #304 (Issue #301), проследивший источник | Номер строки `verify.ps1:44` для `$takeTaskTestScript` в теле смерженного PR #299 был неверен (реально строка 58, `rg -n 'takeTaskTestScript' scripts/verify.ps1` подтверждает: определение на 58, вызов на 363) — Issue #301 унаследовала неточность при заведении. Исполнитель #301 нашёл расхождение и назвал его; review PR #304 проследил источник до тела PR #299. Событие уже описано прозой в разделе «Перечитывание и удалённые записи» ниже («Расхождение с постановкой задачи названо, а не замолчано»); эта запись добавляет только forward-looking условие, которого проза не несла | Ни один файл в дереве не пострадал, текущая запись реестра о закрытии несёт верные числа (проверено там же), PR #299 давно смержен | Второй случай, когда число (номер строки, коммит, метрика) из тела PR переносится в постановку следующей задачи без проверки исполнителем или review. **Условие требует человеческого взгляда при чтении новой постановки против источника, и это признаётся прямо** — автоматической сверки чисел тела PR с числами следующей Issue сегодня нет | «Мораторий правила 11 снят для правила 35», 2026-08-07 | 0 |
| 2026-08-10 | review PR #362 (Issue #349) | `--seed` тихо сужает собственную проверку детерминизма движка: `VerifyDeterministicFixture` (`src/DungeonFortress.Game/Main.Verification.cs:790-796`, вызовы `Main.cs:401,408`) дважды перечитывает документ фикстуры с диска и сравнивает две копии между собой, а `seedOverride` не видит. При `-Seed` прогон печатает `seed: 20260729` и рядом «детерминизм подтверждён», хотя про этот мир не проверено ничего. Путь достижим через санкционированный вход уже сегодня — review прогнал `run-game.ps1 -Fixture baseline -Seed 20260729 -VisibleSmoke` → `EXIT=0`, `{"event":"godot_visible_smoke","status":"ok","fixture":"baseline","seed":20260729}` | Ни один runner и ни один скрипт не сочетает `--seed` со smoke-режимом; путь существует, но им никто не ходит | Первый runner или скрипт, сочетающий `--seed` со smoke-режимом, либо первый отчёт, в котором «детерминизм подтверждён» прочитано как утверждение про seed из аргумента | «Срез 5 — первый заход владельца», 2026-08-10 | 0 |
| 2026-08-10 | review PR #362 (Issue #349), **к самому PR отношения не имеет** | Временные каталоги `verify.ps1` не подметает никто: на машине накопилось **47** каталогов `C:\gamedev\df-verify-<8hex>` — команда `ls -d /c/gamedev/df-verify-*` печатала 47 строк до уборки, сделанной review. [`ENVIRONMENT_SETUP.md`](ENVIRONMENT_SETUP.md) объявляет неудачу уборки предупреждением по принятому решению, но выживших после предупреждения не удаляет ни один прогон. Поведение досталось от `verify.ps1` без изменений, PR #362 его не вносил | Каталоги не мешают ни прогону, ни игроку: каждый следующий прогон делает свой. Это расход диска, а не поведение | Первый прогон, упавший из-за места на диске или из-за коллизии имён, либо первый агент, потративший круг на разбор чужого выжившего каталога | «Срез 5 — первый заход владельца», 2026-08-10 | 0 |

## Перечитывание и удалённые записи

Перечитывание фиксируется здесь, а не в отчёте: правило «запись, пережившая три
gate-решения без изменения условия, разбирается явно» без записанного следа не
срабатывает. До 2026-08-01 то же правило мерило возраст координационными
сессиями; строки таблицы ниже, датированные тем днём, писались в той единице, и
колонка «Прошло gate» для них пуста. С перечитываний 2026-08-02 колонка «Прошло
gate» несёт число gate-решений, прошедших к моменту перечитывания от начала
отсчёта (gate `PROMOTE` по срезу 2) — по ней и по «Срок с» записей проверяется,
не просрочена ли запись.

| Дата | Прошло gate | Записей до | Повышено | Удалено | Осталось |
|---|---|---|---|---|---|
| 2026-08-01 | — | 12 | 0 | 1 | 11 |
| 2026-08-01, сессия по бою и масштабу | — | 24 | 0 | 0 | 24 |
| 2026-08-01, сессия ленты владения и кромки стены | — | 33 | 2 | 3 | 28 |
| 2026-08-01, блок боя | — | 35 | 1 | 0 | 34 |
| 2026-08-02, блок #171/#77/#165 | 2 | 39 | 1 | 0 | 38 |
| 2026-08-02, третий круг PR #181 | 2 | 41 | 1 | 0 | 40 |
| 2026-08-03, блок #201/#210 | 3 | 44 | 0 | 37 | 7 |
| 2026-08-04, блок #201/#210 закрыт | 4 | 9 | 0 | 0 | 9 |
| 2026-08-04, перед пробой ADR 0020 | 4 | 10 | 0 | 0 | 10 |
| 2026-08-04, третий случай по правилу 17 | 4 | 10 | 1 | 0 | 9 |
| 2026-08-04, блок пробы ADR 0020 закрыт | 4 | 9 | 0 | 0 | 9 |
| 2026-08-05, шесть записей блока боя разобраны ([#261](https://github.com/anshushunov/dungeon-fortress/issues/261)) | 5 | 11 | 0 | 6 | 5 |
| 2026-08-08, блок «момент истины» закрыт | 7 | 24 | 1 | 3 | 20 |


**Перечитывание 2026-08-08, координационная сессия после блока «момент истины».**
Записей на момент начала — **24**
(`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на коммите `e21c9eb`); после этой правки та же команда даёт **20**.

**Прошедших gate — 1, и единица засчитана по семантическому определению, а не по
маркеру.** Владелец сыграл 2026-08-08 и тем же сообщением принял решение «больше
здоровья и разброс урона» ([#336](https://github.com/anshushunov/dungeon-fortress/issues/336));
строка журнала за 2026-08-08 маркером `CONTINUE`/`ADJUST`/… не обёрнута, как и
строка «Потолок боя» 2026-08-06, которую разбор 2026-08-07 ([#311](https://github.com/anshushunov/dungeon-fortress/issues/311))
уже засчитал гейтом по той же причине. Прецедент прямой, и следовать ему
обязательно: определение раздела «Кто и когда перечитывает» спрашивает, играл ли
владелец, а не поставлен ли маркер.

Первая редакция этого протокола засчитала **0**, рассуждая, что gate по срезу 3
не выдан. Довод неверен и снят: правило считает **gate-решения**, а решение
владельца после игры им является независимо от того, закрыт ли слайс. Ошибку
нашёл независимый review PR #342, не координатор. Расхождение маркера и
определения — предмет [#319](https://github.com/anshushunov/dungeon-fortress/issues/319).

**Повышена одна запись — от 2026-08-03 (review PR #198, Issue #186), «мутационные
подстановки живут вне дерева».** Названное условие сработало: первый файл
`evidence/*-mutations.json`, добавленный после `evidence/303-mutations.json`, —
это `evidence/312-mutations.json`, и `jq`-команда самой записи даёт на нём
`{"total": 8, "allLiteral": false}`. Список новых файлов снят устойчивой
командой — **не** `--since=2026-08-07`, которую git разрешает от текущего времени
суток и которая поэтому невоспроизводима:

```bash
git log --diff-filter=A --name-only --pretty=format:   --since=2026-08-07T00:00:00 -- 'evidence/*mutations*.json' | rg '\S' | sort -u
```

**Но условие поймало имя поля, а не отсутствие дословности, и это сказано прямо.**
Подстановки в `evidence/312-mutations.json` записаны дословно — в поле `mutation`:
`jq -r '.mutants[0] | .id + ": " + .mutation'` даёт
`M1: PrototypeTuning.cs: LoyaltyFearWound = 8 -> 0 (обнулено одно слагаемое
начисления)`. Ни `change`, ни `before`/`after`, ни `replacement`, ни `edit` —
поэтому проверка записи считает файл нарушением, хотя по существу он им не
является. Это тот же класс, что [#319](https://github.com/anshushunov/dungeon-fortress/issues/319):
команда, записанная в документе, даёт неверный ответ на собственный вопрос
документа.

Исход — **повышение, а не переформулировка**: колонка «Переформулировок» у записи
уже равнялась 1, а вторая переформулировка равносильна удалению. Заведена
[#340](https://github.com/anshushunov/dungeon-fortress/issues/340). Удаление записи и этот протокол
сделаны координационной сессией, а не задачей #340: файл реестра остаётся целиком
за [#319](https://github.com/anshushunov/dungeon-fortress/issues/319), и повышение
записи — действие координатора по разделу «Когда запись повышается до Issue», а не
работа держателя файла.

**Удалены три записи группы «Срок с» = `CONTINUE` (слайс «существо без работы
уходит с места боя»), 2026-08-04** — возраст 3 при пороге 3. Каждое условие
проверено отдельно от возраста, командой, а не суждением:

| Запись (источник) | Условие | Команда / способ проверки | Результат | Исход |
|---|---|---|---|---|
| review PR #256 (Issue #244) — таблица `CHAIN` в `evidence/244-measure-rig-gaps.py` ручная копия углов из `StrikeChain.cs` | Первая правка углов в `StrikeChain.cs` без синхронной правки скрипта | `git log --oneline --since=2026-08-04T00:00:00 -- 'src/**/StrikeChain.cs'` → один коммит `1e58568` — **тот самый PR #256, которым запись и заведена**; правок углов после него нет | Не сработало. Форма даты с `T00:00:00` обязательна: без неё git считает от текущего времени суток и в ту же секунду даёт пусто — та же грабля, что чинится строкой m7 выше | Удалить |
| review PR #256 (Issue #244) — стоимость `DrawRigBody` выросла и ничем не охраняется | Жалоба владельца на плавность в бою **на полном экране тел** либо появление в `verify.ps1` бюджета времени отрисовки | `rg -n 'drawTime\|renderBudget\|frameBudget' scripts/verify.ps1 scripts/HudVerification.ps1` → пусто; журнал gate-решений жалобы на массовый бой не содержит (`rg -n 'массов' docs/product/GATE_DECISIONS.md` находит только план #260) | Не сработало. **Названо честно, почему первая половина не могла сработать:** [#260](https://github.com/anshushunov/dungeon-fortress/issues/260), массовый бой, решением 2026-08-06 переведён из очереди в кандидаты, и владелец полного экрана тел не видел | Удалить |
| review PR #232 (Issue #222), подтверждающий круг — строка легенды потеряла цветовую пару | Владелец назовёт легенду менее читаемой **либо** заголовок «LEGEND» вернётся в отдельную строку | `rg -n 'LEGEND' src tests --glob '*.cs'` → `Main.Hud.cs:612` по-прежнему держит заголовок вложенным в строку (`("LEGEND — amber X = …", 8, "#cbd5e1")`); ни один playtest легенду не называл | Не сработало | Удалить |

**Причина удаления одна для всех трёх и совпадает с применённой к 37 записям
2026-08-03 и к 6 записям 2026-08-05:** условие наблюдаемо, три захода владельца в
игру запись пережила и ни разу не понадобилась. Переформулировка неприменима: ни
одно из трёх условий не оказалось конструктивно незамечаемым — это отличает их от
случая, где переформулировка была нужна именно из-за ненаблюдаемости.

**Тем же набором изменений добавлена одна новая запись** — по предложению
независимого review PR #342 о невоспроизводимых evidence-командах в таблицах
кругов доработки. Добавление перечитыванием не является и в колонки таблицы не
входит, как и в протоколах 2026-08-01, 2026-08-02, 2026-08-03, 2026-08-04 и
2026-08-07. Поэтому строка таблицы говорит «Осталось 20», а команда счёта записей
на финальном состоянии даёт **21**.

**Две записи ждут своей очереди и в таблицу выше не внесены.** Независимый review
PR #337 назначил исход «строка в `DEBT_LEDGER.md`» двум находкам — декоративное
утверждение в `RoomReadingTests.cs` и незаписанный дословно мутант #332. Файл
реестра держит [#319](https://github.com/anshushunov/dungeon-fortress/issues/319),
поэтому обе записи лежат дополнением в её теле и будут внесены её PR.

**Разбор просроченной записи, 2026-08-07 (Issue #311).** Запись от 2026-08-03
(review PR #198, Issue #186 — мутационные подстановки живут вне дерева) несла
«Срок с: `CONTINUE` (срез «цена памяти о месте»), 2026-08-02». Возраст,
названный координатором, — **3**, что при пороге «три и более» делает запись
просроченной. Счёт проверен, а не принят на слово, и **два способа счёта
разошлись**, расхождение названо прямо, а не сглажено:

- Командой раздела «Кто и когда перечитывает»
  (`rg -n '\*\*`(CONTINUE|ADJUST|PROMOTE|DEFER|STOP)`\*\*' docs/product/GATE_DECISIONS.md`)
  после якоря (строка 69) находится только **две** строки-маркера — 2026-08-04
  `CONTINUE` по срезу «существо без работы уходит с места боя» (#201) и
  2026-08-04 `CONTINUE` по пробе ADR 0020 (строки 80 и 86). Формулой «gate в
  выводе минус один» это даёт возраст **2**, не 3 — регэксп один счёт
  координатора не подтверждает.
- Чтение строк журнала за 2026-08-06 (сделано отдельно, как и просила
  постановка) находит третью: **«Потолок боя»** (`GATE_DECISIONS.md:88`) —
  «владелец сыграл дуэльную сцену на `main`... и следом усомнился в самом
  направлении», решение пересмотрело направление визуальной проработки боя.
  Колонка «Решение» этой строки не обёрнута в маркер `CONTINUE`/`ADJUST`/…,
  поэтому регэксп её не находит, но определению раздела «Кто и когда
  перечитывает» — «gate-решение… требует, чтобы владелец сыграл» — строка
  соответствует буквально. Прецедент в самом реестре уже есть: запись про
  deny-записи `.claude/settings.json` несёт «Срок с: «Потолок боя: читаемый,
  не красивый», 2026-08-06» до этой правки.

По семантическому определению («владелец сыграл») после якоря — **три**
gate-решения (2026-08-04 ×2, 2026-08-06), и это совпадает со счётом
координатора. **Возраст подтверждён: 3, запись просрочена.** Обе команды и
разбор — `evidence/311-age-check.json`.

Исход — переформулировка один раз, не удаление: находка жива по существу
(механизма, требующего дословной подстановки, в проекте нет, а прежнее условие
«мутант восстанавливается неоднозначно» было ненаблюдаемым — неоднозначность
никто не проверял). Новое условие сделано проверяемым командой `jq` по
`evidence/*-mutations.json`; факт, на котором оно основано, — с
`evidence/259-mutations.json` (2026-08-05) дословное поле подстановки стало
неформальной практикой (259, 281, 284, 303 подряд; единственное исключение,
`evidence/281-mutations.json` мутант `d-i`, — `git mv` без текстовой
подстановки, полю неоткуда взяться), но нигде не записано как правило.
«Срок с» перенесён на «Потолок боя», 2026-08-06 — последний на момент этой
правки gate по семантическому определению; «Переформулировок» — `0` → `1`.

Тем же набором изменений добавлены **три** новые записи, все по предложению
independent review сессии 2026-08-07: пустые песочницы `scripts/test-verify-stages.ps1`
(PR #308), устойчивость классификатора роли `scripts/token-budget-report.ps1`
(PR #309) и перенос неверного номера строки из тела PR #299 в постановку #301
(PR #304). Добавление перечитыванием не является и в таблицу выше не входит.
Записей на момент начала этой правки — **21**
(`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на коммите до правки); после неё та же команда даёт **24** — три добавленные,
ни одна не удалена и не повышена.

**Закрыто 2026-08-07** — запись от 2026-08-03 (review PR #214, Issue #207) о том,
что docstring `scripts/agent/test-take-task.ps1:31-33` ложно утверждал, что тест
не подключён ни к одной стадии `verify.ps1`. Причина удаления: **предмет исчез**.
Комментарий исправлен в PR #299 (`11f16dc`): `scripts/agent/test-take-task.ps1:31`
теперь гласит «Wired into the "scripts" stage of scripts/verify.ps1
($takeTaskTestScript)». Подключение перепроверено мной напрямую, а не принято на
слово: `$takeTaskTestScript` определена в `scripts/verify.ps1:58`, вызывается на
`scripts/verify.ps1:362-364` — внутри тела стадии `scripts` (сама стадия
объявлена строкой 314, её `Body` занимает строки 315-365).

**Расхождение с постановкой задачи названо, а не замолчано.** Issue #301 при
заведении назвала адреса `scripts/verify.ps1:44` (определение) и
`scripts/verify.ps1:313-315` (вызов) — обе ссылки неточны. `:44` — это
`$guardTestScript`, а не `$takeTaskTestScript`; настоящее определение лежит на
`:58`. `:313-315` — это вводный комментарий в начале тела стадии `scripts`
(«The catalogue is the single source of truth…»), а не место вызова; сам вызов
`$takeTaskTestScript` — на `:362-364`. Факт закрытия эти неточности не меняют:
подключение к стадии `scripts` подтверждено независимо от точных номеров строк,
но точные номера в постановке были неверны, и это стоит назвать по правилу 8, а
не тихо поправить при переносе в реестр.

**Перечитывание 2026-08-05, шесть записей группы «блок боя» разобраны.**
Записей на момент начала — **11**, командой
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на коммите, где журнал уже несёт факт-дату gate по пробе ADR 0020
([#266](https://github.com/anshushunov/dungeon-fortress/issues/266)); после этой
правки та же команда даёт **5**. Прошедших gate — **5**, той же командой, что и
в разделе «Кто и когда перечитывает», с якорем на gate `PROMOTE` по срезу 2 —
пятым стал `CONTINUE` по пробе ADR 0020 (строка журнала `GATE_DECISIONS.md:86`, дата
2026-08-04 после правки #266). Это тот самый gate, который протокол «блок пробы
ADR 0020 закрыт» назвал, не дав ему строки таблицы: разбор шести отложен в
[#261](https://github.com/anshushunov/dungeon-fortress/issues/261) сознательно,
и эта запись — его исполнение.

**Разбираются шесть записей с якорем «Срок с» = `CONTINUE` (блок боя),
2026-08-02** — возраст 3 при пороге 3, список получен той же командой, что и в
протоколе выше (`rg -o '\| \`CONTINUE\` \([^)]*\), [0-9-]+ \|' | sort | uniq -c`
до правки давала **6** для этого якоря). Проверка велась по каждой записи
отдельно от возраста, командами, а не суждением — исполнение урока протокола
2026-08-02, где сработавшее условие пропустили именно из-за того, что
перечитывание смотрело только на возраст:

| Запись (источник) | Условие | Команда / способ проверки | Результат | Исход |
|---|---|---|---|---|
| review PR #175 (Issue #165) — дрейф палитры позы `flinch` | Кадр playtest назовёт `flinch` светлее остальных поз **или** вторая перегенерация любой позы пака edit-режимом | Первая половина — **человеческий взгляд**, и она была наблюдаема: дуэльная сцена #244 (PR #256) рисует риг и `-FlatBody` (прежний пак v2, тот же `goblin_flinch_v2.png`) рядом для A/B — владелец видел позу `flinch` пака v2 на playtest 2026-08-04, и его ответ («на демке плохо видно но движения в целом ок… левый стоит задом») её светлоты не касается (`GATE_DECISIONS.md:86`). Вторая половина — `git log --since=2026-08-02 --oneline -- 'assets/**'` не находит правок PNG пака v2 | Не сработало **и проверено**, а не «не проверялось»: playtest состоялся, поза была на экране, владелец её не назвал | Удалить |
| review PR #178 (Issue #171) — `MemoryYieldsSatiety`/`EatThreshold` два литерала | Первая правка одной константы без другой | `rg -n 'MemoryYieldsSatiety = \|EatThreshold = ' src/DungeonFortress.Simulation/PrototypeTuning.cs` | обе `= 30` | Удалить |
| review PR #178 (Issue #171) — две фикстуры `prepared-ration-zero`/`prepared-watch-zero` вне границ теста | Первый прогон одной из фикстур приходит `fallen` при выигранном бое, виден в батч-отчёте следующей балансной задачи | `dotnet test tests/DungeonFortress.Simulation.Tests --filter FullyQualifiedName~PrototypeMemoryCostTests` → 5 пройдено, 0 не пройдено; `git log --since=2026-08-02 --oneline -- 'evidence/*.json'` не находит нового батч-отчёта — балансная задача с 2026-08-02 не запускалась | Условие **не проверялось**, а не «не сработало»: событие, которое его наблюдает (батч-отчёт балансной задачи), не происходило. Разница названа явно, а не смешана со строкой выше | Удалить |
| review PR #193 (Issue #179) — мутант, назначенный брифом, не отчитан | Второй PR подряд без отчёта о назначенном мутанте | `gh pr view 249/256/258/265 --json body` — все четыре тела содержат раздел о мутантах (таблица подстановок, «мутант неприменим» с причиной или «мутанта нет, и это назван но» по правилу 21) | Ни один из четырёх PR после #193 не пропустил отчёт — счётчик «подряд» не то что не достиг двух, он не начинался | Удалить |
| review PR #199 (Issue #184) — тело PR называет неверную базу прогонов | Второй PR подряд называет базу, на которой заявленных прогонов уже нет | `gh pr view 249/256/258/265 --json body` — поиск «база прогона»/«merge-base»/явного коммита-базы | Ни один из четырёх PR не называет несовпадающую базу; в двух явно назван факт, что полный прогон брался с финального состояния, без ложной базы | Удалить |
| review PR #198 (Issue #186) — `TrafficTarget` не воспроизведена в снапшоте | Публикация `TrafficTarget` в каноническом снапшоте | `rg -n 'TrafficTarget' src/DungeonFortress.Simulation/PrototypeTypes.cs` | пусто | Удалить |

**Исход: 6 записей удалены, ни одна не повышена, ни одна не переформулирована.**
Переформулировка неприменима ни к одной: все шесть условий остаются
наблюдаемыми, ни одно не оказалось конструктивно незамечаемым — это отличает их
от случая записи #105 (2026-08-01), где переформулировка была нужна именно
потому, что формулировка была ненаблюдаемой по себе. Причина удаления одна для
всех шести и совпадает с причиной, применённой к 37 записям 2026-08-03: **условие
наблюдаемо, три захода владельца в игру запись пережила и ни разу не
понадобилась.** Для двух записей выше (`flinch` и две фикстуры) отдельно
названо, проверялось ли условие в буквальном смысле или нет — по уроку раздела
«Кто и когда перечитывает» это обязана различать сама запись протокола, а не
читатель; итог по правилу трёх gate от этого не меняется в обоих случаях,
потому что правило спрашивает не «проверяли ли», а «наступило ли названное
событие», и оно не наступило ни в одном из шести случаев.

**Что осталось в реестре после разбора.** Обе не тронутые группы — «срез «цена
памяти о месте»» (возраст 2, 2 записи) и «слайс «существо без работы уходит с
места боя»» (возраст 1, 3 записи) — их срок не вышел, non-goals задачи прямо
запрещает их трогать здесь, и правило трёх gate к ним не применяется до
следующего перечитывания.

Ниже — протокол предыдущей сессии.

**Перечитывание 2026-08-04, блок пробы ADR 0020 закрыт.** Записей на момент
начала — **9**; после этой правки команда даёт **11**, и разница намеренная:
перечитывание не повысило и не удалило ничего, а тем же набором изменений
добавлены **две** новые записи по находкам независимого review PR #256.
Добавление перечитыванием не является и в колонки таблицы не входит — так же,
как в протоколах 2026-08-01, 2026-08-02 и 2026-08-03.

**Прошедших gate стало 5:** playtest состоялся 2026-08-04, и владелец дал
`CONTINUE` по пробе ADR 0020 — строка в журнале gate-решений
[`GATE_DECISIONS.md`](../product/GATE_DECISIONS.md). Распределение записей по якорю «Срок с»
получено командой раздела «Кто и когда перечитывает»:

```bash
awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md \
  | rg -o '\| `CONTINUE` \([^)]*\), [0-9-]+ \|' | sort | uniq -c
```

→ **6** с якорем «блок боя», **2** с якорем «срез «цена памяти о месте»», **3** с
якорем «слайс «существо без работы…»». Первая редакция этого абзаца называла
6 / 3 / 2 — числа прежнего протокола, перенесённые вместо пересчёта: PR #255
повысил и удалил запись **из группы «цена памяти о месте»**, а к группе
2026-08-04 добавились две новые. Итог 11 сходился при двух неверных слагаемых, и
поэтому ошибка не поймалась счётом; нашёл её независимый review PR #258.

**С gate по пробе шесть записей группы «блок боя» становятся просроченными**
(возраст 3 при пороге 3), группа «цена памяти о месте» переходит в возраст 2,
группа 2026-08-04 — в возраст 1. Разбор шести — переформулировать условие, потому
что оно ненаблюдаемо, либо удалить с причиной; третьего исхода правило не даёт.
Он вынесен в [#261](https://github.com/anshushunov/dungeon-fortress/issues/261),
а не отложен словом «когда-нибудь»: разбирать их в том же PR, который вносит gate, значило бы
решать судьбу шести записей в наборе изменений, чей предмет — закрытие блока.

**Ни одно условие повышения не сработало**, и проверено это отдельно от возраста:

| Условие записи | Команда | Результат |
|---|---|---|
| `EatThreshold` и `MemoryYieldsSatiety` разошлись | `rg -n 'MemoryYieldsSatiety = \|EatThreshold = ' src/DungeonFortress.Simulation/PrototypeTuning.cs` | обе `= 30` — не сработало |
| `TrafficTarget` опубликован в снапшоте | `rg -n 'TrafficTarget' src/DungeonFortress.Simulation/PrototypeTypes.cs` | пусто — не сработало |
| Второй PR подряд не отчитал назначенного брифом мутанта | `gh pr view 256 --json body` | тело PR несёт таблицу трёх мутантов с дословными подстановками — не сработало |

Условия, требующие человеческого взгляда — цвет строки легенды и светлота позы
`flinch`, — снова **не проверялись**, а не «не сработали»: playtest между
блоками не проводился. Различать это обязан протокол, а не читатель.

**Условие сработало через несколько часов после протокола ниже, и это не его
ошибка.** Протокол «перед пробой ADR 0020» проверил четыре условия командами и
верно записал, что ни одно не сработало; **третий случай нарушения правила 17
произошёл позже**, уже в ходе той же сессии: art-дорожка
[#243](https://github.com/anshushunov/dungeon-fortress/issues/243) создала свой
worktree **внутри** корневой рабочей копии — `git worktree list` возвращает
`C:/gamedev/Dungeon fortress/w243`, `git status --short --branch` в корне даёт
`?? w243/`, и каталог не покрыт `.gitignore` (`git check-ignore -v w243` —
пусто). Способ нарушения третий по счёту и **не тот**, что описан в записи:
`HEAD` корня не переключался, коммитов из корня не было, `git reflog` чист.
Условие записи это покрывает: оно говорит не про способ, а про то, что «вопрос
становится не про исполнителя, а про то, чем правило обеспечено технически».

**Запись повышена до [#253](https://github.com/anshushunov/dungeon-fortress/issues/253)
и поэтому удалена из таблицы записей** — реестр устроен как очередь с
храповиком, а не архив: повышенная запись живёт дальше как задача, а её текст
остаётся в истории (`git log -p docs/engineering/DEBT_LEDGER.md`). Задача просит
не более строгую формулировку, а **механизм**: три нарушения за четыре дня — три
разных способа, и ни одно не было злым умыслом.

**Перечитывание 2026-08-04, перед пробой ADR 0020.** Записей на момент начала —
**10**, той же командой, что и прежние протоколы; после этой правки она даёт
**10** же. Прошедших gate — **4**, той же командой с якорем на gate `PROMOTE` по
срезу 2: за блок #221/#228 нового gate-решения не появилось, и это записано в
`NOW` прямо — слайс #228 был измерительным, поведение не менял, играть было не
во что. **Возраст записей поэтому не сдвинулся**: 6 записей с якорем «блок боя»
остаются в возрасте 2, 3 записи с якорем «срез «цена памяти о месте»» — в
возрасте 1, добавленная блоком #221/#228 запись про цвет строки легенды — в
возрасте 0. Не просрочена ни одна; шесть по-прежнему стоят вплотную к порогу, и
следующее gate-решение — оно придёт от [#244](https://github.com/anshushunov/dungeon-fortress/issues/244) — сделает их просроченными.

**Пропуск предыдущего перечитывания назван, а не замолчан.** Блок #221/#228
добавил в реестр запись (review PR #232) и **не оставил строки протокола**:
`rg -n '221' docs/engineering/DEBT_LEDGER.md` до этой правки не возвращал ничего.
По разделу «Кто и когда перечитывает» перечитывание фиксируется здесь, а не в
отчёте, иначе правило трёх gate не срабатывает; строка выше эту дыру закрывает
задним числом — числа в ней сняты сегодня, а не восстановлены по памяти той
сессии. Задним числом восстанавливать чужой протокол нельзя, поэтому отдельной
строки за блок #221/#228 в таблице нет: возраст записей от этого не пострадал —
gate в том блоке не было.

**Ни одно условие повышения не сработало**, и проверено это отдельно от
возраста, командами, а не суждением:

| Условие записи | Команда | Результат |
|---|---|---|
| `EatThreshold` и `MemoryYieldsSatiety` разошлись | `rg -n 'MemoryYieldsSatiety = \|EatThreshold = ' src/DungeonFortress.Simulation/PrototypeTuning.cs` | обе `= 30` — не сработало |
| `TrafficTarget` опубликован в каноническом снапшоте | `rg -n 'TrafficTarget' src/DungeonFortress.Simulation/PrototypeTypes.cs` | пусто — не сработало |
| Второй PR подряд не отчитал назначенного брифом мутанта | `gh pr view <N> --json body` по всем PR блока #221/#228 (#232, #235, #236, #239, #241) | у каждого тело называет мутанты — не сработало, счётчик «подряд» остаётся обнулённым |
| Третий случай записи в корневую рабочую копию | `git reflog -30` в корне; `git status --short --branch` — только как справка о текущем состоянии | в reflog одни ветки `coord/*` координационной сессии, которой корень и принадлежит, — не сработало |

**Команда четвёртого условия исправлена в этом же протоколе, а не после него.**
Первая редакция проверяла его `git status --short --branch`, то есть **текущим**
состоянием копии, тогда как условие говорит об эпизоде «переключился,
закоммитил, вернулся»: сама запись реестра (review PR #213) нашла первый случай
`git reflog`, а не `git status`. Вывод «не сработало» от подмены не изменился —
он перепроверен `git reflog`, — но способ проверки назван неверно, и это поймал
независимый review PR #246. Урок общий: команда условия обязана наблюдать
**событие**, а не его след в моменте.

Условия, требующие человеческого взгляда — цвет строки легенды и светлота позы
`flinch`, — не могли сработать: playtest между блоком #221/#228 и этой сессией не
проводился. Это записано здесь потому, что «не сработало» и «не проверялось»
различать обязан сам протокол, а не читатель.

**Перечитывание 2026-08-04, блок #201/#210 закрыт.** Записей на момент начала —
**9**, той же командой, что и прежние протоколы (она же приведена в протоколе
2026-08-03 ниже); после этой правки она даёт **9** же: ничего не добавлено и
ничего не удалено. Прошедших gate — **4**, той же командой, что в разделе «Кто и
когда перечитывает», с якорем на gate `PROMOTE` по срезу 2; четвёртым стал
`CONTINUE` по слайсу [#201](https://github.com/anshushunov/dungeon-fortress/issues/201)
от 2026-08-04.

**Ни одна запись не просрочена, но возраст у них разный, и это считается
поимённо, а не оптом.** У всех девяти «Срок с» — одно из двух gate-решений
2026-08-02, и распределение получено командой:

```bash
awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md \
  | rg -o '`CONTINUE` \((блок боя|срез «цена памяти о месте»)\), 2026-08-02' \
  | sort | uniq -c
```

→ **6** записей с якорем «блок боя» и **3** с якорем «срез «цена памяти о
месте»». Возраст каждой группы — по формуле раздела «Кто и когда перечитывает»
(число gate-маркеров от якоря до конца журнала минус сам якорь):

| Якорь «Срок с» | Записей | Маркеров в окне | Возраст |
|---|---|---|---|
| `CONTINUE` (блок боя), 2026-08-02 | 6 | 3 | **2** |
| `CONTINUE` (срез «цена памяти о месте»), 2026-08-02 | 3 | 2 | **1** |

При пороге три не просрочена ни одна, но шесть записей подошли к нему вплотную:
**следующее gate-решение делает их просроченными**, и разбирать их придётся не
«когда-нибудь», а на ближайшем перечитывании. Прежняя редакция этого абзаца
говорила «у каждой ровно один» — счёт исправлен независимым review PR #230,
пересчитавшим возраст по формуле самого реестра. Ошибка была ровно того рода,
ради которого формула и записана: механизм, срубивший 2026-08-03 сорок процентов
реестра, опирается на этот счёт.

**Ни одно условие повышения не сработало**, и проверялось это отдельно от
возраста — исполнение урока протокола 2026-08-02. Две проверки стоит назвать,
потому что на них условие подошло близко:

- запись про **два независимых литерала** `MemoryYieldsSatiety` и `EatThreshold`:
  `rg -n 'MemoryYieldsSatiety = |EatThreshold = ' src/DungeonFortress.Simulation/PrototypeTuning.cs`
  — обе `= 30`, не разошлись;
- запись про **мутанта, не отчитанного в теле PR** (условие — второй такой PR
  подряд): оба PR блока с обязательными мутантами отчитались,
  `gh pr view 217 --json body` и `gh pr view 218 --json body` содержат таблицы
  мутантов. Счётчик «подряд» обнулён.

**Одна запись переписана по фактуре, а не по условию** — палитра позы `flinch`
(review PR #175). Её основание держалось на факте «поза не рисуется вовсе»;
merge PR #218 сделал позу достижимой, и факт стал ложным. Ложное утверждение
документа о репозитории в реестр не кладётся — оно правится в документе, поэтому
основание переписано на месте. **Условие повышения не менялось**, колонка
«Переформулировок» остаётся `0`; условие при этом стало наблюдаемым впервые —
поза теперь попадает на кадр, — и на playtest 2026-08-04 не сработало.

**Перечитывание 2026-08-03, блок #201/#210.** Записей на момент начала — **44**,
той же командой, что и прежние протоколы:

```bash
awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'
```

После этой правки она даёт **9**, и это намеренно больше семи: удалено 37 из 44,
осталось **7**, и тем же набором изменений добавлено **две** новые записи.
Колонка «Осталось» таблицы выше считает состояние **на момент перечитывания** —
там 7; команда считает состояние **на сейчас** — там 9. Различие то же самое, что
у протоколов 2026-08-01 и 2026-08-02, и названо здесь по их образцу.

**Добавлено две записи**, обе по предложению независимого review смерженных
PR #213 и #214: устаревший docstring `scripts/agent/test-take-task.ps1` и запись
в корневую рабочую копию внешним исполнителем. Добавление перечитыванием не
является и в таблицу выше не входит.

**Правило трёх gate-решений сработало впервые, и сразу для 37 записей из 44.**
Возраст считается в gate-решениях с начала отсчёта, назначенного переходной
оговоркой 2026-08-01, — gate `PROMOTE` по срезу 2. Прошедшие gate получены той же
командой, что и в протоколе 2026-08-02, и их стало **три**:

```bash
awk '/^\| 2026-08-01 \| \*\*Срез 2 — история влияет/,0' \
  docs/product/GATE_DECISIONS.md \
  | rg -o '\*\*`(CONTINUE|ADJUST|PROMOTE|DEFER|STOP)`\*\*'
```

→ `PROMOTE` (срез 2), `CONTINUE` (блок боя), `CONTINUE` (срез «цена памяти о
месте»). Третий gate появился после протокола 2026-08-02, который считал два.
Под разбор попадает вся эпоха до 2026-08-02 включительно по дате записи: **7**
записей от 2026-07-31 и **30** от 2026-08-01 (в том числе одна с датой
«2026-08-01, текст переписан после merge #147»).

**Проверка срабатывания велась отдельно от проверки возраста** — это исполнение
урока, записанного протоколом 2026-08-02: тогда перечитывание смотрело только на
возраст и пропустило сработавшее условие записи #123. Проверяемые командой
условия проверены командой, а не суждением:

| Условие записи | Команда | Результат |
|---|---|---|
| Второй call-site у `WriteState` | `rg -n 'WriteState\(' -g '*.cs'` | один вызов (`PrototypeCanonical.cs:20`) — не сработало |
| `refused_injured` в каноническом журнале | `rg -n 'refused_injured' -g '*.cs'` | симуляция эмитит только `combat_refused_injured` (`PrototypeWorld.cs:2508`); рукав без префикса остаётся мёртвым — не сработало |
| Правка `BuildTooltip`, третий вызывающий | `rg -n 'BuildTooltip' -g '*.cs'` | два вызывающих, как и было — не сработало |
| `EatThreshold` и `MemoryYieldsSatiety` разошлись | `rg -n 'MemoryYieldsSatiety = \|EatThreshold = ' src/DungeonFortress.Simulation/PrototypeTuning.cs` | обе `= 30` — не сработало (запись молодая, остаётся) |

Остальные условия эпохи разбора — про кадр playtest, про появление второго
спайка, про потребителя снапшота, про отступ глубже 8,625 — за три gate не
наступили ни разу.

**Исход: 37 записей удалены, ни одна не повышена, ни одна не переформулирована.**
Правило даёт ровно два исхода — переформулировать условие, **потому что оно
ненаблюдаемо**, либо удалить с причиной; исход «разобрана и остаётся» отклонён
2026-08-01 независимым review PR #155 как сброс счётчика, делающий храповик
недостижимым. Переформулировка здесь не применима: условия этих записей
наблюдаемы — большинство из них уже переформулированы ради наблюдаемости на
перечитывании 2026-08-01. Значит применён второй исход, и **общая причина одна:
условие наблюдаемо, три захода владельца в игру запись пережила и ни разу не
понадобилась.**

Что это значит по существу, а не по бухгалтерии: реестр — очередь с храповиком, а
не архив находок. Находка, которая за три gate не превратилась ни в дефект, ни в
вопрос, ни в помеху проверке, по устройству правила считается не нужной. Текст
удалённых записей остаётся в истории git (`git log -p docs/engineering/DEBT_LEDGER.md`),
а их источник — review конкретного PR — назван в перечне ниже, поэтому след не
теряется.

**Перечень удалённого.** Порядок — как в таблице записей; «источник» позволяет
поднять исходный вердикт review.

| Источник | Что было записано |
|---|---|
| review PR #105 | Правило версионирования снапшота закрыто тестом на один род ломающих изменений из четырёх |
| review PR #105 | `includeEvents` в `WriteState`: недостижимая ветка `false` |
| review PR #121 | Вывод 9 калибровки утверждает про разведчиков без источника |
| review PR #121, круг 2 | Вывод 11 калибровки стоит на одном измерении одной задачи |
| review PR #123 | Правило 7 называет одно из двух ослаблений ADR 0014 |
| review PR #123 | Пропала фраза про условия отката как рабочие условия координатора |
| review PR #124 | Мёртвый рукав `refused_injured` в `EventNarration.Sentence` |
| review #127 | Задача писала в `Main.cs` вне своего именованного региона |
| review #127, круг 2 | `BuildTooltip` можно заставить игнорировать параметр `scale` |
| review #127, круг 2 | `$hudTooltipReadabilityFailure` присвоена и не прочитана |
| review #52 | Состав `Room.perimeter` не описан в контракте |
| review #52 | Невоспроизводимые «16 строк нужно / 15 помещается» |
| review #52 | Мутационный проход не касается `RoomReadings.cs` и `DescribeRooms` |
| review #128/#125 | Стадия «две половины» в `evidence/125-false-refusals.json` без разбивки |
| review #128/#125 | Оговорка «счёт не ниже `score_floor`» не упражняется матрицей |
| review #128/#125 | Раннеры мутационных прогонов не отгружены (`.artifacts` в `.gitignore`) |
| review #128/#125 | `MemoryFreeWinnerOfRefusedTile` зависит от порядка обхода `Dictionary` |
| review #128/#125 | Контрфактический проб на 6 партиях против 9 в `MatrixScenarios` |
| review #128/#125 | `Reading_a_creature_s_story_needs_no_tick_to_run` сравнивает проекцию с собой |
| review #128/#125 | `CreatureStoryPanels()` на smoke-тике меряет пол бюджета, а не потолок |
| review PR #124, круг 2 | Кадры #83 в `PROTOTYPE_GRAYBOX.md` проверены наполовину |
| review #142 | Каталог `spikes/` не описан ни одним документом |
| review #139/#147 | Вырождение свойства #52 в смешанном случае |
| review #142, круг 2 | `parentCommit` в `evidence/142-scale-spike.json` указывает на дерево до правки |
| review #142, круг 2 | Нулевой запас бюджета панели истории |
| review #139, круг 2 | Подпись однотайловой комнаты под стеной выходит за свою обводку |
| review #147 | `RoomWallClearanceTests.Measure` исключает юг слишком широко |
| review #147 | Между ступенью 8,625 и потолком 10,0 нет проверки |
| review #147, ретроспективно к #139 | Критерий #139 закрыт против прямоугольника фасада, а не полосы |
| review PR #152 | У ленты владения нет дополненного худшего случая |
| review PR #152, круг 2 | Невоспроизводимый замер заголовка в `evidence/145-bound.json` |
| review PR #152, круг 2 | «Лента прокручивается» в `PROTOTYPE_GRAYBOX.md` после #145 неверно |
| review PR #166, круг 2 | Необъяснённая разница толщины кромок larder и kitchen |
| review PR #166, круг 2 | Устаревший `imageSha256` в `evidence/156-mutations.json` |
| review PR #166, круг 2 | Пустое утверждение про боковую кромку за стеной |
| review PR #166, круг 2 | Ориентация сегмента границы выводится дважды разными выражениями |
| review PR #152, круг 2 | Два ослабленных утверждения в `HudTextTests` |

**Отдельно: у оставшейся записи про палитру `flinch` исправлена фактура, а не
условие.** Запись объясняла отсутствие последствия в том числе тем, что «рантайм
грузит v1». После merge PR #185 это неверно: `BodySprites.PackVersion` = `"v2"`,
и путь строится через `BodySprites.FileName` (`Main.cs:2112`). Ложное утверждение
документа о репозитории в реестре не отлёживается — оно правится в документе, —
поэтому текст записи приведён к факту. Условие не менялось, счётчик возраста не
сбрасывается. Практическое следствие названо там же: поза `flinch` сегодня не
рисуется вовсе, потому что оба вызова адаптера передают `BodyActionPhase.None`;
[#210](https://github.com/anshushunov/dungeon-fortress/issues/210) делает её
достижимой, и с её merge условие записи впервые становится проверяемым на кадре.

**Перечитывание 2026-08-02, третий круг PR #181.** Записей на момент начала —
**41**, той же командой, что ниже, на `0016fe1`; после этой правки она даёт
**40**.

**Повышена одна запись — и повышена не по возрасту, а по сработавшему условию.**
Запись от 2026-07-31 (review PR #123) о преамбуле раздела «Темп разработки»,
которая называет ослабляющие ADR правила поимённо и не называет общего правила
приоритета. Условие звучало: «появление четвёртого ослабляющего правила, текст
которого не называет явно, чья буква действует при расхождении с ADR». Оно
сработало **дословно**: правило 24 забирает у [ADR 0014](../decisions/0014-parallel-agents-and-autonomy.md)
§7 два основания эскалации и в первой своей редакции ADR 0014 не упоминало
вовсе, а преамбула продолжала перечислять «2, 5 и 7». Заведена
[#183](https://github.com/anshushunov/dungeon-fortress/issues/183) (`gh issue
view 183 --json state` — `OPEN`); запись удалена по пути 1, её условие и
измерение живут в теле задачи. Правило 24 и преамбула исправлены тем же набором
изменений — это лечение случая, а не механизма, механизм и есть предмет #183.

**Перечитывание 2026-08-02 по блоку это условие пропустило, и причина
называется.** Оно смотрело на **возраст** записей — сколько gate-решений запись
пережила — и на этом основании заключило, что правило трёх gate не сработало ни
для одной. Вопрос «не сработало ли у какой-нибудь записи её собственное
условие» отдельно не задавался ни по одной из 39, кроме той, о которой уже
знал review. Условие записи #123 сработало **в том же наборе изменений**, что и
перечитывание, и осталось незамеченным до подтверждающего круга review. Возраст
и срабатывание — две разные проверки, и раздел «Кто и когда перечитывает»
требует обе; выполнена была одна.

**Перечитывание 2026-08-02, блок #171/#77/#165.** Записей на момент начала —
**39**; число получено
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на `origin/main` (коммит `949ca50`). Той же командой считалось состояние после
той правки — **41**, и это намеренно больше: колонка «Осталось» считает
состояние на момент перечитывания (39 − 1 = 38), а команда — на тот момент,
то есть вместе с тремя добавленными. Текущее число — в протоколе третьего круга
выше.

**Повышена одна запись.** Невоспроизводимая половина происхождения пака существ
v2 (2026-08-01, review PR #162) — сработало названное условие дословно:
независимый review PR #175 столкнулся ровно с тем, что оно описывало, — пак
перегенерирован, а ни исходного листа, ни `remove_chroma_key.py` в репозитории
нет, поэтому доказать, что конкретный PNG получен описанной обработкой,
невозможно. Заведена
[#179](https://github.com/anshushunov/dungeon-fortress/issues/179) (`gh issue
view 179 --json state` — `OPEN`); запись удалена отсюда по пути 1 раздела
«Когда запись повышается до Issue», её условие и измерения живут в теле задачи.

**Добавлено три записи**, все по предложению независимого review: дрейф палитры
позы `flinch` (PR #175); молчаливая копия `EatThreshold` в
`MemoryYieldsSatiety` и две фикстуры вне границ правки #171 (обе — PR #178).
Добавление перечитыванием не является и в таблицу выше не входит.

**Правило трёх gate-решений не сработало ни для одной записи.** С начала отсчёта
— gate `PROMOTE` по срезу 2, переходная оговорка 2026-08-01 — прошло **два**
gate-решения: сам `PROMOTE` и `CONTINUE` по блоку боя 2026-08-02. Проверено по
журналу, а не принято на слово — команда даёт ровно эти два:

```bash
awk '/^\| 2026-08-01 \| \*\*Срез 2 — история влияет/,0' \
  docs/product/GATE_DECISIONS.md \
  | rg -o '\*\*`(CONTINUE|ADJUST|PROMOTE|DEFER|STOP)`\*\*'
```

Остальные строки журнала того же периода — решения владельца, а не заходы в
игру, и единицей возраста не являются.

**Перечитывание 2026-08-01, блок боя.** Записей на момент начала сессии — **35**;
число получено
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на `74af087`, той же командой считается и текущее состояние — **на момент этой
записи** она давала **39**, и это намеренно больше: колонка «Осталось» в таблице
выше считает состояние на момент перечитывания (35 − 1 = 34), а команда — на
тогдашнее «сейчас», то есть вместе с пятью добавленными. Текущее число — в
протоколе 2026-08-02 выше.

**Правило трёх gate-решений не сработало ни для одной записи, и это следствие
переходной оговорки, а не недосмотра.** Отсчёт для всех записей, существовавших на
2026-08-01, начинается с gate `PROMOTE` по срезу 2, а новых gate-решений на момент
той сессии не было: `PROMOTE` оставался последним. Ближайшая к срабатыванию по **условию**, а
не по возрасту — запись про выход задачи #127 за именованный регион `Main.cs`: её
условие требует второй одновременной задачи, реально пишущей в этот файл. В блоке
боя `Main.cs` держала единственная задача (#156), и это проверено при раздаче
партиции чтением `DrawMap`, а не предположено.

**Повышена одна запись.** Планка «бегство — это ходьба» (2026-07-31, review
PR #124) — сработало названное условие «доля ниже 80 % на любой фикстуре»:
независимый review PR #164 измерил 104 из 123 (84,5 %) против 76 из 108 (70,4 %),
ниже 80 % оказались пять ячеек из шести. Заведена
[#168](https://github.com/anshushunov/dungeon-fortress/issues/168); запись удалена
отсюда, её условие и оба измерения перенесены в тело задачи. Там же исправлено
само условие: «на любой фикстуре» читается по ячейкам, а по ячейкам оно было
истинным уже в момент написания (худшая 11 из 16 = 68,75 %) — незапускаемое
условие условием не является.

**Добавлено пять записей**, все по предложению независимого review: одна от
PR #162 — невоспроизводимая половина происхождения пака v2; четыре от PR #166 —
видимая игроку цена larder, устаревшие хеши кадров в evidence, пустое утверждение
про боковую кромку и двойной вывод ориентации сегмента. Добавление перечитыванием
не является и в таблицу выше не входит.

**Наблюдение, которое стоит проверить следующим перечитыванием.** Обе смены
единицы измерения в #156 — «объединение полос вместо одной» и «кусок кромки вместо
кромки» — поймал не тест и не мутант, а кадр: review оба раза смотрел на картинку.
Записей о геометрии представления в реестре уже пять, и ни одна не имеет условия,
которое срабатывает без человеческого взгляда. Если это подтвердится на четвёртой
точке, вывод не про реестр, а про то, чем проверяется слой представления.

Ниже — протокол предыдущей сессии.

В ту же сессию **добавлено тринадцать** записей, все по предложению независимого
review: три от ветки #127 — выход за именованный регион `Main.cs` (первый круг),
необязательность параметра `scale` внутри `BuildTooltip` и мёртвое присваивание в
`verify.ps1` (подтверждающий круг); три от ветки #52 — состав `perimeter` в
контракте, невоспроизводимые «16 / 15» и узость мутационного прохода; семь от
ветки #128/#125. Добавление перечитыванием не является и в таблицу выше не
входит.

**Перечитывание 2026-08-01, сессия по бою и масштабу.** Записей на момент
перечитывания — **24**, до добавления девяти этой же сессией; текущее число даёт
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md |
rg -c '^\| 2026-'`, и оно намеренно больше: команда считает состояние на сейчас, а
не на момент перечитывания. Ни одно условие повышения не сработало, удалять
нечего.
Три координационные сессии не пролежала ни одна запись: одиннадцать записей от
2026-07-31 проходят **второе** перечитывание, тринадцать от 2026-08-01 —
**первое**. Ближайшая к срабатыванию — запись про выход задачи #127 за
именованный регион `Main.cs`: её условие требует второй одновременной задачи,
реально пишущей в этот файл. В блоке, запущенном этой сессией, `Main.cs` держит
единственная задача (#139), поэтому условие не сработало; это проверено при
раздаче партиции, а не предположено.

В ту же сессию **добавлены девять** записей, все по предложению независимого review:
недокументированный каталог `spikes/` и `parentCommit` манифеста (ветка #142); вырождение свойства #52 в
смешанном случае и молчаливая копия половины толщины штриха (ветка #139); незащищённый порядок уровней значимости и нулевой запас бюджета панели (ветка #140); подпись однотайловой комнаты снаружи своей обводки, третье незащищённое звено проводки подписи и два сравнения референсной величины с экранным потолком (#139, круги 2–3).
Добавление перечитыванием не является и в таблицу выше не входит.

Тринадцать записей за одну сессию против одиннадцати, накопленных за всё
предыдущее время, — это само по себе наблюдение, а не бухгалтерия. Оно означает
одно из двух: либо review стал находить больше, либо реестр начал принимать то,
что раньше становилось Issue. Разбирать это надо на следующем перечитывании, с
числами, а не сейчас.

**Третье перечитывание 2026-08-01, сессия ленты владения и кромки стены.** Записей
на момент начала сессии — **33**; число получено
`awk '/^## Записи/,/^## Перечитывание/' docs/engineering/DEBT_LEDGER.md | rg -c '^\| 20'`
на `f03f41f`, той же командой считается и текущее состояние.

Это первая сессия, на которой сработало правило трёх перечитываний: одиннадцать
записей эпохи 2026-07-31 (десять с этой датой плюс запись по подтверждающему кругу
PR #124, датированная следующим днём) дожили до **третьего** разбора. Разобраны
поимённо:

- **Правило версионирования снапшота (#105)** — условие переформулировано, потому
  что было ненаблюдаемым по собственной формулировке: «изменение, прошедшее в
  `main` **незамеченным**», заметить нельзя. Новая формулировка ловит момент
  правки, а не её последствие.
- **`includeEvents` (#105)** — условие наблюдаемо и проверено командой:
  `rg -n 'WriteState\(' -g '*.cs'` даёт единственный вызов. Запись остаётся.
- **Запись о выводе 8 калибровки (#121)** — **удалена**, причина своя, не общая:
  её условие звучит «оценка блока, построенная **по этому выводу**, снова
  промахнётся», а вывод 8 переехал в `archive/CALIBRATION_HISTORY.md`, тогда как
  оценки строятся по трём выводам, оставленным в `MULTI_AGENT_WORKFLOW.md`.
  Сработать это условие больше не может.
- **Записи о выводах 9 и 11 калибровки (#121)** — **остаются**, и это исправление
  ошибки той же сессии. Первая редакция этого разбора удалила все три по общей
  посылке «условие каждой начинается словами „оценка, построенная по этому
  выводу“». Независимый review PR #155 показал, что посылка верна только для
  вывода 8: условие записи 9 — «появление строки калибровки с вложенным
  субагентом», условие записи 11 наполовину — «оборванный прогон, стоивший
  заметно дороже», и ни то, ни другое от выводов калибровки не зависит. Обе
  восстановлены дословно. Отдельно опровергнута и вторая половина посылки:
  «в архиве — значит ни на что не опирается» неверно, `MULTI_AGENT_WORKFLOW.md`
  цитирует архивный вывод 14, а оговорка про вложенного субагента прямо помечена
  там как не подлежащая архивации.
- **Три записи о разделе «Темп разработки» (#123)** — остаются, условия
  наблюдаемы и ни одно не сработало. Ближайшая к срабатыванию — запись про
  правило 7: её условие требует **спора** о том, нужна ли команда `MERGE #N`, и в
  этой сессии вопрос действительно вставал на PR #150, но спором не стал —
  `.claude/commands/coord.md` отвечает на него прямо, и обе стороны прочитали
  ответ одинаково. Запись про четвёртое ослабляющее правило проверена отдельно:
  PR #150 нового ослабления не вводит, он задаёт значение параметра правила 14.
- **Планка бегства 80 % и мёртвый рукав `refused_injured` (#124)** — остаются, не
  сработали. У второй появился названный адресат:
  [#154](https://github.com/anshushunov/dungeon-fortress/issues/154) включает
  `refused_injured` в свой scope, и если задача его закроет, запись уйдёт вместе
  с ней.
- **Кадры #83 в `PROTOTYPE_GRAYBOX.md` (#124, подтверждающий круг)** — остаётся,
  условие наблюдаемо: кадр снимается по команде из документа, и расхождение
  видно сразу.

**Порядок применения правила: предложение координатора отклонено, вместо него
изменена единица возраста.** Координатор предлагал добавить правилу третий исход
— «разобрана, условие подтверждено, остаётся». Независимый review PR #155
показал, что предложение несло в себе сброс счётчика, а он делает храповик
недостижимым по устройству: реестр перечитывает каждая сессия, поэтому «три
сессии без изменения» не набрала бы ни одна запись никогда. Отсюда выбор в пользу
другого лечения — исходов по-прежнему **два**, а изменилась единица, в которой
считается возраст: gate-решения вместо сессий, см. раздел «Кто и когда
перечитывает».

Записи, разобранные 2026-08-01 и оставленные с наблюдаемым несработавшим
условием, начинают отсчёт заново не потому, что их перечитали, а потому, что
переходная оговорка нового правила назначает началом отсчёта gate `PROMOTE` по
срезу 2.

Ниже, **до конца этого абзаца**, — прежний текст разбора, сохранённый как
протокол; абзацы после него («Повышено две записи», «Добавлено семь записей»,
«Удалены сверх разбора», «Наблюдение прошлой сессии») — действующий протокол
сессии, а не отклонённое предложение. Правило предлагает два исхода —
переформулировать условие или удалить запись. Оба применены там, где условие
оказалось дефектным. Записи с наблюдаемым и несработавшим условием оставлены, и
сам этот разбор был объявлен для них исходом. **Это и есть отклонённое
предложение**; действующий порядок — абзацем выше.

**Повышено две записи.** Третье незащищённое звено проводки подписи (#139, круг 3)
— сработало названное условие «любая правка `DrawRoomLabel`»: правку внесла #147,
и review её ветки перепроверил мутанта на новом дереве, получив 451/451 зелёных.
Заведена [#153](https://github.com/anshushunov/dungeon-fortress/issues/153).
Незащищённый порядок уровней `StoryWeight` (#140) — получил наблюдаемое
последствие иначе, чем предполагало его условие: #145 вывела ту же шкалу на экран
по умолчанию, и на фикстуре `neglected` шапка ленты сообщает «0 значащих» о
партии, вымирающей от истощения. Заведена
[#154](https://github.com/anshushunov/dungeon-fortress/issues/154); запись удалена
как техдолг, у которого появился исполнитель.

**Добавлено семь записей**, все по предложению независимого review: **три** от
ветки #147 — широкое исключение юга в `RoomWallClearanceTests`, ослабленный
потолок лестницы и ретроспективная запись о неполноте #139; **четыре** от PR #152
— отсутствие дополненного худшего случая у ленты, невоспроизводимый замер
заголовка в `evidence/145-bound.json`, устаревшее «лента прокручивается» в
`PROTOTYPE_GRAYBOX.md` и два ослабленных утверждения в `HudTextTests`. Разбивка
исправлена по находке review PR #155: прежняя редакция называла 4 и 3 и числила
среди добавленных запись про формулировки 10,0, которая была добавлена и удалена
внутри этой же сессии — её закрыл круг доработки #147, — а фактически добавленную
запись про замер заголовка не называла. Добавление перечитыванием не является и в
таблицу выше не входит.

**Удалены сверх разбора одиннадцати, причина на каждую.** Запись про молчаливую
копию `BorderStrokeHalfWidth` — предмет исчез: `RoomGeometry.BorderStrokeWidth`
стала публичной, адаптер читает её вместо литерала, связь закреплена проверкой и
мутантом (#147, PR #151), и это подтвердил независимый review ветки. Запись про
два сравнения `32 / 4.0` в `RoomGeometryTests.cs` — предмет исчез там же: оба
сравнения заменены на `RoomGeometry.MaximumBorderInset`, величина референсная и
привязана к `CameraView` исполняемо. Обе удалялись первой редакцией без
записанной причины, чего требует раздел «Когда запись повышается до Issue»; на
обе ссылались docstring в `RoomGeometry.cs`, и эти ссылки поправлены тем же
набором изменений, иначе после merge код утверждал бы о реестре неправду.

**Наблюдение прошлой сессии проверено числом.** Прошлое перечитывание отметило,
что тринадцать записей за одну сессию против одиннадцати за всё предыдущее время
означают одно из двух: либо review стал находить больше, либо реестр начал
принимать то, что раньше становилось Issue. Эта сессия даёт данные против второй
версии: при семи новых записях два review той же сессии предложили и **два
повышения до Issue**, и **пять возвратов исполнителю** — то есть разделение
исходов работает, а не сползает в реестр. Считать тенденцию по трём точкам рано;
следующее перечитывание получит четвёртую.

**Удалено 2026-08-01** — запись от 2026-07-31 (review PR #121) о том, что раздел
`NOW` обосновывает последовательность #117 → #115 ссылкой на ADR 0017 §3 вместо
базового файлового правила. Причина удаления: **предмет исчез**. #117 закрыт
(PR #124), раздел `NOW` переписан в этой же сессии, и обоснования, к которому
относилась запись, в документе больше нет. Ни условие повышения, ни сама находка
к новому тексту `NOW` не применимы.

## Что сюда не кладётся

- находка, из-за которой не выполнен критерий готовности своей Issue, — она
  возвращается исполнителю кругом доработки;
- находка с наблюдаемым последствием — она становится Issue;
- **утверждение документа о репозитории, оказавшееся ложным.** Оно не проходит
  проверку на «нет наблюдаемого последствия» по букве — игрок его не видит, ни
  одна проверка от него не краснеет, — и именно поэтому названо здесь отдельно:
  это дефект самого документа, он правится в документе тем же набором изменений.
  Отложить его сюда значит оставить в репозитории утверждение, которое читатель
  примет за факт. Ровно на таком утверждении закрыт **черновик ADR 0018**
  (PR #111) — не следует путать с действующим
  [ADR 0018](../decisions/0018-creature-memory-of-place.md), который получил этот
  номер 2026-07-31 и решает другой вопрос. Как не создавать такую копию —
  правило 8 в [`DEVELOPMENT_PACE.md`](DEVELOPMENT_PACE.md);
- продуктовая идея, вопрос или наблюдение владельца — они идут в coordination
  inbox [#33](https://github.com/anshushunov/dungeon-fortress/issues/33);
- незакрытый вопрос, требующий решения, — он идёт в
  [`OPEN_QUESTIONS.md`](../OPEN_QUESTIONS.md);
- технический долг, который кто-то намерен исправить в ближайшем блоке, — у него
  есть исполнитель, значит есть и Issue.
