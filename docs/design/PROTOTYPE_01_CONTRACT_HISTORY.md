# История контракта Prototype 1 — раздел 13.4

Действующий контракт — [`PROTOTYPE_01_PREPARE_FOR_RAID.md`](PROTOTYPE_01_PREPARE_FOR_RAID.md),
раздел 13 «Сценарии», подраздел 13.4 «Ожидаемые наблюдаемые различия»: там
остались таблица коридоров и список из одиннадцати действующих инвариантов.

Этот файл несёт **историю получения** той таблицы и того списка: снятые и
переписанные инварианты, разбор разменов боевых задач #101 и #129, чем красные
проверки и какая находка review их закрыла, и что показало измерение вне
матрицы Issue #12. Ничего из этого не является требованием контракта —
требования перечислены в 13.4, здесь — только то, как к ним пришли.

Вынесено из `PROTOTYPE_01_PREPARE_FOR_RAID.md` решением
[#280](https://github.com/anshushunov/dungeon-fortress/issues/280) дословно;
заголовки понижены на один уровень (`####` → `##`), текст не менялся.

## Что изменилось в инвариантах 4 и 11 и по какому решению

**Решение владельца 2026-08-01, журнал gate-решений `docs/product/GATE_DECISIONS.md`:**
принять новые коридоры вместе с новым основанием для инвариантов. Развилка
возникла из [#129](https://github.com/anshushunov/dungeon-fortress/issues/129):
правило подхода (10.3) сделало бой эффективнее и сдвинуло всё, что стоит после
него. Голодная смерть после выигранного боя вынесена отдельной задачей
[#171](https://github.com/anshushunov/dungeon-fortress/issues/171) и здесь не
чинится.

**Прежние формулировки, действовавшие до этого решения** (сохранены как
свидетельство на момент его принятия, а не как требование):

> 4. среди сценариев, доживших до волны, подготовка удешевляет набег:
>    `mealsStolen(prepared) < mealsStolen(baseline)` и
>    `combat_fled_morale(prepared) < combat_fled_morale(baseline)` за партию;
>
> 11. **счёт партии (10.8) ранжирует качество игры:** `score(baseline) >
>     score(neglected)` и `score(prepared) > score(neglected)` — выжившая партия
>     выше погибшей; `score(prepared) > score(baseline)` — подготовка выше её
>     отсутствия. Оба соотношения проверяются попарно на одном seed и по итогам
>     партии.

**Почему инвариант 4 переписан.** Он состоял из двух счётов, которые можно
разменять друг на друга, а значит был не одним утверждением, а двумя. После #129
размен и произошёл: на seed 20260727 у `prepared` унесено на 12 порций меньше и
сломлено на 5 защитников больше, то есть набег очевидно дешевле, а вторая
половина прежнего инварианта не выполняется. Цена набега берётся теперь в той же
валюте, в которой её уже считает счёт партии (10.8), с теми же весами владельца,
и считает **всех** потерянных защитников, а не только сбежавших — иначе подготовку
можно было бы «купить», разменяв бегство на выбывание.

Деление на разрешённые волны нормирует цену на длину партии: `baseline` на seed
20260728 падает до четвёртой волны и платит за три волны против четырёх у
`prepared`, и сравнение полных сумм льстило бы погибшей партии.

**Но seed 20260728 чинит не деление, и это измерено независимым review** (мутант
«инвариант 4 без деления на волны» — 275 из 275 зелёных): цена без нормировки на
том же seed даёт 263 против 283, то есть сравнение проходит и так. Этот seed
чинит переход от двух отдельных счётов к **одной цене**, считающей всех
потерянных защитников; деление лишь расширяет запас с 7 % до 30 %. Прежняя
редакция этого абзаца приписывала заслугу делению — утверждение снято, а не
подновлено.

Отсюда следствие, названное прямо: **деление — вторая клауза инварианта 4, не
покрытая собственным мутантом.** Матрица сегодня не содержит пары, где партиям
разрешено разное число волн и ответ без деления отличался бы от ответа с ним.

**Почему инвариант 11 переписан.** Его первая половина называла фикстуры —
`baseline` и `prepared` выше `neglected`, — и перестала быть утверждением про
выживание в тот момент, когда падать смогла не только `neglected`: после #129 на
seed 20260728 падает `baseline` со счётом −223. Условие названо исходом, и то же
самое утверждение теперь покрывает больше пар.

Его вторая половина — `score(prepared) > score(baseline)` на каждом seed — меряет
**разрыв** между планами, а правило подхода этот разрыв сжимает, потому что
сильнее помогает плану с худшей геометрией: у `baseline` на seed 20260727 была
единственная в матрице ячейка, где рядом с налётчиком ни разу не оказывалось двух
защитников, поэтому она выигрывает больше всех (налётчиков выбито 5 → 14 против
11 → 13), отражает вторую волну и обходит `prepared` — 831 против 803. Владелец
принял эту цену. Обещанным осталось то, ради чего подготовка существует: она не
может закончить партию хуже, чем её отсутствие, и обязана окупаться на матрице.

**Почему новое основание — не подгонка под текущий прогон.** Все три клаузы
инварианта 11 и инвариант 4 в новой форме выполняются **и на `main`, и на ветке
#129**, то есть до изменения и после него:

| Утверждение | `main` (`cbbd588`) | после #129 |
|---|---|---|
| выжившая выше павшей, по seed | выполняется | выполняется |
| полоса `prepared` ≥ полосы `baseline` | 1 = 1 на всех трёх | 1 = 1, 1 = 1, **1 > 0** |
| сумма счёта по матрице | 2525 > 2193 | 2526 > 1366 |
| цена набега на волну, `prepared` < `baseline` | 70,3 < 105,8; 65,5 < 78,5; 85,5 < 120,0 | 93,5 < 98,0; 89,3 < 92,3; 65,8 < 94,3 |

Числа получены командой:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\evaluate-prototype.ps1 `
  -OutputPath .\.artifacts\129-after-batch.json
python .artifacts\ground-129.py
```

и сведены в `evidence/129-invariants.json`. Проверки —
`PrototypeScenarioTests.The_contract_invariants_hold_on_every_seed_of_the_matrix`
и `PrototypeScenarioTests.Preparation_outscores_its_absence_over_the_matrix`.

**Как они стояли на подземелье (Issue #117) до правила подхода — в формулировках,
действовавших до 2026-08-01.** Таблица сохранена как свидетельство на момент
принятия и не перемеряется: строки 4 и 11 излагают **отменённые** формулировки —
инвариант 4 как два отдельных счёта и инвариант 11 как сравнение счёта по каждому
seed. Действующие формулировки и их числа — выше в этом разделе; здесь они не
дублируются сознательно, чтобы копия не разошлась с источником.

| Инвариант | 20260726 | 20260727 | 20260728 |
|---|---|---|---|
| 1. `readinessAtRaid` | 57 > 46 | 53 > 47 | 59 > 48 |
| 2. `strength` на волне 1 | 51 > 14 | 34 > 17 | 52 > 16 |
| 3. `renown` против `neglected` | 125/153 > 4 | 124/154 > 4 | 138/164 > 4 |
| 4. `mealsStolen` | 21 < 43 | 22 < 26 | 30 < 56 |
| 4. `combat_fled_morale` | 18 < 23 | 16 < 21 | 22 < 23 |
| 5. `combat_refused_starving` у `prepared` | 13 | 21 | **0** |
| 11. счёт | 799 > 727 | 868 > 806 | 858 > 660 |

Единственная клетка, которой нужно было решение владельца, — ноль в строке 5.
Решение принято 2026-07-31 вместе с новым эталонным планом, чьим следствием этот
ноль является; цепочка и проверяемый факт под ней — в оговорке к инварианту 5
выше. Инвариант 4 на зале не выполнялся вовсе в своей второй половине на одном
seed (22 против 22) и теперь выполняется на всех трёх.

## Что сдвинуло правило подхода (10.3) — измерение под решением владельца 2026-08-01

Матрица перемерена после того, как целью боевого шага стала свободная клетка
рядом с врагом ([#129](https://github.com/anshushunov/dungeon-fortress/issues/129)).
Пока решение не было принято, числа таблицы выше **не заменялись**: правило конца
этого раздела говорит, что невыполнение инвариантов 1–5 и 11 — escalation trigger,
а не повод переписать коридоры. Владелец принял новые коридоры вместе с новым
основанием инвариантов, поэтому таблица выше теперь несёт новые числа, а этот
раздел остаётся тем измерением, на котором решение принималось. Источник — прогон

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\evaluate-prototype.ps1 `
  -OutputPath .\.artifacts\129-after-batch.json
```

сведённый в `evidence/129-matrix.json`.

| Наблюдение (было → стало) | 20260726 | 20260727 | 20260728 |
|---|---|---|---|
| Счёт `baseline` | 727 → 758 | 806 → **831** | 660 → **−223** |
| Счёт `prepared` | 799 → 796 | 868 → **803** | 858 → 927 |
| Исход `baseline` | `raided` → `raided` | `raided` → `raided` | `raided` → **`fallen`** |
| Налётчиков выбито, `baseline`/`prepared` | 5/11 → 8/17 | 5/11 → 14/13 | 8/13 → 7/16 |
| `refused_place_of_panic`, `baseline` | 28 → 187 | 6 → 0 | 12 → **412** |
| Произведено порций, `baseline` | 102 → 99 | 78 → 108 | 117 → **75** |

Инвариант 11 не выполняется на **двух** seed, и по разным причинам: на 20260727
(`prepared` 803 против `baseline` 831) и на 20260728 (`baseline` −223 против 0 у
`neglected`).

**Инвариант 4 нарушен один раз, а не два.** На 20260728 он сравнивает партию из
четырёх волн с партией из трёх: `baseline` пал на тике 2300, четвёртая волна
приходит на 2350, `wavesResolved` 3 против 4, и сбежавших в несостоявшейся волне
0 по определению. Такое сравнение меряет длину партии, а не мораль, и нарушением
не является. Настоящее нарушение одно — 20260727, где обе партии прожили все
четыре волны и `combat_fled_morale(prepared)` 21 против `baseline` 16.

## Три канала, и ни один не покрывает оба падения

Бой сам по себе стал строго эффективнее: налётчиков выбито больше на пяти
прогонах из шести, отражённых волн больше. Ломается то, что стоит **после** боя,
и каналов у этого три. Разложение счёта ниже сходится до единицы по формуле
10.8; веса — 15.8.

**Канал 1 — память о месте (5.1). Работает только на 20260728.** Защитники
теперь занимают клетки **вокруг** налётчика, а налётчик стоит у кладовой, —
значит и паникуют они на клетках кладовой и кухни, а не в коридоре за спиной у
своих. Дальше действует уже записанное правило: существо не берёт работу там, где
у него сдало сердце. На `baseline`/20260728 это 412 отказов против 12,
произведено 75 порций против 117, средняя сытость 1 против 45; владение падает на
тике 2299 **от голода, а не от набега**. В счёте это видно прямо: из −883 очков
−1000 даёт смена полосы `raided` → `fallen` и −80 обнулившиеся выжившие, тогда
как боевые слагаемые стали **лучше** (унесено 56 → 31, потеряно защитников
25 → 16).

> **Канал 1 закрыт [#171](https://github.com/anshushunov/dungeon-fortress/issues/171)
> 2026-08-02.** Ослаблена не память, а её цена: у отказа появились срок
> (`T.memory_avoid_ticks`) и уступка нужде (`T.memory_yields_satiety`), правило
> 5.1 «цена памяти ограничена сроком и нуждой». Перемерено на `main` перед
> правкой и после неё одной и той же командой:
> `baseline`/20260728 — `fallen` t2299 счёт −223 → `raided` t2403 счёт 795,
> произведено 75 → 90 порций, средняя сытость 1 → 39, отказов 412 → 122 и уже у
> четырёх существ вместо трёх. Ни одна партия, доживающая до волны, больше не
> заканчивается голодной смертью: все шесть ячеек `baseline` и `prepared` —
> `raided`. `neglected` по-прежнему `fallen` на всех трёх seed, и это не тот же
> исход — она падает до первой волны, памятей не пишет вовсе, и канал 1 её никогда
> не касался. Числа с командами — `evidence/171-before.json` и
> `evidence/171-after.json`; абзацы выше оставлены как измерение своего дерева.

**Канал 2 — правило подхода помогает слабой фикстуре сильнее, чем сильной.
Работает на 20260727, где память о месте инертна** (`refused_place_of_panic` на
`baseline` 6 → 0, на `prepared` 27 → 31). У `baseline` на этом seed была худшая
геометрия во всей матрице — единственная ячейка, где рядом с налётчиком **ни
разу** не оказывалось двух защитников, — поэтому она и выигрывает больше всех:
налётчиков выбито 5 → 14 (×2,8) против 11 → 13 (×1,2) у `prepared`. Инвариант 11
меряет **разрыв** между планами, а правка его сжимает: 62 → −28.

| Слагаемое счёта, 20260727 | `baseline` | `prepared` |
|---|---|---|
| Полоса исхода | 1000 → 1000 | 1000 → 1000 |
| Отражённые волны | 60 → **120** | 120 → 120 |
| Выжившие | 60 → 80 | 10 → 40 |
| Унесено | −130 → **−225** | −110 → −165 |
| Потеряно защитников | −184 → **−144** | −152 → −192 |
| **Итог** | **806 → 831** | **868 → 803** |

**Канал 3 — обратная связь через дурную славу, самый слабый.** Больше выбитых
налётчиков → выше слава → крупнее следующая волна (10.1). На матрице состав
поздних волн вырос на одного-двух налётчиков на четырёх прогонах из шести; на
`baseline`/20260726 и `baseline`/20260728 он не изменился.

## Какой выход выбран и что стало с остальными

**Владелец 2026-08-01 выбрал третий: принять новые коридоры вместе с новым
основанием для инвариантов.** Голодная смерть после выигранного боя — канал 1 —
вынесена отдельной задачей
[#171](https://github.com/anshushunov/dungeon-fortress/issues/171) и в это решение
не входит. Новое основание и его проверки — в записи «Что изменилось в
инвариантах 4 и 11» выше.

Список ниже сохранён таким, каким он был на момент решения: он показывает, между
чем выбирали, и почему один канал не закрывается тем, что закрывает другой.
**Выходы закрывают разные каналы, и ни один не закрывает оба**:

- ослабить память о месте рядом с боем — закрывает 20260728 и **не** закрывает
  20260727, где ослаблять нечего;
- перебалансировать славу или веса счёта (15.8, 10.8) — действует на канал 3 и
  частично на 2: на 20260727 у `baseline` +60 приходит от лишней отражённой
  волны, то есть вес `ScorePerWaveRepelled` канал 2 задевает;
- принять новые коридоры вместе с новым основанием для инвариантов — выход,
  покрывающий канал 2 целиком. Наблюдаемый факт, который при этом принимается:
  на 20260727 неподготовленное владение обгоняет подготовленное по счёту партии,
  разрыв 62 → −28. Приемлема ли эта цена — вопрос владельца, а не следствие
  измерения: «подготовка окупается» держат инвариант 11 и
  [ADR 0016](../decisions/0016-party-score-separate-from-renown.md), и это
  продуктовое обещание, а не артефакт метрики;
- отказаться от правила подхода.

## Красных проверок семь, и три из них принадлежат этой же развилке

`DungeonFortress.Presentation.Tests`: 462 из 462 зелёных на `74af087`, 459 из 462
на голове ветки, при том что ветка не касается ни одного файла Presentation.
Значит краснеют они **от изменения поведения симуляции**, а не от чужой задачи, и
тексты падений называют тот же канал 1:

- `MemoryInspectorTests.The_panel_of_a_creature_that_remembers_places_lists_them_newest_first`
  — «no creature carried two remembered places 500 ticks after the first wave»;
- `CreatureStoryTests.Every_creature_that_refused_by_memory_reads_that_refusal_on_its_panel`
  — «only 2 creatures of the baseline party at tick 2400 ever refused by memory»;
- `CreatureStoryTests.Most_of_what_a_creature_decides_is_routine` — 89,0 % против
  90 %.

Два из трёх — независимые утверждения о том, что память о месте вообще работает
на эталонной фикстуре, и оба говорят, что отказов стало **меньше по числу
существ**: на `baseline`/20260726 отказов 187 против 28 — те же числа, что в
строке `refused_place_of_panic` таблицы выше, — но у двух существ против трёх.
Канал 1 не только усиливает память, он её концентрирует. **Перемерено после
[#171](https://github.com/anshushunov/dungeon-fortress/issues/171):**
концентрация снята — на `baseline`/20260726 отказов 97 у двух существ, на
`baseline`/20260728 122 у **четырёх** против трёх, всего по шести партиям
`baseline` и `prepared` 253 против 639. Знаменатель здесь шесть, а не пятнадцать:
`neglected` памятей не пишет, а обе причинные пары в этот счёт не входят —
оговорка при правиле «цена памяти ограничена сроком и нуждой» в 5.1. Прежняя
редакция этого
абзаца называла 232 против 55: это `entries + Sum(Repeats)`, где каждое первое
вхождение посчитано дважды, тогда как канонический счёт — `Sum(Repeats)`
(`PrototypeWorld.cs`, комментарий у склейки повторов; так же считает `CountEvents`
в `PrototypeScenarioTests`). Копия исправлена ссылкой на единственное место
записи, а не подновлением второго числа.

Остальные четыре — `The_contract_invariants_hold_on_every_seed_of_the_matrix` на
двух seed, `Most_broken_defenders_actually_leave_the_tile_they_broke_on` (76 из
108 против планки 80 %) и мягкая граница разброса `yieldCount` в
`Traffic_arbitration_preserves_one_move_no_overlap_and_no_swap` (74 против 40).
Последняя к развилке отношения не имеет: измерено, что на `main` тот же разброс
пробегает по матрице от 15 до 235, а граница 40 построена вокруг единственной
самой спокойной ячейки; подробности и четыре варианта прогона —
`evidence/129-matrix.json`, ключ `theTrafficBound`.

**Что стало с каждой из семи после решения владельца** — все зелёные, и ни одно
число не подогнано под прогон:

| Проверка | Исход |
|---|---|
| три теста памяти в Presentation | держат то же свойство на новой выборке; прежние формы были подогнаны под одну ячейку и ломались на `main` (`evidence/129-presentation.json`) |
| инварианты 4 и 11 на двух seed | новое основание по решению владельца, выполняется и на `main`, и после #129 (запись «Что изменилось в инвариантах 4 и 11») |
| планка бегства | [#168](https://github.com/anshushunov/dungeon-fortress/issues/168): обещание переформулировано с доли на то, что паника не запирает существо (10.3) |
| граница разброса `yieldCount` | [#170](https://github.com/anshushunov/dungeon-fortress/issues/170): снята — проверка не отличала изменение кода от смены seed |

## Что было снято и почему

Инварианты снимаются, только если измерение показало, что они не выполняются, и
снятие фиксируется здесь вместе с числами. Строка таблицы, к которой инвариант
относился, остаётся: она перестаёт быть требованием, но не перестаёт быть
фактом.

**Снят: «число вступивших в бой: `prepared > baseline > neglected`».** Он не
выполняется — и не «вничью», а с обратным знаком. Измерено по каноническому
журналу (`combat_joined` с `details.wave`, различные `creatureId` на волну), три
seed матрицы:

| seed | `baseline` по волнам | `prepared` по волнам |
|---|---|---|
| 20260726 | 9, 9, 9, 8 | 9, 8, 8, 6 |
| 20260727 | 9, 9, 9, 6 | 9, 8, 7, 7 |
| 20260728 | 9, 9, 9, 9 | 9, 9, 9, 8 |

Причина не в `neglected`, а в двух изменениях этого среза. Во-первых, кладовая
стала центром тяжести владения (`T.meal_target` 30), поэтому радиус вступления в
бой и без сбора захватывает почти всех: `muster_lead_ticks` больше не покупает
явку. Во-вторых, `prepared` дерётся всерьёз, накапливает тяжёлые травмы и к
поздним волнам выставляет **меньше** бойцов, но лучших. Подготовка теперь
покупает не явку, а состояние тех, кто вышел, и цену набега — это и записано
инвариантом 4.

Отдельно: «число вступивших» перестало быть численностью. Голодное существо
выходит из боя поесть и вступает снова, поэтому число событий `combat_joined`
за волну может превышать девять (у `baseline` доходит до 13). Считать нужно
различные `creatureId`, как в таблице выше.

**Снят: `renown(prepared) > renown(baseline)`.** Он был снят потому, что на seed
20260727 давал `baseline` 163 против `prepared` 147: дурная слава заметно зависит
от числа выбитых налётчиков, а оно шумит от джиттера боя. Дурная слава надёжно
отделяет владение, которое живёт, от владения, которое погибло (130–165 против
4), но **на одной партии не ранжирует два живых плана**. Для сравнения планов
между собой служат инварианты 1, 2 и 4. Это ограничение функции ценности, а не
дефект реализации.

*Переизмерено после #101:* на матрице контрпример больше не воспроизводится —
слава `prepared` теперь выше на всех трёх seed (169/147/173 против 137/133/149),
потому что подготовленное владение стало выбивать заметно больше налётчиков
(14 / 9 / 15 против 7 / 6 / 10). Снятие инварианта от этого не отменяется: роль
ранжирования планов передана счёту партии решением владельца
([ADR 0016](../decisions/0016-party-score-separate-from-renown.md)), а не тем
конкретным seed, и веса 15.8 не тронуты. Возврат инварианта на славу был бы
новым решением, а не восстановлением старого.

**Инвариант вернулся в новой форме и на другой величине.** Владелец решил
развилку #75 в пользу разведения ролей
([ADR 0016](../decisions/0016-party-score-separate-from-renown.md)): дурная слава
осталась мерой заметности и драйвером силы волны, а ранжирует качество игры счёт
партии (10.8), инвариант 11. Веса 15.8 при этом не тронуты — снятый инвариант не
восстановлен на славе и восстановлен не будет.

**По той же причине сюда не взяты** `combat_attack` и `combat_raider_downed`:
на матрице они не держатся (`attacks` `baseline` 84 / 76 / 140 против
`prepared` 138 / 103 / 129; `raidersDowned` см. выше).

## Что измерено вне матрицы

Матрица Issue #12 — три seed. Что делают фикстуры за её пределами, известно из
измерений, а не из дизайна, и записано здесь, чтобы коридоры выше нельзя было
прочитать как обещание.

**Двенадцати соседних seed для этого мало, и это выяснилось дорого.** Первая
редакция этого раздела после #101 читала двенадцать seed вокруг матрицы
(`20260726`–`20260737`), находила там ухудшение и записывала его как ухудшение
вообще. Review расширило выборку и показало, что двенадцать соседних seed — не
маленькая выборка, а **смещённая**: соседние seed дают похожие партии, поэтому
блок вокруг матрицы измеряет один участок пространства, а не пространство.
Правило на будущее: вне матрицы измеряем **несколькими непересекающимися
блоками**, а не одним отрезком.

Протокол: три блока по 60 seed каждый — `A` = `20260726`–`20260785`,
`B` = `20270101`–`20270160`, `C` = `20280501`–`20280560`, всего 180 партий на
каждую из фикстур `baseline` и `prepared`. `neglected` в прогон не входит: он
падает до первой волны и боевые правила его партии не касаются.

| Наблюдение | `origin/main` | С изменениями #101 |
|---|---|---|
| `score(prepared) > score(baseline)` строго | 153 из 180 (85,0 %) | 147 из 180 (81,7 %) |
| — из них нарушений, где `prepared` **пал** | 22 | 17 |
| — из них нарушений, где обе партии выжили | 5 | 16 |
| Средний зазор между планами | +70 | +70 |
| Медианный зазор | +158 | +158 |
| Худший случай | −1043 | **−963** |
| Лучший случай | +1124 | +396 |
| Нарушения по блокам (A/B/C) | 7 / 10 / 10 | 10 / 13 / 10 |

Отсюда следуют три вещи.

Первая: `score(prepared) > score(baseline)` — инвариант **матрицы**, а не
свойство дизайна. Он не выполняется примерно на каждой шестой партии за её
пределами, и так было и до #101. Нарушения распределены по всем трём блокам, то
есть это свойство функции ценности, а не особенность одного участка seed.

Вторая: изменение #101 сдвинуло эту картину в обе стороны сразу, и обе стороны
надо называть. Строгая доля упала на 3,3 пункта. При этом средний и медианный
зазор не изменились вовсе, подготовленное владение стало падать **реже** (17
против 22), а худший случай стал **менее** катастрофическим (−963 против −1043).
Причина у всего этого одна: бегство стало настоящей альтернативой гибели,
поэтому владение реже теряет людей насовсем — и реже проигрывает партию целиком,
— но чаще платит счётом за сломленных, потому что счёт берёт за сломленного
столько же, сколько за выбитого (10.8). Это измерение, а не escalation trigger:
правило выше ограничивает инварианты матрицей ровно затем, чтобы такие seed не
читались как дефект.

Третья: ранжирование «живое выше погибшего» вне матрицы измерено, но не
доказано; строго гарантируется только ранжирование исхода при прочих равных
(10.8).

Второе review PR #73 нашло на дополнительных seed и нарушения инварианта 4:
`mealsStolen` на seed 7 (63 против 54) и `combat_fled_morale` на seed 99 (23
против 17). Они остаются в силе как измерение и как причина, по которой заголовок
списка инвариантов ограничен матрицей.

## История 4.1 «Карта»: толчея, сбор по тревоге и уход в покои (#170, #186, #201, #228)

Действующие правила остались в 4.1: занятость тайлов, арбитраж движения, Yield/Clear, доступ к кладовой, раскладка карты, что копается. Здесь — измерения и разборы решений, которые эти правила сопровождали в исходном файле построчно.

### Равномерность уступок (#170)

Разброс `yieldCount` по девяти существам был ограничен сверху числом 40, и
проверка снята, потому что она не отличала изменение кода от смены seed: на
`main` тот же разброс идёт от **15** на ячейке, которую гоняет тест, до **226 и
235** на seed 20260727 — то есть шестикратное превышение границы достижимо без
единой правки. Граница была построена вокруг самой спокойной партии из шести;
подогнать её под новое число значило бы повторить ту же ошибку большим числом.
Правила арбитража — один ход за тик, отсутствие наложений, обменов и телепортов —
остаются требованием и проверяются там же
(`Traffic_arbitration_preserves_one_move_no_overlap_and_no_swap`). Измерение
четырьмя вариантами прогона — `evidence/129-matrix.json`, ключ
`theTrafficBound`.

### Слепота пути к телам после боя (#186)

**Слепота пути к телам измерена и названа ценой —
[Issue #186](https://github.com/anshushunov/dungeon-fortress/issues/186),
измерение под жалобой владельца с playtest 2026-08-02 «сгрудились после боя и не
могут разойтись».** Правило «BFS не считает существ препятствиями» выше — не
оговорка реализации, а то, из чего толчея состоит. За 60 тиков после конца волны
существа, бывшие в бою, получают на матрице **782** отказа в шаге против **526**
за столько же тиков перед приходом волны (**1,49**; на `baseline`, где нет сбора
по тревоге, **426** против **205**, то есть **2,08**). У **728 из 782 (93,1 %)**
до той же цели существовал маршрут длиной не более чем на четыре шага больше,
если считать чужие тела стенами; у **516** он был строго длиннее слепого, то есть
это другой маршрут, а не тот же самый. Уступка при этом достаёт максимум до
**344 (44,0 %)**: **290** отказов пришлись на существо, стоящее на цели, к
которой оно шло, и **54** — на голодного или собирающегося, а таких `Yield/Clear`
не двигает по правилу; ещё у **93** на клетке вообще никто не стоял — их отказал
сам арбитраж. На сопоставимом подмножестве (там, где тело действительно стояло на
пути) обход достаёт до **639**, уступка — до **344**, то есть в **1,86** раза
больше. Отсюда решение: **это поднимает
[Issue #76](https://github.com/anshushunov/dungeon-fortress/issues/76)** (занятость
клетки в поиске пути), а не арбитраж уступок и не правило подхода 10.3. Правило
подхода — усилитель, а не механизм: после него плотность отказов после боя
выросла в **1,40** раза (0,147 → 0,207 на существо-тик когорты) при фоне,
выросшем в 1,12, и число отказов в сторону клетки, которую в тот же тик просит
сосед, — в **восемнадцать** (10 → 186); при этом отношение к собственному фону
было выше единицы и до него (1,19), а доля отказов с обходом не сдвинулась вовсе
(0,916 → 0,931). Величина, её определения и все три дерева —
`evidence/186-measure-now.json` и `evidence/186-before-after-129.json`; проверки —
`PrototypePostCombatDispersalTests`.

**Контрольное окно кончается там, где начинается сбор по тревоге, и это не
деталь.** `IsMusterActive` истинно с тика `arrive − muster_lead_ticks`, а журнал
`prepared` ставит этот параметр в 60 на тике 880 — то есть при окне ровно в 60
тиков «мирный контроль» половины матрицы совпадал бы со сбором тик в тик и нёс
бы 532 отказа из 737. Первое измерение #186 так и было снято и дало обратный
ответ (1,06 и 0,79); ошибку нашёл независимый review. Контроль сдвинут на
`muster_lead_ticks`: `baseline` не двигается вовсе, у `prepared` он кончается
там, где начинается муштра. Держит это `The_control_window_ends_before_the_domain_stops_working`.

**Вторая половина того же симптома блокировкой не является, и она старше правила
подхода.** В том же окне **1989** существо-тиков когорты из 3780 прошли без
единого шага и без единого отказа, **1121** из них в режиме `Waiting`. Самая
частая причина такого стояния — `waiting_stock_sufficient` (850 раз): работы
нет. Вторая — `refused_zone_unreachable` (232 раза, **20,7 %**): зона недостижима,
и это уже другой диагноз, а не тот же самый. На `baseline`/20260728 семеро из
восьми бывших в бою за 60 тиков не сошли с клетки, на которой кончился бой, при
нуле отказов за всё окно. Существо без работы стоит там, где стоит, и после боя
это середина боя. Что с этим делать — продуктовое решение (куда девается бывший
боец, которому нечего делать), и оно здесь не принимается.

### Уход в покои после боя: решение, его цена и разбор трёх механизмов (#201, #228)

Ниже — историческая часть каждого пункта решения из 4.1 (полные формулировки правил остались в контракте): почему триггер выбран именно так, почему исключены койки, почему цель выбирается по идентификатору существа, почему «уход» не считается работой, и что показало измерение после внедрения.

Это не осторожность, а измерение: версия, срабатывавшая на любом
  простое, отправляла партию ходить через всю карту весь сеанс, и
  `prepared/20260726` кончал `fallen` на тике 2032 со средней сытостью 0 — пищевая
  цепь проигрывала дороге;

**Это измерение снято на первой редакции правила**, где
  уход срабатывал на любом простое; на отгружаемой редакции — где триггером стал
  конец волны — откат исключения даёт **3 красных из 305**, и все три —
  `Going_off_duty_never_takes_a_bunk` на трёх seed `baseline`. Различие названо,
  потому что читатель проверит очевидной командой и получит 3, а не 12
  (опровергнуто независимым review PR #217);

Величина — та, которую утверждает
  `Walking_round_reaches_more_of_the_clinch_than_a_yield_could`: отношение
  «обход достаёт / уступка достала бы» при планке 1,5. С разносом **937 / 605 =
  1,55**; без него **916 / 639 = 1,43**, ниже планки, и проверка краснеет. Для
  сравнения, до этого правила вовсе — **639 / 344 = 1,86**. Прежняя редакция
  называла здесь 1,49; это число независимый review PR #217 опроверг — оно из
  другой метрики (`blockedRatio` строки MATRIX), и утверждение было
  самоопровергающимся: при 1,49 проверка была бы красной уже на ветке;

Прежняя редакция говорила «доступно работе на **каждом**
  тике»; это измерено и **оказалось сильнее измеренного**
  ([Issue #228](https://github.com/anshushunov/dungeon-fortress/issues/228)).
  Из **3591** существо-тика, которые бывшие в бою проводят в покоях без работы,
  сопоставление видело существо на **3025 (84,24 %)**; остальные **566** названы
  поимённо и ни один из них уходом не создан: **563** — зарезервированная еда
  (`MatchJobs` не берёт в пул существо в режиме `Eating`, правило старше #201),
  **3** — тик, на котором работа `Rest` кончилась уже после сопоставления. До
  правила #201 та же доля — **609 из 717 (84,94 %)**. Отклик при этом быстрый:
  когда работа, которую существо могло взять, появлялась, оно бралось за работу
  **на следующий тик в 31 случае из 33** и максимум через **3** тика, а первый
  шаг делало не позже чем через **2** тика после взятия. Величины и команды —
  `evidence/228-response.json`.

**Что это дало и чего стоило.** Доля существо-тиков когорты «стоит без шага и без
отказа» — **1121 из 3780 (29,7 %) → 688 из 4020 (17,1 %)**; последний уходит с
места боя за **33** тика вместо **60**. Цена названа и не спрятана: группа пришла в
движение, поэтому отказов в шаге стало больше — **60,2 на окно → 75,1**. Это и есть
[Issue #76](https://github.com/anshushunov/dungeon-fortress/issues/76), занятость
клетки, оставленная владельцем на срезе 6; правило её не чинит и не претендует.
Величины и команды — `evidence/201-before.json` и `evidence/201-after.json`,
проверки — `PrototypeOffDutyTests`.

**Цена оказалась больше названной, и она собрана в покоях —
[Issue #228](https://github.com/anshushunov/dungeon-fortress/issues/228),
измерение под наблюдением владельца с playtest 2026-08-04 «все встали в
больницу и не могли оттуда выйти».** Доля существо-тиков когорты, проведённых
в зоне `Quarters` и в тайлах вокруг неё, — **1630 из 17916 (9,1 %) → 4657 из
19180 (24,3 %)**, окно здесь тихий промежуток до следующего сбора, а не 60
тиков расхода. Из этих 4657 разложение покрывает **100 %** и выглядит так:
отказ в шаге **1897 (40,7 %)**, «нет работы» **1129 (24,2 %)**, сон **947
(20,3 %)**, `refused_zone_unreachable` **389 (8,4 %)**, сам шаг **292
(6,3 %)**, конец работы в этом же тике **3**. Отказов в шаге внутри покоев
было **101** и стало **1897**; на существо-тик покоев **0,062 → 0,407**.

**Проксимальный механизм назван: это занятость клетки, но не там, где её
ждали.** **1376 из 1897 (72,5 %)** отказов — шаги в сторону **собственной**
клетки ухода: группа не заперта внутри, она не может туда **дойти**; до правила
такого класса отказов было **0**. Койки в этом участвуют, но меньшинством: на
них пришлось **838 из 1848 (45,3 %)** отказов, у которых тело на клетке
опознано, — плюральность, а не большинство. Самый частый тайл отказа
действительно койка — `(22,4)`, **454**, — но следом идут `(22,3)` **320** и
`(23,3)` **216**, которые койками не являются; на остальные три койки
приходится **204** (`21,3`), **180** (`21,4`) и **ноль** (`20,3`, ни одного
отказа за всю матрицу). Правило исключает койки как **цель**, но путь их не
обходит, потому что BFS не считает существ препятствиями (правило выше в этом
же разделе). Обход был возможен у **1235 из 1848 (66,8 %)** отказов, у 748 из
них строго длиннее слепого маршрута; замуровано **110 (6,0 %)**. Самая длинная
непрерывная череда отказов у одного существа — **183** тика против **64** до
правила. Это
[Issue #76](https://github.com/anshushunov/dungeon-fortress/issues/76), и
здесь она не чинится: владелец оставил её на срезе 6.

**«Работы нет» этим не исключено — оно звено той же цепи, и это меняет вес
вывода.** На **1376 из тех же 1897** тиков отказа сопоставление в тот же тик
уже написало существу код из множества «работы нет»; вместе с корзиной
`NoWork` это **2505 из 4657 (53,8 %)** существо-тиков в покоях, на которых
сопоставлению нечего было дать. Лестница разложения это прячет: рунга «отказ в
шаге» забирает тик первой, потому что вопрос был «что **держит**». Порядок
причинности обратный: работы нет → правило шлёт существо на его клетку → шаг
отвергается телом. Поэтому отказ в шаге — **проксимальный** механизм
наблюдаемого симптома, а «работы нет» остаётся живым и принадлежит
[Issue #72](https://github.com/anshushunov/dungeon-fortress/issues/72).
Правило #201 не создало нехватку работы и не устранило её: доля `NoWork` почти
не сдвинулась (**26,7 % → 24,2 %**), тогда как доля отказа в шаге выросла с
**6,2 % до 40,7 %**. Практическое следствие названо здесь, чтобы его не
пришлось выводить: правка того, **куда** правило паркует людей, убрала бы
толчею, но не убрала бы картину «все стоят в покоях» — половина этих тиков
существует потому, что работы нет.

**Измерением исключён один механизм из трёх — «зона недостижима».** Все
**389** несут `JobKind.Haul`, то есть это диагностика «не дойти до зоны
работы», а не «не дойти до покоев»: `RouteUnreachable` и `LarderUnreachable` на
всей матрице **нули**. Правило переполнения покоев не срабатывает вовсе: в зоне
**16** клеток без коек при **9** существах — проверка
`The_quarters_never_overflow_on_this_map`. Величины и команды —
`evidence/228-before.json`, `evidence/228-after.json`,
`evidence/228-decomposition.json`, `evidence/228-mechanism.json`; проверки —
`PrototypeQuartersIdleTests`.

### Снятое утверждение про карман демонстрационных журналов

Прежнее измеренное утверждение — что три сценария контракта ходят одинаково с
карманом и без него — **снято, а не перенесено**: оно было свойством карты из
открытого пола. На подземелье карман лежит за покоями, и это стоит труда:
сессия, которая его копает, производит 21 порцию к первой волне против 69 у
сессии, которая его не трогает
(`Stone_and_food_share_one_haul_priority_without_starving_the_food_chain`).

## Дополнительная история 13.4 (#101, #117, #129)

Продолжение истории 13.4 сверх семи `####`-подразделов, вынесенных решением #280 первым кругом. Действующая таблица коридоров и список из 11 инвариантов остались в контракте; здесь — провенанс отдельных строк и клауз.

### Боевые строки таблицы переизмерены после #101

**Боевые строки переизмерены после #101** — личная потиковая проверка морали и
бегство как перемещение (10.3) сдвинули всё, что стоит после боя: сколько людей
ломается, сколько их гибнет, сколько уносят налётчики, сколько остаётся к концу
партии здоровых и, через потери, счёт. Сдвиг разобран числами в 15.6; здесь
записан результат.

### Таблица перемерена после #129

**Таблица перемерена после [#129](https://github.com/anshushunov/dungeon-fortress/issues/129)**
решением владельца 2026-08-01 «принять новые коридоры». Источник — тот же файл и
та же команда, что и выше; строки «на прибытии волны 1» — прогон до тика 1301.

### Снята: строка «защитников вступило в бой по волнам»

**Строка «защитников вступило в бой по волнам» снята.** Она была снята как
инвариант ещё до этого среза (см. ниже) и оставалась как факт; на подземелье
пересчитывать её значило бы платить прогоном за число, которое ничего не
ранжирует. Способ, которым её считают, если она понадобится, записан там же:
различные `creatureId` у `combat_joined` с `details.wave`.

### Зазор между планами на старой карте («зал»)

На зале коридоры не
пересекались, и разница ровно в этом: минимальный зазор упал с 87 до **62**, а
разброс `prepared` при этом сжался вдвое.

### Размен, который сделал срез #117

**Размен, который сделал срез #117.** Минимальный зазор между планами на
матрице: **87** на зале, **62** на подземелье, и обе цифры получены одной и той
же командой над одним и тем же файлом (`evidence/117-matrix.json`, ключ
`minimumPlanGap`). Куплено на это подземелье, без которого память о месте
(5.1) была бы ненаблюдаема, и вместе с ним — вдвое меньший разброс `prepared` и
вдвое меньше унесённых порций у подготовленного владения (21–30 против 26–56,
на зале было 24–33 против 40–51). Инвариант 4 при этом впервые за срез
выполняется на всех трёх seed в обеих своих половинах.

### Размен, который сделали два круга #101, и его цена

**Размен, который сделали два круга #101, и его цена.** Минимальный зазор между
планами на матрице: **112** на `origin/main`, **135** после первого круга,
**87** после второго — то есть он ушёл ниже, чем был до #101. Куплено на это
было то, ради чего задача заводилась: момент паники стал личным (наибольшая
когорта 6 → 3 → 3 из девяти), а бегство стало перемещением, которое видно —
на `origin/main` его вообще не было, позиция присваивалась одним тиком на
расстояние **до 35 тайлов** (27, 25, 28 на `baseline` и 33, 35, 33 на
`prepared` по матрице). Число 26, стоявшее здесь до третьего круга review,
описывало наибольшую **ходьбу после исправления**, то есть подписывало дефект
измерением уже починенной сборки и заодно занижало его. Второй круг улучшил уже саму ходьбу: суммарные
простойные тики 1477 → 1117, доля бегств, сдвинувших существо, 88,2 % → 91,7 %.
Самый длинный единичный простой при этом вырос, 67 → 78 тиков, и его причина —
открытый вопрос в 10.3, а не веса. Ни один из двух кругов не измерил этот размен до
того, как его сделал: в первом круге веса подбирались по числу бегств и по
исходам волн, во втором трафик менялся ради легибельности, и в обоих случаях
зазор функции ценности был проверен только на «больше нуля». Роль ранжирования
плана снята с дурной славы решением владельца, а не измерением (10.6,
ADR 0016).

### Прежняя (вводящая в заблуждение) форма сводки силы владения у `neglected`

Раньше в этом же тике сводка
показывала силу 86 — выше, чем у выжившего `baseline`, — и разрыв читался как
«иду отлично». Правило 10.7 убрало эту ложь: сила теперь считает готовность, и
единственное место, где сводка вводила в заблуждение, было именно отрицательным
примером.

### Почему оговорка про матрицу в инварианте 5 — решение, а не подгонка

Оговорка про матрицу — **следствие решения владельца 2026-07-31 переписать
   эталонный план** (13.2), а не результат измерения, под которое подогнали
   правило. Цепочка целиком: карта стала подземельем → журнал, написанный для
   зала, перестал изображать правильную подготовку → владелец решил переписать
   план → у нового плана `prepared` даёт 13, 21 и **0** таких событий, и ноль
   стоит на seed 20260728.

   **Что делает эту цепочку проверяемой, а не рассказом.** Ноль порождён планом,
   а не подземельем и не сломавшимся механизмом, и это отдельный измеримый факт:
   соседняя фикстура `prepared-watch-zero` отличается от `prepared` **одной
   командой** — караул выключен на тике 900 — и на том же seed 20260728 даёт
   **2** события `combat_refused_starving`
   (`evidence/117-matrix.json`, ключ `combatRefusedStarving`). Значит код на этом
   seed достижим, и ослаблять пришлось ровно то, что зависит от выбранного плана.

### Прежняя формулировка инварианта 10 (без оговорки про матрицу)

Раньше здесь стояло «не нарушает инварианты
    1–8» без оговорки, и это читалось как обещание про любой seed — обещание,
    которого измерение не подтверждает (см. ниже);

## История 15.5 «Выбор работы»: цена и радиус памяти о месте (#125, #171)

Таблица значений `T.*` осталась в контракте. Здесь — как получены `T.memory_avoid_ticks`, `T.memory_yields_satiety`, `T.memory_avoid_radius`, их перепроверка после #129/#171 и разбор двух дефектов подсчёта, из-за которых числа отказов по памяти были завышены.

`T.memory_avoid_ticks` = 200 и `T.memory_yields_satiety` = 30 введены
[#171](https://github.com/anshushunov/dungeon-fortress/issues/171): это две
величины цены памяти, и до неё существовала только первая строка таблицы —
дальность. 200 короче `T.wave_interval_ticks` = 350, поэтому испуг не переживает
тихое окно между волнами; на 300 тот же прогон оставляет `baseline`/20260728 при
сытости 22 и 241 отказе против 39 и 122. 30 равно `T.eat_threshold` — точке, где
голод и так перебивает всё; на 20 владение выживает, но заканчивает при сытости
13, потому что уступает уже после того, как кладовая пуста. Обе прогнаны по
**шести партиям `baseline` и `prepared`** — тому же знаменателю, что и строка
радиуса ниже, и не по пятнадцатиячеечной матрице 13.4; таблица прогона —
`evidence/171-after.json`, ключ `sweep`; каждая половина по отдельности видна
проверке — `evidence/171-mutations.json`.

`T.memory_avoid_radius` = 4 — измерение, а не вкус. Обе половины сравнения
считаются по **одному и тому же знаменателю**: шесть партий `baseline` и
`prepared`. Три партии `neglected` из счёта исключены — они не доживают до
волны, памяти не пишут и дают ноль при любом радиусе, поэтому включать их в одну
половину и не включать в другую значит сравнивать разные вещи:

| радиус | по партиям (`baseline` 726/727/728, `prepared` 726/727/728) | диапазон | партий с нулём |
|---|---|---|---|
| 2 | 0 / 0 / 0, 0 / 0 / 3 | 0–3 | 5 из 6 |
| 4 | 28 / 6 / 14, 0 / 28 / 4 | 0–28 | 1 из 6 |

На двух дорога налётчиков проходит в трёх-четырёх шагах восточнее кладовой, а
работать там нечего, и правило почти не существует. На четырёх различитель
выживает, и решение оставить 4 состоятельно.

**Перепроверено на дереве после #129 и #171.** Карта, о которой говорит абзац
выше, изменилась: правило подхода ставит защитников вокруг налётчика, налётчик
стоит у кладовой, и памяти теперь пишутся на самих рабочих клетках — четыре из
шестнадцати на `baseline`/20260728. Довод «на дороге отказывать нечему» перестал
работать, и вопрос был задан заново. Ответ прежний, но по другой причине: на
`baseline` радиус можно уменьшать, а на `prepared` памяти по-прежнему ложатся у
ворот, и единственная работа в их досягаемости — караульный пост, поэтому при
радиусе 2 отказы там падают с 40 за три seed до 5 — ниже планки в 10, которой
`PrototypeMemoryTests` держит наблюдаемость среза 2. Радиус — то, чем механика
вообще видна на `prepared`; цену памяти ограничили срок и нужда, а не он.
Прогон — `evidence/171-after.json`, ключ `sweep.reach`.

Обе строки перемерены после Issue #125. Раньше здесь стояло «на двух — ни разу
за партию, на четырёх — 9–69», и оба числа были взяты до того, как отказ
перестал называть работу, которую существо не взяло бы и без памяти: три отказа
из пяти были такими, и они попадали в этот счёт наравне с настоящими. Ноль на
четырёх — это `prepared/20260726`, партия, где после исправления память не
изменила ни одного решения; двадцать отказов, которые она показывала раньше,
называли работу, за которую существо и так не бралось.

Число получено суммированием `repeats` у кодов `refused_place_of_panic` и
`refused_place_of_wound` в каноническом журнале каждой из девяти партий матрицы:

```powershell
dotnet run --project .\tests\DungeonFortress.Scenarios -- `
  --prototype --commands .\scenarios\prototype1\<fixture>.commands.v2.json `
  --ticks 2700 --snapshot .\.artifacts\<fixture>.json
```

До 9–69 здесь стояло 12–89, и это было завышено первым из двух дефектов: отказ
записывался дважды за тик, а `repeats` считает записи, а не тики. Цена той
ошибки была не в этой строке, а в том, что `ReasonCodeOccurrences` суммирует
`repeats`: **всякий** счёт этих двух кодов, взятый из сводки, был вдвое завышен,
пока дефект жил. Второй дефект — Issue #125, и он снял ещё три отказа из пяти.
Оба перемера сделаны заново, а не пересчитаны из прежнего числа.

## История 15.6 «Готовность и бой»: веса морали переизмерены под личную потиковую проверку (#101)

Таблица значений `T.*` осталась в контракте вместе с правилом «инвариантом является форма проверки из 10.3, а не эти числа». Здесь — таблица «было / стало» и что показало измерение по каждому весу.

**Три веса морали переизмерены под личную потиковую проверку (#101), и вот
почему именно они.** Спрашивать каждый тик, а не раз на чужое выбывание, — это
не то же самое при тех же числах, и цена видна в исходах волн:

| Вес | Было | Стало | Что показало измерение |
|---|---|---|---|
| `T.morale_per_downed` | 10 | 14 | множитель считает уже другое: не всех потерянных владением (0–8 у девяти существ), а упавших в поле зрения (0–2). Локальному счёту нужен больший вес, чтобы говорить о том же бое то же самое |
| `T.morale_health_weight` | — | 40 | собственные раны — самое крупное различие между двумя защитниками одного боя, поэтому именно этот член решает, кто уходит. На 24 не держался никто: `defendersDowned` за партию падал до 0–1, и вместе с ним из партии исчезали травмы, лежанки и цена проигранной волны |
| `T.morale_per_raider_near` | — | 5 | давка толкает, но слабее, чем упавший рядом. На 7 защитник с двумя налётчиками в упор ломался **до** первой потери, и волна за волной получала `overrun`: никто не стоял достаточно долго, чтобы выбить хоть одного налётчика |
