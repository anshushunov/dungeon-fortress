# Issue #409 — входная записка следующему

Остановка на рубеже 2 (две части из четырёх). Ветка `feature/409-localised-injury`,
рабочая копия `../_wt-409-injury`, база — `origin/main` = `0998b18` на момент взятия
(на момент остановки `origin/main` ушёл на `9c39b0b`, PR #411, docs-only: `src/` и
`tests/` не тронуты, конфликта нет — но перед PR базу перечитать).

Дерево зелёное: симуляция 372/373 (1 skip), представление 735/735. Полный
`verify.ps1` **ещё не прогонялся** — это рубеж 7.

## Что сделано

| рубеж | состояние | коммит |
|---|---|---|
| 1. Модель локализации | **готово** | `65ea050` |
| 2а. Рука — роняет оружие | **готово**, мутант зелёный | `f100e3b`, `3ef63a4` |
| 2б. Нога — хромает | **готово**, мутант зелёный | `f921761` |
| 2в. Голова — оглушён | не начато | |
| 2г. Торс | не начато | |
| 3. Заживление по частям | не начато | |
| 4. Переизмерение двух инвариантов | не начато | |
| 5. Представление и панель существа | не начато | |
| 6. Контракт и фикстуры | не начато | |
| 7. Полный `verify.ps1` | не начато | |

Замеры — `evidence/409-localisation.json`, мутанты — `evidence/409-mutants.json`.

## Что проверено и держится

- **Рубеж 1 — замена модели без единого сдвига поведения.** Канонический документ
  конца партии совпадает с `origin/main` побайтово, если убрать новый массив
  `injuries` и новые записи `injury_localised`, — на всех трёх поставляемых
  журналах. Это держится на одном решении: часть тела тянется из **своего** потока
  `PrototypeWorld._injuryRandom`, а не из `_combatRandom`. **Не сливай их.** Слей —
  и джиттер каждого удара после первой раны в партии сдвинется, и ни один замер
  рубежей 2–4 больше не будет сравним с тем, что было.
- `injury` остался в снапшоте и стал **функцией**: худшая из четырёх частей.
  Пятнадцать мест симуляции читают его и не переписаны ни одно. Инвариант
  «`injury` всегда худшая из `injuries`» проверяется на каждом существе каждого
  опубликованного тика всех 12 партий.
- Все четыре части достижимы: 140 травм по матрице,
  `head=21 torso=43 arm=26 leg=50`.
- 35% существ входят в следующую волну с непустой локализованной травмой.

## Что **опровергнуто замером** — не плати за это дважды

1. **Сырой урон удара не измеряет руку.** Проверка «раненой рукой бьют слабее» по
   сырому урону была вакуумной: мутант, отключающий следствие руки целиком,
   оставлял её зелёной (16,57 против 19,04). Разрыв делал торс через готовность.
   Мерить надо вес оружия отдельно: `урон − готовность/T.damage_readiness_divisor`,
   делённое на силу. Целая рука читает 4,08 — ровно `T.damage_might_weight`.
   **Тот же класс ошибки ждёт торс и голову:** любая величина, в которую входит
   готовность, будет двигаться от любой травмы, потому что готовность сегодня
   штрафуется сводным `Injury`, а не торсом.
2. **Хромота на каждом шаге не работает — и уменьшение периода это не чинит.**
   Хромота, приложенная ко всякому движению, двигает экономику целиком (раненый
   носильщик позже доносит еду → сдвигаются кривые сытости), и четыре посторонних
   обещания краснеют на разных seed — включая документированный headless-проход
   `build-demo`, фикстуру **без боя вообще**. Период 3/2 → 6/3 не починил, а сломал
   другое: чувствительность хаотична. Верное решение — **охват, а не размер**:
   хромота только в бою (`Fighting`, `Fled`, сбор). Это ещё и точнее по питчу.
3. **Молчаливое следствие ловится гардом.** Первая редакция хромоты забирала шаг,
   не говоря об этом в журнале, и покраснела на
   `Every_tick_a_runner_stands_still_is_explained_in_the_log`. Контракт 11 обещает,
   что не сдвинувшееся существо сказало почему. Всякое следствие, которое отнимает
   действие, обязано оставить запись. Спама не будет: свёртка склеивает запись с
   собственной последней записью существа, а идущее существо между шагами ничего не
   пишет — переход через всё владение оставляет одну запись со счётчиком.
4. **Слияние потока травмы с боевым** — не пробовал и не надо (см. выше, п. 1
   раздела «что держится»).

## Земля, которая уже хожена в чужих проверках

Слайс удлиняет бой, и это сдвинуло выборку у нескольких проверок, привязанных к
форме партии. **Ни одна не ослаблена**; в каждой правке названо, что именно
случилось. Если увидишь их в диффе — это уже разобрано, не разбирай заново:

- `PrototypePostCombatDispersalTests`, `PrototypeMemoryTests` — четвёртый seed
  (`20260729`) вместо трёх. Порог выборки 100 остался порогом 100.
- `PrototypeMusterParticipationTests` — расстояние до налётчика мерится **путём**, а
  не Манхэттеном, и по доске, которую видела перекличка (`previous`, не `current`).
  Собственный комментарий гарда называл Манхэттен «индикатором, а не вердиктом», а
  рядом стояло утверждение. Ложное обвинение: `baseline/20260726` t2020,
  `toLarder=11`, Манхэттен до налётчика 6, пути — больше `T.engage_radius`.
- `PrototypeReturningRaiderTests` — «застрял из-за памяти» больше не включает убитых
  по дороге. `20260735/Кистень` убит в волне 4 (`mode=Downed`, `hp=0`) и считался
  застрявшим. Разделено на два счёта; **цена размена названа строкой в самой
  проверке**: после исключения убитых клаузула почти вакуумна, и это напечатано,
  а не спрятано. **Это находка для координатора**, а не закрытый вопрос.
- `CreatureStoryTests.A_decision_that_held_for_several_ticks...` — читает шесть
  партий вместо одной. `WorldLabelFocusTests` — `Single()` по единственному
  возвращенцу со шрамом заменён обходом всех плюс отдельная проверка непустоты.
  `WorldLabelInspectorTests` — кадр «своя в спину налётчику» ищется по своей форме
  (новый `OwnerFrame.WhereACrewMemberStandsInFrontOfARaider`).

## Что делать дальше и на что смотреть

**Рубеж 2в — голова, «оглушён».** По образцу ноги: следствие отнимает **действие**
(тик боя), считается опубликованным счётчиком и пишет запись в журнал. Ограничь
боем сразу — не повторяй п. 2 выше. Мутант: константа периода в 0.

**Рубеж 2г — торс. Это самая крупная развилка, и она не техническая.** Сегодня
`ComputeReadiness` вычитает `InjuryLightPenalty`/`InjuryHeavyPenalty` по **сводному**
`Injury`, а `UpdateCombatParticipation` не пускает в бой всякого с `Injury == Heavy`.
Замысел слайса — «следствие вместо вычитания числа» — требует, чтобы штраф
готовности стал следствием **торса**, а не всякой раны, и, вероятно, чтобы тяжёлая
**конечность** больше не выбрасывала существо из боя целиком: тяжёлая рана сегодня —
крупнейшая причина неучастия (18 из 27 отсутствий по матрице, измерено PR #408).
Это ровно половина ответа владельцу на его наблюдение с playtest. **Но это же и
главный риск для инварианта «намеренное разорение не окупается»** (`HpRecoveryStep`,
PR #402): если раненые перестанут выбывать, заброшенное владение перестанет платить
за то, что не лечится. Мери рубежом 4 **до** того, как объявишь торс сделанным.

**Рубеж 4 нельзя предполагать.** `Deliberately_losing_creatures_and_stock_never_
scores_better` держится на `HpRecoveryStep`, а не на `DamageReadinessDivisor` —
это измерено в PR #402 и записано в комментарии у самой константы. После правки
готовности и мендинга **переизмерь мутантами по каждому кандидату**, а не по одному.

**Рубеж 5 — представление.** Ничего ещё не сделано. Панель существа
(`InspectorText.Build`) выводит `Hp`, `mode`, `readiness` и про травму молчит;
надпись над головой — `WorldLabels.CreatureLine` через
`HudText.CreatureStateShort`. Питч называет цену прямо: иконка над головой и
хромающая походка, больше этого не делать. Гард читаемости (пол 12 px, легенда —
названное исключение) обязан остаться зелёным.

**Рубеж 6 — контракт.** Не тронут ни одной строкой. Разделы из scope: §2.1 («1
состояние травмы» — теперь четыре части), §5.2, §10.2 (**не чини здесь #412**),
§10.5, §12.1 (состав снапшота: добавились `injuries` и `stepsLostToLimp`), §12.5,
§12.6, §12.7 (новые reason codes `injury_localised`, `injury_limped`), §15.6
(таблица тюнинга: `InjuryPartWeights`, `Arm*MightPercent`, `Leg*LimpPeriod`).

## Контрольные суммы

`scenarios/prototype1/*.json` **не изменены** — это журналы команд, а не снапшоты.
Каноническая контрольная сумма партии сдвинулась дважды и по объявленной причине
(новый массив и новый счётчик присутствуют у каждого существа с тика 0), золотые
кадры `tests/golden/ui/*` перегенерированы штатным `scripts/update-golden-ui.ps1` —
в каждом изменился ровно один токен. Числа «до» и «после» — в
`evidence/409-localisation.json`.

## Расширения партиции, которые уже сделаны и подлежат объявлению в теле PR

- `tests/golden/ui/**` — не назван ни своим, ни чужим; перегенерирован скриптом
  репозитория, руками не правился.
- `tests/DungeonFortress.Simulation.Tests/**` и
  `tests/DungeonFortress.Presentation.Tests/**` — свои по постановке, но правки
  задели файлы чужих Issue (#358, #405, #201, #364, #389). Ни одна проверка не
  ослаблена; каждая правка объяснена в самом файле.
- `src/DungeonFortress.Presentation/EventNarration.cs`, `HudText.cs` — свои
  (`src/DungeonFortress.Presentation/**`), правка ограничена двумя новыми reason
  codes.

---

# Раздел второго исполнителя — слайс доведён до PR

Взято на рубеже 2 из 7, доведено до конца. Ветка та же, рабочая копия та же.
Полный `verify.ps1` зелёный один раз на финальном состоянии: 10 из 10 стадий,
`deterministicChecksum e65273aa`. Симуляция 376/377 (1 skip), представление
740/740.

## Что добавлено к таблице предшественника

| рубеж | состояние | коммит |
|---|---|---|
| 2в. Голова — оглушён | **готово**, мутант изолирован | `c2b05d1` |
| 2г. Торс — готовность и допуск в бой | **готово**, два мутанта | `1ebbc03` |
| 4. Переизмерение инвариантов | **готово**, девять кандидатов | `7dc8669` |
| 5. Представление и панель | **готово**, кадр приложен | `ed0240a` |
| 3. Заживление по частям | **готово**, замер в тиках | `f39d22d` |
| 6. Контракт | **готово** | этот коммит |
| 7. Полный `verify.ps1` | **готово** | — |

## Что опровергнуто замером — не плати за это дважды

1. **Порог здоровья на входе в бой не работает.** Довод сильный: старый гард
   `injury != heavy` на деле означал «залечен выше двух пятых с тех пор, как его
   унесли с пола», а `RaiseTheDowned` поднимает с одним очком. Порог 20 % дал
   **шесть** красных вместо одной, включая контрактные инварианты и
   `Preparation_outscores_its_absence`. Это переустановка баланса боя.
2. **Клетка `baseline/20260728` по памяти места — совпадение, а не сигнал.**
   Отказов: 214 при периоде оглушения 4, ноль при 5, два при 6, один при 7, 121
   при 8; на исходном дереве — два. Не подгоняй под неё тюнинг: зависимость не
   монотонная.
3. **`baseline/20260729` по памяти места — наоборот, следствие, а не шум.** Ноль
   при всех пяти периодах. Причина — торс: раненые идут в строй, их чаще роняют,
   и окно между волнами они проводят в койках, а не мимо запомненных мест.
4. **Тюнинг головы обязан выбираться на готовом дереве.** До торса тяжёлая
   голова не могла попасть в бой вообще (тяжёлая рана ставится вместе с
   падением, а старый гард не пускал), поэтому половина числа была мертва.
   Выбранное до торса 5/3 на готовом дереве краснит; выбрано 6/3.

## Три чужих гарда уточнены — все одного класса

Утверждение уже механизма, о котором говорит. Исход по ослаблению #364 и #389
выносит независимый review, не координатор и не исполнитель.

- `A_gesture_never_takes_a_name_from_a_body_on_another_cell` (#389) и две
  проверки `WorldLabelLayoutTests` (#364): исключение «только общая клетка»
  расширено до «в пределах тайла», потому что `MaximumAttachmentRef` = один тайл
  и соседи борются за пересекающуюся землю по устройству. Взамен добавлена
  клаузула, которой не было: жест не имеет права уменьшить число названных тел.
- `A_place_is_remembered_by_one_creature_and_not_by_the_group` (#117): «двое
  запомнили один тайл за тик» ≠ наследование, если второй на этом тайле стоял.
- `A_remembered_place_changes_what_the_creature_does_next` (#333): возвращена на
  три seed, против которых правило выбиралось; четвёртый добавлен на этой же
  ветке ради другой проверки и молча ужесточил правило вида «в каждой партии».

## Что осталось долгом

- Клаузула гарда #358 не тронута, как и просил бриф. Состояние: почти вакуумна,
  счёт напечатан в самой проверке. Исход — независимому review.
- `baseline/20260729` больше не показывает память места и не показывает
  подписанного возвращенца. Оба — следствия решения по торсу, оба названы в
  коде и в теле PR. Принадлежат #171 и #364.
- `DamageReadinessDivisor` при подстановке 6 → 1 краснит первый инвариант,
  вопреки записи #402. Подстановка крайняя; разбирать — не этому срезу.
